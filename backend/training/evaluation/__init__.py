"""
Evaluation scripts for the MT-TCN form detection model.

These are stand-alone diagnostics / validation tools; they are not imported
during training or inference. Run each with `python -m backend.training.evaluation.<name>`.
"""
