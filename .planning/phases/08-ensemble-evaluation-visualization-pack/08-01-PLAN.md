---
phase: 08-ensemble-evaluation-visualization-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/notebooks/12_full_evaluation.py
  - docs/notebooks/12_full_evaluation.ipynb
  - docs/figures/all_errors_vs_paper.png
  - docs/figures/cross_exercise_macro.png
  - docs/figures/methodology_diagram.png
  - docs/figures/highlights_panel.png
autonomous: true
requirements: [EVAL-01, EVAL-03]
user_setup: []

must_haves:
  truths:
    - "A reviewer can open docs/notebooks/12_full_evaluation.ipynb pre-executed and see all 5 headline F1 values recomputed from the three results.pkl"
    - "Every headline F1 in the notebook is recomputed from pickle raw scores + the stored threshold and asserted equal (<=1e-6) to the stored test_f1 — no hand-typed literal"
    - "The EVAL-01 master comparison (ours vs Parmar baseline + SSL, all 5 errors) renders as a printed table and a grouped-bar figure"
    - "All 4 master figures exist as PNGs in docs/figures/ and the notebook asserts their existence after the figure cells"
    - "The notebook pre-executes cleanly start-to-finish via nbconvert --execute (exit 0)"
  artifacts:
    - path: "docs/notebooks/12_full_evaluation.py"
      provides: "jupytext source: 3-pickle load + no-fabrication validation cell + EVAL-01 master table + 4 figure-build cells + figure-exists asserts"
      contains: "f1_per_error"
    - path: "docs/notebooks/12_full_evaluation.ipynb"
      provides: "pre-executed consolidated evaluation notebook"
    - path: "docs/figures/all_errors_vs_paper.png"
      provides: "EVAL-01 headline grouped bar — 5 errors, ours vs Parmar baseline vs Parmar SSL"
    - path: "docs/figures/cross_exercise_macro.png"
      provides: "per-exercise macro-F1 vs paper (Squat/OHP/Shallow)"
    - path: "docs/figures/methodology_diagram.png"
      provides: "net-new two-method architecture schematic (no fabricated numbers)"
    - path: "docs/figures/highlights_panel.png"
      provides: "per-exercise headline + SSL-lift + val->test-gap panel"
  key_links:
    - from: "docs/notebooks/12_full_evaluation.py"
      to: "backend.training.aqa.eval.metrics.f1_per_error"
      via: "import + recompute-and-assert against stored test_f1"
      pattern: "from backend.training.aqa.eval.metrics import"
    - from: "docs/notebooks/12_full_evaluation.py"
      to: "three results.pkl"
      via: "pickle.load of squat/ohp/shallow pickles"
      pattern: "results.pkl"
---

<objective>
Build the consolidated evaluation notebook `docs/notebooks/12_full_evaluation.{py,ipynb}` and the 4 master synthesis figures from the three committed `results.pkl` — entirely LOCAL, no Colab, no model re-run. This plan delivers the EVAL-01 comparison artifact (master table + headline figure) and the EVAL-03 figure pack, with structural no-fabrication enforcement: every headline F1 is recomputed in code from the pickle raw scores + the stored threshold and asserted equal to the stored value.

Purpose: This is the artifact engine for the final milestone deliverable. The notebook + figures are what the supervisor sees; the in-notebook assertions are what guarantee no number was hand-typed (the Phase-3 bug the user caught).
Output: `12_full_evaluation.py` (jupytext source) + `12_full_evaluation.ipynb` (pre-executed) + 4 PNGs in `docs/figures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ensemble-evaluation-visualization-pack/08-CONTEXT.md
@.planning/phases/08-ensemble-evaluation-visualization-pack/08-RESEARCH.md
@.planning/phases/08-ensemble-evaluation-visualization-pack/08-PATTERNS.md
@.planning/phases/08-ensemble-evaluation-visualization-pack/08-VALIDATION.md

<interfaces>
<!-- Reused UNCHANGED. Do not redefine. Source: backend/training/aqa/eval/metrics.py -->
from backend.training.aqa.eval.metrics import f1_per_error, threshold_sweep, pr_auc_per_error, confusion_matrix_per_error

f1_per_error(y_true: np.ndarray, y_pred: np.ndarray) -> float          # binary F1, zero_division=0
pr_auc_per_error(y_true: np.ndarray, y_score: np.ndarray) -> float      # average precision; asserts scores in [0,1]
threshold_sweep(y_true: np.ndarray, y_score: np.ndarray) -> tuple[float, float]
confusion_matrix_per_error(y_true: np.ndarray, y_pred: np.ndarray) -> np.ndarray   # 2x2, labels=[0,1]
</interfaces>

<pickle_key_paths>
<!-- VERIFIED schemas (RESEARCH.md "The Three results.pkl"). Read EVERY value from these keys — NO hand-typed F1 literals in code cells. -->
Squat   (.planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl):
  squat["final"]["test_f1"]["KIE"]   == 0.41975308641975306
  squat["final"]["test_f1"]["KFE"]   == 0.841025641025641
  squat["final"]["test_macro"]       == 0.630389363722697
  squat["final"]["thresholds"]["KIE"] == 0.61448742486136
  squat["final"]["thresholds"]["KFE"] == 0.38465035725739627
  squat["raw"]["ens_test_scores"]    -> (244, 2)   # col 0 = KIE, col 1 = KFE
  squat["raw"]["test_labels"]        -> (244, 2)
  squat["comparison"]["paper_md"]["KIE"|"KFE"]        # Parmar SSL  (0.4186 / 0.8338)
  squat["comparison"]["paper_kinetics"]["KIE"|"KFE"]  # Parmar baseline (0.297 / 0.8184)
  squat["comparison"]["phase3"]["macro"]              # our Phase-3 baseline macro 0.5429 (for SSL lift)
OHP     (.planning/phases/06-overhead-press/figures/results.pkl):
  ohp["test_f1"]["elbows"|"knees"|"macro"]            # 0.4474 / 0.8770 / 0.6622
  ohp["thresholds"]["elbows"|"knees"]                  # 0.3574 / 0.4765
  ohp["ensemble_test_scores"]        -> (339, 2)   # col 0 = elbows, col 1 = knees
  ohp["test_labels"]                 -> (339, 2)
  ohp["paper_targets"]["elbows"|"knees"]               # Parmar SSL (0.4552 / 0.8452); NO paper baseline
  ohp["baseline_control"]["macro"]   == 0.6118
  ohp["ssl_lift"] == 0.0504 ; ohp["val_test_gap"] == 0.0109 ; ohp["n_seeds"] == 2
Shallow (.planning/phases/07-image-based-errors-cvcspc/figures/results.pkl):
  ss["test_f1"]["cvcspc_ensemble"]   == 0.8902195608782435   # THE headline
  ss["ensemble_test_scores"]         -> (540,)    # 1-D single output, NOT (540,2)
  ss["test_labels"]                  -> (540,)
  ss["best_threshold"]               == 0.3953738510608673
  ss["paper_targets"]["cvcspc"|"simsiam"|"openpose_tdm"]   # 0.8694 / 0.8286 / 0.834; NO paper baseline
  ss["baseline_control"]["test_f1"]  == 0.875
  ss["ssl_lift"] == 0.0152 ; ss["val_test_gap"] == 0.0217 ; ss["loss_deviation_note"] -> str
</pickle_key_paths>

# Analog files to mirror (read before writing)
@docs/notebooks/11_shallow_squat_evaluation.py
@docs/notebooks/08_ohp_evaluation.py
@backend/scripts/ohp_headline_chart.py
@backend/scripts/ohp_eval_viz.py
@backend/scripts/render_squat_result.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12_full_evaluation.py — load 3 pickles, no-fabrication validation cell, EVAL-01 master table, 4 master figures</name>
  <files>docs/notebooks/12_full_evaluation.py</files>
  <read_first>
    - docs/notebooks/11_shallow_squat_evaluation.py (lines 1-40: jupytext markers, the dual-path `_CANDS` pkl resolution, the FIG/EVAL dual-path + mkdir, the headline markdown block)
    - docs/notebooks/08_ohp_evaluation.py (the printed headline-table pattern: fixed-width f-string columns)
    - backend/scripts/ohp_headline_chart.py (the grouped-bar offset math x = np.arange(n); w = 0.26; ax.bar(x + (i-1)*w, ...), per-bar ax.text annotation, fig.savefig dpi=120 bbox_inches tight)
    - backend/scripts/ohp_eval_viz.py (multi-subplot skeleton fig, ax = plt.subplots(...) + fig.tight_layout() before savefig)
    - backend/scripts/render_squat_result.py (lines 1-33: matplotlib.patches usage — the only patches analog; the methodology diagram is net-new)
    - backend/training/aqa/eval/metrics.py (the f1_per_error signature — reused UNCHANGED, do NOT redefine F1)
    - .planning/phases/08-ensemble-evaluation-visualization-pack/08-RESEARCH.md (Pattern 1 validation asserts for all 3 exercises; Pattern 2 master table; Pattern 3 methodology diagram spec; Pitfalls 1-5)
    - .planning/phases/08-ensemble-evaluation-visualization-pack/08-PATTERNS.md (per-figure analog assignments + the 5 carried pitfalls)
  </read_first>
  <action>
    Create `docs/notebooks/12_full_evaluation.py` as a jupytext percent-format source (`# %% [markdown]` for prose, `# %%` for code), mirroring `11_shallow_squat_evaluation.py`'s structure. Match the analog's imports exactly — the analog .py notebooks start straight with stdlib imports, NOT with `from __future__ import annotations`: use `import pickle`, `from pathlib import Path`, `import matplotlib.pyplot as plt`, `import numpy as np`, plus `from backend.training.aqa.eval.metrics import f1_per_error`. Do NOT call `matplotlib.use("Agg")` inside the notebook (the notebook backend is already set; that call is only for standalone scripts).

    Cells, in order:

    1. Title markdown: "# Full Evaluation — All 3 Exercises (5 Errors)". One-paragraph framing: consolidated cross-exercise synthesis assembled LOCALLY from the three committed `results.pkl` (no GPU, no re-run); identical metric (F1) and official splits as Parmar et al. (ECCV 2022). State the 5-error headline spine in prose: Squat KIE 0.420 / KFE 0.841 (macro 0.6304), OHP Elbows 0.447 / Knees 0.877 (macro 0.6622), Shallow-Squat 0.8902.

    2. Setup cell: load all three pickles using the dual-path `_CANDS` fallback pattern from notebook 11 (each pickle gets its own `_CANDS = [Path(repo-relative), Path("../../" + repo-relative)]`; `pickle.load(open(next(p for p in _CANDS if p.exists()), "rb"))`). Bind `squat`, `ohp`, `ss`. Resolve `FIG = Path("docs/figures") if Path("docs/figures").exists() else Path("../../docs/figures")` and the same for `EVAL`; then `FIG.mkdir(parents=True, exist_ok=True)`. Assert each pickle loaded.

    3. No-fabrication VALIDATION cell (the structural enforcement — RESEARCH Pattern 1). For each of the 5 errors, recompute F1 from the pickle raw score array + the STORED threshold (do NOT re-sweep on test), then assert abs(computed - stored) < 1e-6:
       - Squat: ens = np.asarray(squat["raw"]["ens_test_scores"]); labs = np.asarray(squat["raw"]["test_labels"]).astype(int); thr = squat["final"]["thresholds"]. kie_pred = (ens[:,0] >= thr["KIE"]).astype(int); kfe_pred = (ens[:,1] >= thr["KFE"]).astype(int). Assert f1_per_error(labs[:,0], kie_pred) equals squat["final"]["test_f1"]["KIE"] within 1e-6; same for KFE. Also assert abs((kie_f1+kfe_f1)/2 - squat["final"]["test_macro"]) < 1e-6 (macro = arithmetic mean — Pitfall 3).
       - OHP: ens = np.asarray(ohp["ensemble_test_scores"]); labs = np.asarray(ohp["test_labels"]).astype(int); thr = ohp["thresholds"]. elbow_pred = (ens[:,0] >= thr["elbows"]); knee_pred = (ens[:,1] >= thr["knees"]). Assert against ohp["test_f1"]["elbows"] / ["knees"]; macro against ohp["test_f1"]["macro"].
       - Shallow: ens = np.asarray(ss["ensemble_test_scores"]); labs = np.asarray(ss["test_labels"]).astype(int); thr = float(ss["best_threshold"]). NOTE ens is 1-D (540,) — apply pred = (ens >= thr).astype(int), NEVER ens[:,0] (Pitfall 2). Assert against ss["test_f1"]["cvcspc_ensemble"].
       Print a one-line "all 5 headline F1 recomputed == stored (<=1e-6)" confirmation after the asserts pass.

    4. EVAL-01 master-table cell: build the 5-row table (RESEARCH Pattern 2) reading every value from pickle keys — columns Exercise/Error, Modality, Ours, Parmar SSL, Parmar baseline. Rows from squat["final"]["test_f1"], squat["comparison"]["paper_md"], squat["comparison"]["paper_kinetics"]; ohp["test_f1"], ohp["paper_targets"] (baseline = None); ss["test_f1"]["cvcspc_ensemble"], ss["paper_targets"]["cvcspc"] (baseline = None). Print as a fixed-width table; show a dash for the 3 None baseline cells (OHP Elbows/Knees, Shallow have no paper supervised row).

    5. Figure cell — all_errors_vs_paper.png: grouped bar, 5 error groups (two-line labels for Squat KIE, Squat KFE, OHP Elbows, OHP Knees, Shallow-Squat). Three series: Ours, Parmar SSL, Parmar baseline. ours = [kie_f1, kfe_f1, elb_f1, kne_f1, ss_f1] (from the validated locals). paper_ssl from the same paper_md / paper_targets keys. paper_base has real values only for indices 0,1 (Squat) — draw baseline bars ONLY for Squat KIE/KFE (base_x = [x[i]+w for i in range(2)]), skip OHP/Shallow baseline entirely. figsize=(13, 5.5), value annotations on every bar via the ax.text pattern (offset +0.008), ax.set_ylim(0, 1.05), title "All 5 errors — FitNova vs Parmar et al. (ECCV 2022)", ax.legend(), ax.grid(axis="y", alpha=0.3). plt.savefig(FIG / "all_errors_vs_paper.png", dpi=130, bbox_inches="tight") BEFORE plt.show().

    6. Figure cell — cross_exercise_macro.png: 3 exercise groups (Squat, OHP, Shallow-Squat), 2 series (Ours vs Parmar SSL). Macros: Squat squat["final"]["test_macro"], OHP ohp["test_f1"]["macro"], Shallow ss["test_f1"]["cvcspc_ensemble"] (single error = its own macro). Paper: Squat (squat["comparison"]["paper_md"]["KIE"] + ["KFE"]) / 2, OHP (ohp["paper_targets"]["elbows"] + ["knees"]) / 2, Shallow ss["paper_targets"]["cvcspc"]. Same offset / annotation / save pattern; save before show.

    7. Figure cell — methodology_diagram.png (net-new schematic, RESEARCH Pattern 3, no fabricated numbers): pure box-and-arrow figure drawn with ax.text boxes + matplotlib.patches.FancyArrowPatch (or ax.annotate arrowprops). Left branch: Video errors (Squat KIE/KFE, OHP Elbows/Knees) -> Motion-Disentangling SSL (barbell half-cycle contrast) -> R(2+1)D-18 (32-frame, 112px, Kinetics-init) -> multi-label head. Right branch: Image errors (Shallow-Squat depth) -> CVCSPC pose-contrastive SSL (phase anchor/pos/neg) -> ResNet-18 (224px, ImageNet-init) -> single binary head. Both merge into: Official train/val/test splits -> F1-per-error vs Parmar (ECCV 2022). ax.axis("off"). Save before show. No numbers anywhere in this figure.

    8. Figure cell — highlights_panel.png: fig, ax = plt.subplots(1, 3, figsize=(14, 5)) — one column per exercise. Each panel shows a compact text summary pulled from pickle keys: headline F1, SSL lift, val->test gap. Squat: macro squat["final"]["test_macro"], lift computed in code as squat["final"]["test_macro"] - squat["comparison"]["phase3"]["macro"] (do not hard-type the lift); OHP: ohp["test_f1"]["macro"], ohp["ssl_lift"], ohp["val_test_gap"]; Shallow: ss["test_f1"]["cvcspc_ensemble"], ss["ssl_lift"], ss["val_test_gap"]. Use ax[i].text(0.5, y, line, transform=ax[i].transAxes, ha="center") and ax[i].axis("off"), or small horizontal bars. Do NOT duplicate the existing per-phase PR/confusion/score-dist figures. fig.tight_layout() then savefig before show.

    9. Figure-existence assertion cell: after all figure cells, loop over the four names (all_errors_vs_paper, cross_exercise_macro, methodology_diagram, highlights_panel) and assert (FIG / (name + ".png")).exists() for each, with a clear failure message. Print a "4/4 master figures written" confirmation.

    CLEAN CODE: NO AI-narration comments anywhere (no "per D8", "mirrors Phase 6", "as per CONTEXT", citation comments). Markdown cells and section headers are fine; a one-line `#` comment is allowed ONLY for a non-obvious WHY (e.g. the 1-D Shallow shape, the stored-threshold rule). No hand-typed F1-literal dicts in any code cell.
  </action>
  <verify>
    <automated>python -m py_compile docs/notebooks/12_full_evaluation.py</automated>
  </verify>
  <acceptance_criteria>
    - `docs/notebooks/12_full_evaluation.py` exists and compiles as valid Python (`python -m py_compile` exits 0)
    - File contains `from backend.training.aqa.eval.metrics import f1_per_error` (Grep returns >=1 match)
    - File loads all three pickles (Grep `results.pkl` returns >=3 occurrences across the 3 `_CANDS` lists)
    - File contains a `< 1e-6` assertion for the no-fabrication validation cell (Grep `1e-6` returns >=5 occurrences — one per error)
    - File references `ss["ensemble_test_scores"]` and thresholds it via `(ens >=` — Grep confirms NO `ens[:, 0]` / `ens[:,0]` applied to the Shallow array (Shallow is 1-D)
    - File contains a `savefig` reference for each of the 4 figure names — Grep each of `all_errors_vs_paper`, `cross_exercise_macro`, `methodology_diagram`, `highlights_panel` returns a match near `savefig`
    - No hand-typed F1 literal in a code cell — Grep `0.8902`, `0.6304`, `0.4474` returns matches ONLY inside `# %% [markdown]` prose or a `#` WHY-comment, never as an assigned value
    - No AI-narration comment — case-insensitive Grep for `per D`, `mirrors`, `as per`, `per CONTEXT` returns zero matches in code lines
  </acceptance_criteria>
  <done>`12_full_evaluation.py` is a valid jupytext source with: 3-pickle dual-path load, a 5-error recompute-and-assert validation cell (<=1e-6 vs stored test_f1, stored thresholds, macro as arithmetic mean), an EVAL-01 master-table print, 4 figure-build cells reading every value from pickle keys, and a figure-existence assert cell. No hand-typed F1 in code, no AI-narration comments.</done>
</task>

<task type="auto">
  <name>Task 2: Pair to .ipynb, pre-execute via nbconvert, confirm 4 PNGs written + AQA suite green</name>
  <files>docs/notebooks/12_full_evaluation.ipynb, docs/figures/all_errors_vs_paper.png, docs/figures/cross_exercise_macro.png, docs/figures/methodology_diagram.png, docs/figures/highlights_panel.png</files>
  <read_first>
    - docs/notebooks/12_full_evaluation.py (the source authored in Task 1 — confirm it is complete before pairing)
    - docs/notebooks/README.md (lines 55-58: the Reproduce command `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/<name>.ipynb` — the exact pre-execution invocation used for every notebook 01-11)
    - .planning/phases/08-ensemble-evaluation-visualization-pack/08-VALIDATION.md (sampling rate: suite green after every task; notebook executes cleanly with the in-notebook asserts running)
  </read_first>
  <action>
    Run from the repo root (the in-notebook `_CANDS` dual-path resolves the repo-relative branch when cwd is the repo root):
    1. Pair the source to a notebook: `jupytext --to notebook docs/notebooks/12_full_evaluation.py` (produces `docs/notebooks/12_full_evaluation.ipynb`).
    2. Pre-execute it in place: `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/12_full_evaluation.ipynb`. This MUST exit 0 — the in-notebook validation asserts (the 5 F1 recompute checks at <=1e-6) and the 4 figure-existence asserts run here. If nbconvert raises an AssertionError / KeyError / IndexError, the fix is in Task 1's `.py` source: correct the cell, re-run `jupytext --to notebook` to regenerate the `.ipynb`, then re-execute. Do NOT hand-edit the `.ipynb` JSON.
    3. Confirm all 4 PNGs now exist in `docs/figures/`.
    4. Run the AQA regression gate: `python -m pytest backend/training/aqa/ -m "not slow" -q` — expect exactly `45 passed, 4 deselected` (Phase 8 adds no production code; this confirms no import-time regression).
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace docs/notebooks/12_full_evaluation.ipynb</automated>
    <automated>python -m pytest backend/training/aqa/ -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/12_full_evaluation.ipynb` exits 0 (all in-notebook asserts pass during execution)
    - `docs/notebooks/12_full_evaluation.ipynb` exists and has executed (non-empty) output cells
    - All 4 figure files exist (Glob `docs/figures/all_errors_vs_paper.png`, `cross_exercise_macro.png`, `methodology_diagram.png`, `highlights_panel.png` each return the file)
    - `python -m pytest backend/training/aqa/ -m "not slow" -q` outputs `45 passed, 4 deselected`
  </acceptance_criteria>
  <done>`12_full_evaluation.ipynb` is paired and pre-executed (nbconvert exits 0, in-notebook validation + figure-exists asserts all pass), all 4 master PNGs exist in `docs/figures/`, and the AQA suite is green (45 passed / 4 deselected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary crossed — local read of committed repo pickles + write of docs/figures/notebook |

## STRIDE Threat Register

No new attack surface — local documentation/figure generation from committed artifacts; no runtime, network, input, authentication, session, access-control, or cryptography changes. The only "inputs" are three committed repo `results.pkl` files (not user-controlled). ASVS V2 (auth) / V3 (sessions) / V4 (access control) / V6 (crypto) N/A; V5 (input validation) trivially satisfied — inputs are committed repo files.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | results.pkl number integrity (silent drift / hand-typed literal) | mitigate | In-notebook validation cell recomputes each of the 5 headline F1 from raw scores + stored threshold via `f1_per_error` and asserts <=1e-6 equality with stored `test_f1` — fails the build on any drift |
| T-08-02 | Tampering | pickle deserialization | accept | `pickle.load` runs only on three repo-committed files authored by prior phases; no external/untrusted pickle source |
</threat_model>

<verification>
- AQA suite green: `python -m pytest backend/training/aqa/ -m "not slow" -q` outputs `45 passed, 4 deselected`
- Notebook pre-executes cleanly: `jupyter nbconvert --to notebook --execute --inplace docs/notebooks/12_full_evaluation.ipynb` exits 0 (runs the 5-error <=1e-6 validation asserts + the 4 figure-exists asserts)
- All 4 master figures exist in `docs/figures/`
- No hand-typed F1 in code cells; no AI-narration comments (Grep gates in Task 1 acceptance criteria)
</verification>

<success_criteria>
- `docs/notebooks/12_full_evaluation.{py,ipynb}` created; the `.ipynb` is pre-executed
- Every one of the 5 headline F1 values is recomputed-in-code and asserted equal (<=1e-6) to the stored `test_f1` — structural no-fabrication enforcement (EVAL-01, D8)
- The EVAL-01 master comparison (ours vs Parmar baseline + SSL, 5 errors) renders as a printed table and `all_errors_vs_paper.png` (EVAL-01)
- All 4 master figures (`all_errors_vs_paper`, `cross_exercise_macro`, `methodology_diagram`, `highlights_panel`) exist and render (EVAL-03)
- AQA suite stays green (45 passed / 4 deselected)
</success_criteria>

<output>
Create `.planning/phases/08-ensemble-evaluation-visualization-pack/08-01-SUMMARY.md` when done.
</output>
