---
phase: 05-backend-inference-integration-squat
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/models/form_model_squat_md/README.md
  - .gitignore
  - backend/scripts/probe_squat_inference.py
autonomous: false
requirements: [API-01, API-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The 3 Phase-4 best.pt files are staged at backend/models/form_model_squat_md/seed{42,1337,7}/best.pt"
    - "SquatFormService loads all 3 staged seeds and reports model_ready True"
    - "Per-clip CPU forward latency is measured (single-seed and ensemble) on the target machine and logged"
    - "The model runs end-to-end on a real user phone-camera squat clip and its KIE/KFE confidences are recorded for domain-shift diagnosis"
  artifacts:
    - path: "backend/models/form_model_squat_md/README.md"
      provides: "documented Drive-to-local staging paths for the 3 best.pt files"
      contains: "md_finetune_seed"
    - path: "backend/scripts/probe_squat_inference.py"
      provides: "latency probe + single-clip classify on a real clip"
      contains: "classify_clip"
  key_links:
    - from: "backend/scripts/probe_squat_inference.py"
      to: "backend/services/squat_form_service.py"
      via: "SquatFormService.classify_clip"
      pattern: "SquatFormService"
    - from: ".gitignore"
      to: "backend/models/form_model_squat_md/"
      via: "ignore the .pt weights, keep the README"
      pattern: "form_model_squat_md"
---

<objective>
Stage the Phase-4 production weights (3x `best.pt`) from Google Drive into `backend/models/form_model_squat_md/`, verify `SquatFormService` loads all 3 seeds, MEASURE per-clip CPU latency (single-seed vs ensemble — D-08), and run the model end-to-end on a REAL user phone-camera squat clip to surface domain shift EARLY (D-11) — before Plan 03 polishes the full API.

Purpose: Two human-required gates the code cannot self-satisfy — (1) the weights are not in the repo (D-09: ~378 MB, too large), and (2) only the user can provide a real phone-camera clip to test the offline-to-live domain gap that Phase 4 explicitly flagged ([[project_realtime_demo_expectation]]). Running the service on real weights + real input now catches preprocessing/threshold problems while there is still time to mitigate.
Output: staged weights, a `.gitignore` entry + a committed README documenting the staging step, a `probe_squat_inference.py` script that times the forward and classifies a clip, and the recorded real-clip confidences.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-backend-inference-integration-squat/05-CONTEXT.md
@.planning/phases/05-backend-inference-integration-squat/05-RESEARCH.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-04-SUMMARY.md

<interfaces>
<!-- Drive source paths (Phase-4 SUMMARY, VERIFIED) and the service API the probe consumes. -->
Drive source (user's Google Drive):
- `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed42/best.pt`
- `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed1337/best.pt`
- `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed7/best.pt`

Local target layout (what SquatFormService._load expects — Plan 01):
- `backend/models/form_model_squat_md/seed42/best.pt`
- `backend/models/form_model_squat_md/seed1337/best.pt`
- `backend/models/form_model_squat_md/seed7/best.pt`

Service API (from Plan 01):
- `SquatFormService(model_dir).model_ready -> bool`
- `SquatFormService.classify_clip(frames_tchw_uint8, *, n_seeds=None) -> dict`  (D-05 schema)
- For a video file path: `idx = uniform_sample_indices(total_frames, 32, jitter=0)` then `frames = decode_clip(path, idx)` (uint8 [32,3,H,W]) then pass to `classify_clip`.

Measured baseline (RESEARCH §4, base architecture, no real weights): single-seed ~0.93 s, ensemble ~2.8 s; fp16-on-CPU HANGS.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Stage the 3 best.pt weights from Drive (human action)</name>
  <read_first>
    - backend/services/squat_form_service.py (the seed-dir layout the staged files must match)
    - .planning/phases/04-squat-motion-disentangling-ssl/04-04-SUMMARY.md (the Drive paths + production-checkpoint declaration)
  </read_first>
  <action>
    This is a human-action gate — the executor does NOT download the files (the weights live in the user's Google Drive). The executor's job: present the exact staging instructions below, then PAUSE and wait for the user to stage the 3 `best.pt` and reply with their sizes. On resume, sanity-check the reported sizes (~126 MB each) before Task 2 attempts to load them. Do not proceed past this gate until the user replies "staged" or "skip".
  </action>
  <what-built>Plan 01 built `SquatFormService`, which loads `seed{42,1337,7}/best.pt` from `backend/models/form_model_squat_md/`. The weights are NOT in the repo (D-09: 3x ~126 MB = ~378 MB, too large for git). The user must stage them from Google Drive.</what-built>
  <how-to-verify>
    1. On the machine running the backend, create the directories: `backend/models/form_model_squat_md/seed42/`, `.../seed1337/`, `.../seed7/`.
    2. Download from Google Drive and place each file:
       - `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed42/best.pt`  -> `backend/models/form_model_squat_md/seed42/best.pt`
       - `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed1337/best.pt` -> `backend/models/form_model_squat_md/seed1337/best.pt`
       - `MyDrive/FitNova/checkpoints/phase04/md_finetune_seed7/best.pt`   -> `backend/models/form_model_squat_md/seed7/best.pt`
    3. Confirm each `best.pt` is ~126 MB (not a small Drive HTML "download too large" stub).
    4. Reply with the three file sizes so the executor can sanity-check before loading.
  </how-to-verify>
  <resume-signal>Type "staged" with the three file sizes, or "skip" to proceed with model_ready=False (the latency probe + domain-shift test then become Plan-03 follow-ups).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Staging README + .gitignore entry + load verification</name>
  <read_first>
    - .gitignore (the existing form-analysis-pipeline ignore block ~lines 100-160 — match the comment style; note `pose_landmarker_full.task` is already ignored at line 120)
    - backend/services/squat_form_service.py (the `_load` seed-dir layout this README documents)
    - .planning/phases/05-backend-inference-integration-squat/05-RESEARCH.md (§ 6 Weights Transfer — README + .gitignore recommendation)
  </read_first>
  <action>
    1. Add a `.gitignore` block under the form-analysis-pipeline section that ignores the staged weights but KEEPS the README: ignore `backend/models/form_model_squat_md/seed*/` (and `backend/models/form_model_squat_md/**/*.pt`), then a negation `!backend/models/form_model_squat_md/README.md`. Comment: the 3 best.pt (~378 MB) are staged from Drive, never committed (D-09).
    2. Create `backend/models/form_model_squat_md/README.md` (committed): the Drive source paths, the local target layout, ~126 MB-per-file expected size, that `SquatFormService` auto-detects `seed{42,1337,7}/best.pt`, the production protocol (3-seed mean-of-sigmoids; thresholds KIE 0.614 / KFE 0.385; no TTA — D-01), and the graceful-degradation note (absent weights -> model_ready=False -> endpoints return neutral output).
    3. If the human-action task reported "staged": verify load — `SquatFormService(model_dir="backend/models/form_model_squat_md")` has `model_ready is True` and `len(svc._models) == 3`. If "skip": document model_ready=False in the SUMMARY and stop after steps 1-2.
  </action>
  <files>backend/models/form_model_squat_md/README.md, .gitignore</files>
  <verify>
    <automated>python -m pytest backend/tests/test_squat_form_api.py::test_service_loads -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore backend/models/form_model_squat_md/seed42/best.pt` prints the path (ignored)
    - `backend/models/form_model_squat_md/README.md` exists, is NOT ignored (`git check-ignore` on it prints nothing), and contains the string `md_finetune_seed`
    - If weights staged: a Python check shows `SquatFormService(model_dir="backend/models/form_model_squat_md").model_ready` is True and exactly 3 models loaded; recorded in the SUMMARY
    - `test_service_loads` still passes
  </acceptance_criteria>
  <done>The staged-weights directory is documented + correctly gitignored (README tracked, .pt ignored), and either all 3 seeds load (model_ready True) or the skip path is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Latency probe + early real-clip domain-shift test (D-08 + D-11)</name>
  <read_first>
    - backend/services/squat_form_service.py (classify_clip + the n_seeds knob; the preprocessing it expects)
    - backend/training/aqa/datasets/transforms.py (uniform_sample_indices + decode_clip — how the probe turns a video path into the [32,3,H,W] uint8 the service wants)
    - backend/scripts/build_exercise_videos.py (OPTIONAL — an existing backend/scripts/* module for the script header/style convention)
    - .planning/phases/05-backend-inference-integration-squat/05-RESEARCH.md (§ 4 CPU Latency — measurement protocol; § Open Questions 1 domain-shift; § 2 single-rep fallback)
    - .planning/phases/04-squat-motion-disentangling-ssl/04-04-SUMMARY.md (expected behavior: strong KFE ~97% recall, modest KIE ~47% — the baseline to compare the real-clip output against)
  </read_first>
  <action>
    Create `backend/scripts/probe_squat_inference.py` (a standalone diagnostic script, run with `python -m backend.scripts.probe_squat_inference <clip.mp4>`; not wired into the server). Header: `from __future__ import annotations`, module docstring (latency probe + domain-shift diagnostic; D-08 + D-11), print to stdout.
    It must:
    1. Construct `SquatFormService(model_dir="backend/models/form_model_squat_md")`; if not model_ready, print a clear "weights not staged" message and exit non-zero.
    2. LATENCY (D-08): build a synthetic `np.zeros((32,3,128,128), uint8)` clip (or decode the provided clip's first 32 frames); time `classify_clip(clip, n_seeds=1)` and `classify_clip(clip, n_seeds=3)` over 3 runs each; print median single-seed and ensemble per-clip seconds (confirms RESEARCH §4 ~0.93 s / ~2.8 s on THIS machine with REAL weights).
    3. REAL CLIP (D-11): take the clip path from argv; `total = len(read_video_timestamps)`; `idx = uniform_sample_indices(total, 32, jitter=0)`; `frames = decode_clip(path, idx)`; convert to numpy uint8 [32,3,H,W]; run `classify_clip(frames.numpy(), n_seeds=3)`; print the full D-05 response (KIE + KFE confidence, detected, severity_word) AND the raw ensemble sigmoid scores before thresholding.
    4. fp32 only — the script must not call `.half()` (Pitfall 4 — hangs on CPU).
    Then run the script on the user's real phone-camera squat clip (provided at the checkpoint below) and capture the output.
  </action>
  <files>backend/scripts/probe_squat_inference.py</files>
  <verify>
    <automated>python -c "import backend.scripts.probe_squat_inference"</automated>
  </verify>
  <what-built>A standalone `probe_squat_inference.py` that (a) times the CPU forward for single-seed and ensemble on the staged weights and (b) classifies a real squat clip through the EXACT serving path, printing KIE/KFE confidences + raw sigmoid scores.</what-built>
  <how-to-verify>
    1. The user provides a REAL phone-camera squat clip (a short ~3 s single-rep `.mp4`) — this is the domain-shift test input (D-11). If you don't have one, record one: phone, side-ish angle, one squat rep, good lighting.
    2. Run: `python -m backend.scripts.probe_squat_inference path\to\your_squat.mp4`
    3. Paste back: (a) the single-seed and ensemble median latency, (b) the KIE confidence + detected + severity_word, (c) the KFE confidence + detected + severity_word, (d) the raw ensemble sigmoid scores.
    4. Compare to expected behavior (Phase-4 SUMMARY): KFE should be strong on a clip with knees-forward travel (~97% recall offline); KIE is modest (~47% recall, surfaced as "possible"). A WILDLY off result (e.g. both ~0.0 or both ~1.0 regardless of form) signals a preprocessing/domain-shift problem to diagnose now.
  </how-to-verify>
  <resume-signal>Paste the latency numbers + the KIE/KFE confidences + raw scores. Reply "looks plausible" or describe what looks wrong (which becomes a gap-closure item — thresholds are LOCKED per D-01, so a mismatch is diagnosed, not silently re-tuned here).</resume-signal>
  <done>The probe script exists and imports; with weights staged it has been run on a real phone-camera clip and the latency + KIE/KFE confidences + raw scores are pasted back and recorded in the SUMMARY (the domain-shift datapoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive -> disk (manual staging) | Developer downloads model weights from their own Google Drive to the local repo |
| disk -> process (model load) | `torch.load` deserializes the staged `best.pt` into the probe process |
| local file -> process (real clip) | A user-provided `.mp4` is decoded by `decode_clip`/`read_video` for the domain-shift test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | committed README + .gitignore | mitigate | The README documents Drive paths only (no secrets, no key material). The `.gitignore` negation is scoped to the README; the `.pt` weights stay ignored so 378 MB binaries never enter git history. |
| T-05-05 | Tampering | staged `best.pt` (torch.load weights_only=False) | accept | Same as T-05-01: developer-produced Phase-4 checkpoints from a trusted Drive path; not attacker-supplied. Local single-user demo. |
| T-05-06 | Denial of Service | `decode_clip` on a user-provided clip in the probe | accept | Diagnostic CLI run by the developer on their own clip; not a network-exposed endpoint. The upload-size cap for the network path is added in Plan 03. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH § Package Legitimacy Audit). No install task -> no slopcheck gate needed. |
</threat_model>

<verification>
- `git check-ignore backend/models/form_model_squat_md/seed42/best.pt` prints the path; `git check-ignore` on the README prints nothing (README tracked).
- `test_service_loads` passes; `python -c "import backend.scripts.probe_squat_inference"` exits 0.
- If weights staged: `SquatFormService(...).model_ready` True, 3 models loaded; latency probe prints single-seed + ensemble medians; real-clip probe prints the D-05 response + raw scores (pasted back at the checkpoint).
</verification>

<success_criteria>
- The 3 production seeds are staged and load (model_ready True), OR the skip path is explicitly recorded.
- Per-clip CPU latency is measured on the target machine for both single-seed and ensemble (D-08 evidence: confirms which config is feasible for live vs upload).
- The model has been run end-to-end on a REAL phone-camera squat clip and its KIE/KFE confidences recorded — domain shift is surfaced EARLY (D-11), before the full API is polished.
</success_criteria>

<output>
Create `.planning/phases/05-backend-inference-integration-squat/05-02-SUMMARY.md` when done. Record the staged file sizes, the measured latencies, and the real-clip confidences (the domain-shift datapoint for the dissertation + any follow-up gap).
</output>
