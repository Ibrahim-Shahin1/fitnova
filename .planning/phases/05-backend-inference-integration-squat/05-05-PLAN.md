---
phase: 05-backend-inference-integration-squat
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - backend/_archive_form_v4_v6/README.md
  - backend/scripts/render_squat_result.py
  - .gitignore
autonomous: true
requirements: [API-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The old TF+MediaPipe form subsystem (form_analyzer, form_session, form_geometry, mediapipe_config, exercise_sanity, old form weights, old form training scripts) is moved to backend/_archive_form_v4_v6/, not hard-deleted"
    - "After archival the app still imports and the full backend suite (excluding training) is green"
    - "The recommender / plan-generation feature is untouched and still works"
    - "A rendered visualization overlays the Squat form-error result on a sample clip"
  artifacts:
    - path: "backend/_archive_form_v4_v6/README.md"
      provides: "manifest of archived modules + why (dissertation-appendix evidence)"
      contains: "form_analyzer"
    - path: "backend/scripts/render_squat_result.py"
      provides: "renders KIE/KFE result overlaid on a sample squat clip"
      contains: "classify_clip"
  key_links:
    - from: "backend/app.py"
      to: "backend/services/squat_form_service.py"
      via: "no remaining import of the archived modules"
      pattern: "squat_form_service"
    - from: "backend/scripts/render_squat_result.py"
      to: "backend/services/squat_form_service.py"
      via: "SquatFormService.classify_clip on a sample clip"
      pattern: "SquatFormService"
---

<objective>
Archive (not delete) the old TF+MediaPipe form subsystem now that `app.py` no longer references it (D-06), verify the app + recommender still work, and produce the phase visualization deliverable — a rendered overlay of the Squat KIE/KFE result on a sample clip ([[project_supervisor_visualizations]]).

Purpose: Close API-01's "old subsystem removed" with a reversible archive (dissertation-appendix evidence), and satisfy the standing visualization requirement. This runs LAST because moving the old files is only safe after every app.py rewrite (Plans 03-04) has removed the imports — RESEARCH archival ordering: never leave app.py in a partially-broken import state.
Output: `backend/_archive_form_v4_v6/` (with a README manifest) holding the old modules/weights/training scripts; `.gitignore` updates for the archived binary weights; `backend/scripts/render_squat_result.py` + a rendered figure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-inference-integration-squat/05-CONTEXT.md
@.planning/phases/05-backend-inference-integration-squat/05-RESEARCH.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- The exact archival inventory (RESEARCH § 8, VERIFIED against the filesystem) + the service API for the viz. -->
Services to archive (git-tracked -> use `git mv`):
- backend/services/form_analyzer.py
- backend/services/form_session.py
- backend/services/form_geometry.py
- backend/services/mediapipe_config.py
- backend/services/exercise_sanity.py   (only used by form_session — cascades)
Co-located tests to archive:
- backend/services/test_form_analyzer_v6.py
- backend/services/test_form_session_v6_routing.py
- backend/services/test_form_geometry.py
Old form training code to archive (git-tracked):
- backend/training/models/mt_tcn.py, st_gcn.py, st_gcn_v6.py, st_gcn_v6_1.py (+ their test_*.py)
- backend/training/train_form_model.py, train_form_model_v6.py, train_form_model_v6_1.py, train_ssl_pretrain.py
Old form weight dirs (git-tracked):
- backend/models/form_model/, form_model_v5/, form_model_v5_1/, form_model_v5_2/, form_model_v6/
  (form_model_v1_quality_broken_*, *_backup_* are separately tracked/ignored — see CONCERNS.md stale inventory)
Already gitignored (filesystem move only, NO git rm):
- backend/models/pose_landmarker_full.task (gitignore line 120)
- backend/models/form_model_v6_smoke/, form_model_v6_1_smoke/ (gitignore lines 156-157)

MUST NOT archive (the new service imports these — they are the shared inference contract):
- backend/training/aqa/**  (transforms.py, eval/ensemble.py, eval/metrics.py, harness/md_finetune.py, datasets/squat.py)
- backend/models/form_model_squat_md/  (the new staged weights)
- All recommender services: recommender.py, content_filter.py, neumf_ranker.py, llm_adapter.py, chat_service.py, and backend/models/neumf_*.keras / *.pkl

Service API for the viz (Plan 01):
- `SquatFormService(model_dir).classify_clip(frames_tchw_uint8) -> dict`  (D-05)
- `uniform_sample_indices` + `decode_clip` from transforms.py to sample a clip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive the old form subsystem (git mv) + .gitignore + manifest + green-suite verify</name>
  <read_first>
    - .planning/phases/05-backend-inference-integration-squat/05-RESEARCH.md (§ 8 Removal / Archival Impact — the COMPLETE inventory + the "archive files first" ordering + the app.py-already-clean precondition)
    - .planning/codebase/CONCERNS.md (Stale / Dead Code section — confirms form_geometry/old-weights/old-training-scripts are dead; the existing _archive_* pattern)
    - .gitignore (the form-analysis ignore block; what is already ignored vs tracked)
    - backend/app.py (CONFIRM Plans 03-04 already removed FormAnalyzer/FormSession imports — a `grep -v '^#'` on app.py must show 0 before moving the files; if not, STOP)
  </read_first>
  <action>
    Archive the old subsystem to `backend/_archive_form_v4_v6/` (D-06 — archive, NOT delete; reversible dissertation evidence). PRECONDITION: confirm `app.py` already imports clean (Plans 03-04 done) — a `grep -v '^#'` on app.py shows 0 `FormAnalyzer` and 0 `FormSession`. If it does not, STOP (out of order).
    1. Create the archive tree: `backend/_archive_form_v4_v6/services/`, `.../models/`, `.../training/`.
    2. `git mv` the git-tracked files into the archive (preserves history): the 5 services + 3 co-located tests -> `services/`; the old training models (mt_tcn/st_gcn*) + their tests + the 4 old `train_*` scripts -> `training/`; the old weight dirs (form_model, form_model_v5, v5_1, v5_2, v6) -> `models/`. Use `git mv` per file/dir; `git mv` fails on ignored paths — handle those in step 3.
    3. For the ALREADY-gitignored artifacts (`pose_landmarker_full.task`, `form_model_v6_smoke/`, `form_model_v6_1_smoke/`): move them with a plain filesystem move (not `git mv`) into `backend/_archive_form_v4_v6/models/`. Update `.gitignore` so the moved binary weights under the archive stay ignored — add `backend/_archive_form_v4_v6/models/` (and `backend/_archive_form_v4_v6/**/*.weights.h5`, `**/*.task`) so the ~130 MB of old weights are not re-committed in their new location; keep `backend/_archive_form_v4_v6/README.md` tracked (add a negation if a glob would catch it).
    4. Write `backend/_archive_form_v4_v6/README.md` (tracked): a manifest listing every archived path, the reason (replaced by the PyTorch Squat service in Phase 5 — API-01), the date, and a note that these are kept for dissertation-appendix evidence + ablation, NOT runtime. State explicitly that `backend/training/aqa/**` was NOT archived (it is the shared inference contract the new service imports).
    5. VERIFY the app still works: importing `backend.app` succeeds (the test suite imports it at collection time), and the backend suite excluding training + the now-archived tests is green.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_squat_form_api.py backend/tests/test_app.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/services/` no longer contains `form_analyzer.py`, `form_session.py`, `form_geometry.py`, `mediapipe_config.py`, `exercise_sanity.py` (all under `backend/_archive_form_v4_v6/services/`)
    - `backend/_archive_form_v4_v6/README.md` exists, is tracked (not gitignored), and contains `form_analyzer`
    - `git check-ignore backend/_archive_form_v4_v6/models/form_model_v6/v6_supervised.weights.h5` prints the path (archived weights stay ignored)
    - `backend/training/aqa/` is INTACT (transforms.py, eval/ensemble.py, harness/md_finetune.py still present — NOT archived)
    - `python -c "import backend.app"` exits 0 and `python -m pytest backend/tests/test_squat_form_api.py backend/tests/test_app.py -x -q` exits 0 (recommender + new form path both fine)
  </acceptance_criteria>
  <done>The old form subsystem is reversibly archived with a manifest, the shared aqa inference contract + recommender are untouched, the archived binary weights stay out of git, and the app + suite are green — API-01's "old subsystem removed" is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Visualization — render Squat KIE/KFE result on a sample clip</name>
  <read_first>
    - backend/services/squat_form_service.py (classify_clip — the D-05 result to render)
    - backend/training/aqa/datasets/transforms.py (uniform_sample_indices + decode_clip — sample frames from a clip for the overlay)
    - backend/scripts/build_exercise_videos.py (OPTIONAL — an existing backend/scripts/* for header/style + how scripts locate assets)
    - .planning/phases/04-squat-motion-disentangling-ssl/04-04-SUMMARY.md (the figures/ deliverable convention from Phase 4; expected behavior to caption — strong KFE, modest KIE)
    - .planning/phases/05-backend-inference-integration-squat/05-CONTEXT.md (§ domain item 7 — "rendered result overlaid on a sample clip")
  </read_first>
  <action>
    Create `backend/scripts/render_squat_result.py` (run with `python -m backend.scripts.render_squat_result <clip.mp4> [--out figure.png]`; standalone, not wired into the server) — the phase visualization deliverable ([[project_supervisor_visualizations]]).
    It must:
    1. Construct `SquatFormService(model_dir="backend/models/form_model_squat_md")`. If not model_ready, print a clear message and exit non-zero (the figure needs real weights from Plan 02).
    2. Sample a representative frame grid from the clip: `total = len(read_video_timestamps)`; `idx = uniform_sample_indices(total, 32, jitter=0)`; `frames = decode_clip(path, idx)`; pick ~6 evenly-spaced frames for display.
    3. Run `classify_clip(frames.numpy())` to get the D-05 result.
    4. Render with matplotlib: the 6 sample frames in a row, and a caption/legend panel showing per-error `type / detected / confidence / severity_word` for KIE and KFE (binary + the confidence-derived word; NO raw percentage as the headline per D-05 — show the word, with the confidence in small text for the dissertation). Color-code detected vs not. Save to `backend/scripts/out/squat_result_overlay.png` (or the `--out` path). Print the saved path.
    5. fp32 only (no `.half()` — Pitfall 4). Use a non-interactive matplotlib backend (`matplotlib.use("Agg")`) so it runs headless.
    Then run it on the user's real clip (the one staged in Plan 02) and confirm the figure is produced.
  </action>
  <verify>
    <automated>python -c "import importlib.util, sys; sys.exit(0 if importlib.util.find_spec('backend.scripts.render_squat_result') else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/render_squat_result.py` exists, contains `classify_clip`, `matplotlib`, `uniform_sample_indices`, and `decode_clip`
    - The module imports without error (`python -c "import backend.scripts.render_squat_result"` exits 0)
    - Source contains NO `.half(` and uses `matplotlib.use("Agg")`
    - When run on a real clip with weights staged, it writes a PNG showing sample frames + the KIE/KFE detected/severity result, and prints the saved path (recorded in the SUMMARY)
    - The figure surfaces binary detection + severity word (not a raw percentage as the headline), consistent with D-05
  </acceptance_criteria>
  <done>A headless visualization script renders the Squat form-error result (KIE/KFE detected + severity, sample frames) on a clip and produces a committed-or-recorded figure for the supervisor demo / dissertation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> process (model load) | The viz script `torch.load`s the staged `best.pt` to render a result |
| local file -> process (sample clip) | A clip path is decoded by `decode_clip`/`read_video` for the overlay |
| (no network surface in this plan — archival is filesystem; the viz is a CLI) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering / Repudiation | `git mv` of tracked weight dirs | mitigate | Use `git mv` (preserves history; reversible); archive READMEd; binary weights re-added to `.gitignore` under the archive path so they are not duplicated into history. Reversible per D-06 (archive, not delete). |
| T-05-17 | Denial of Service | `decode_clip` on a clip in the viz CLI | accept | Diagnostic/visualization CLI run by the developer on their own clip; not a network-exposed endpoint. |
| T-05-13 | Tampering | `torch.load(best.pt, weights_only=False)` in the viz | accept | Developer-produced Phase-4 checkpoints from a trusted Drive path (D-09); not attacker-supplied. Local single-user. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH § Package Legitimacy Audit — matplotlib is already used for Phase-4 figures; torch/opencv already installed). No install task -> no slopcheck gate. |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_squat_form_api.py backend/tests/test_app.py -x -q` is green AFTER the archive move (proves app.py + recommender + new form path survive).
- `backend/training/aqa/` is intact; `backend/services/` no longer has the 5 old form modules.
- `git check-ignore` on an archived `.weights.h5` prints the path (old weights stay ignored); the archive README is tracked.
- The viz script imports and (with weights staged) produces a PNG; recorded in the SUMMARY.
- Manual (optional): `python -m backend.scripts.render_squat_result path\to\clip.mp4` writes `squat_result_overlay.png`.
</verification>

<success_criteria>
- The old TF+MediaPipe form subsystem is reversibly archived (API-01 closed), the shared aqa contract + recommender are untouched, and the app + suite are green.
- A rendered Squat KIE/KFE overlay figure exists for the supervisor demo / dissertation appendix.
</success_criteria>

<output>
Create `.planning/phases/05-backend-inference-integration-squat/05-05-SUMMARY.md` when done. This is the final plan — also note in the SUMMARY any domain-shift follow-up surfaced by Plan 02 so the phase verification picks it up.
</output>
