---
phase: 04-squat-motion-disentangling-ssl
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - backend/training/aqa/harness/md_finetune.py
  - backend/training/aqa/harness/test_md_finetune.py
  - backend/training/aqa/notebooks/04_squat_md_ssl.py
autonomous: false
requirements: [SQUAT-05]
must_haves:
  truths:
    - "md_finetune.run_md_finetune_epoch fine-tunes the MD backbone (loaded from Plan 02's backbone.pt, NOT Kinetics) on the labeled 1136-clip Squat train split with a fresh Dropout(0.2)+Linear(512,2) head, AdamW (lr=1e-4, wd=1e-4), BCEWithLogitsLoss(pos_weight), cosine annealing, 50-epoch budget with 8-epoch val-macro-F1 early-stop — paralleling Phase 3's run_supervised_epoch with the 4 D3 deltas (AdamW not Adam, dropout head, MD init not Kinetics, the D6 runtime overfit monitor)"
    - "The D6 runtime overfit monitor fires correctly: if the train/val BCE loss ratio exceeds 10x before epoch 10, the seed aborts and the trainer returns an aborted_overfit flag so the notebook can bump regularization (wd=5e-4, dropout=0.3); seed 42 runs FIRST and on a trip its bumped recipe is locked for seeds 1337 and 7 (ensemble homogeneity, D6)"
    - "Three fine-tune seeds (42, 1337, 7) run sequentially on one L4 from the SHARED Plan 02 backbone, each producing a best.pt at phase04/md_finetune_seed{42,1337,7}/best.pt; each seed's val macro-F1 at its best epoch is at or above Phase 3's val-best 0.5454 (SQUAT-05-d — SSL transferred; NOTE 0.5454 = Phase 3 VAL-best macro-F1 at epoch 3 per 03-01-SUMMARY.md, the correct bar for fine-tune val monitoring — distinct from the 0.5429 TEST bar used in Plan 04 / SQUAT-05-e) OR the gap is honestly documented per the D6 no-lift policy"
    - "Each fine-tune is disconnect-safe: checkpoints every epoch via the Phase 2 atomic-write contract (best.pt write uses the Phase 3 best.pt contract), resumes from latest.txt with map_location='cpu', and a mid-run disconnect on any seed resumes cleanly without restarting that seed"
    - "Per-seed val macro-F1 numbers are recorded (variance evidence for the Plan 04 ensemble) — proving any ensemble lift is real and not one lucky seed (D4 Lakshminarayanan rationale)"
  artifacts:
    - path: "backend/training/aqa/harness/md_finetune.py"
      provides: "FINALIZED — run_md_finetune_epoch (parallels supervised_train.run_supervised_epoch + the 4 D3 deltas + the D6 runtime train/val-loss-ratio monitor with the aborted_overfit early-return); _build_dataloaders/_val_pass either reused from supervised_train patterns or copied (D9 — do NOT edit supervised_train.py)"
      contains: "def run_md_finetune_epoch"
    - path: "backend/training/aqa/harness/test_md_finetune.py"
      provides: "EXTENDED — adds test_d6_overfit_monitor (the train/val loss ratio > 10x before epoch 10 path returns aborted_overfit=True) on top of the Plan 01 SQUAT-05-a build test"
      contains: "test_d6_overfit_monitor"
    - path: "backend/training/aqa/notebooks/04_squat_md_ssl.py"
      provides: "Step 6 appended — 3-seed fine-tune (seed 42 first with the D6 reg-bump gate, then 1337 + 7 at the locked recipe), each a resume=True cell; per-seed val macro-F1 recorded"
  key_links:
    - from: "backend/training/aqa/harness/md_finetune.py"
      to: "Plan 02 backbone.pt (My Drive/FitNova/checkpoints/phase04/md_pretrain_v1/backbone.pt)"
      via: "build_finetune_model(md_backbone_path) loads backbone_state_dict via load_state_dict(strict=False), map_location='cpu'"
      pattern: "load_state_dict.*backbone_state_dict"
    - from: "backend/training/aqa/harness/md_finetune.py"
      to: "backend/training/aqa/datasets/squat.py (SquatKIEKFEDataset — the labeled pipeline + pos_weight, reused UNCHANGED)"
      via: "import + _build_dataloaders constructs the labeled train/val/test loaders"
      pattern: "from backend.training.aqa.datasets.squat import SquatKIEKFEDataset"
    - from: "backend/training/aqa/harness/md_finetune.py"
      to: "backend/training/aqa/eval/metrics.py (f1_per_error, pr_auc_per_error — val metrics per epoch) + colab.py (atomic checkpoint, resume, RNG, prune)"
      via: "import"
      pattern: "from backend.training.aqa.eval.metrics import"
---

<objective>
Fine-tune the MD-pretrained backbone (Plan 02's backbone.pt) for joint KIE/KFE detection across 3 seeds (42, 1337, 7), producing 3 best.pt checkpoints for Plan 04's ensemble (SQUAT-05). This finalizes md_finetune.py's training loop (paralleling Phase 3's run_supervised_epoch with the 4 D3 deltas + the D6 runtime overfit monitor) and runs the 3 fine-tunes sequentially on one L4 with the seed-42-first reg-bump policy.

Purpose: SQUAT-05 + ROADMAP Phase 4 success criterion 2 ("The MD-pretrained model, fine-tuned for KIE/KFE, reports F1 at or above the Phase-3 baseline"). The fine-tune recipe is locked by CONTEXT D3 (AdamW wd=1e-4, dropout=0.2 head-only, lr=1e-4, 50ep/8-patience, flip OFF) and confirmed by RESEARCH §9 (the paper specifies no separate downstream LR — 1e-4 is defensible). Phase 3's end-to-end Kinetics fine-tune overfit hard (train loss 0.83->0.07, val loss 0.81->2.27, 32x ratio by epoch 8); the D3 moderate regularization + the MD init are the hypothesized fix, and the D6 runtime monitor catches a recurrence at zero wasted compute.

Output: the finalized md_finetune.py (run_md_finetune_epoch + the D6 monitor) + an extended unit test; the notebook Step 6 (3-seed fine-tune); 3 best.pt checkpoints on Drive at phase04/md_finetune_seed{42,1337,7}/; per-seed val macro-F1 recorded for the Plan 04 ensemble variance evidence.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on the L4 Colab. The 3 fine-tunes run SEQUENTIALLY (D6 ensemble-homogeneity policy — seed 42 first, lock the recipe, then 1337 + 7). Each seed is a resume=True cell (disconnect-safe). Per Phase 3 anchor: fine-tune was ~5.3 min/epoch on L4; 50ep x 3 seeds is roughly 13-16h with early-stop bounding it (Phase 3 stopped at 11). After git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-VALIDATION.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-01-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-PLAN.md
@.planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md
@backend/training/aqa/harness/md_finetune.py
@backend/training/aqa/harness/test_md_finetune.py
@backend/training/aqa/harness/supervised_train.py
@backend/training/aqa/harness/colab.py
@backend/training/aqa/datasets/squat.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/notebooks/04_squat_md_ssl.py
@CLAUDE.md

<interfaces>
This plan FINALIZES the md_finetune.py stub from Plan 01 (build_finetune_model is ALREADY implemented + tested in Plan 01 Task 8 — do NOT re-implement it; implement run_md_finetune_epoch). Plan 04 consumes the 3 best.pt checkpoints.

Produced artifact contract (Plan 04 input):
- Drive paths: My Drive/FitNova/checkpoints/phase04/md_finetune_seed42/best.pt, md_finetune_seed1337/best.pt, md_finetune_seed7/best.pt
- Each best.pt payload mirrors the Phase 3 schema EXACTLY (so Phase 3 tools load it): model_state_dict, optimizer_state_dict, scheduler_state_dict, rng_state, metrics_history, best_f1_val, best_thresholds (None until Plan 04's sweep), config_hash, config_repr, code_version "phase04-md-finetune".
- Plan 04 loads each best.pt's model_state_dict into build_finetune_model's architecture, runs val/test passes, then ensembles (mean of sigmoids).

run_md_finetune_epoch signature (finalize the Plan 01 stub; do NOT change the signature):
- run_md_finetune_epoch(*, run_name, md_backbone_path, drive_root, videos_root, seed=42, config=None, resume=True, max_epochs=None) -> dict
- Returns epoch, metrics_history, best_f1_val, best_thresholds (None), checkpoint_path, best_checkpoint_path, config_hash, aborted_overfit (bool).
- Parallels supervised_train.run_supervised_epoch with the 4 D3 deltas (PATTERNS md_finetune section):
  Delta 1: build_finetune_model(md_backbone_path, dropout=config.dropout) instead of build_model() — loads the MD backbone, NOT Kinetics.
  Delta 2: torch.optim.AdamW (NOT torch.optim.Adam) with weight_decay=config.weight_decay.
  Delta 3: the D6 runtime overfit monitor — after each epoch's val pass, train_val_ratio = train_loss_mean / (val_loss_mean + 1e-9); if epoch < 10 and train_val_ratio > 10.0, log a D6-ABORT warning and return early with aborted_overfit=True.
  Delta 4: code_version "phase04-md-finetune" in the payload.
  Everything else (DataLoader construction with persistent_workers, _val_pass, the best.pt write contract, early-stop, prune) mirrors supervised_train.py.

D6 reg-bump policy (notebook Step 6 orchestrates, NOT the trainer):
- Run seed 42 first with the base FinetuneConfig (wd=1e-4, dropout=0.2).
- If seed 42 returns aborted_overfit=True: abort, bump to FinetuneConfig(weight_decay=5e-4, dropout=0.3), restart seed 42 until clean; LOCK that bumped recipe for seeds 1337 + 7 (ensemble homogeneity — 3 seeds, 1 recipe).
- If seed 42 is clean: run 1337 + 7 at the base recipe.
- A later-seed (1337/7) trip at the LOCKED recipe is documented as genuine seed variance, NOT auto-bumped (D6).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D3-recipe | FinetuneConfig (from Plan 01, LOCKED): AdamW, lr=1e-4 (CITED §9 — no separate downstream LR), weight_decay=1e-4, dropout=0.2 head-only, batch_size=16, 50 epochs, 8-epoch val-macro-F1 patience, cosine annealing, num_frames=32, crop_size=112, jitter +/-2, flip OFF. The labeled pipeline (SquatKIEKFEDataset) is reused UNCHANGED — fine-tune uses 32-frame sampling, NOT the 16-frame half-cycles. | CONTEXT D3 + RESEARCH §9 |
| D3-init | build_finetune_model loads the MD backbone (Plan 02 backbone.pt) via load_state_dict(backbone_state_dict, strict=False), map_location='cpu'. NOT Kinetics — the MD backbone IS the init (§9 anti-pattern). Fresh Dropout(0.2)+Linear(512,2) head. (Already implemented + tested in Plan 01 Task 8.) | RESEARCH §9 |
| D3-deviation | The paper used 20 downstream epochs; Phase 4 uses 50 with 8-epoch early-stop for clean Phase-3-vs-Phase-4 comparison parity (Phase 3 stopped at 11). A deliberate, justified deviation — documented in the Plan 04 SUMMARY. | CONTEXT D3 + RESEARCH §9 |
| D6-monitor | Runtime train/val BCE loss ratio per epoch; > 10x before epoch 10 => abort that seed (aborted_overfit=True) + bump reg (wd=5e-4, dropout=0.3). Phase 3 hit 32x by epoch 8 with wd=0/no-dropout; the D3 moderate reg should keep Phase 4 well below — this likely will NOT fire, but the monitor is mandatory. | CONTEXT D6 monitor 1 |
| D6-homogeneity | Seed 42 runs FIRST; on a trip, bump + restart 42, then LOCK that recipe for 1337 + 7 (ensemble homogeneity — 3 seeds, 1 recipe, zero wasted compute in the common case). A later-seed trip at the locked recipe is documented as seed variance, not auto-bumped. | CONTEXT D6 reg-bump policy |
| D4-variance | Per-seed val macro-F1 (and the best epoch) recorded for each of 42/1337/7 — the variance evidence the Plan 04 ensemble cites to prove the lift is real, not one lucky seed (Lakshminarayanan et al. 2017). | CONTEXT D4 |
| D7 | persistent_workers=num_workers>0 on the labeled loaders; map_location='cpu' on resume + on the backbone load; best.pt write uses the Phase 3 contract (latest.txt preserved — but note: md_finetune's best.pt write may pass update_latest as appropriate; the Phase 3 inline save/restore OR the Plan 01 update_latest kwarg both work — prefer the Plan 01 kwarg for cleanliness); metrics_history from latest.txt-pointed checkpoint for viz; one runnable unit at a time; restart runtime / importlib.reload after git pull. | CONTEXT D7 |
| D9 | SquatKIEKFEDataset / transforms / splits / metrics reused UNCHANGED; supervised_train.py NOT edited (md_finetune is the new module — copy the patterns, do NOT bloat the Phase 3 trainer). | CONTEXT D9 + RESEARCH §11 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 3 + D7)

1. _set_global_seed(seed) per seed (seed varies: 42, 1337, 7) covers Python/NumPy/torch-CPU/torch-CUDA RNG.
2. persistent_workers = config.num_workers > 0 on the labeled DataLoaders ([[reference_pytorch_persistent_workers]]).
3. map_location='cpu' on load_latest_checkpoint resume AND on the backbone.pt load in build_finetune_model (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume.
5. best.pt write does not clobber latest.txt — use the Plan 01 update_latest=False kwarg for the best.pt write (cleaner than Phase 3's inline save/restore) OR the Phase 3 inline approach; either way latest.txt keeps pointing at the latest epoch_NNN.pt.
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch per seed.
7. metrics_history loaded from the latest.txt-pointed checkpoint for the per-seed training-curve viz (NOT best.pt — [[reference_best_pt_metrics_history_is_stale]]).
8. config_hash via hash_config over the FinetuneConfig recipe keys (mirrors Phase 3 D12 key set; seed IS in the run_name, not necessarily the hash — but each seed gets its own run_dir so no collision).
9. No AMP (fp32) — Phase 3 contract (RESEARCH §11 carry).
10. Each seed gets its OWN run_dir (md_finetune_seed42 / _seed1337 / _seed7) so resume never crosses seeds; restart runtime / importlib.reload after git pull.

</determinism_checklist>

<threat_model>
## Trust Boundaries

Offline ML training in Colab; no network surface, no auth, no untrusted runtime input, no PII (RESEARCH Security Domain — ASVS V2-V6 N/A). Relevant boundaries: backbone/checkpoint integrity, per-seed resume isolation, ensemble homogeneity. Proportionate.

| Boundary | Description |
|----------|-------------|
| Plan 02 backbone.pt -> fine-tune init | build_finetune_model loads backbone_state_dict (strict=False) into a fresh r2plus1d_18(weights=None). A wrong load (Kinetics instead of MD, or a key mismatch) would silently discard the SSL contribution. |
| In-memory fine-tune state -> Drive (3 seeds, multi-session) | 3 separate run_dirs (seed42/1337/7); each best.pt write must not clobber its own latest.txt. |
| Recipe drift across seeds | The 3-seed ensemble must be homogeneous (1 recipe); the D6 reg-bump policy locks the recipe on seed 42 to prevent silent per-seed recipe drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | build_finetune_model could load Kinetics instead of the MD backbone (silently discarding the SSL contribution) | mitigate | build_finetune_model uses r2plus1d_18(weights=None) + explicit load_state_dict(ckpt["backbone_state_dict"]) — proven in Plan 01 Task 8 (test_finetune_model_build round-trips a fake MD checkpoint). Step 6 also asserts the backbone_path points to phase04/md_pretrain_v1/backbone.pt (not a Kinetics path). MEDIUM — tested in Plan 01 + path-asserted at run time. |
| T-04-10 | Integrity | Per-seed resume crossing seeds (a seed-1337 resume loading seed-42's checkpoint) | mitigate | Each seed gets its own run_dir (md_finetune_seed{42,1337,7}); load_latest_checkpoint reads that seed's latest.txt only; config_hash verify catches a recipe mismatch. The best.pt write does not clobber latest.txt (D7). MEDIUM — run-dir isolation + hash verify. |
| T-04-11 | Tampering | Ensemble inhomogeneity — seeds trained on different recipes (silent per-seed reg drift) producing a meaningless ensemble | mitigate | The D6 reg-bump policy is orchestrated in Step 6: seed 42 first, lock the recipe (base or bumped) for 1337 + 7; a later-seed trip at the locked recipe is documented as seed variance, NOT auto-bumped. Per-seed val F1 recorded (D4 variance evidence). MEDIUM — the policy enforces 1 recipe across 3 seeds. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe (Plan 02) is the guard. No [ASSUMED]/[SUS] packages. |

All threats MEDIUM at worst; T-04-09 is proven in Plan 01's test + a run-time path assertion; T-04-11 is the ensemble-validity guard (the D6 homogeneity policy).

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Finalize md_finetune.py — run_md_finetune_epoch + D6 overfit monitor</name>
  <files>backend/training/aqa/harness/md_finetune.py</files>
  <read_first>
    backend/training/aqa/harness/md_finetune.py (the run_md_finetune_epoch stub + FinetuneConfig + build_finetune_model all from Plan 01 — build_finetune_model is DONE, do NOT re-implement it),
    backend/training/aqa/harness/supervised_train.py (run_supervised_epoch lines 332+ — the FULL trainer to parallel: _build_dataloaders line 187, _val_pass line 275, resume branch 436-468, the train loop, best.pt write contract, early-stop, prune; the Adam line 420 that becomes AdamW),
    backend/training/aqa/harness/colab.py (atomic_save_checkpoint with the Plan 01 update_latest kwarg, load_latest_checkpoint, capture/restore_rng_state, hash_config, prune_checkpoints),
    backend/training/aqa/eval/metrics.py (f1_per_error, pr_auc_per_error — per-epoch val metrics),
    backend/training/aqa/datasets/squat.py (SquatKIEKFEDataset + pos_weight — the labeled pipeline reused UNCHANGED),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§9 — the fine-tune recipe delta table + the AdamW-vs-Adam note + the 20-vs-50 epoch deviation),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (md_finetune section — the 4 Deltas verbatim: Config, build_finetune_model, AdamW optimizer, the D6 monitor early-return block, the payload code_version)
  </read_first>
  <action>
    Finalize md_finetune.py by implementing run_md_finetune_epoch — a near-copy of supervised_train.run_supervised_epoch with the 4 D3 deltas (PATTERNS md_finetune section). Signature per Plan 01 interfaces: run_md_finetune_epoch(*, run_name, md_backbone_path, drive_root, videos_root, seed=42, config=None, resume=True, max_epochs=None) -> dict. Body in order, mirroring supervised_train.py: config = config or FinetuneConfig(); effective_max_epochs = max_epochs or config.max_epochs; _set_global_seed(seed); build the labeled dataloaders (copy supervised_train._build_dataloaders pattern OR reuse it via import — do NOT edit supervised_train.py per D9; copying the helper body is acceptable, importing the public pattern is acceptable, but the SquatKIEKFEDataset construction + persistent_workers guard must be present); pull pos_weight = train_loader.dataset.pos_weight; device = cuda if available; DELTA 1: model = build_finetune_model(md_backbone_path, dropout=config.dropout).to(device) (loads MD backbone, NOT Kinetics); criterion = nn.BCEWithLogitsLoss(pos_weight=pos_weight.to(device)); DELTA 2: optimizer = torch.optim.AdamW(model.parameters(), lr=config.learning_rate, weight_decay=config.weight_decay) (AdamW NOT Adam); scheduler = CosineAnnealingLR(optimizer, T_max=config.scheduler_t_max); config_hash via hash_config; run_dir = drive_root/FitNova/checkpoints/phase04/run_name; os.makedirs. Resume branch (mirror supervised_train 436-468): load_latest_checkpoint(run_dir, expected_config_hash, map_location="cpu"); restore model/optimizer/scheduler + restore_rng_state + metrics_history + best_f1_val + start_epoch. Main loop: model.train(); per-batch train (tqdm); scheduler.step() at epoch end; _val_pass (copy the supervised_train pattern — gather scores+labels, no per-batch F1) for val_loss_mean + scores + labels; compute val_f1_kie/kfe via f1_per_error at 0.5, val_pr_auc via pr_auc_per_error, val_macro_f1. DELTA 3 (the D6 monitor): train_val_ratio = train_loss_mean / (val_loss_mean + 1e-9); store it in the metrics_history entry; if epoch < 10 and train_val_ratio > 10.0: log "D6 ABORT: train/val loss ratio %.1f > 10 at epoch %d (seed=%d). Abort; bump wd=5e-4, dropout=0.3." and RETURN early with aborted_overfit=True (+ the partial metrics_history, best_f1_val, checkpoint paths, config_hash). Append the metrics_history entry (with train_val_loss_ratio + epoch_wall_time_s). DELTA 4: build the payload with code_version "phase04-md-finetune"; atomic_save_checkpoint(payload, run_dir/epoch_NNN.pt) ALWAYS; if val_macro_f1 > best_f1_val: best_f1_val = val_macro_f1; atomic_save_checkpoint(payload, run_dir/best.pt, update_latest=False) (the Plan 01 kwarg — cleaner than Phase 3's inline save/restore; best.pt does not clobber latest.txt); reset epochs_since_improve. prune_checkpoints(run_dir, keep_last=3, keep_best=True). Early-stop at 8-epoch patience. Return epoch, metrics_history, best_f1_val, best_thresholds=None, checkpoint_path, best_checkpoint_path, config_hash, aborted_overfit=False. seed_worker + _set_global_seed copied verbatim from supervised_train.py (do NOT import — D9). Inline-cite D3 deltas + D6 + §9.

    Single runnable unit (LOCAL): user pastes back (1) the AST-parse of md_finetune.py prints "parse ok"; (2) the Plan 01 SQUAT-05-a build test STILL passes via pytest of test_md_finetune.py::test_finetune_model_build (slow). The 3-seed GPU run is Task 3.

    Atomic commit: feat(04): finalize md_finetune run_md_finetune_epoch + D6 monitor (Task 1, D3/D6 / RESEARCH §9).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/harness/md_finetune.py').read()); print('parse ok')" && grep -q "AdamW" backend/training/aqa/harness/md_finetune.py && grep -q "aborted_overfit" backend/training/aqa/harness/md_finetune.py && grep -q "phase04-md-finetune" backend/training/aqa/harness/md_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - md_finetune.py parses; run_md_finetune_epoch implemented (no NotImplementedError remaining)
    - Optimizer is torch.optim.AdamW (NOT Adam); model built via build_finetune_model (MD backbone, NOT Kinetics); payload code_version is "phase04-md-finetune"
    - The D6 monitor is present: epoch < 10 and train_val_ratio > 10.0 returns aborted_overfit=True early
    - best.pt write passes update_latest=False (does not clobber latest.txt); per-epoch atomic checkpoint + prune
    - supervised_train.py is UNCHANGED (D9 — verify git diff touches only md_finetune.py)
    - Runnable unit: user pastes back the AST-parse + the SQUAT-05-a build test passing
  </acceptance_criteria>
  <dependencies>Plan 02 (backbone.pt exists) — but this task is LOCAL code finalization, runnable before the GPU seeds</dependencies>
  <done>The fine-tune trainer is finalized: AdamW + MD-backbone init + Dropout head + the D6 runtime overfit monitor, paralleling Phase 3 without editing it; ready for the unit test + the 3-seed run</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_md_finetune.py — test_d6_overfit_monitor (the abort path)</name>
  <files>backend/training/aqa/harness/test_md_finetune.py</files>
  <read_first>
    backend/training/aqa/harness/md_finetune.py (the finalized run_md_finetune_epoch from Task 1 — the D6 monitor logic to test),
    backend/training/aqa/harness/test_md_finetune.py (the Plan 01 SQUAT-05-a test + the importorskip + _arr helper),
    backend/training/aqa/harness/test_supervised_train.py (the test patterns — how to test a trainer helper without a full GPU run),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D6 — the abort condition: train/val ratio > 10x before epoch 10)
  </read_first>
  <action>
    Extend test_md_finetune.py with test_d6_overfit_monitor — a UNIT test of the D6 abort decision logic WITHOUT a full GPU training run. Since run_md_finetune_epoch needs real data + GPU, factor the D6 decision into a tiny pure helper if it is not already one: add (or test, if inline) a function like _d6_should_abort(epoch: int, train_val_ratio: float, *, threshold: float = 10.0, before_epoch: int = 10) -> bool that returns epoch < before_epoch and train_val_ratio > threshold. (If Task 1 inlined the check, refactor it into this tiny helper so it is unit-testable — a 2-line pure function, D6 logic.) Test cases: _d6_should_abort(8, 32.0) is True (Phase 3's actual epoch-8 ratio of 32x trips it); _d6_should_abort(8, 5.0) is False (below threshold); _d6_should_abort(12, 32.0) is False (past epoch 10 — the monitor only guards early epochs); _d6_should_abort(0, 11.0) is True (just over threshold, epoch 0). Use the importorskip("torch") at module top (already there from Plan 01). This proves the D6 monitor fires exactly when CONTEXT D6 specifies (before epoch 10, ratio > 10x) — the guard that prevents a repeat of Phase 3's 32x overfit at zero wasted compute.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_finetune.py::test_d6_overfit_monitor -x -v (passes; pure logic, sub-second).

    Atomic commit: test(04): D6 overfit-monitor unit test (Task 2, D6).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_finetune.py::test_d6_overfit_monitor -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_d6_overfit_monitor passes; the D6 abort logic returns True iff (epoch < 10 and train_val_ratio > 10.0)
    - The four cases (8/32 True, 8/5 False, 12/32 False, 0/11 True) all assert correctly
    - The D6 decision is a unit-testable pure helper (refactored out if Task 1 inlined it)
    - Runnable unit: user pastes back the pytest output ending with 1 passed
  </acceptance_criteria>
  <dependencies>Task 1 (the finalized trainer with the D6 monitor)</dependencies>
  <done>The D6 overfit-monitor decision logic is regression-tested before the 3-seed run; the Phase-3-32x-overfit guard is proven to fire at the CONTEXT-D6 threshold</done>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 6 — 3-seed fine-tune (seed 42 first + D6 reg-bump gate, then 1337 + 7)</name>
  <files>backend/training/aqa/notebooks/04_squat_md_ssl.py</files>
  <read_first>
    backend/training/aqa/harness/md_finetune.py (run_md_finetune_epoch + FinetuneConfig — Step 6 calls it per seed),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 4 full-training-run pattern — resume=True single-cell-per-run + per-epoch paste-back + disconnect recovery),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the Step 5 SSL pretrain that produced backbone.pt — Step 6 reads BACKBONE_PATH from it),
    .planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md (the overfit pattern Phase 3 hit — 32x ratio by epoch 8; the val best 0.5454 at epoch 3; the ~5.3 min/epoch anchor — the SQUAT-05-d bar this plan must clear),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D6 reg-bump policy — seed 42 first, lock recipe for 1337/7; D4 per-seed F1 recording),
    .planning/phases/04-squat-motion-disentangling-ssl/04-VALIDATION.md (SQUAT-05-d — fine-tune val macro-F1 at or above Phase 3 best 0.5454)
  </read_first>
  <action>
    Append Step 6 to the notebook: the 3-seed fine-tune, orchestrating the D6 reg-bump policy. BACKBONE_PATH = os.path.join(MYDRIVE, "FitNova/checkpoints/phase04/md_pretrain_v1/backbone.pt"); assert os.path.exists(BACKBONE_PATH), "Plan 02 backbone.pt must exist" (T-04-09 path assertion — NOT a Kinetics path). Structure as SEQUENTIAL per-seed cells (one runnable unit each, D6 homogeneity):

    Step 6a (seed 42, the recipe-locking seed): base_config = FinetuneConfig(md_backbone_path=BACKBONE_PATH); result42 = run_md_finetune_epoch(run_name="md_finetune_seed42", md_backbone_path=BACKBONE_PATH, drive_root=MYDRIVE, videos_root=VIDEOS_ROOT, seed=42, config=base_config, resume=True). Paste-back: result42["aborted_overfit"], result42["best_f1_val"], final epoch. IF aborted_overfit: bump bumped_config = FinetuneConfig(md_backbone_path=BACKBONE_PATH, weight_decay=5e-4, dropout=0.3); restart seed 42 with bumped_config until clean; the LOCKED recipe for 1337+7 becomes bumped_config. ELSE the locked recipe is base_config. Record locked_config + result42 best_f1_val (D4 variance evidence). The D6 monitor likely will NOT fire (D3 moderate reg vs Phase 3's wd=0) — but the gate is mandatory.

    Step 6b (seed 1337, locked recipe): result1337 = run_md_finetune_epoch(run_name="md_finetune_seed1337", md_backbone_path=BACKBONE_PATH, ..., seed=1337, config=locked_config, resume=True). Paste-back best_f1_val + aborted_overfit. A trip here at the LOCKED recipe is documented as seed variance, NOT auto-bumped (D6).

    Step 6c (seed 7, locked recipe): result7 = run_md_finetune_epoch(run_name="md_finetune_seed7", md_backbone_path=BACKBONE_PATH, ..., seed=7, config=locked_config, resume=True). Paste-back best_f1_val + aborted_overfit.

    After all 3: print the per-seed val macro-F1 table (seed 42 / 1337 / 7 best_f1_val + best epoch) — the D4 variance evidence. SQUAT-05-d check: each seed's best_f1_val at or above Phase 3's 0.5454 (SSL transferred). If a seed is below: NOT a hard fail here — record it; the ensemble (Plan 04) is the headline, and the D6 no-lift policy (one remediation pass) applies at the phase level.

    Each seed cell is resume=True (disconnect-safe — re-run after a Colab restart resumes that seed from its latest.txt). On a mid-seed disconnect: re-run Step 0 + skip to the seed's cell + re-run; the first post-resume epoch logs "Resumed from epoch_NNN.pt".

    Atomic commit (per seed completion): feat(04): notebook Step 6 fine-tune seed{N} (Task 3, D3/D6/D4).
  </action>
  <verify>
    <automated>grep -q "run_md_finetune_epoch" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "md_finetune_seed42" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "md_finetune_seed1337" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "md_finetune_seed7" backend/training/aqa/notebooks/04_squat_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 6 present with 3 sequential per-seed cells (42 first, then 1337, 7); BACKBONE_PATH asserted to be the md_pretrain_v1/backbone.pt path (T-04-09)
    - Runnable unit (Colab, ~13-16h across the 3 seeds, multi-session): per-seed paste-back of best_f1_val + aborted_overfit; the per-seed val-F1 table at the end (D4 variance evidence)
    - SQUAT-05-d: each seed's best_f1_val at or above Phase 3's 0.5454 (or the gap documented per the D6 no-lift policy)
    - D6 reg-bump policy honored: if seed 42 trips, recipe bumped + locked for 1337/7; a later-seed trip at the locked recipe is documented as variance, not auto-bumped
    - 3 best.pt on Drive at phase04/md_finetune_seed{42,1337,7}/; on any mid-seed resume, paste-back includes "Resumed from epoch_NNN.pt"
  </acceptance_criteria>
  <dependencies>Task 2 (D6 monitor tested), Plan 02 Task 6 (backbone.pt on Drive)</dependencies>
  <done>3 fine-tune best.pt checkpoints on Drive from the shared MD backbone; per-seed val macro-F1 recorded (D4 variance evidence); the ensemble homogeneity policy honored; SQUAT-05 substantially advanced; the 3 inputs for Plan 04's ensemble exist</done>
</task>

</tasks>

<verification>
## Mapping to Phase 4 ROADMAP Success Criteria

| ROADMAP success criterion | Tasks that deliver it | Acceptance proof |
|---|---|---|
| (2) The MD-pretrained model, fine-tuned for KIE/KFE, reports F1 at or above the Phase-3 baseline | Tasks 1 (trainer), 2 (D6 test), 3 (3-seed run) | Task 3 per-seed paste-back: each seed's best_f1_val at or above Phase 3's 0.5454 (SQUAT-05-d); the 3 best.pt are the ensemble inputs whose test F1 (Plan 04) is the headline |

## Mapping to Requirements

| Requirement | Tasks | Acceptance |
|---|---|---|
| SQUAT-05 (MD-pretrained fine-tuned for KIE/KFE; F1 vs baseline) | Tasks 1-3 (this plan) + Plan 04 (test eval + comparison) | Task 3 produces the 3 fine-tuned best.pt + per-seed val F1; Plan 04 computes the test F1 + the comparison chart |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|---|---|---|---|
| SQUAT-05-a (build_finetune_model — MD backbone + Dropout+Linear) | Plan 01 Task 8 (already done) + reconfirmed in Task 1's parse | unit (slow) | 0 |
| SQUAT-05-d (fine-tune val macro-F1 at or above Phase 3 best 0.5454) | Task 3 per-seed paste-back | integration (Colab) | 3 |
| (D6 monitor logic) | Task 2 test_d6_overfit_monitor | unit | 3 |

(SQUAT-05-e/f — ensemble+TTA test F1 + the val-test gap — are Plan 04.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user on the L4 Colab
- md_finetune.py finalized: run_md_finetune_epoch parallels supervised_train.run_supervised_epoch with the 4 D3 deltas (AdamW not Adam, build_finetune_model MD-init, Dropout head, code_version phase04-md-finetune) + the D6 runtime overfit monitor; supervised_train.py UNCHANGED (D9)
- test_d6_overfit_monitor green: the D6 abort fires iff (epoch < 10 and train/val ratio > 10x)
- 3 fine-tune seeds (42, 1337, 7) ran SEQUENTIALLY from the shared Plan 02 backbone; the D6 reg-bump policy honored (seed 42 first, lock recipe for 1337/7); 3 best.pt on Drive at phase04/md_finetune_seed{42,1337,7}/
- Per-seed val macro-F1 recorded (D4 variance evidence); SQUAT-05-d met (each at or above 0.5454) OR the gap documented per the D6 no-lift policy
- Each fine-tune disconnect-safe: per-epoch atomic checkpoint, map_location='cpu' resume, best.pt write does not clobber latest.txt; a mid-seed disconnect resumed cleanly
- Every D3/D6 decision carries an inline citation — [[feedback_ai_correctness]] satisfied
- One concise threat_model block; T-04-09 (MD-not-Kinetics load) proven in Plan 01 + path-asserted; T-04-10 (per-seed resume isolation) by separate run_dirs; T-04-11 (ensemble homogeneity) by the D6 locked-recipe policy
</success_criteria>

<output>
After Task 3 (3 best.pt on Drive), this plan is complete. Plan 04 (Wave 4) loads the 3 best.pt, runs the ensemble (mean of sigmoids) + val-tuned TTA + test eval + the 9 figures + the comparison chart + results.pkl, and authors the phase SUMMARY. No SUMMARY.md for this plan individually. The orchestrator advances to Plan 04 once Task 3's per-seed table confirms the 3 best.pt on Drive.
</output>

<revision_log>
Phase 4 Plan 03 written 2026-05-21. Wave 3: the 3-seed fine-tune. 3 tasks. md_finetune.run_md_finetune_epoch is finalized as a near-copy of Phase 3's run_supervised_epoch with the 4 D3 deltas (PATTERNS md_finetune section) + the D6 runtime overfit monitor; build_finetune_model was already done + tested in Plan 01 Task 8. The 3 seeds run SEQUENTIALLY (D6 ensemble-homogeneity policy — seed 42 first, lock the recipe for 1337+7). The D6 monitor likely will not fire (D3 moderate reg vs Phase 3's wd=0/no-dropout 32x overfit) but is mandatory.

D9 honored: supervised_train.py is NOT edited; md_finetune copies the _build_dataloaders/_val_pass patterns (or imports the public ones) but is a new module. best.pt write uses the Plan 01 update_latest=False kwarg (cleaner than Phase 3's inline latest.txt save/restore). Each seed gets its own run_dir so resume never crosses seeds.

SQUAT-05-d bar = Phase 3's val best 0.5454 (each seed at or above it = SSL transferred). The test F1 + comparison chart are Plan 04's headline; this plan produces the 3 ensemble inputs + per-seed variance evidence (D4).

Security: proportionate. T-04-09 (MD-not-Kinetics load — proven in Plan 01 + run-time path assert), T-04-10 (per-seed resume isolation — separate run_dirs), T-04-11 (ensemble homogeneity — D6 locked-recipe policy), T-04-SC accept.

No revisions yet — initial PLAN.md.
</revision_log>
