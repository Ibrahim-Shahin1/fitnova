---
phase: 04-squat-motion-disentangling-ssl
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - backend/training/aqa/notebooks/04_squat_md_ssl.py
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/.gitkeep
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/ssl_loss_curve.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/linear_probe_curve.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/finetune_curves_per_seed.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/ensemble_vs_single.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kie.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kfe.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kie.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kfe.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/sample_predictions.png
  - .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl
  - .planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md
autonomous: false
requirements: [SQUAT-05]
must_haves:
  truths:
    - "The 3 fine-tune seeds (42, 1337, 7) are aggregated by mean-of-sigmoids per error head (aggregate_sigmoid_mean, output in [0,1]) and a single per-error decision threshold is tuned on the ENSEMBLE val scores via threshold_sweep (NOT per-seed thresholds then vote, D4) — verified by a Colab paste-back of the ensemble val F1 + the tuned thresholds"
    - "The TTA recipe is val-tuned (select_tta_recipe over candidates temporal_jitter / spatial_5crop / flip; flip is OOD and only kept if it raises ensemble val macro-F1, D5) and applied to test; the aggregation order is per-seed -> average its TTA copies -> mean-of-sigmoids across seeds -> the val-tuned per-error threshold"
    - "Final test F1 per error is reported on the official 244-clip test split at the val-tuned thresholds, with BOTH the full ensemble+TTA headline number AND a single-model/no-TTA number (for the Phase 5 latency-budget choice, D5); the comparison places our F1 against the paper's Kinetics row (KIE 0.2970 / KFE 0.8184 / macro 0.5577) and MD row (KIE 0.4186 / KFE 0.8338 / macro 0.6262) and Phase 3 (KIE 0.2857 / KFE 0.8000 / macro 0.5429) on identical metrics — every number matching RESEARCH §16 (arithmetic double-checked)"
    - "The three D6 post-hoc monitors are reported: (2) val-test macro-F1 gap < 0.05 (Phase 3 was 0.008); (3) KIE test F1 NOT below Phase 3's 0.2857 (no regression — the harder, headline error); plus the no-lift policy outcome (one documented remediation pass if the ensemble does not beat macro 0.5429, else honest deviation analysis)"
    - "Nine supervisor figures + results.pkl are written as PNG to .planning/phases/04-squat-motion-disentangling-ssl/figures/ BEFORE plt.show() (SSL loss curve, linear-probe-during-SSL curve, per-seed fine-tune curves, ensemble-vs-single-seed, the headline Phase3-vs-Phase4 comparison bar chart, KIE/KFE test confusion, KIE/KFE test PR, sample predictions); results.pkl holds the full numeric results + raw ensemble scores/labels/clip_ids; the phase SUMMARY declares the production checkpoint + per-error thresholds + inference protocol + latency for the Phase 5 handoff"
  artifacts:
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png"
      provides: "THE HEADLINE — Phase 3 baseline vs Phase 4 MD-SSL (single + ensemble + ensemble+TTA) vs paper Kinetics row vs paper MD row (and GYMetricPose/LMM if sourced) on KIE/KFE/macro F1, identical metrics (D8)"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/ssl_loss_curve.png"
      provides: "SSL triplet loss vs epoch (from md_pretrain_v1's metrics_history, loaded from the latest.txt-pointed checkpoint per D7)"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/linear_probe_curve.png"
      provides: "Linear-probe macro-F1 (+ per-error) during SSL vs epoch — the convergence evidence (RESEARCH §6)"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/finetune_curves_per_seed.png"
      provides: "Per-seed (42/1337/7) fine-tune train/val loss + val macro-F1 curves — the overfit-vs-Phase-3 comparison + variance evidence"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/ensemble_vs_single.png"
      provides: "Ensemble vs each single-seed test F1 (KIE/KFE/macro) — proves the ensemble lift is real, not one lucky seed (D4)"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kie.png"
      provides: "KIE test confusion matrix at the ensemble val-tuned threshold"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kfe.png"
      provides: "KFE test confusion matrix at the ensemble val-tuned threshold"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kie.png"
      provides: "KIE test PR curve with the ensemble operating point + random baseline"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kfe.png"
      provides: "KFE test PR curve with the ensemble operating point + random baseline"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/sample_predictions.png"
      provides: "Sample-prediction grid (top TP/FP/FN per error) on the ensemble scores — the supervisor visual"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl"
      provides: "Pickled dict — ensemble + per-seed + TTA test F1/PR-AUC/confusion, val-tuned thresholds, the chosen TTA recipe, single-model and ensemble+TTA headline numbers, raw ensemble test scores/labels/clip_ids, the comparison-chart numbers, run provenance — the source of truth for the SUMMARY + Phase 5 handoff"
    - path: ".planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md"
      provides: "Phase 4 closeout — what shipped, the headline F1 vs Phase 3 + paper rows (RESEARCH §16), per-seed variance, the chosen TTA recipe + final aug set + actual SSL epoch count, deviations, the D6 monitor outcomes + no-lift-policy decision, the Phase 5 handoff (production checkpoint, per-error thresholds, inference protocol single-vs-ensemble+TTA, expected latency)"
  key_links:
    - from: "backend/training/aqa/notebooks/04_squat_md_ssl.py"
      to: "the 3 best.pt (phase04/md_finetune_seed{42,1337,7}) + eval/ensemble.aggregate_sigmoid_mean + eval/tta.{tta_forward,select_tta_recipe} + eval/metrics.{threshold_sweep,f1_per_error,pr_auc_per_error,confusion_matrix_per_error}"
      via: "notebook cell imports + per-seed val/test passes -> ensemble -> threshold -> test eval"
      pattern: "from backend.training.aqa.eval"
    - from: ".planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png"
      to: ".planning/phases/03-squat-supervised-baseline/figures/results.pkl (Phase 3 frozen numbers) + RESEARCH §16 (paper rows)"
      via: "the chart loads Phase 3 results.pkl + the §16-cited paper Table 2 numbers"
      pattern: "results.pkl"
    - from: ".planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md"
      to: "Phase 5 (the production checkpoint declaration — inference protocol + per-error thresholds + latency)"
      via: "SUMMARY Phase 5 handoff section"
      pattern: "Phase 5 handoff"
---

<objective>
Aggregate the 3 fine-tune seeds into the headline Phase 4 result: mean-of-sigmoids ensemble (D4) + val-tuned TTA (D5) + test eval at val-tuned thresholds, then produce the 9 supervisor figures + results.pkl + the headline Phase-3-vs-Phase-4 comparison chart, and author the phase SUMMARY with the Phase 5 handoff (SQUAT-05 + ROADMAP Phase 4 success criteria 2 & 3).

Purpose: ROADMAP Phase 4 success criterion 2 ("F1 at or above the Phase-3 baseline") + criterion 3 ("a comparison table/chart places our F1 against Parmar and GYMetricPose on identical metrics"). This is where the phase's headline number is computed and compared. The comparison-chart arithmetic is pre-verified in RESEARCH §16 (paper Table 2): Phase 3 macro 0.5429 (KIE 0.2857 / KFE 0.8000), paper Kinetics macro 0.5577 (0.2970 / 0.8184), paper MD macro 0.6262 (0.4186 / 0.8338). EVERY numeric acceptance criterion here matches §16 — Phase 3's plan had wrong F1 arithmetic that the user caught; this plan double-checks every number against §16.

Output: the notebook Steps 7-10 (ensemble + TTA + test eval + figures); the 9 PNG figures + results.pkl under figures/; the phase SUMMARY (04-01-SUMMARY.md) declaring the production checkpoint + per-error thresholds + the single-model-vs-ensemble+TTA inference protocol + expected latency for Phase 5.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on the L4 Colab. Each figure subsection is its own runnable unit (paste back the rendered figure). The figures are first-class per [[project_supervisor_visualizations]] (defense 2026-06-03 weights visualizations heavily). Saved as PNG BEFORE plt.show() with defensive mkdir ([[feedback_notebook_disconnect_safe]]). The no-lift policy (D6): if the ensemble does NOT beat Phase 3's macro 0.5429, ONE documented remediation pass (a regularization sweep or SSL-recipe adjustment), else close with honest deviation analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-VALIDATION.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-01-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-03-PLAN.md
@.planning/phases/03-squat-supervised-baseline/03-01-PLAN.md
@.planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md
@.planning/phases/03-squat-supervised-baseline/figures/results.pkl
@backend/training/aqa/eval/ensemble.py
@backend/training/aqa/eval/tta.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/harness/md_finetune.py
@backend/training/aqa/datasets/transforms.py
@backend/training/aqa/notebooks/04_squat_md_ssl.py
@CLAUDE.md

<interfaces>
This plan CONSUMES the Plan 03 best.pt checkpoints + the Plan 01 ensemble/tta/metrics functions (all already implemented + unit-tested). It writes only notebook cells + figures + results.pkl + the SUMMARY. No new production module logic — the pure functions exist; this plan WIRES them on real test data.

Inputs:
- 3 best.pt at My Drive/FitNova/checkpoints/phase04/md_finetune_seed{42,1337,7}/best.pt (Plan 03), each with model_state_dict + metrics_history.
- aggregate_sigmoid_mean(per_seed_logits) (Plan 01, tested) — mean-of-sigmoids, output in [0,1].
- tta_forward(model, clip, recipe, device) + select_tta_recipe(val_logits_per_recipe, val_labels) (Plan 01, tested).
- threshold_sweep / f1_per_error / pr_auc_per_error / confusion_matrix_per_error (Phase 3 eval/metrics.py, UNCHANGED).
- Phase 3 frozen numbers: .planning/phases/03-squat-supervised-baseline/figures/results.pkl (KIE 0.2857 / KFE 0.8000 / macro 0.5429 — for the comparison chart).

The aggregation order (D5): per-seed -> average its TTA copies (tta_forward returns mean logits) -> sigmoid -> mean-of-sigmoids across seeds (aggregate_sigmoid_mean) -> single per-error threshold tuned on the ensemble VAL scores (threshold_sweep) -> apply to test.

Comparison-chart numbers (RESEARCH §16, CITED from paper Table 2 — use EXACTLY these; arithmetic pre-verified):
- Phase 3 (us): KIE 0.2857, KFE 0.8000, macro 0.5429.
- Paper Kinetics (video R(2+1)D-18, frozen backbone): KIE 0.2970, KFE 0.8184, macro 0.5577.
- Paper MD (video, Phase 4 target row): KIE 0.4186, KFE 0.8338, macro 0.6262.
- Phase 4 success arithmetic: ROADMAP min = macro at or above 0.5429; paper MD target = macro approx 0.6262; D6 monitor 3 = KIE at or above 0.2857 (no regression); D6 monitor 2 = val-test gap < 0.05.
- GYMetricPose (Gallardo 2024) + LMM (Dibenedetto 2025): NOT in the Parmar paper (RESEARCH §16 A16); Task 6 sources them from FLEX Dataset Paper.pdf / the LMM paper at repo root if the chart needs the bars. The core Phase3-vs-Phase4-vs-Parmar bar does NOT require them.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D4-ensemble | Mean of sigmoid scores across the 3 seeds (aggregate_sigmoid_mean, output in [0,1]); a SINGLE per-error threshold tuned on the ensemble VAL scores via threshold_sweep (NOT per-seed thresholds then vote). Per-seed test F1 also reported (variance evidence). | CONTEXT D4 + RESEARCH §13 |
| D5-tta | TTA recipe val-tuned via select_tta_recipe over candidates {temporal_jitter, spatial_5crop, flip}; flip is OOD (Phase 3 trained flip OFF) and kept only if it raises ensemble val macro-F1. Aggregation order: per-seed -> average TTA copies -> mean-of-sigmoids across seeds -> val-tuned threshold. Report BOTH the ensemble+TTA headline AND a single-model/no-TTA number (Phase 5 latency choice). | CONTEXT D5 + RESEARCH §14 |
| D6-monitor2 | Post-hoc val-test macro-F1 gap target < 0.05 (Phase 3 was 0.008). Larger => over-tuned to val (threshold + TTA selection); report honestly + widen tuning. | CONTEXT D6 monitor 2 |
| D6-monitor3 | Post-hoc KIE test F1 must NOT regress below Phase 3's 0.2857. If ensemble macro improves but KIE drops below 0.2857, flag prominently (a bad KFE-for-KIE trade — KIE is the paper's headline lift + the harder error). | CONTEXT D6 monitor 3 |
| D6-nolift | If the final ensemble does NOT beat Phase 3 (macro 0.5429): ONE documented remediation pass (the regularization sweep, or an SSL-epoch/recipe adjustment informed by the linear-probe signal). If still no lift, close with honest deviation analysis ("faithfully implemented MD-SSL, did not lift over Kinetics baseline in our regime") — a valid result. Open-ended iteration rejected. | CONTEXT D6 no-lift policy |
| D8-figs | 9 figures + results.pkl, PNG to figures/ BEFORE plt.show(), defensive mkdir, Drive backup. Headline = phase3_vs_phase4_comparison.png. Numbers match RESEARCH §16. | CONTEXT D8 + [[project_supervisor_visualizations]] |
| D8-chart-arithmetic | EVERY expected metric value in acceptance criteria + the chart matches RESEARCH §16 EXACTLY (Phase 3 0.5429, Kinetics 0.5577, MD 0.6262). Phase 3's plan had WRONG F1 arithmetic the user caught — double-check each number. macro = (KIE + KFE) / 2. | RESEARCH §16 + CONTEXT canonical_refs note |
| D9 | eval/metrics.py + ensemble.py + tta.py reused UNCHANGED (all unit-tested in Plan 01); no new production logic — this plan WIRES them on test data + produces figures. results.pkl mirrors the Phase 3 results.pkl structure. | CONTEXT D9 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 3 + D7)

1. metrics_history (SSL loss curve, per-seed fine-tune curves) loaded from the latest.txt-pointed checkpoint, NOT best.pt (frozen at the best epoch — [[reference_best_pt_metrics_history_is_stale]]).
2. linear_probe_history loaded from the md_pretrain_v1 latest.txt-pointed checkpoint for the linear-probe curve.
3. map_location='cpu' (or cuda for inference) on every torch.load of the 3 best.pt + the backbone checkpoint.
4. All val/test scores gathered on CPU then a single sklearn call (no per-batch F1 — aliasing on the 14% KIE positive rate, RESEARCH §15 aliasing guards).
5. The ensemble + TTA produce sigmoid means in [0,1] (aggregate_sigmoid_mean asserts it) so pr_auc_per_error's [0,1] assertion (metrics.py:72) holds.
6. Figures saved BEFORE plt.show() with Path(out).parent.mkdir(parents=True, exist_ok=True); Drive backup of figures/ + results.pkl.
7. results.pkl written BEFORE the figures (the source of truth — if a figure cell crashes, results.pkl alone rebuilds them).
8. One runnable unit per figure subsection ([[feedback_one_cell_at_a_time_strict]]); restart runtime / importlib.reload after git pull.

</determinism_checklist>

<threat_model>
## Trust Boundaries

Offline ML evaluation in Colab; no network surface, no auth, no untrusted runtime input, no PII (RESEARCH Security Domain — ASVS V2-V6 N/A). Relevant boundaries: result-integrity (the headline number must be arithmetically correct), val-test leakage (the held-out test stays held out), the non-commercial dataset license. Proportionate.

| Boundary | Description |
|----------|-------------|
| Ensemble/TTA tuning -> the held-out test split | TTA recipe + threshold are tuned on VAL only; test stays held out. The D6 val-test-gap monitor (< 0.05) guards against over-tuning val. |
| Computed metrics -> the headline chart | The comparison numbers must match RESEARCH §16 exactly (Phase 3's plan had wrong arithmetic the user caught) — a wrong headline number misrepresents the contribution. |
| 3 best.pt + Phase 3 results.pkl -> results.pkl + chart | Loading the right checkpoints + the right Phase 3 baseline; mean-of-sigmoids (not logits) so pr_auc's [0,1] assertion holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | Headline-number arithmetic error (Phase 3's plan had wrong F1/PR-AUC literals the user caught) | mitigate | EVERY expected metric value matches RESEARCH §16 (Phase 3 0.5429, Kinetics 0.5577, MD 0.6262); macro = (KIE+KFE)/2 computed in code, not hand-typed; the chart loads Phase 3's results.pkl for the baseline (not a hand-typed number); Task 5 acceptance recomputes macro from the per-error F1 and asserts it matches the printed macro. MEDIUM — double-checked against §16 + recomputed in code. |
| T-04-13 | Information disclosure | Val-test leakage — the TTA recipe + threshold over-tuned to val, inflating the test number | mitigate | TTA recipe (select_tta_recipe) + threshold (threshold_sweep) tuned on VAL only; the D6 monitor 2 (val-test gap < 0.05, Phase 3 was 0.008) is reported + flagged if violated. MEDIUM — the gap monitor is the leakage guard. |
| T-04-14 | Tampering | Mean-of-logits instead of mean-of-sigmoids would fail pr_auc_per_error's [0,1] assertion (and is the wrong D4 aggregation) | mitigate | aggregate_sigmoid_mean (Plan 01, unit-tested) applies sigmoid BEFORE the mean + asserts [0,1]; the notebook calls it (not a hand-rolled mean-of-logits). LOW — the function enforces it; tested in Plan 01 Task 9. |
| T-04-15 | Integrity | GYMetricPose/LMM chart numbers fabricated (they are NOT in the Parmar paper, §16 A16) | mitigate | Task 6 sources GYMetricPose/LMM from FLEX Dataset Paper.pdf / the LMM paper at repo root with a page citation; if not found, the chart omits those bars + the SUMMARY notes the absence (the core Phase3-vs-Phase4-vs-Parmar bar does not need them). MEDIUM — sourced-or-omitted, never fabricated ([[feedback_ai_correctness]]). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). No [ASSUMED]/[SUS] packages. |

T-04-12 (headline arithmetic) is the highest-priority threat given the Phase 3 precedent; it is mitigated by matching §16 + recomputing macro in code + loading Phase 3's results.pkl rather than hand-typing the baseline.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Notebook Step 7 — per-seed val/test score gather + mean-of-sigmoids ensemble + val-tuned threshold</name>
  <files>backend/training/aqa/notebooks/04_squat_md_ssl.py</files>
  <read_first>
    backend/training/aqa/eval/ensemble.py (aggregate_sigmoid_mean — Plan 01, tested; mean-of-sigmoids in [0,1]),
    backend/training/aqa/eval/metrics.py (threshold_sweep — the single per-error threshold on ensemble val scores; f1_per_error),
    backend/training/aqa/harness/md_finetune.py (build_finetune_model — to reconstruct each seed's model; the labeled loaders),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 5 threshold-sweep pattern — gather val scores, threshold_sweep per head),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§13 — the ensemble aggregation + single-threshold-on-ensemble-val; the aggregation order),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D4 — mean-of-sigmoids, single ensemble-val threshold, per-seed F1 also reported)
  </read_first>
  <action>
    Append Step 7: load the 3 best.pt, gather per-seed val + test logits, ensemble by mean-of-sigmoids, tune a single per-error threshold on the ensemble VAL scores. Content: for seed in (42, 1337, 7): ckpt = torch.load(phase04/md_finetune_seed{seed}/best.pt, map_location='cuda'); model = build_finetune_model(BACKBONE_PATH, dropout=0.2); model.load_state_dict(ckpt["model_state_dict"]); model.eval(); run a val pass + a test pass (gather logits on CPU, no per-batch F1) -> per_seed_val_logits[seed] (243,2), per_seed_test_logits[seed] (244,2). Also capture test_clip_ids once (from the test loader's records). Ensemble (NO TTA yet — TTA is Step 8): ensemble_val = aggregate_sigmoid_mean([per_seed_val_logits[42], [1337], [7]]) (243,2) in [0,1]; ensemble_test = aggregate_sigmoid_mean([per_seed_test_logits...]) (244,2). Tune the single per-error threshold on the ensemble VAL scores: t_kie, f1_kie_val = threshold_sweep(val_labels[:,0], ensemble_val[:,0]); t_kfe, f1_kfe_val = threshold_sweep(val_labels[:,1], ensemble_val[:,1]) (D4 — single threshold on the ensemble, NOT per-seed-then-vote). Print: per-seed val macro-F1 (each seed at threshold 0.5, the D4 variance evidence), the ensemble val F1 + the two tuned thresholds. Store ensemble_val, ensemble_test, per_seed_*, thresholds, test_clip_ids in the notebook namespace for Steps 8-9.

    Single runnable unit (Colab) — user pastes back: the per-seed val macro-F1 (3 numbers), the ensemble val F1 (KIE + KFE), and the two tuned thresholds. The ensemble val F1 should be at or above the best single seed (the ensemble lift).

    Atomic commit: feat(04): notebook Step 7 ensemble + val threshold sweep (Task 1, D4 / RESEARCH §13).
  </action>
  <verify>
    <automated>grep -q "aggregate_sigmoid_mean" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "threshold_sweep" backend/training/aqa/notebooks/04_squat_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 7 present; loads the 3 best.pt; gathers per-seed val+test logits; ensembles via aggregate_sigmoid_mean; tunes a SINGLE per-error threshold on the ensemble VAL scores (NOT per-seed then vote)
    - Runnable unit (Colab): user pastes back per-seed val macro-F1 (3 numbers, D4 variance evidence) + ensemble val F1 (KIE+KFE) + the two tuned thresholds
    - The ensemble uses mean-of-sigmoids (in [0,1]), not mean-of-logits (T-04-14)
  </acceptance_criteria>
  <dependencies>Plan 03 (3 best.pt on Drive)</dependencies>
  <done>The 3 seeds are ensembled by mean-of-sigmoids; the single per-error threshold is tuned on the ensemble val scores; per-seed variance recorded; ready for the TTA sweep</done>
</task>

<task type="auto">
  <name>Task 2: Notebook Step 8 — val-tuned TTA sweep (flip OOD-validated) + final aggregation order</name>
  <files>backend/training/aqa/notebooks/04_squat_md_ssl.py</files>
  <read_first>
    backend/training/aqa/eval/tta.py (tta_forward + select_tta_recipe — Plan 01, tested; the 5-crop inline; mean-logits return),
    backend/training/aqa/eval/ensemble.py (aggregate_sigmoid_mean — the across-seeds step after per-seed TTA averaging),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§14 — the candidates, the val-tuned selection, the flip-is-OOD note, the aggregation order, the report-both-numbers requirement),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D5 — val-tuned recipe; flip a candidate not assumed-good; report ensemble+TTA AND single-model/no-TTA)
  </read_first>
  <action>
    Append Step 8: val-tune the TTA recipe, then apply the full aggregation order. Candidates: the recipe combos over {temporal_jitter, spatial_5crop, flip} (e.g. (), (temporal_jitter,), (spatial_5crop,), (temporal_jitter, spatial_5crop), and the same +flip). For each candidate recipe: for each seed, for each val clip, val_logits = tta_forward(model_seed, clip, recipe, device) (mean logits over the recipe's TTA copies); sigmoid; then aggregate_sigmoid_mean across seeds -> ensemble val scores for that recipe. select_tta_recipe(val_logits_per_recipe, val_labels) returns the val-macro-F1-maximizing recipe (D5 — measure, don't guess; flip kept only if it raises val macro-F1, RESEARCH §14). Print the per-recipe val macro-F1 table + the chosen recipe; explicitly note whether flip was selected (the OOD-validation outcome). Then compute the FINAL ensemble+TTA scores on VAL and TEST using the chosen recipe + the aggregation order (D5): per-seed -> average its TTA copies (tta_forward) -> sigmoid -> mean-of-sigmoids across seeds -> the val-tuned per-error threshold (re-tune the threshold on the ensemble+TTA val scores). Store ensemble_tta_val, ensemble_tta_test, the chosen recipe, the re-tuned thresholds. Also keep the single-model/no-TTA number from Step 7's ensemble (for the Phase 5 latency choice, D5). Print the TTA forward-pass cost (3 seeds x N_tta copies per clip — the latency note).

    Single runnable unit (Colab) — user pastes back: the per-recipe val macro-F1 table, the chosen TTA recipe (and whether flip was kept), the ensemble+TTA val F1, and the TTA cost estimate.

    Atomic commit: feat(04): notebook Step 8 val-tuned TTA sweep (Task 2, D5 / RESEARCH §14).
  </action>
  <verify>
    <automated>grep -q "select_tta_recipe" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "tta_forward" backend/training/aqa/notebooks/04_squat_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 8 present; select_tta_recipe picks the val-macro-F1-maximizing recipe over the candidates; flip kept ONLY if it raised val macro-F1 (the OOD-validation outcome printed)
    - The aggregation order is per-seed -> average TTA copies -> mean-of-sigmoids across seeds -> val-tuned threshold (D5)
    - Both the ensemble+TTA val F1 AND the single-model/no-TTA number from Step 7 are retained (Phase 5 latency choice)
    - Runnable unit (Colab): user pastes back the per-recipe table + the chosen recipe + the TTA cost estimate
  </acceptance_criteria>
  <dependencies>Task 1 (the ensemble + per-seed logits)</dependencies>
  <done>The TTA recipe is val-tuned (flip OOD-validated, not assumed); the full aggregation order is applied; both the ensemble+TTA and single-model numbers are ready for the test eval</done>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 9 — final test eval + D6 post-hoc monitors + results.pkl</name>
  <files>
    backend/training/aqa/notebooks/04_squat_md_ssl.py,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/.gitkeep,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl
  </files>
  <read_first>
    backend/training/aqa/eval/metrics.py (f1_per_error, pr_auc_per_error, confusion_matrix_per_error — the final test metrics),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 6 test-eval + results.pkl pattern — the summary table + the pickle structure to mirror),
    .planning/phases/03-squat-supervised-baseline/figures/results.pkl (Phase 3 numbers: KIE 0.2857 / KFE 0.8000 / macro 0.5429 — for the comparison + the D6 monitors),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 — the EXACT comparison numbers + the verified arithmetic; double-check every value),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D6 monitor 2 val-test gap < 0.05; monitor 3 KIE no-regress below 0.2857; the no-lift policy)
  </read_first>
  <action>
    Append Step 9: final test eval at the val-tuned thresholds + the D6 post-hoc monitors + results.pkl. FIRST create figures/.gitkeep (defensive). Apply the val-tuned thresholds to the ensemble+TTA TEST scores: test_pred_kie = (ensemble_tta_test[:,0] > thresholds["kie"]).astype(int); same KFE. Compute test_f1_kie = f1_per_error(test_labels[:,0].astype(int), test_pred_kie); test_f1_kfe similarly; test_macro_f1 = (test_f1_kie + test_f1_kfe) / 2 (computed in code, NOT hand-typed — T-04-12); test_pr_auc_kie/kfe = pr_auc_per_error(...); test_cm_kie/kfe = confusion_matrix_per_error(...). ALSO compute the single-model/no-TTA test F1 (from Step 7's ensemble, no TTA) for the Phase 5 latency choice (D5). ALSO compute per-seed test F1 (D4 variance). Print the summary table with the comparison rows from RESEARCH §16 (use EXACTLY these — arithmetic pre-verified): our ensemble+TTA KIE/KFE/macro; our single-model KIE/KFE/macro; Phase 3 (loaded from Phase 3's results.pkl): 0.2857 / 0.8000 / 0.5429; paper Kinetics: 0.2970 / 0.8184 / 0.5577; paper MD: 0.4186 / 0.8338 / 0.6262.

    D6 POST-HOC MONITORS (print + assert-with-flag, not hard-fail): (monitor 2) val_test_gap = abs(ensemble_tta_val_macro - test_macro_f1); target < 0.05 (Phase 3 was 0.008); if larger, flag "over-tuned to val" (D6). (monitor 3) KIE no-regress: assert-with-flag test_f1_kie at or above 0.2857; if below, flag prominently "KIE regressed below Phase 3 — bad KFE-for-KIE trade" (D6 — KIE is the headline lift + the harder error). NO-LIFT CHECK: if test_macro_f1 < 0.5429 (does not beat Phase 3), print the D6 no-lift policy: ONE documented remediation pass is authorized (a regularization sweep or an SSL-recipe adjustment informed by the linear-probe signal); record this for the SUMMARY decision.

    Write results.pkl (BEFORE any figure — the source of truth, T-04-12) mirroring the Phase 3 results.pkl structure with the Phase 4 additions: ensemble+TTA test F1/PR-AUC/confusion per error, single-model/no-TTA test F1, per-seed test F1 (variance), val-tuned thresholds, the chosen TTA recipe, val_test_gap, ensemble_tta_val_macro, raw ensemble_tta_test scores + test_labels + test_clip_ids, the comparison-chart numbers dict (ours + the §16 paper rows + Phase 3), the final SSL aug set + actual SSL epoch count (from Plan 02), run provenance. mkdir parents before writing.

    Single runnable unit (Colab) — user pastes back the full summary table (the headline numbers) + the D6 monitor outcomes (val-test gap, KIE no-regress) + the no-lift decision + the results.pkl write confirmation. The orchestrator reads the numbers carefully + double-checks macro = (KIE+KFE)/2 against §16.

    Atomic commit: feat(04): notebook Step 9 test eval + D6 monitors + results.pkl (Task 3, D6 / RESEARCH §16).
  </action>
  <verify>
    <automated>grep -q "results.pkl" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "test_f1_kie" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "val_test_gap" backend/training/aqa/notebooks/04_squat_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 9 present; test eval at the val-tuned thresholds; test_macro_f1 computed in code as (test_f1_kie + test_f1_kfe)/2 (NOT hand-typed — T-04-12)
    - The comparison rows match RESEARCH §16 EXACTLY: Phase 3 0.2857/0.8000/0.5429 (loaded from Phase 3 results.pkl), Kinetics 0.2970/0.8184/0.5577, MD 0.4186/0.8338/0.6262
    - D6 monitor 2 (val-test gap < 0.05) + monitor 3 (KIE at or above 0.2857) reported + flagged if violated; no-lift policy stated if test_macro < 0.5429
    - results.pkl written (BEFORE figures) with ensemble+TTA + single-model + per-seed test F1 + raw scores/labels/clip_ids + the comparison numbers
    - Runnable unit (Colab): user pastes back the summary table + the D6 outcomes + the results.pkl write confirmation
  </acceptance_criteria>
  <dependencies>Task 2 (the ensemble+TTA scores)</dependencies>
  <done>The headline Phase 4 test F1 is computed (ensemble+TTA + single-model) at the val-tuned thresholds; the comparison rows match §16; the D6 monitors are reported; results.pkl (the source of truth) is on disk; the no-lift decision is recorded</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: D6 no-lift decision gate (BLOCKING — proceed to figures or run the one remediation pass)</name>
  <files></files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl (Task 3's numbers — the headline ensemble+TTA macro vs Phase 3's 0.5429),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D6 no-lift policy — ONE remediation pass then honest deviation analysis; the Area-1 regularization sweep / SSL-recipe adjustment options),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 — the realistic outcome distribution: macro 0.62-0.66 likely; ~35% beat by +0.03, ~45% match, ~15% slightly below, ~5% no lift; §9 the remediation candidates)
  </read_first>
  <decision>
    Did the ensemble+TTA beat Phase 3's macro 0.5429, and is KIE at or above 0.2857? If yes -> proceed to figures (Task 5). If no -> run the ONE documented remediation pass (D6) or accept the honest deviation result.
  </decision>
  <context>
    RESEARCH §16 realistic outcome distribution: macro 0.62-0.66 is the likely outcome (paper MD is 0.6262); ~5% chance of no meaningful lift. The D6 no-lift policy bounds the schedule: if the ensemble does NOT beat Phase 3 (macro 0.5429) OR KIE regresses below 0.2857, ONE remediation pass is authorized — NOT open-ended iteration. Read Task 3's results.pkl: test_macro_f1 vs 0.5429, test_f1_kie vs 0.2857, the val-test gap vs 0.05.
  </context>
  <options>
    <option id="option-a">
      <name>Lift confirmed (macro > 0.5429, KIE at or above 0.2857, gap < 0.05) -> proceed to figures</name>
      <pros>The SSL contribution is demonstrated; proceed straight to the 9 figures + SUMMARY. The expected/likely case (RESEARCH §16: ~80% match-or-beat).</pros>
      <cons>None — this is the success path.</cons>
    </option>
    <option id="option-b">
      <name>No lift OR KIE regression -> run ONE remediation pass (D6)</name>
      <pros>The D6 no-lift policy's single documented pass — a regularization sweep (light/moderate/aggressive on seed 42, the designated remediation per CONTEXT Deferred Ideas) OR an SSL-epoch/recipe adjustment informed by the linear-probe signal. Bounds the schedule hit; preserves Phases 5-8.</pros>
      <cons>Adds compute (a re-fine-tune or a partial re-pretrain). Requires a follow-up plan or an in-notebook sweep cell. After this ONE pass, if still no lift -> option-c.</cons>
    </option>
    <option id="option-c">
      <name>No lift after the one pass -> close with honest deviation analysis</name>
      <pros>A valid undergraduate result ("faithfully implemented MD-SSL, did not lift over the Kinetics baseline in our regime"). The faithful reproduction + the honest analysis IS defensible ([[feedback_ai_correctness]]). Preserves the schedule for Phases 5-8.</pros>
      <cons>Does not hit the paper's MD row — but the SUMMARY documents why (data regime, the 1136-clip overfit, etc.) with the linear-probe + per-seed evidence.</cons>
    </option>
  </options>
  <action>This is a BLOCKING decision checkpoint. The executor presents Task 3's headline numbers (ensemble+TTA macro vs 0.5429, KIE vs 0.2857, val-test gap vs 0.05) from results.pkl. The user replies option-a (lift confirmed -> Task 5 figures), option-b (no lift -> the one remediation pass; the executor adds a remediation sweep cell or flags a follow-up plan), or option-c (no lift after the pass -> honest deviation analysis in the SUMMARY). No code change unless option-b. This is the D6 no-lift policy enforcement point — it bounds the schedule per [[feedback_working_style]] (no deadline-pressure framing, but no open-ended iteration either).</action>
  <verify>
    <automated>test -f .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl</automated>
  </verify>
  <done>User replies option-a / option-b / option-c; if option-a, Task 5 (figures) is authorized; if option-b, the one remediation pass runs then re-evaluates; if option-c, the SUMMARY frames the honest deviation result</done>
  <resume-signal>Type "option-a" (lift confirmed, proceed to figures), "option-b" (no lift, run the one remediation pass), or "option-c" (no lift after the pass, honest deviation analysis). Or paste back the headline numbers and ask for guidance.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Notebook Step 10 — 9 supervisor figures (headline = Phase3-vs-Phase4 comparison chart)</name>
  <files>
    backend/training/aqa/notebooks/04_squat_md_ssl.py,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/ssl_loss_curve.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/linear_probe_curve.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/finetune_curves_per_seed.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/ensemble_vs_single.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kie.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/confusion_kfe.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kie.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/pr_kfe.png,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/sample_predictions.png
  </files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl (Task 3 — all numeric results + raw scores the figures plot),
    .planning/phases/03-squat-supervised-baseline/03-01-PLAN.md (Task 14 figure subsections 7a-7d — the confusion/PR/sample-prediction figure recipes to mirror; the save_show helper),
    .planning/phases/03-squat-supervised-baseline/figures/results.pkl (Phase 3 numbers for the comparison chart),
    backend/training/aqa/datasets/transforms.py (KINETICS_MEAN/STD + decode_clip + spatial_val — for the sample-prediction denorm grid),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 — the chart numbers; §6 the linear-probe curve; §12 the collapse metrics for the SSL loss curve),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (D8 — the 9-figure list; headline = the comparison bar chart)
  </read_first>
  <action>
    Append Step 10: the 9 supervisor figures, each a subsection (its own runnable unit per [[feedback_one_cell_at_a_time_strict]]). Shared helper: FIG_DIR = Path(".planning/phases/04-squat-motion-disentangling-ssl/figures"); FIG_DIR.mkdir(parents=True, exist_ok=True); def save_show(fig, name): out=FIG_DIR/name; fig.savefig(out, dpi=150, bbox_inches="tight"); plt.show(); print("Saved", out) (PNG BEFORE plt.show() — D8). Load metrics_history + linear_probe_history from the md_pretrain_v1 latest.txt-pointed checkpoint (NOT backbone.pt — [[reference_best_pt_metrics_history_is_stale]]); load each seed's metrics_history from its latest.txt-pointed checkpoint.

    10a ssl_loss_curve.png: SSL triplet loss vs epoch (+ embedding_std on a twin axis — the no-collapse evidence, §12).
    10b linear_probe_curve.png: linear-probe macro-F1 (+ per-error KIE/KFE) vs SSL epoch; annotate the plateau / the backbone.pt-best epoch (the convergence evidence, §6).
    10c finetune_curves_per_seed.png: 3-panel (one per seed) train/val loss + val macro-F1 vs epoch; annotate each seed's best epoch; this is the overfit-vs-Phase-3 comparison (Phase 3 diverged val 0.81->2.27 — show whether D3 reg + MD init tamed it).
    10d ensemble_vs_single.png: grouped bars — each single seed's test F1 (KIE/KFE/macro) vs the ensemble vs ensemble+TTA; proves the ensemble lift is real (D4 variance evidence).
    10e phase3_vs_phase4_comparison.png (THE HEADLINE, D8): grouped bars KIE/KFE/macro for: Phase 3 baseline (0.2857/0.8000/0.5429, from Phase 3 results.pkl), our Phase 4 single-model, our ensemble, our ensemble+TTA, paper Kinetics (0.2970/0.8184/0.5577), paper MD (0.4186/0.8338/0.6262), and GYMetricPose/LMM IF Task 6 sourced them. Annotate the macro bars with values; a horizontal line at Phase 3's 0.5429 (the bar to beat) + at the paper MD 0.6262 (the target). Numbers EXACTLY per §16 (T-04-12).
    10f/10g confusion_kie.png / confusion_kfe.png: 2x2 test confusion at the ensemble val-tuned threshold (mirror Phase 3 Task 14 sub-7b).
    10h/10i pr_kie.png / pr_kfe.png: test PR curve with the ensemble operating point + random baseline (mirror sub-7c).
    10j sample_predictions.png: top TP/FP/FN per error on the ensemble scores (mirror sub-7d; one combined or per-error figure — match the Phase 3 D14 layout). Denorm via KINETICS_MEAN/STD.

    Each subsection saves BEFORE plt.show(); user pastes back each rendered figure. Step 10 ends with len(list(FIG_DIR.glob('*.png'))) == 9 + the Drive backup of figures/ + results.pkl.

    Single runnable unit per subsection (10 cells) — user pastes back each figure (or screenshot) as it completes. The headline 10e is the defense centerpiece.

    Atomic commit (per subsection or at the end): feat(04): notebook Step 10 + 9 supervisor figures (Task 5, D8).
  </action>
  <verify>
    <automated>grep -q "phase3_vs_phase4_comparison" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "linear_probe_curve" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "ensemble_vs_single" backend/training/aqa/notebooks/04_squat_md_ssl.py && grep -q "save_show" backend/training/aqa/notebooks/04_squat_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 10 present with 10 subsections referencing all 9 figure filenames + the save_show helper
    - The headline phase3_vs_phase4_comparison.png uses the EXACT §16 numbers (Phase 3 0.5429 from results.pkl, Kinetics 0.5577, MD 0.6262) with the 0.5429 + 0.6262 reference lines (T-04-12)
    - metrics_history + linear_probe_history loaded from the latest.txt-pointed checkpoints (NOT best.pt/backbone.pt — D7)
    - All 9 PNGs land on disk BEFORE plt.show(); len(FIG_DIR.glob('*.png')) == 9; figures/ + results.pkl backed up to Drive
    - Runnable unit (Colab): user pastes back 9 saved-PNG confirmations + screenshots; the comparison chart is defense-ready
  </acceptance_criteria>
  <dependencies>Task 4 (no-lift decision — figures reflect the final/remediated numbers), Task 3 (results.pkl)</dependencies>
  <done>All 9 supervisor figures produced (headline = the Phase3-vs-Phase4 comparison chart with §16-exact numbers); figures + results.pkl backed up to Drive; ROADMAP Phase 4 criterion 3 (comparison chart) satisfied</done>
</task>

<task type="auto">
  <name>Task 6: Source GYMetricPose / LMM comparison numbers (or document absence) + finalize the comparison chart</name>
  <files>
    backend/training/aqa/notebooks/04_squat_md_ssl.py,
    .planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png
  </files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 A16 + Open Question 4 — GYMetricPose/LMM are NOT in the Parmar paper; source from FLEX Dataset Paper.pdf / the LMM paper at repo root),
    Fitness-AQA/Fine-Tuning Large Multimodal Models forFitness Action Quality Assessment.pdf (the LMM / Dibenedetto 2025 paper — read for Squat KIE/KFE F1 if present),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (canonical_refs — GYMetricPose + LMM are listed comparison points for the headline chart)
  </read_first>
  <action>
    Source the GYMetricPose (Gallardo 2024) + LMM (Dibenedetto 2025) Squat KIE/KFE F1 numbers for the headline chart, per RESEARCH §16 A16 / Open Question 4 (they are NOT in the Parmar paper). Read the FLEX Dataset Paper.pdf (repo root) and Fitness-AQA/Fine-Tuning Large Multimodal Models...pdf for Squat KIE/KFE F-score numbers on the same official splits. IF found: record the exact numbers + the page citation, add the bars to phase3_vs_phase4_comparison.png (regenerate the figure with the GYMetricPose/LMM bars), and add them to results.pkl's comparison dict. IF NOT found (the numbers are on different metrics/splits, or not reported): document the absence in the SUMMARY ("GYMetricPose/LMM Squat KIE/KFE F1 on the official split not available in the repo PDFs") — the core Phase3-vs-Phase4-vs-Parmar bar does NOT require them, and fabricating numbers is forbidden ([[feedback_ai_correctness]] / T-04-15). This is a small read task; it does NOT block the phase if the numbers are unavailable.

    Single runnable unit — user pastes back: the sourced GYMetricPose/LMM numbers + page citations (or a clear "not found in the repo PDFs, chart uses Parmar rows only"), and the regenerated comparison chart if the bars were added.

    Atomic commit: feat(04): source GYMetricPose/LMM chart numbers (Task 6, RESEARCH §16 A16) — or docs(04) if documenting absence.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png</automated>
  </verify>
  <acceptance_criteria>
    - The FLEX + LMM PDFs are read for Squat KIE/KFE F1; numbers either sourced with a page citation OR the absence is documented (NEVER fabricated — T-04-15)
    - If sourced: phase3_vs_phase4_comparison.png regenerated with the GYMetricPose/LMM bars + the numbers added to results.pkl
    - If not found: the SUMMARY notes the absence; the core Phase3-vs-Phase4-vs-Parmar chart stands
    - Runnable unit: user pastes back the sourced numbers + citations (or the documented absence) + the chart
  </acceptance_criteria>
  <dependencies>Task 5 (the comparison chart exists; this task adds the GYMetricPose/LMM bars or documents their absence)</dependencies>
  <done>The headline comparison chart is finalized — GYMetricPose/LMM bars sourced with citations or their absence honestly documented; no fabricated numbers (ROADMAP criterion 3 fully satisfied)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Human verification — 9 figures + headline F1 + D6 monitors + results.pkl</name>
  <files></files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl (the final numbers),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 — to cross-check the chart numbers),
    .planning/phases/03-squat-supervised-baseline/03-01-PLAN.md (Task 15 human-verify pattern — the figure-by-figure visual criteria)
  </read_first>
  <what-built>
    - Nine PNG figures under .planning/phases/04-squat-motion-disentangling-ssl/figures/: ssl_loss_curve, linear_probe_curve, finetune_curves_per_seed, ensemble_vs_single, phase3_vs_phase4_comparison (the headline), confusion_kie, confusion_kfe, pr_kie, pr_kfe, sample_predictions
    - results.pkl — ensemble+TTA + single-model + per-seed test F1/PR-AUC/confusion, val-tuned thresholds, the chosen TTA recipe, val-test gap, raw ensemble test scores/labels/clip_ids, the comparison numbers, run provenance
    - 3 fine-tune best.pt + the MD backbone.pt on Drive (the production candidates)
    - The notebook 04_squat_md_ssl.py + .ipynb end-to-end (probe -> SSL pretrain -> 3-seed fine-tune -> ensemble -> TTA -> test eval -> 9 figures)
  </what-built>
  <how-to-verify>
    1. Open phase3_vs_phase4_comparison.png (THE HEADLINE). Confirm: Phase 3 macro bar = 0.5429; paper Kinetics = 0.5577; paper MD = 0.6262 (cross-check against RESEARCH §16); our ensemble+TTA bar is plotted with its actual value; the 0.5429 + 0.6262 reference lines are present. Confirm the bars are visually compelling for the 2026-06-03 defense ([[project_supervisor_visualizations]]).
    2. Open ssl_loss_curve.png + linear_probe_curve.png. Confirm: SSL loss trends down; embedding_std stays above the collapse threshold; the linear-probe macro-F1 rose then plateaued (the convergence evidence).
    3. Open finetune_curves_per_seed.png. Confirm: per-seed val loss did NOT diverge like Phase 3's 0.81->2.27 (or if it did, it is honestly shown) — the D3-reg + MD-init overfit-taming evidence.
    4. Open ensemble_vs_single.png. Confirm: the ensemble at or above the best single seed (the lift is real, not one lucky seed).
    5. Open confusion_kie/kfe.png + pr_kie/kfe.png + sample_predictions.png. Confirm: confusion cells sum to 244; the F1 in the title reconciles with TP/(TP+0.5(FP+FN)); PR curves bow toward upper-right; sample predictions show recognizable lifters.
    6. Paste back the results.pkl headline: test_f1_kie, test_f1_kfe, test_macro_f1 (ensemble+TTA + single-model), the val-test gap, the chosen TTA recipe. Confirm: macro = (KIE+KFE)/2 (recompute by hand); val-test gap < 0.05 (D6 monitor 2); test_f1_kie at or above 0.2857 (D6 monitor 3); the lift-vs-Phase-3 outcome.
    7. Confirm git status is clean except the planned files_modified; Fitness-AQA-Code/ + backend/data/distillation/ gitignored.
  </how-to-verify>
  <action>This is a BLOCKING human-verify checkpoint. No executor code changes. The user opens the 9 figures + pastes back the results.pkl headline numbers + the D6 monitor outcomes. The orchestrator reads the resume-signal + either advances to Task 8 (SUMMARY) or restarts the upstream task that failed verification. Per [[feedback_ai_correctness]]: the user verifies the headline number is real + the chart numbers match §16.</action>
  <verify>
    <automated>test -f .planning/phases/04-squat-motion-disentangling-ssl/figures/phase3_vs_phase4_comparison.png && test -f .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl && test -f .planning/phases/04-squat-motion-disentangling-ssl/figures/ssl_loss_curve.png</automated>
  </verify>
  <done>User replies "approved" (or "approved-flag-deviation" if no lift); Task 8 (SUMMARY) authorized</done>
  <resume-signal>Type "approved" to advance to the SUMMARY (Task 8), or describe any figure / metric issue. If the ensemble did not beat Phase 3 after the Task 4 remediation, reply "approved-flag-deviation" to proceed to closeout with the honest deviation analysis (D6 no-lift policy).</resume-signal>
</task>

<task type="auto">
  <name>Task 8: Phase closeout — 04-01-SUMMARY.md (headline F1 + Phase 5 handoff)</name>
  <files>.planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/figures/results.pkl (all final numbers),
    .planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md (the Phase 3 SUMMARY structure to mirror — headline numbers table, key findings, deviations, handoff),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§16 — the comparison numbers + the realistic outcome framing),
    .planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md (the Phase Boundary deliverable 9 — the SUMMARY must declare the production checkpoint + inference protocol + per-error thresholds + expected inference latency for Phase 5)
  </read_first>
  <action>
    Author 04-01-SUMMARY.md mirroring the Phase 3 SUMMARY structure (this is the SINGLE phase SUMMARY — Plans 01-04 are one phase). Sections: ## Phase: 04-squat-motion-disentangling-ssl; ## Status: complete (date); ## What was done (across Plans 01-04: the SSL machinery + colab edits (Plan 01), the trajectory probe + SSL pretrain (Plan 02), the 3-seed fine-tune (Plan 03), the ensemble+TTA+test+figures (Plan 04); file paths; the backbone.pt + 3 best.pt + 9 figures + results.pkl); ## Headline numbers (the comparison table from results.pkl: our single-model / ensemble / ensemble+TTA KIE/KFE/macro vs Phase 3 0.2857/0.8000/0.5429 vs paper Kinetics 0.2970/0.8184/0.5577 vs paper MD 0.4186/0.8338/0.6262 — every number per RESEARCH §16; macro = (KIE+KFE)/2); ## Key findings (whether SSL lifted over Phase 3; whether the D3 reg + MD init tamed the Phase 3 overfit; the per-seed variance; the chosen TTA recipe + whether flip helped; the final SSL aug set + actual SSL epoch count; the linear-probe convergence behavior; any collapse events); ## D6 monitor outcomes (monitor 1 runtime ratio — did any seed trip; monitor 2 val-test gap vs 0.05; monitor 3 KIE no-regress vs 0.2857; the no-lift-policy decision from Task 4); ## Deviations from PLAN (the 20-vs-50 epoch deviation, the loss-form 3-term-squared choice, any probe-driven format/sign surprises, the GYMetricPose/LMM sourcing outcome, any remediation pass); ## Acceptance criteria (the Plan 01-04 task checkboxes + the ROADMAP Phase 4 criteria); ## SQUAT-04 + SQUAT-05 status (pass/fail with the headline numbers); ## Phase 5 handoff (deliverable 9 — DECLARE: the PRODUCTION checkpoint (the ensemble of 3 best.pt OR a single best.pt, per the latency choice); the per-error thresholds; the inference protocol (single-model/no-TTA vs ensemble+TTA — give Phase 5 BOTH numbers + the forward-pass cost so it picks against its latency budget, D5); the expected inference latency per clip; the model-construction recipe (build_finetune_model + the head)); ## Next (orchestrator: ROADMAP marks SQUAT-04 + SQUAT-05 complete, STATE updated, Phase 5 ready). Mark SQUAT-04 + SQUAT-05 status in the SUMMARY (the ROADMAP/REQUIREMENTS update is the orchestrator's follow-up).

    Single runnable unit — user pastes back the rendered SUMMARY (cat the file).

    Atomic commit: docs(04): close Phase 4 - MD-SSL + ensemble + comparison (Task 8).
  </action>
  <verify>
    <automated>test -s .planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md && grep -q "SQUAT-04" .planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md && grep -q "SQUAT-05" .planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md && grep -q "Phase 5 handoff" .planning/phases/04-squat-motion-disentangling-ssl/04-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY exists; SQUAT-04 + SQUAT-05 named with status (pass/fail per the headline); the headline numbers table matches RESEARCH §16; all Plan 01-04 task checkboxes ticked
    - The Phase 5 handoff section DECLARES the production checkpoint + per-error thresholds + the single-vs-ensemble+TTA inference protocol (BOTH numbers + the forward-pass cost) + the expected latency (CONTEXT deliverable 9)
    - The D6 monitor outcomes + the no-lift-policy decision are recorded; deviations documented
    - Runnable unit: user pastes back cat of the SUMMARY
  </acceptance_criteria>
  <dependencies>Tasks 1-7 (every prior task)</dependencies>
  <done>Phase 4 closeout written; SQUAT-04 + SQUAT-05 status declared with the headline F1 vs §16; the Phase 5 handoff (production checkpoint + thresholds + inference protocol + latency) is complete; ready to advance to Phase 5</done>
</task>

</tasks>

<verification>
## Mapping to Phase 4 ROADMAP Success Criteria

| ROADMAP success criterion | Tasks that deliver it | Acceptance proof |
|---|---|---|
| (2) The MD-pretrained model, fine-tuned for KIE/KFE, reports F1 at or above the Phase-3 baseline | Tasks 1 (ensemble), 2 (TTA), 3 (test eval), 4 (no-lift gate) | Task 3 headline: ensemble+TTA test macro-F1 vs Phase 3's 0.5429 (SQUAT-05-e); the D6 no-lift policy (Task 4) bounds the outcome |
| (3) A comparison table/chart places our F1 against Parmar and GYMetricPose on identical metrics | Tasks 5 (the chart), 6 (GYMetricPose/LMM sourcing) | Task 5 phase3_vs_phase4_comparison.png with the §16-exact paper rows; Task 6 adds GYMetricPose/LMM (sourced or documented-absent) |

## Mapping to Requirements

| Requirement | Tasks | Acceptance |
|---|---|---|
| SQUAT-05 (MD-pretrained fine-tuned for KIE/KFE; F1 vs baseline AND vs published numbers) | Tasks 1-8 | Task 3 (test F1 + the §16 comparison) is the headline; Task 5/6 (the chart vs published); Task 8 (the SUMMARY declares the result + Phase 5 handoff) |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|---|---|---|---|
| SQUAT-05-e (ensemble+TTA test macro-F1 at or above 0.5429; KIE NOT below 0.2857) | Task 3 + Task 7 human-verify | integration (Colab) | 4 |
| SQUAT-05-f (val-test F1 gap < 0.05) | Task 3 (D6 monitor 2) + Task 7 | integration (Colab) | 4 |

(SQUAT-05-b/c — the ensemble/TTA aggregation unit tests — are in Plan 01.)
</verification>

<success_criteria>
- All 8 tasks complete with paste-back acceptance from the user on the L4 Colab
- The 3 seeds aggregated by mean-of-sigmoids (D4); a single per-error threshold tuned on the ensemble val scores; the TTA recipe val-tuned (D5, flip OOD-validated); both ensemble+TTA AND single-model/no-TTA test numbers reported
- The headline test F1 computed at the val-tuned thresholds; macro = (KIE+KFE)/2 in code (NOT hand-typed); the comparison matches RESEARCH §16 EXACTLY (Phase 3 0.5429, Kinetics 0.5577, MD 0.6262) — the Phase 3 arithmetic error NOT repeated (T-04-12)
- The D6 post-hoc monitors reported: val-test gap < 0.05 (monitor 2), KIE at or above 0.2857 (monitor 3); the no-lift-policy decision made (Task 4 gate — lift / one remediation pass / honest deviation)
- 9 supervisor figures + results.pkl written BEFORE plt.show(); headline = phase3_vs_phase4_comparison.png with the §16-exact numbers + reference lines; figures + results.pkl backed up to Drive
- GYMetricPose/LMM chart numbers sourced with citations OR their absence documented (NEVER fabricated — T-04-15)
- 04-01-SUMMARY.md written: SQUAT-04 + SQUAT-05 status, the headline numbers vs §16, the D6 outcomes, the Phase 5 handoff (production checkpoint + per-error thresholds + single-vs-ensemble+TTA inference protocol + expected latency per CONTEXT deliverable 9)
- git status clean except the planned files_modified; every D4/D5/D6/D8 decision carries an inline citation ([[feedback_ai_correctness]])
- One concise threat_model block; T-04-12 (headline arithmetic — highest priority given Phase 3) mitigated by §16-match + in-code macro + loading Phase 3 results.pkl; T-04-13 (val-test leakage) by the gap monitor; T-04-14 (mean-of-sigmoids) by the tested function; T-04-15 (GYMetricPose fabrication) by sourced-or-omitted; T-04-SC accept
</success_criteria>

<output>
After Task 8 (the SUMMARY), Phase 4 is complete. This is the LAST plan of the phase — 04-01-SUMMARY.md is the single phase SUMMARY (Plans 01-04 are one phase, per the Phase 3 single-SUMMARY precedent). The orchestrator follows up with ROADMAP / REQUIREMENTS / STATE updates marking SQUAT-04 + SQUAT-05 complete and Phase 5 ready to plan. The Phase 5 handoff (the production checkpoint + per-error thresholds + the single-vs-ensemble+TTA inference protocol + the expected latency) is the input contract for Phase 5's FastAPI inference service.
</output>

<revision_log>
Phase 4 Plan 04 written 2026-05-21. Wave 4: ensemble + val-tuned TTA + test eval + the 9 figures + the headline comparison chart + the phase SUMMARY. 8 tasks. This plan WIRES the Plan 01 ensemble/tta/metrics pure functions (all unit-tested) on the Plan 03 best.pt checkpoints — no new production logic. The aggregation order is D5-exact (per-seed -> average TTA copies -> mean-of-sigmoids across seeds -> val-tuned threshold). Two checkpoints: Task 4 (the D6 no-lift decision gate — lift / one remediation pass / honest deviation) and Task 7 (human-verify the figures + headline numbers).

CRITICAL arithmetic discipline (the constraint that Phase 3's plan-checker missed + the user caught): every numeric acceptance criterion + the chart matches RESEARCH §16 EXACTLY — Phase 3 macro 0.5429 (0.2857/0.8000), paper Kinetics 0.5577 (0.2970/0.8184), paper MD 0.6262 (0.4186/0.8338). macro = (KIE+KFE)/2 is computed in code, not hand-typed; the chart loads Phase 3's results.pkl for the baseline rather than a hand-typed number (T-04-12).

GYMetricPose/LMM (RESEARCH §16 A16 / Open Question 4): NOT in the Parmar paper; Task 6 sources them from FLEX Dataset Paper.pdf / the LMM paper at repo root (both confirmed present at repo root) with a page citation, or documents their absence — never fabricated (T-04-15). The core Phase3-vs-Phase4-vs-Parmar bar does not require them.

The phase SUMMARY (04-01-SUMMARY.md) is the single SUMMARY for Plans 01-04 (Phase 3 single-SUMMARY precedent). It declares the Phase 5 handoff per CONTEXT deliverable 9: the production checkpoint, per-error thresholds, the single-vs-ensemble+TTA inference protocol (BOTH numbers + the forward-pass cost), and the expected latency.

Security: proportionate. T-04-12 (headline arithmetic — highest priority), T-04-13 (val-test leakage — the gap monitor), T-04-14 (mean-of-sigmoids — the tested function), T-04-15 (GYMetricPose fabrication — sourced-or-omitted), T-04-SC accept.

note on human_verify_mode=end-of-phase: Task 7 (figure + headline-number human-verify) is the end-of-phase verification — consistent with the config default AND the Phase 3 Task 15 precedent. Task 4 (the D6 no-lift decision) is a checkpoint:decision required by CONTEXT D6's no-lift policy (a substantive design choice, not a routine gate).

No revisions yet — initial PLAN.md.
</revision_log>
