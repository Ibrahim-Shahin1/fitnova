---
phase: 04-squat-motion-disentangling-ssl
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/training/aqa/datasets/squat_ssl.py
  - backend/training/aqa/datasets/ssl_augs.py
  - backend/training/aqa/harness/md_pretrain.py
  - backend/training/aqa/harness/md_finetune.py
  - backend/training/aqa/harness/colab.py
  - backend/training/aqa/eval/ensemble.py
  - backend/training/aqa/eval/tta.py
  - backend/training/aqa/harness/test_md_pretrain.py
  - backend/training/aqa/harness/test_md_finetune.py
  - backend/training/aqa/eval/test_ensemble.py
  - backend/training/aqa/eval/test_tta.py
  - backend/training/aqa/harness/test_colab_update_latest.py
autonomous: true
requirements: [SQUAT-04, SQUAT-05]
must_haves:
  truths:
    - "The half-cycle splitter split_half_cycles(traj_y, frames_per_half=16) returns two 16-element frame-index arrays where every descent index is <= the detected bottom and every ascent index is >= the bottom, on a synthetic single-parabola trajectory — proving the descent/ascent contract before any real Colab trajectory is touched"
    - "md_triplet_loss(phi_a, phi_p, phi_n) matches a hand-computed value on known L2-normalized embeddings for both the 3-term (default, official-code) and 2-term (Eq.1) denominator forms, and is strictly lower when anchor approx positive and far-from-negative than the reverse — proving the reconstructed-from-paper loss arithmetic before the 12-24h GPU burn"
    - "ProjectionHead(512, 512, 128) produces L2-normalized [B,128] output (per-row norm approx 1.0) and aggregate_sigmoid_mean produces ensemble scores strictly in [0,1] (satisfying pr_auc_per_error's [0,1] assertion at metrics.py:72)"
    - "atomic_save_checkpoint(payload, path, update_latest=False) writes the checkpoint file but leaves latest.txt byte-unchanged, while the default update_latest=True preserves the exact Phase 2/3 behavior — proving the D7 latest.txt-clobber landmine fix without breaking the existing epoch-resume pointer"
    - "python -m pytest backend/training/aqa/ -x -q exits 0 with all new unit tests green (splitter, triplet-loss arithmetic, projector L2-norm, ensemble/TTA aggregation, both checkpoint schemas, the update_latest kwarg) — the Wave 0 GPU-burn gate is locked behind a green pure-function suite"
  artifacts:
    - path: "backend/training/aqa/datasets/squat_ssl.py"
      provides: "SSL triplet dataset scaffold + split_half_cycles pure function (the only genuinely new sampling logic). __getitem__ returns {anchor, positive, negative}; trajectory-loading body is a documented stub finalized in Plan 02 after the probe."
      contains: "def split_half_cycles"
    - path: "backend/training/aqa/datasets/ssl_augs.py"
      provides: "Per-branch SSL augmentation primitives (§7): temporal_shift, horizontal_flip, top_mask, color_jitter, translation — operate on [T,3,H,W] uint8 before spatial_*"
      contains: "def horizontal_flip"
    - path: "backend/training/aqa/harness/md_pretrain.py"
      provides: "MDConfig, ProjectionHead, md_triplet_loss (Eq.1 + official train_test.py 3-term form, config-flagged), build_md_model (R(2+1)D-18 fc=Identity + projector), SSL checkpoint payload schema. Training-loop body finalized in Plan 02."
      exports: ["MDConfig", "ProjectionHead", "md_triplet_loss", "build_md_model"]
    - path: "backend/training/aqa/harness/md_finetune.py"
      provides: "FinetuneConfig (AdamW wd=1e-4 / dropout=0.2 / lr=1e-4 / 50ep / 8-patience), build_finetune_model (loads MD backbone NOT Kinetics, fresh Dropout+Linear head). Training-loop body finalized in Plan 03."
      exports: ["FinetuneConfig", "build_finetune_model"]
    - path: "backend/training/aqa/harness/colab.py"
      provides: "EDITED — atomic_save_checkpoint gains update_latest: bool = True kwarg (D7 latest.txt-clobber fix); stage_unlabeled_squat_videos added (4,970-clip + trajectory staging analog of stage_squat_videos)"
      contains: "update_latest"
    - path: "backend/training/aqa/eval/ensemble.py"
      provides: "aggregate_sigmoid_mean(per_seed_logits) — mean-of-sigmoids across seeds, output in [0,1] (D4). Pure numpy, no torch, no I/O."
      exports: ["aggregate_sigmoid_mean"]
    - path: "backend/training/aqa/eval/tta.py"
      provides: "tta_forward(model, clip, recipe, device) + select_tta_recipe(val_logits_per_recipe, val_labels) (val-tuned, D5) + inline 5-crop. Reuses transforms.py; does NOT modify it."
      exports: ["tta_forward", "select_tta_recipe"]
    - path: "backend/training/aqa/harness/test_md_pretrain.py"
      provides: "Unit tests SQUAT-04-a/b/c/d/e/f — half-cycle split on synthetic parabola, triplet-loss known value + directionality, projector L2-norm, model build (slow), SSL checkpoint schema"
    - path: "backend/training/aqa/harness/test_md_finetune.py"
      provides: "Unit test SQUAT-05-a — build_finetune_model loads MD backbone (not Kinetics), head = Dropout(0.2)+Linear(512,2) (slow)"
    - path: "backend/training/aqa/eval/test_ensemble.py"
      provides: "Unit test SQUAT-05-b — aggregate_sigmoid_mean averages per-seed sigmoids; output in [0,1]"
    - path: "backend/training/aqa/eval/test_tta.py"
      provides: "Unit test SQUAT-05-c — select_tta_recipe returns the val-F1-maximizing combo on synthetic scores"
    - path: "backend/training/aqa/harness/test_colab_update_latest.py"
      provides: "Unit test for the D7 fix — update_latest=False leaves latest.txt unchanged; update_latest=True (default) preserves Phase 2/3 behavior"
  key_links:
    - from: "backend/training/aqa/harness/md_pretrain.py"
      to: "backend/training/aqa/eval/metrics.py (f1_per_error for the linear-probe monitor)"
      via: "import"
      pattern: "from backend.training.aqa.eval.metrics import"
    - from: "backend/training/aqa/eval/ensemble.py"
      to: "backend/training/aqa/eval/metrics.py (pr_auc_per_error's [0,1] assertion — ensemble must satisfy it)"
      via: "design contract (sigmoid mean in [0,1])"
      pattern: "ensemble.min.*0.0 and ensemble.max.*1.0"
    - from: "backend/training/aqa/harness/md_finetune.py"
      to: "MD backbone.pt produced by md_pretrain.py (Plan 02)"
      via: "build_finetune_model loads backbone_state_dict, NOT Kinetics weights"
      pattern: "load_state_dict.*backbone_state_dict"
---

<objective>
Land all genuinely-new Phase 4 code modules as scaffolds with their pure-function cores FULLY IMPLEMENTED and unit-tested, plus the two colab.py modifications (the update_latest kwarg and stage_unlabeled_squat_videos). This is Wave 0 per RESEARCH §15: the half-cycle splitter (§1), the triplet distance-ratio loss (§2), the projection head (§4), the ensemble/TTA aggregation (§13/§14), and the checkpoint schemas are reconstructed-from-paper logic that MUST be verified on synthetic data BEFORE the 12-24h SSL GPU burn. The training-loop bodies that need GPU + real Colab data are landed as documented stubs here and finalized in Plans 02/03 once their preconditions (the trajectory-format probe, the VRAM probe) are met.

Purpose: RESEARCH §15 + the "Key insight" at RESEARCH line 849 — the only genuinely novel logic in this phase is (1) the half-cycle splitter and (2) the triplet loss + 3-branch forward; both have unit-testable pure-function cores, and both must be verified on synthetic data before the GPU burn. A wrong triplet-loss sign or a wrong splitter contract would waste 12-24h of L4 compute. This plan locks that risk behind a green pytest suite.

Output: 11 new + 1 edited .py files under backend/training/aqa/{datasets, harness, eval}/ — 6 production modules (squat_ssl, ssl_augs, md_pretrain, md_finetune, ensemble, tta), 1 edited module (colab.py), and 5 test files. Each pure-function core is implemented and tested; each GPU/Colab-dependent body is a documented stub with the exact finalization plan referenced inline.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]]: each task hands the user exactly one runnable unit (a pytest invocation or a python -c smoke) and stops until output is pasted back. NEVER pre-write task N+1 before task N's paste-back is in. These tasks run locally (no GPU, no Colab, no Drive) — they are CPU-only pure-function tests, so paste-back is fast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-CONTEXT.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-VALIDATION.md
@backend/training/aqa/datasets/squat.py
@backend/training/aqa/datasets/transforms.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/eval/test_metrics.py
@backend/training/aqa/harness/supervised_train.py
@backend/training/aqa/harness/test_supervised_train.py
@backend/training/aqa/harness/colab.py
@CLAUDE.md

<interfaces>
Contracts this plan creates that Plans 02/03/04 consume. The executor implements these EXACT signatures — no codebase scavenger hunt. Extracted from RESEARCH §1,§2,§4,§5,§9,§10,§13,§14 + PATTERNS file map + CONTEXT D2,D3,D4,D5,D7. Full code excerpts for each live in 04-PATTERNS.md (cited per task in read_first); do NOT re-derive them.

datasets/squat_ssl.py:
- split_half_cycles(traj_y: np.ndarray, *, frames_per_half=16, smooth_sigma=2.0, bottom_is_argmax=True) -> tuple[np.ndarray, np.ndarray] — returns (descent_indices[16], ascent_indices[16]); descent[-1] <= bottom <= ascent[0]; uses scipy.ndimage.gaussian_filter1d + argmax/argmin + uniform_sample_indices. SIGN (bottom_is_argmax) resolved empirically in Plan 02 probe (§1 pt.3), NOT hard-coded.
- class SquatSSLDataset(Dataset) — __getitem__ returns dict {anchor, positive, negative}, each [3,16,H,W] float32; _load_trajectory body STUBBED (Plan 02 Task 2 finalizes after trajectory-format probe).
- build_ssl_loader(...) — DataLoader with persistent_workers=num_workers>0, shuffle=True.

datasets/ssl_augs.py (operate on [T,3,H,W] uint8, return same dtype; §7):
- temporal_shift(clip, *, max_shift=2, generator=None); horizontal_flip(clip, *, p=0.5, generator=None); top_mask(clip, *, mask_frac=0.4, generator=None); color_jitter(clip, *, brightness=0.2, contrast=0.2, generator=None); translation(clip, *, max_px=10, generator=None).

harness/md_pretrain.py:
- @dataclass MDConfig — learning_rate=1e-4 [CITED §5], weight_decay=1e-4 [ASSUMED D2], batch_size=8 [ASSUMED; paper 5 CITED §5], num_workers=4, max_epochs=60 [CITED 20 baseline; ASSUMED <=60], frames_per_half=16 [CITED §5], crop_size=112, linear_probe_cadence=5 [ASSUMED §6], projector_hidden=512, projector_out_dim=128 [ASSUMED §4], loss_squared=True [§2], loss_three_term=True [§2], scheduler_name="cosine", model_arch="r2plus1d_18_md_ssl_v1"; __post_init__ sets scheduler_t_max=max_epochs.
- class ProjectionHead(nn.Module) — 2-layer MLP 512->512->128, BN+ReLU, forward returns F.normalize(net(x), dim=-1, p=2) shape [B,128]. L2-norm CITED train_test.py:51.
- md_triplet_loss(phi_a, phi_p, phi_n, *, squared=True, three_term=True, eps=1e-9) -> Tensor — d_ap=((phi_a-phi_p)**2).sum(-1); num=exp(-d_ap); den=num+exp(-d_an)[+exp(-d_pn) if three_term]; (-log(num/(den+eps))).mean(). Paper Eq.1 p.6 + official train_test.py:60-71.
- build_md_model() -> tuple[nn.Module, nn.Module] — r2plus1d_18(KINETICS400_V1), assert fc.in_features==512, fc=Identity, return (backbone, ProjectionHead).
- SSL checkpoint payload keys (test_ssl_checkpoint_schema asserts): epoch, backbone_state_dict, projector_state_dict, optimizer_state_dict, scheduler_state_dict, rng_state, metrics_history, linear_probe_history, config_hash, config_repr, code_version="phase04-md-pretrain".

harness/md_finetune.py:
- @dataclass FinetuneConfig — learning_rate=1e-4 [CITED §9], weight_decay=1e-4 [LOCKED D3], dropout=0.2 [LOCKED D3], batch_size=16, num_workers=4, max_epochs=50 [LOCKED D3], early_stop_patience=8 [LOCKED D3], num_frames=32, crop_size=112, train_jitter_frames=2, flip_aug=False [LOCKED Phase 2 D5], scheduler_name="cosine", model_arch="r2plus1d_18_md_finetuned", loss_name="BCEWithLogitsLoss(pos_weight=dataset.pos_weight)", md_backbone_path=""; __post_init__ sets scheduler_t_max=max_epochs.
- build_finetune_model(md_backbone_path: str, *, dropout=0.2) -> nn.Module — r2plus1d_18(weights=None), torch.load(map_location='cpu'), load_state_dict(backbone_state_dict, strict=False), assert fc.in_features==512, fc=nn.Sequential(nn.Dropout(dropout), nn.Linear(512,2)). NOT Kinetics (§9).

eval/ensemble.py (pure numpy, no torch, no I/O):
- aggregate_sigmoid_mean(per_seed_logits: list[np.ndarray]) -> np.ndarray — sigmoid each (N,2) logit array, mean across seeds, assert output in [0,1]. D4 mean-of-sigmoids. Raises ValueError on empty/inconsistent shapes.

eval/tta.py:
- tta_forward(model, clip[3,32,112,112], recipe: list[str], device) -> np.ndarray (2,) — apply augs from {temporal_jitter, spatial_5crop, flip}, run each through model, return MEAN LOGITS (NOT sigmoid; caller sigmoids before ensemble). 5-crop inline (does NOT modify transforms.py, D9).
- select_tta_recipe(val_logits_per_recipe: dict[tuple, np.ndarray], val_labels: np.ndarray) -> tuple[str, ...] — return val-macro-F1-maximizing recipe via eval/metrics.py threshold_sweep + f1_per_error. D5.

harness/colab.py (EDIT — D7/§10):
- atomic_save_checkpoint(payload, target_path, *, update_latest=True) -> None — Step 4 (latest.txt write) guarded by if update_latest. Default True = Phase 2/3 behavior.
- stage_unlabeled_squat_videos(drive_root, *, local_videos_root="/content/squat_unlabeled_videos", local_traj_root="/content/squat_trajectories", expect_count=4970) -> tuple[str, str] — stages videos.zip + bar_trajectories_raw.zip by REUSING _copy_with_resume_and_progress + _extract_with_resume_and_progress (do NOT duplicate resume logic).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

Locked from CONTEXT.md (D1-D9) + RESEARCH overrides. The decisive RESEARCH override: the SSL loss is the triplet distance-ratio loss (Eq.1 / official train_test.py), NOT NT-Xent (RESEARCH §2 — CITED, overrides CONTEXT D2's provisional "SimCLR-family default").

| # | Decision | Source |
|---|----------|--------|
| D2-loss | Loss = triplet distance-ratio (Hoffer & Ailon 2015), squared L2, 3-term denominator as primary (matches official train_test.py:60-71 active line), with squared/three_term config flags exposing the exact-Eq.1 (non-squared, 2-term) form for ablation. NO temperature, NO stop-gradient, NO in-batch negatives. | RESEARCH §2 (CITED) overriding CONTEXT D2 |
| D2-proj | ProjectionHead = 2-layer MLP 512->512->128, BN+ReLU, L2-normalized output (F.normalize dim=-1 p=2). L2-norm CITED train_test.py:51; dims SimCLR-standard [ASSUMED]. Discarded at fine-tune (backbone-only transfer). | RESEARCH §4 |
| D1-splitter | split_half_cycles = global extremum of smoothed y (gaussian_filter1d sigma=2 then argmax), 16 frames/half via uniform_sample_indices. The argmax-vs-argmin SIGN is a bottom_is_argmax bool param resolved empirically in Plan 02's probe — NOT hard-coded here. | RESEARCH §1 |
| D3 | FinetuneConfig = AdamW, wd=1e-4, dropout=0.2 head-only, lr=1e-4, 50ep/8-patience, flip OFF. build_finetune_model loads MD backbone (NOT Kinetics). | CONTEXT D3 + RESEARCH §9 |
| D4 | aggregate_sigmoid_mean = mean of sigmoids across seeds, output in [0,1] (satisfies pr_auc_per_error assertion). NOT mean-of-logits. | CONTEXT D4 + RESEARCH §13 |
| D5 | tta_forward returns mean LOGITS; select_tta_recipe is val-tuned. 5-crop inline (does NOT modify transforms.py). Flip is a candidate, not assumed-good (OOD — Phase 3 trained flip OFF). | CONTEXT D5 + RESEARCH §14 |
| D7-kwarg | atomic_save_checkpoint gains update_latest: bool = True. Default True = Phase 2/3 behavior; backbone.pt/best.pt writes pass update_latest=False so latest.txt epoch-resume pointer is never clobbered. | CONTEXT D7 + RESEARCH §10 + Pitfall 4 |
| D7-stage | stage_unlabeled_squat_videos REUSES _copy_with_resume_and_progress + _extract_with_resume_and_progress — ~30 lines parameterizing existing primitives. Do NOT duplicate the 3-layer resume logic. | RESEARCH §10 |
| D9 | eval/metrics.py reused UNCHANGED; transforms.py reused UNCHANGED (5-crop is inline in tta.py); datasets/squat.py reused for fine-tune; supervised_train.py NOT edited (md_finetune is a new module). New code uses logger = logging.getLogger("aqa.phase04"). | CONTEXT D9 |

## Stub policy (Plan 01 scaffold discipline)

This plan FULLY IMPLEMENTS every pure-function core (splitter, loss, projector, aggregation, model-build, colab edits) and unit-tests them. It STUBS only the GPU/Colab-data-dependent training-loop bodies, each as a documented NotImplementedError with the exact finalization plan/task referenced inline:
- SquatSSLDataset._load_trajectory -> NotImplementedError("Plan 02 Task 2 — finalize after trajectory-format probe (§8)")
- md_pretrain.run_md_pretrain_epoch, _linear_probe, collapse-detection helpers -> NotImplementedError("Plan 02 — finalize after VRAM probe (§5) + probe-confirmed dataset")
- md_finetune.run_md_finetune_epoch -> NotImplementedError("Plan 03 — finalize; parallels supervised_train.run_supervised_epoch + D6 monitor")

Rationale ([[feedback_ai_correctness]] + RESEARCH "Key insight" line 849): the novel logic that can be verified WITHOUT GPU is verified WITHOUT GPU, before the burn. The bodies that need real Colab trajectories (whose format is [ASSUMED] until Plan 02's probe) or measured VRAM are deferred to the plan that has that information — writing them blind would violate [[feedback_interactive_execution]] "understand the data before building."

</decisions>

<determinism_checklist>
## Determinism / hygiene preconditions (carry Phase 2/3 F12 + D7 landmines)

1. logger = logging.getLogger("aqa.phase04") in every new module (PATTERNS §5 — phase04, NOT phase02/03).
2. from __future__ import annotations as the first import in every new file (CLAUDE.md backend convention; every existing backend/training/aqa/ file has it).
3. persistent_workers = config.num_workers > 0 guard wired into every DataLoader-constructing helper (squat_ssl build_ssl_loader, md_pretrain/md_finetune dataloader builders) — encoded now even though those run only in Plans 02/03 ([[reference_pytorch_persistent_workers]] / D7).
4. map_location='cpu' on every torch.load / load_latest_checkpoint call (D7; Phase 3 fix a0841b4 — CUDA ByteTensors break set_rng_state_all). Encoded in build_finetune_model and the stubbed resume branches.
5. Lazy torchvision import inside build_md_model / build_finetune_model (PATTERNS §4 — keeps pytest fast on CPU-only machines; the slow model-build tests use @pytest.mark.slow).
6. torch import guarded in test files via pytest.importorskip("torch") at module top for torch-dependent tests; test_ensemble.py is pure numpy and needs no torch (PATTERNS test-file rule).
7. atomic_save_checkpoint backward compatibility: the update_latest kwarg defaults True so the Phase 2/3 call sites (supervised_train.py, tiny_train.py) are byte-for-byte unaffected. Verified by test_colab_update_latest.py.

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is OFFLINE ML training in Colab with no network-facing surface, no authentication, no untrusted user input at runtime, no PII (RESEARCH Security Domain — ASVS V2-V6 N/A). The only relevant boundaries are data/checkpoint integrity and the non-commercial dataset license. Proportionate threat model per the planning brief.

| Boundary | Description |
|----------|-------------|
| In-memory checkpoint -> Drive | backbone.pt / epoch_NNN.pt written over Drive FUSE (non-atomic rename) — Phase 2 atomic-write contract mitigates; the new update_latest=False path must NOT corrupt the resume pointer. |
| Reconstructed-from-paper logic -> trained weights | The triplet loss (§2) and half-cycle splitter (§1) are reconstructed from paper text + official code (the MD code release is empty). A wrong sign or wrong denominator silently wastes 12-24h GPU. |
| Source-artifact decisions -> code | Every [ASSUMED] value (projector dims, augmentation magnitudes, loss form) carries an inline citation/label so examiners see the audit trail ([[feedback_ai_correctness]]). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | md_triplet_loss reconstructed from paper Eq.1 + official code (paper-vs-code discrepancy: 2-term vs 3-term, squared vs not) — a wrong form trains a meaningless representation | mitigate | Task 5 unit tests test_triplet_loss_known (hand-computed value, both forms) + test_triplet_loss_direction (anchor approx pos and far-from-neg gives lower loss than reverse). Both squared/three_term exposed as MDConfig flags so the exact-Eq.1 form is ablatable (§2 / A4). Docstring cites paper Eq.1 p.6 AND train_test.py:68. MEDIUM — caught in pytest before GPU burn. |
| T-04-02 | Tampering | split_half_cycles argmax-vs-argmin SIGN is [ASSUMED] (image-y-down vs amplitude-up) — wrong sign swaps descent/ascent | mitigate | Sign is a bottom_is_argmax PARAM (not hard-coded); Task 2 test asserts descent<=bottom<=ascent on a synthetic parabola for BOTH sign values; the real-data sign resolution is Plan 02's checkpoint:human-verify probe BEFORE any GPU burn (RESEARCH §1 pt.3 / Pitfall 2). MEDIUM — deferred to the gated probe, contract proven now. |
| T-04-03 | Integrity | atomic_save_checkpoint(update_latest=False) for backbone.pt/best.pt — if the guard is wrong, latest.txt points to backbone.pt and SSL resume jumps to the wrong epoch | mitigate | Task 12 unit test test_colab_update_latest.py asserts (a) update_latest=False leaves latest.txt byte-unchanged, (b) update_latest=True (default) rewrites it — directly tests Pitfall 4 / §10. MEDIUM — caught in pytest. |
| T-04-04 | Tampering | Dependency supply chain — zero new pip installs this phase (RESEARCH Package Legitimacy Audit: scipy/sklearn/numpy already in requirements.txt; torch/torchvision Colab-provided) | accept | No new packages -> no slopcheck/registry audit applies. The Phase 3 Step-0 dep-version probe (carried into Plan 02's notebook) is the supply-chain guard. LOW — no new attack surface. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package-manager installs in this plan (RESEARCH Package Legitimacy Audit: "(none added)"). No [ASSUMED]/[SUS] packages exist; the legitimacy checkpoint is not applicable. |

All threats MEDIUM at worst; none block execution. T-04-01/02/03 each have an explicit Task-level pytest acceptance proof — the whole point of Wave 0 is to catch them before the GPU burn.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold all 6 new modules + 4 test files (stubs + contracts only)</name>
  <files>
    backend/training/aqa/datasets/squat_ssl.py,
    backend/training/aqa/datasets/ssl_augs.py,
    backend/training/aqa/harness/md_pretrain.py,
    backend/training/aqa/harness/md_finetune.py,
    backend/training/aqa/eval/ensemble.py,
    backend/training/aqa/eval/tta.py,
    backend/training/aqa/harness/test_md_pretrain.py,
    backend/training/aqa/harness/test_md_finetune.py,
    backend/training/aqa/eval/test_ensemble.py,
    backend/training/aqa/eval/test_tta.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat.py (analog for squat_ssl — imports, docstring, Dataset skeleton, build_loader),
    backend/training/aqa/datasets/transforms.py (analog for ssl_augs — function-signature + smoke-test patterns; the decode/sample/spatial contracts squat_ssl reuses),
    backend/training/aqa/harness/supervised_train.py (analog for md_pretrain/md_finetune — module docstring, imports, dataclass, build_model),
    backend/training/aqa/eval/metrics.py (analog for ensemble/tta — pure-function module, no torch/IO),
    backend/training/aqa/eval/test_metrics.py (analog for test files — _arr helper, section separators, importorskip),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (every section — exact import blocks, docstring rules, dataclass/skeleton/test patterns for all 6 files)
  </read_first>
  <action>
    Create all 6 production modules and 4 test files as scaffolds. Implementation lands in Tasks 2-11; this task lands ONLY module docstrings, imports, the public function/class SIGNATURES from the interfaces block, and stub bodies. Pure-function stub bodies raise NotImplementedError("Task N") naming the implementing task. GPU/Colab-dependent stub bodies raise NotImplementedError("Plan 0X Task Y — reason") per the decisions stub policy. Do NOT inline any implementation in this task.

    Per-file (follow the PATTERNS analog exactly):

    squat_ssl.py — docstring per PATTERNS squat_ssl section (yields anchor/positive/negative dict); from __future__ import annotations; imports logging, numpy as np, scipy.ndimage, torch, Dataset, and decode_clip/spatial_train/uniform_sample_indices from transforms, plus ssl_augs; logger aqa.phase04. split_half_cycles signature from interfaces, body NotImplementedError("Task 2"). class SquatSSLDataset(Dataset) with __init__/__len__/__getitem__/_load_trajectory signatures; _load_trajectory body NotImplementedError("Plan 02 Task 2 — finalize after trajectory-format probe (§8)"); __getitem__/__len__ body NotImplementedError("Plan 02"). build_ssl_loader signature, body NotImplementedError("Plan 02").

    ssl_augs.py — docstring per PATTERNS ssl_augs section; from __future__ import annotations; imports logging, random, torch, torchvision.transforms.functional as TF; logger aqa.phase04. Five function signatures (temporal_shift, horizontal_flip, top_mask, color_jitter, translation) from interfaces; bodies NotImplementedError("Task 3").

    md_pretrain.py — docstring per PATTERNS md_pretrain section (close with: See: 04-CONTEXT.md — D2 / D7 / D8); imports per PATTERNS (dataclasses, logging, os, random, time, typing.Any, numpy, torch, torch.nn as nn, torch.nn.functional as F, the eval.metrics + colab imports); logger aqa.phase04. Land @dataclass MDConfig fully per interfaces WITH inline CITED/ASSUMED comments + __post_init__. class ProjectionHead(nn.Module) signature; __init__ and forward bodies NotImplementedError("Task 6"). md_triplet_loss signature, body NotImplementedError("Task 5"). build_md_model signature, body NotImplementedError("Task 8"). run_md_pretrain_epoch + _linear_probe signatures, bodies NotImplementedError("Plan 02 — finalize after VRAM probe (§5)").

    md_finetune.py — docstring per PATTERNS md_finetune section; imports mirror supervised_train.py (add SquatKIEKFEDataset import path); logger aqa.phase04. Land @dataclass FinetuneConfig fully per interfaces WITH inline LOCKED D3/CITED comments + __post_init__. build_finetune_model signature, body NotImplementedError("Task 9"). run_md_finetune_epoch signature, body NotImplementedError("Plan 03").

    ensemble.py — docstring per PATTERNS ensemble section (pure numpy, no torch/IO; close with See: ... — D4); from __future__ import annotations; import logging; import numpy as np; logger aqa.phase04. aggregate_sigmoid_mean signature, body NotImplementedError("Task 10").

    tta.py — docstring per PATTERNS tta section (close with See: ... — D5); from __future__ import annotations; import logging; import numpy as np; import torch; logger aqa.phase04. tta_forward + select_tta_recipe signatures, bodies NotImplementedError("Task 11").

    Test files — docstrings + from __future__ import annotations + import numpy as np + import pytest. test_md_pretrain.py and test_md_finetune.py: torch = pytest.importorskip("torch") at module top (they need torch). test_ensemble.py: pure numpy, NO module-top torch import. test_tta.py: pytest.importorskip("torch") only inside torch-needing tests, not at module top. Add box-drawing section separators per PATTERNS. All test bodies are pytest.skip("Task N") naming the implementing task. Add the _arr helper (from test_metrics.py:30) to each file that builds synthetic numpy arrays.

    Single runnable unit — user pastes back the two-command smoke: (1) the AST-parse of all 6 production files prints "parse ok"; (2) pytest of the 4 test files shows all skipped, 0 failures.

    Atomic commit: chore(04): scaffold SSL modules + test stubs (Task 1, D2/D3/D4/D5).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/training/aqa/datasets/squat_ssl.py','backend/training/aqa/datasets/ssl_augs.py','backend/training/aqa/harness/md_pretrain.py','backend/training/aqa/harness/md_finetune.py','backend/training/aqa/eval/ensemble.py','backend/training/aqa/eval/tta.py']]; print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 production .py files parse as valid Python; each has a module docstring + from __future__ import annotations + logger = logging.getLogger("aqa.phase04")
    - MDConfig() defaults: batch_size==8, frames_per_half==16, loss_three_term==True, loss_squared==True, max_epochs==60, scheduler_t_max==60 (set by __post_init__)
    - FinetuneConfig() defaults: learning_rate==1e-4, weight_decay==1e-4, dropout==0.2, max_epochs==50, early_stop_patience==8, flip_aug==False, scheduler_t_max==50
    - python -c "from backend.training.aqa.harness.md_pretrain import MDConfig, ProjectionHead, md_triplet_loss, build_md_model; from backend.training.aqa.harness.md_finetune import FinetuneConfig, build_finetune_model; from backend.training.aqa.eval.ensemble import aggregate_sigmoid_mean; from backend.training.aqa.eval.tta import tta_forward, select_tta_recipe; from backend.training.aqa.datasets.squat_ssl import split_half_cycles; print('imports ok')" succeeds (attributes exist even though they raise NotImplementedError when called)
    - pytest of the 4 new test files: all skipped, 0 failures
    - Runnable unit: user pastes back the AST-parse "parse ok" + the config-contracts assertion + the pytest skip summary
  </acceptance_criteria>
  <dependencies>None — Wave 0 entry task</dependencies>
  <done>All 6 SSL modules + 4 test files scaffolded with contracts + stubs; Tasks 2-11 have somewhere to land implementation</done>
</task>

<task type="auto">
  <name>Task 2: Implement split_half_cycles + SQUAT-04-a test (synthetic parabola)</name>
  <files>
    backend/training/aqa/datasets/squat_ssl.py,
    backend/training/aqa/harness/test_md_pretrain.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat_ssl.py (the split_half_cycles stub from Task 1),
    backend/training/aqa/datasets/transforms.py (uniform_sample_indices at line 33 — split_half_cycles calls it for both half-cycles; understand its signature and return type),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§1 — the full splitter algorithm, the argmax-vs-argmin sign note pt.3, the gaussian_filter1d sigma=2 choice, the multi-rep handling),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (No Analog Found section — split_half_cycles has no codebase analog; use RESEARCH §1)
  </read_first>
  <action>
    Implement split_half_cycles(traj_y, *, frames_per_half=16, smooth_sigma=2.0, bottom_is_argmax=True) per RESEARCH §1. Steps: (1) sm = scipy.ndimage.gaussian_filter1d(traj_y.astype(float), sigma=smooth_sigma); (2) bottom = int(np.argmax(sm)) if bottom_is_argmax else int(np.argmin(sm)); (3) descent = uniform_sample_indices(num_frames=bottom+1, target=frames_per_half); ascent = uniform_sample_indices(num_frames=len(sm)-bottom, target=frames_per_half) then add bottom as an offset; (4) return (descent.numpy(), ascent.numpy()) both length frames_per_half. Inline comment cites RESEARCH §1 + the sign note: the bottom_is_argmax param exists because Fig.3 plots amplitude (possibly inverted) and image-y increases downward — the real sign is resolved in Plan 02's probe. Defensive: if len(traj_y) < 2 raise ValueError. Do NOT hard-code a sign; do NOT implement multi-rep find_peaks here (Plan 02 adds it after the probe measures multi-rep frequency).

    In test_md_pretrain.py, replace the SQUAT-04-a skip with test_half_cycle_split: build a synthetic single-parabola trajectory traj = -(np.linspace(-1,1,80)**2) (a downward parabola — bottom at the MIDDLE for argmin) AND traj_inv = (np.linspace(-1,1,80)**2) (bottom at middle for argmax). For each, call split_half_cycles with the matching bottom_is_argmax. Assert: both returned arrays have length 16; all descent indices <= bottom <= all ascent indices (the contract); descent[0] near 0, ascent[-1] near len-1; indices are sorted and within [0, len). Also assert that flipping bottom_is_argmax on the same trajectory moves the split point (proves the sign param is wired, T-04-02 mitigation).

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_half_cycle_split -x -v (must pass; under 2 s, no torch beyond the importorskip).

    Atomic commit: feat(04): implement split_half_cycles + SQUAT-04-a test (Task 2, D1 / RESEARCH §1).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_half_cycle_split -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_half_cycle_split passes; pytest exits 0
    - split_half_cycles returns two length-16 arrays; descent[-1] <= bottom <= ascent[0] holds for BOTH bottom_is_argmax values
    - Flipping bottom_is_argmax moves the detected bottom (sign param is wired, not hard-coded)
    - No multi-rep find_peaks logic present yet (deferred to Plan 02 per RESEARCH §1)
    - Runnable unit: user pastes back the pytest output ending with 1 passed
  </acceptance_criteria>
  <dependencies>Task 1 (scaffold)</dependencies>
  <done>The only genuinely-new sampling logic (half-cycle splitter) is implemented + the descent/ascent contract is proven on synthetic data; the real-data sign is deferred to Plan 02's gated probe</done>
</task>

<task type="auto">
  <name>Task 3: Implement ssl_augs.py (5 augmentations) + smoke</name>
  <files>backend/training/aqa/datasets/ssl_augs.py</files>
  <read_first>
    backend/training/aqa/datasets/ssl_augs.py (the 5 stubs from Task 1),
    backend/training/aqa/datasets/transforms.py (the [T,3,H,W] uint8 convention + TF usage + the __main__ smoke-test pattern at the file tail),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§7 — the augmentation table with magnitudes + the form-signal tension; the per-branch independent application rule),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (ssl_augs section — exact function signatures + the key augmentations list)
  </read_first>
  <action>
    Implement the 5 augmentations per RESEARCH §7, each operating on a uint8 [T,3,H,W] clip and returning the same dtype/shape (so the existing spatial_* normalize chain is unchanged). All take an optional generator for reproducibility. temporal_shift(clip, max_shift=2): roll frames by a random offset in [-max_shift, max_shift] along dim 0 (the half-cycle is temporal; matches Phase 3 jitter). horizontal_flip(clip, p=0.5): with prob p, TF.hflip each frame (paper includes it; note this differs from the fine-tune flip-OFF — SSL augs are independent of fine-tune augs). top_mask(clip, mask_frac=0.4): zero the top mask_frac rows of every frame (occlusion robustness; preserves the lower-body knee region — RESEARCH §7). color_jitter(clip, brightness=0.2, contrast=0.2): TF.adjust_brightness / adjust_contrast with random factors in [1-x, 1+x] (appearance invariance; does not move joints). translation(clip, max_px=10): TF.affine translate by random (dx,dy) in [-max_px, max_px] (small only — large translation crops out the knees). Inline-cite RESEARCH §7 per function + the form-signal caveat for translation/rotation (rotation is intentionally NOT implemented here — it is an ablation toggle per §7, added only if the linear-probe improves with it in Plan 02). Add a __main__ smoke block (per PATTERNS) that builds a random uint8 [16,3,112,112] clip, runs each aug, asserts shape+dtype preserved, prints "ssl_augs.py smoke: ok".

    Single runnable unit — user pastes back python backend/training/aqa/datasets/ssl_augs.py (the __main__ smoke prints "ssl_augs.py smoke: ok" with no AssertionError).

    Atomic commit: feat(04): implement ssl_augs.py 5 augmentations (Task 3, RESEARCH §7).
  </action>
  <verify>
    <automated>python backend/training/aqa/datasets/ssl_augs.py</automated>
  </verify>
  <acceptance_criteria>
    - python backend/training/aqa/datasets/ssl_augs.py prints "ssl_augs.py smoke: ok"; exits 0
    - All 5 functions preserve uint8 dtype and [T,3,H,W] shape
    - Rotation is NOT implemented (deferred to Plan 02 ablation toggle per RESEARCH §7)
    - Each function has an inline RESEARCH §7 citation; translation/color carry the form-signal caveat
    - Runnable unit: user pastes back the smoke output
  </acceptance_criteria>
  <dependencies>Task 1 (scaffold)</dependencies>
  <done>Per-branch SSL augmentation primitives implemented + shape/dtype-preserving; ready for squat_ssl __getitem__ to apply them independently per branch in Plan 02</done>
</task>

<task type="auto">
  <name>Task 4: Implement ProjectionHead + SQUAT-04-d test (L2-norm)</name>
  <files>
    backend/training/aqa/harness/md_pretrain.py,
    backend/training/aqa/harness/test_md_pretrain.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (the ProjectionHead stub + MDConfig from Task 1),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§4 — the ProjectionHead class definition, the 512->512->128 BN+ReLU rationale, the L2-norm-is-CITED-from-train_test.py:51 note),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (No Analog Found — ProjectionHead has no codebase analog; use RESEARCH §4)
  </read_first>
  <action>
    Implement ProjectionHead(in_dim=512, hidden=512, out_dim=128) per RESEARCH §4: nn.Sequential(nn.Linear(in_dim, hidden), nn.BatchNorm1d(hidden), nn.ReLU(inplace=True), nn.Linear(hidden, out_dim)); forward(x) returns F.normalize(self.net(x), dim=-1, p=2). Inline comment: L2-norm before the loss is CITED from official train_test.py:51; the 2-layer-MLP dims are SimCLR-standard [ASSUMED §4]. This is a pure nn.Module — no torchvision, so it is fast to test (no @pytest.mark.slow needed).

    In test_md_pretrain.py, replace the SQUAT-04-d skip with test_projector_l2norm: construct ProjectionHead(512, 512, 128); set it to eval() (so BatchNorm1d uses running stats and does not choke on the per-row variance); feed x = torch.randn(4, 512); out = head(x); assert out.shape == (4, 128); norms = out.norm(dim=-1); assert torch.allclose(norms, torch.ones(4), atol=1e-5). (Use a batch >= 2 so BatchNorm1d is valid in train mode if eval is not set; prefer eval() to avoid the single-sample BN error.)

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_projector_l2norm -x -v (must pass).

    Atomic commit: feat(04): implement ProjectionHead + SQUAT-04-d L2-norm test (Task 4, RESEARCH §4).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_projector_l2norm -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_projector_l2norm passes; ProjectionHead output shape (4,128); per-row L2 norm approx 1.0 within 1e-5
    - forward uses F.normalize(.., dim=-1, p=2) (not a manual x/x.norm())
    - L2-norm carries the train_test.py:51 CITED comment; dims carry the [ASSUMED §4] label
    - Runnable unit: user pastes back the pytest output ending with 1 passed
  </acceptance_criteria>
  <dependencies>Task 1 (scaffold)</dependencies>
  <done>ProjectionHead implemented + L2-norm output proven; the embedding-normalization contract the triplet loss depends on is locked</done>
</task>

<task type="auto">
  <name>Task 5: Implement md_triplet_loss + SQUAT-04-b/c tests (known value + directionality)</name>
  <files>
    backend/training/aqa/harness/md_pretrain.py,
    backend/training/aqa/harness/test_md_pretrain.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (the md_triplet_loss stub + MDConfig loss flags from Task 1),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§2 — the FULL loss derivation: paper Eq.1 p.6, the official train_test.py:60-71 VERBATIM block, the squared-vs-non-squared + 2-term-vs-3-term discrepancy, the recommended vectorized implementation, the no-temperature/no-stop-gradient notes; AND the Code Examples section near the file tail with the verified md_triplet_loss),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (test_md_pretrain.py triplet-loss test patterns — test_triplet_loss_direction and test_triplet_loss_known examples)
  </read_first>
  <action>
    Implement md_triplet_loss(phi_a, phi_p, phi_n, *, squared=True, three_term=True, eps=1e-9) per RESEARCH §2 (vectorized, NOT the official per-sample loop): d_ap = ((phi_a - phi_p)**2).sum(-1); d_an = ((phi_a - phi_n)**2).sum(-1); if not squared, take sqrt of each; num = torch.exp(-d_ap); den = num + torch.exp(-d_an); if three_term, add torch.exp(-d_pn) where d_pn is the (positive,negative) squared distance (also sqrt'd if not squared); return (-torch.log(num / (den + eps))).mean(). Docstring cites paper Eq.1 §3.2 p.6 AND official train_test.py:68 (the active 3-term line), and documents BOTH discrepancies (the released code uses squared distance + 3-term; Eq.1 is non-squared + 2-term) per RESEARCH §2 / A4 — the flags expose the exact-Eq.1 form for ablation. NO temperature, NO stop-gradient (RESEARCH §2).

    In test_md_pretrain.py, replace the SQUAT-04-b and SQUAT-04-c skips:
    - test_triplet_loss_known: hand-computed sanity. With phi_a == phi_p (d_ap=0 -> num=exp(0)=1) and phi_n antipodal (-phi_a), for the 2-term form loss = -log(1/(1+exp(-d_an))) -> a known small value as d_an grows. Use a concrete unit vector (e.g. a 4-d one-hot) and assert the returned scalar matches the hand-computed value within 1e-5 for BOTH three_term=False (2-term) and three_term=True (3-term), and for BOTH squared=True/False. Compute the four expected scalars by hand in the test (write them as literals with a comment showing the arithmetic) so this is a true known-value test, not a tautology.
    - test_triplet_loss_direction: L2-normalize random anchor; pos_close = normalize(anchor + 0.01*noise); neg_far = normalize(-anchor + 0.01*noise). Assert md_triplet_loss(anchor, pos_close, neg_far) < md_triplet_loss(anchor, neg_far, pos_close) (anchor approx pos and far-from-neg gives a strictly lower loss than the swapped version). This is the directionality guarantee that the loss actually pulls positives together and pushes negatives apart (T-04-01 mitigation).

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_pretrain.py -k "triplet" -x -v (both triplet tests pass).

    Atomic commit: feat(04): implement md_triplet_loss + SQUAT-04-b/c tests (Task 5, D2-loss / RESEARCH §2).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_pretrain.py -k "triplet" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_triplet_loss_known passes for all four (squared x three_term) combinations against hand-computed literals (the arithmetic is shown in test comments — not a self-referential assertion)
    - test_triplet_loss_direction passes: loss_good < loss_bad (positives pulled together, negatives pushed apart)
    - md_triplet_loss docstring cites paper Eq.1 p.6 AND train_test.py:68, and documents the squared/3-term discrepancy per RESEARCH §2 / A4
    - No temperature parameter and no stop-gradient in the implementation
    - Runnable unit: user pastes back both triplet tests passing
  </acceptance_criteria>
  <dependencies>Task 4 (ProjectionHead — embeddings the loss consumes are L2-normalized)</dependencies>
  <done>The reconstructed-from-paper triplet distance-ratio loss is implemented + its arithmetic and directionality are proven on synthetic embeddings BEFORE the 12-24h GPU burn (RESEARCH Key insight); both loss-form flags are ablatable</done>
</task>

<task type="auto">
  <name>Task 6: Implement build_md_model + SQUAT-04-e test (slow — backbone fc=Identity, 512-d, projector)</name>
  <files>
    backend/training/aqa/harness/md_pretrain.py,
    backend/training/aqa/harness/test_md_pretrain.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (the build_md_model stub + ProjectionHead from Task 4),
    backend/training/aqa/harness/supervised_train.py (build_model at line 114 — the r2plus1d_18 construction + the fc.in_features==512 assertion to mirror),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§4 — the fc=Identity construction note + the Code Examples R(2+1)D-18 backbone snippet; §5 for the KINETICS400_V1 init),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (md_pretrain build_md_model pattern + the @pytest.mark.slow test_md_model_build example)
  </read_first>
  <action>
    Implement build_md_model() per RESEARCH §4: lazy-import from torchvision.models.video import r2plus1d_18, R2Plus1D_18_Weights inside the function (PATTERNS §4 — keeps pytest fast); backbone = r2plus1d_18(weights=R2Plus1D_18_Weights.KINETICS400_V1); assert backbone.fc.in_features == 512, f"...expected 512, got {...}"; backbone.fc = nn.Identity(); projector = ProjectionHead(in_dim=512, hidden=config.projector_hidden if config else 512, out_dim=config.projector_out_dim if config else 128) — but keep the signature simple: build_md_model() with no args defaulting to 512/512/128 (Plan 02 can pass an MDConfig later if needed; for now match the interfaces signature). Return (backbone, projector). Log the param count. Inline comment: Kinetics-V1 init matches Phase 3 for clean comparison (§5); fc=Identity exposes 512-d pooled features (§4).

    In test_md_pretrain.py, replace the SQUAT-04-e skip with @pytest.mark.slow test_md_model_build (downloads ~120 MB weights on first call — slow): pytest.importorskip("torchvision"); backbone, projector = build_md_model(); assert isinstance(backbone.fc, torch.nn.Identity); x = torch.zeros(2, 3, 16, 112, 112); with torch.no_grad(): feat = backbone(x); assert feat.shape == (2, 512); proj_out = projector(feat); assert proj_out.shape == (2, 128); assert torch.allclose(proj_out.norm(dim=-1), torch.ones(2), atol=1e-5). Note input frame depth 16 (a single half-cycle), not 32.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_md_model_build -x -v (the slow test on its own; runs in Colab where weights are cached, or locally on first invocation downloading weights once).

    Atomic commit: feat(04): implement build_md_model + SQUAT-04-e test (Task 6, RESEARCH §4 / §5).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_md_model_build -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_md_model_build passes (skip OK if torchvision missing); backbone.fc is nn.Identity; backbone(zeros[2,3,16,112,112]).shape == (2,512); projector output (2,128) L2-normed
    - torchvision import is lazy (inside build_md_model) — module-level import of md_pretrain stays fast
    - Kinetics-V1 init used (not weights=None); fc.in_features==512 asserted before the Identity swap
    - Runnable unit: user pastes back the slow test passing (1 passed)
  </acceptance_criteria>
  <dependencies>Task 4 (ProjectionHead), Task 5 (loss — model feeds it)</dependencies>
  <done>build_md_model wires R(2+1)D-18 (fc=Identity, 512-d) + ProjectionHead; the SSL forward path the trainer will use in Plan 02 is proven on a zeros input</done>
</task>

<task type="auto">
  <name>Task 7: Implement SSL checkpoint schema + SQUAT-04-f test (payload keys)</name>
  <files>
    backend/training/aqa/harness/md_pretrain.py,
    backend/training/aqa/harness/test_md_pretrain.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (current state after Task 6),
    backend/training/aqa/harness/supervised_train.py (the Phase 3 checkpoint payload schema around lines 554-567 — the analog the SSL schema extends),
    backend/training/aqa/harness/test_supervised_train.py (the test_checkpoint_schema_keys pattern with the _EXPECTED keys tuple),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§10 — the SSL checkpoint payload schema with backbone/projector/optimizer/scheduler/rng/metrics/linear_probe keys),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (md_pretrain SSL checkpoint payload schema + the test_ssl_checkpoint_schema example with _EXPECTED_SSL_KEYS)
  </read_first>
  <action>
    Add a module-level constant in md_pretrain.py documenting the SSL checkpoint payload key set per RESEARCH §10: a tuple _SSL_CHECKPOINT_KEYS = ("epoch", "backbone_state_dict", "projector_state_dict", "optimizer_state_dict", "scheduler_state_dict", "rng_state", "metrics_history", "linear_probe_history", "config_hash", "config_repr", "code_version"). Add a small pure helper build_ssl_checkpoint_payload(*, epoch, backbone, projector, optimizer, scheduler, metrics_history, linear_probe_history, config_hash, config_repr) -> dict that assembles exactly those keys (calls capture_rng_state() for rng_state, sets code_version="phase04-md-pretrain"). This is pure assembly (no I/O, no GPU) so it can be unit-tested now and reused by run_md_pretrain_epoch in Plan 02. Inline comment cites RESEARCH §10 + the D7 note that backbone.pt writes pass update_latest=False.

    In test_md_pretrain.py, replace the SQUAT-04-f skip with test_ssl_checkpoint_schema (per PATTERNS): build a dummy payload dict with all 11 keys (rng_state a dict with the 4 sub-keys; empty state dicts; metrics_history=[]; linear_probe_history=[]; code_version="phase04-md-pretrain"); assert missing = [k for k in _SSL_CHECKPOINT_KEYS if k not in payload] == []; assert payload["code_version"] == "phase04-md-pretrain". This is a pure-Python schema test (no GPU, no atomic_save_checkpoint round-trip — that is exercised on real data in Plan 02).

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_ssl_checkpoint_schema -x -v.

    Atomic commit: feat(04): SSL checkpoint schema + SQUAT-04-f test (Task 7, RESEARCH §10).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_ssl_checkpoint_schema -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_ssl_checkpoint_schema passes; all 11 SSL payload keys present; code_version == "phase04-md-pretrain"
    - _SSL_CHECKPOINT_KEYS constant present in md_pretrain.py; includes both backbone_state_dict and projector_state_dict and linear_probe_history
    - build_ssl_checkpoint_payload assembles exactly those keys (pure assembly, no I/O)
    - Runnable unit: user pastes back the pytest output ending with 1 passed
  </acceptance_criteria>
  <dependencies>Task 6 (model — payload saves backbone/projector state dicts)</dependencies>
  <done>The SSL checkpoint payload schema is locked + the key set is regression-tested; Plan 02's run_md_pretrain_epoch assembles via build_ssl_checkpoint_payload (disconnect-safe resume contract foundation)</done>
</task>

<task type="auto">
  <name>Task 8: Implement build_finetune_model + SQUAT-05-a test (slow — MD backbone load, Dropout+Linear head)</name>
  <files>
    backend/training/aqa/harness/md_finetune.py,
    backend/training/aqa/harness/test_md_finetune.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_finetune.py (the build_finetune_model stub + FinetuneConfig from Task 1),
    backend/training/aqa/harness/md_pretrain.py (build_md_model + the SSL checkpoint schema — build_finetune_model loads backbone_state_dict from that payload),
    backend/training/aqa/harness/supervised_train.py (build_model at line 114 — the analog, but Phase 4 uses weights=None not Kinetics, and a Dropout+Linear head),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§9 — the build_finetune_model construction detail; the Pattern 2 backbone-only-transfer snippet; the AdamW-vs-Adam note; the NO-Kinetics anti-pattern),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (md_finetune build_finetune_model Delta 1 + the map_location='cpu' shared pattern)
  </read_first>
  <action>
    Implement build_finetune_model(md_backbone_path, *, dropout=0.2) per RESEARCH §9 / PATTERNS Delta 1: lazy from torchvision.models.video import r2plus1d_18; model = r2plus1d_18(weights=None) (architecture only — NO Kinetics, the MD backbone IS the init, §9 anti-pattern); assert model.fc.in_features == 512; ckpt = torch.load(md_backbone_path, map_location="cpu", weights_only=False) (map_location='cpu' is the D7 landmine fix); model.load_state_dict(ckpt["backbone_state_dict"], strict=False) (strict=False because projector keys are absent from the backbone-only load); model.fc = nn.Sequential(nn.Dropout(dropout), nn.Linear(512, 2)) (D3 head-only dropout). Return model. Inline-cite §9 (end-to-end fine-tune, no separate transfer LR), D3 (AdamW + dropout deltas), and the NO-Kinetics anti-pattern.

    In test_md_finetune.py, replace the SQUAT-05-a skip with @pytest.mark.slow test_finetune_model_build: build a minimal fake MD checkpoint on a tmp path — construct build_md_model()[0] (a Kinetics backbone with fc=Identity), torch.save({"backbone_state_dict": backbone.state_dict()}, tmp_path) (use pytest tmp_path fixture); then model = build_finetune_model(str(tmp_path)); assert isinstance(model.fc, torch.nn.Sequential) and isinstance(model.fc[0], torch.nn.Dropout) and isinstance(model.fc[1], torch.nn.Linear) and model.fc[1].out_features == 2; x = torch.zeros(2,3,32,112,112); with torch.no_grad(): y = model(x); assert y.shape == (2,2). Note the fine-tune input depth is 32 frames (the labeled pipeline), NOT the 16-frame half-cycle. This proves the round-trip: an MD backbone state-dict loads into a fresh r2plus1d_18 (weights=None) + Dropout+Linear(512,2) head.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_md_finetune.py::test_finetune_model_build -x -v (slow; downloads weights once for the fake-checkpoint backbone).

    Atomic commit: feat(04): implement build_finetune_model + SQUAT-05-a test (Task 8, D3 / RESEARCH §9).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_md_finetune.py::test_finetune_model_build -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_finetune_model_build passes (skip OK if torchvision missing); model.fc is Sequential(Dropout(0.2), Linear(512,2)); forward on zeros[2,3,32,112,112] gives (2,2)
    - build_finetune_model uses r2plus1d_18(weights=None) (NOT Kinetics) + torch.load(map_location='cpu') + load_state_dict(backbone_state_dict, strict=False)
    - Inline NO-Kinetics anti-pattern comment present (the MD backbone IS the init, §9)
    - Runnable unit: user pastes back the slow test passing (1 passed)
  </acceptance_criteria>
  <dependencies>Task 7 (SSL checkpoint schema — build_finetune_model loads backbone_state_dict from it)</dependencies>
  <done>The MD-backbone-to-fine-tune transfer (load backbone, fresh Dropout+Linear head, NOT Kinetics) is proven on a round-tripped fake checkpoint; Plan 03's run_md_finetune_epoch builds on this</done>
</task>

<task type="auto">
  <name>Task 9: Implement aggregate_sigmoid_mean + SQUAT-05-b test (mean-of-sigmoids in [0,1])</name>
  <files>
    backend/training/aqa/eval/ensemble.py,
    backend/training/aqa/eval/test_ensemble.py
  </files>
  <read_first>
    backend/training/aqa/eval/ensemble.py (the aggregate_sigmoid_mean stub from Task 1),
    backend/training/aqa/eval/metrics.py (the pr_auc_per_error [0,1] assertion at line 72 — the ensemble output MUST satisfy it),
    backend/training/aqa/eval/test_metrics.py (the _arr helper + assertion style),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§13 — the mean-of-sigmoids aggregation; the [0,1] satisfies-pr_auc note),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (ensemble.py aggregate_sigmoid_mean reference implementation)
  </read_first>
  <action>
    Implement aggregate_sigmoid_mean(per_seed_logits) per RESEARCH §13 / D4 (pure numpy, no torch): if len(per_seed_logits) == 0 raise ValueError; verify all arrays share the same (N,2) shape else raise ValueError; sigmoid each via 1.0/(1.0+np.exp(-logits)); ensemble = np.mean(sigmoid_scores, axis=0); assert ensemble.min() >= 0.0 and ensemble.max() <= 1.0 (this is the contract that satisfies pr_auc_per_error's assertion at metrics.py:72); return ensemble. Inline-cite D4 (mean-of-sigmoids NOT mean-of-logits) + the pr_auc [0,1] contract.

    In test_ensemble.py, replace the SQUAT-05-b skip with test_sigmoid_mean: build 3 seed logit arrays shape (5,2) with known values (e.g. mix of large-positive, large-negative, zero logits); compute the expected mean-of-sigmoids by hand for at least one entry (e.g. logit 0 -> sigmoid 0.5; three seeds of [0,0] -> ensemble 0.5) and assert the returned array matches within 1e-6; assert ensemble.min() >= 0 and ensemble.max() <= 1; assert output shape (5,2). Add test_sigmoid_mean_raises: assert aggregate_sigmoid_mean([]) raises ValueError, and a shape-mismatch list raises ValueError.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/eval/test_ensemble.py -x -v (both tests pass; pure numpy, sub-second).

    Atomic commit: feat(04): implement aggregate_sigmoid_mean + SQUAT-05-b test (Task 9, D4 / RESEARCH §13).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/eval/test_ensemble.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_sigmoid_mean passes; output in [0,1]; hand-computed entry (e.g. all-zero logits -> 0.5) matches within 1e-6; shape preserved
    - test_sigmoid_mean_raises passes; empty list and shape-mismatch both raise ValueError
    - aggregate_sigmoid_mean is pure numpy (no torch import in ensemble.py)
    - Runnable unit: user pastes back both tests passing
  </acceptance_criteria>
  <dependencies>Task 1 (scaffold)</dependencies>
  <done>The ensemble aggregation (mean-of-sigmoids, [0,1] output satisfying pr_auc_per_error) is implemented + tested; Plan 04's ensemble step feeds it cleaned per-seed score arrays</done>
</task>

<task type="auto">
  <name>Task 10: Implement select_tta_recipe (+ tta_forward) + SQUAT-05-c test (val-F1-max recipe)</name>
  <files>
    backend/training/aqa/eval/tta.py,
    backend/training/aqa/eval/test_tta.py
  </files>
  <read_first>
    backend/training/aqa/eval/tta.py (the tta_forward + select_tta_recipe stubs from Task 1),
    backend/training/aqa/eval/metrics.py (threshold_sweep + f1_per_error — select_tta_recipe calls them per recipe),
    backend/training/aqa/datasets/transforms.py (spatial_val center-crop at line 190 — the 5-crop inline extension adds 4 corner crops; do NOT modify this file),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§14 — the TTA candidates, the val-tuned recipe selection, the flip-is-OOD note, the mean-logits-then-sigmoid aggregation order),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (tta.py tta_forward + select_tta_recipe signatures + the 5-crop inline note)
  </read_first>
  <action>
    Implement select_tta_recipe(val_logits_per_recipe, val_labels) per RESEARCH §14 / D5 (this is the unit-testable pure-function core — measure, don't guess): for each recipe in val_logits_per_recipe, sigmoid its (N,2) logits, run threshold_sweep per error head to get a tuned threshold, threshold to predictions, compute f1_per_error per head, macro = (f1_kie + f1_kfe)/2; return the recipe tuple with the max macro-F1 (ties broken by the first/shortest recipe — a deterministic tiebreak). Inline-cite D5 (val-tuned; flip is a candidate not assumed-good).

    Implement tta_forward(model, clip, recipe, device) per RESEARCH §14 (it needs torch + a model — only its CALL path is exercised at GPU time in Plan 04, so its body is implemented now but its test is a light shape test): for each aug in recipe applied to the [3,32,112,112] clip (temporal_jitter via index roll; spatial_5crop via inline center+4-corner crops reusing the resize_short=128 then crop=112 convention from transforms.py spatial_val, but NOT modifying transforms.py per D9; flip via TF.hflip), run model(aug_clip) under no_grad, collect logits, return the MEAN LOGITS (2,) as numpy (NOT sigmoid — the caller sigmoids before ensemble, RESEARCH §14 aggregation order). Inline-cite the 5-crop-inline + the mean-logits-not-sigmoid note.

    In test_tta.py, replace the SQUAT-05-c skip with test_recipe_selection (pure — no torch needed, so do NOT importorskip torch at module top): build a synthetic val_logits_per_recipe dict with 2-3 candidate recipe tuples whose (N,2) logits are constructed so ONE recipe clearly separates the classes best (highest macro-F1) and the others are worse; val_labels a known (N,2) int array; assert select_tta_recipe returns the known-best recipe tuple. Add a determinism check: identical macro-F1 across two recipes returns the first by the documented tiebreak.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/eval/test_tta.py -x -v (test_recipe_selection passes; pure numpy/metrics path, sub-second).

    Atomic commit: feat(04): implement select_tta_recipe + tta_forward + SQUAT-05-c test (Task 10, D5 / RESEARCH §14).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/eval/test_tta.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_recipe_selection passes; select_tta_recipe returns the val-macro-F1-maximizing recipe on synthetic scores; ties broken deterministically
    - select_tta_recipe uses eval/metrics.py threshold_sweep + f1_per_error (does NOT reimplement F1)
    - tta_forward returns MEAN LOGITS (not sigmoid); 5-crop is inline in tta.py (transforms.py NOT modified)
    - test_tta.py has NO module-top torch import (the recipe-selection test is pure numpy); torch importorskip only inside any torch-needing test
    - Runnable unit: user pastes back test_recipe_selection passing
  </acceptance_criteria>
  <dependencies>Task 9 (ensemble — TTA feeds into the same metrics path)</dependencies>
  <done>The val-tuned TTA recipe selection (the measure-don't-guess core) is implemented + tested; tta_forward's mean-logits path is wired for Plan 04's GPU-time TTA sweep</done>
</task>

<task type="auto">
  <name>Task 11: Edit colab.py — add update_latest kwarg + stage_unlabeled_squat_videos</name>
  <files>backend/training/aqa/harness/colab.py</files>
  <read_first>
    backend/training/aqa/harness/colab.py (atomic_save_checkpoint at line 538 — the latest.txt write is Step 4 at line 589; stage_squat_videos at line 317; _copy_with_resume_and_progress at line 93; _extract_with_resume_and_progress at line 168),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§10 — the update_latest kwarg fix + the stage_unlabeled_squat_videos design reusing the existing resume helpers; the disk-budget probe note),
    .planning/phases/04-squat-motion-disentangling-ssl/04-PATTERNS.md (colab.py MODIFY section — the exact Change 1 (update_latest guard) + Change 2 (stage_unlabeled_squat_videos) shapes),
    .planning/phases/01-dataset-consolidation-eda/01-DATASET-REPORT.md (the unlabeled set: 4,970 clips, the videos.zip + bar_trajectories_raw.zip names under Squat/Unlabeled_Dataset/)
  </read_first>
  <action>
    Two changes to colab.py, both backward-compatible (D9 — do NOT break Phase 2/3 call sites):

    Change 1 (D7 / §10 / Pitfall 4): add a keyword-only update_latest: bool = True parameter to atomic_save_checkpoint. Guard the existing Step-4 latest.txt write (line ~589) behind if update_latest:. When update_latest is False, skip the latest.txt write and log "atomic_save_checkpoint: %s (latest.txt NOT updated)". Default True preserves the exact Phase 2/3 behavior (the supervised_train.py and tiny_train.py call sites are unaffected). Update the docstring to document the kwarg and the backbone.pt/best.pt use case.

    Change 2 (§10): add stage_unlabeled_squat_videos(drive_root, *, local_videos_root="/content/squat_unlabeled_videos", local_traj_root="/content/squat_trajectories", expect_count=4970) -> tuple[str, str]. Mirror stage_squat_videos' three-layer resume EXACTLY by REUSING _copy_with_resume_and_progress + _extract_with_resume_and_progress (do NOT duplicate the resume logic — the new function is ~30 lines parameterizing the existing primitives, RESEARCH §10). Source paths: drive_root/Fitness-AQA_dataset_release/Squat/Unlabeled_Dataset/videos.zip and .../bar_trajectories_raw.zip. Add a module-level constant _SQUAT_UNLABELED_VIDEOS_EXPECT_COUNT = 4970 (Phase 1 verified). Cache-hit: if local_videos_root already has expect_count .mp4 files, return early. Probe + log the videos.zip size at stage time (disk-budget note, §10). Return (local_videos_root, local_traj_root). Inline-cite §10 + Phase 1 count.

    Single runnable unit — user pastes back: (1) python -c "import inspect; from backend.training.aqa.harness.colab import atomic_save_checkpoint, stage_unlabeled_squat_videos; sig=inspect.signature(atomic_save_checkpoint); assert 'update_latest' in sig.parameters and sig.parameters['update_latest'].default is True, sig; print('colab edits ok')"; (2) python -c "import ast; ast.parse(open('backend/training/aqa/harness/colab.py').read()); print('parse ok')". The behavioral test of the kwarg is Task 12.

    Atomic commit: feat(04): colab.py update_latest kwarg + stage_unlabeled_squat_videos (Task 11, D7 / RESEARCH §10).
  </action>
  <verify>
    <automated>python -c "import inspect; from backend.training.aqa.harness.colab import atomic_save_checkpoint, stage_unlabeled_squat_videos; sig=inspect.signature(atomic_save_checkpoint); assert 'update_latest' in sig.parameters and sig.parameters['update_latest'].default is True, sig; print('colab edits ok')"</automated>
  </verify>
  <acceptance_criteria>
    - atomic_save_checkpoint has a keyword-only update_latest parameter defaulting to True; the latest.txt write is guarded behind if update_latest
    - stage_unlabeled_squat_videos exists with the signature from interfaces; it REUSES _copy_with_resume_and_progress + _extract_with_resume_and_progress (no duplicated resume logic)
    - Phase 2/3 call sites unaffected (default True) — verified behaviorally in Task 12
    - colab.py still parses; existing functions (mount_drive, load_latest_checkpoint, prune_checkpoints, capture/restore_rng_state, hash_config) untouched
    - Runnable unit: user pastes back the inspect-signature check + the AST parse
  </acceptance_criteria>
  <dependencies>Task 1 (scaffold) — independent of Tasks 2-10, but sequenced here so the colab edit lands after the new modules</dependencies>
  <done>The D7 latest.txt-clobber landmine fix (update_latest kwarg) + the unlabeled-clip staging analog are in colab.py, backward-compatible; Plan 02 uses both</done>
</task>

<task type="auto">
  <name>Task 12: Implement test_colab_update_latest.py (T-04-03 — latest.txt clobber fix)</name>
  <files>backend/training/aqa/harness/test_colab_update_latest.py</files>
  <read_first>
    backend/training/aqa/harness/colab.py (the edited atomic_save_checkpoint from Task 11 — the update_latest guard + the _atomic_write_text helper at line 529),
    backend/training/aqa/harness/test_supervised_train.py (the importorskip + tmp-path test pattern),
    .planning/phases/04-squat-motion-disentangling-ssl/04-RESEARCH.md (§10 + Pitfall 4 — the exact failure mode the test guards against: latest.txt pointing to backbone.pt instead of epoch_NNN.pt)
  </read_first>
  <action>
    Create test_colab_update_latest.py: torch = pytest.importorskip("torch") at module top (atomic_save_checkpoint round-trips a torch payload). Two tests using the pytest tmp_path fixture:

    - test_update_latest_true_writes_pointer (default behavior): write a tiny payload via atomic_save_checkpoint(payload, tmp_path/"epoch_000.pt") (default update_latest=True); assert (tmp_path/"latest.txt").exists() and its content names epoch_000.pt. Then write another via atomic_save_checkpoint(payload, tmp_path/"epoch_001.pt"); assert latest.txt now names epoch_001.pt. This proves the Phase 2/3 behavior is preserved.

    - test_update_latest_false_preserves_pointer (the D7 fix / Pitfall 4): after the above (latest.txt -> epoch_001.pt), record latest.txt's bytes; write atomic_save_checkpoint(payload, tmp_path/"backbone.pt", update_latest=False); assert (tmp_path/"backbone.pt").exists() (the checkpoint WAS written) AND (tmp_path/"latest.txt") bytes are UNCHANGED (still -> epoch_001.pt, NOT backbone.pt). This directly tests the failure mode in RESEARCH Pitfall 4 (SSL resume jumping to the wrong epoch because backbone.pt clobbered latest.txt).

    Use a minimal payload (e.g. {"epoch": 0, "model_state_dict": {}, "config_hash": "abc"}) — atomic_save_checkpoint does a torch.load round-trip verify, so the payload must be torch.save-able. Keep it tiny.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/harness/test_colab_update_latest.py -x -v (both tests pass).

    Atomic commit: test(04): colab update_latest kwarg behavioral test (Task 12, T-04-03 / Pitfall 4).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/harness/test_colab_update_latest.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_update_latest_true_writes_pointer passes; latest.txt tracks the most recent epoch_NNN.pt write
    - test_update_latest_false_preserves_pointer passes; backbone.pt is written BUT latest.txt bytes are unchanged (still pointing to the prior epoch checkpoint)
    - Both tests use tmp_path (no Drive, no real checkpoints)
    - Runnable unit: user pastes back both tests passing (2 passed)
  </acceptance_criteria>
  <dependencies>Task 11 (the colab.py edit)</dependencies>
  <done>The D7 latest.txt-clobber fix is behaviorally proven: backbone.pt writes never corrupt the SSL epoch-resume pointer (RESEARCH Pitfall 4 closed before any 12-24h SSL run)</done>
</task>

<task type="auto">
  <name>Task 13: Wave 0 gate — full pytest run, confirm green before any GPU work</name>
  <files></files>
  <read_first>
    .planning/phases/04-squat-motion-disentangling-ssl/04-VALIDATION.md (the Wave 0 Requirements checklist + the full-suite command),
    backend/training/aqa/eval/test_metrics.py (the existing Phase 3 tests that must STILL pass — regression),
    backend/training/aqa/harness/test_supervised_train.py (the existing Phase 3 tests that must STILL pass — confirms the colab.py edit did not break Phase 3)
  </read_first>
  <action>
    Wave 0 gate. No new files. Run the full Phase 4 + regression pytest discovery from repo root and confirm everything is green BEFORE Wave 1 (the Colab probe + GPU work in Plan 02) starts. This is the GPU-burn gate per RESEARCH §15 — the reconstructed-from-paper logic (splitter, triplet loss, projector, aggregation, schemas) is locked behind green tests before 12-24h of L4 compute is authorized.

    Two runnable units the user pastes back:
    (1) python -m pytest backend/training/aqa/ -x -q -m "not slow" — the fast suite: all Phase 3 tests (9 metrics + 1 schema) STILL green (regression — proves the colab.py edit did not break them) PLUS the new fast Phase 4 tests (split_half_cycles, triplet known/direction, projector L2-norm, SSL schema, ensemble, TTA recipe, update_latest x2). Expect 0 failures.
    (2) The slow model-build tests on their own: python -m pytest backend/training/aqa/harness/test_md_pretrain.py::test_md_model_build backend/training/aqa/harness/test_md_finetune.py::test_finetune_model_build -x -v (both pass; download ~120 MB weights once if not cached).

    If anything is red, the orchestrator stops here and the executor fixes — no new tasks added, no GPU work authorized. No commit (no files modified).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/ -x -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest backend/training/aqa/ -x -q -m "not slow" exits 0 — all Phase 3 tests STILL green (regression) + all new fast Phase 4 tests green
    - The two slow model-build tests pass on standalone invocation
    - No Phase 3 regression: eval/test_metrics.py and harness/test_supervised_train.py still pass (the colab.py edit is backward-compatible)
    - Runnable unit: user pastes back the fast-suite summary (N passed) + the two slow tests passing
  </acceptance_criteria>
  <dependencies>Tasks 2-12 (every implementation + the colab edit)</dependencies>
  <done>Wave 0 closed; the reconstructed-from-paper logic is locked behind a green pure-function suite; the 12-24h SSL GPU burn (Plan 02) is authorized to proceed</done>
</task>

</tasks>

<verification>
## Mapping to Phase 4 ROADMAP Success Criteria (this plan's contribution)

This is Wave 0 — it builds and unit-tests the machinery; the ROADMAP criteria are SATISFIED in Plans 02/03/04 (which run the GPU). This plan's contribution:

| ROADMAP success criterion | This plan's role |
|---|---|
| (1) MD-SSL pretraining runs + converges | Builds + tests split_half_cycles (§1), md_triplet_loss (§2), ProjectionHead (§4), build_md_model (§5), the SSL checkpoint schema (§10) — the machinery Plan 02 runs |
| (2) MD-pretrained fine-tuned reports F1 >= baseline | Builds + tests build_finetune_model (§9, MD-backbone-not-Kinetics + Dropout+Linear head) — the machinery Plan 03 runs |
| (3) Comparison table/chart vs Parmar + GYMetricPose | Builds + tests aggregate_sigmoid_mean (§13) + select_tta_recipe (§14) — the ensemble+TTA machinery Plan 04's chart consumes |

## Mapping to Requirements

| Requirement | This plan's tasks | Acceptance |
|---|---|---|
| SQUAT-04 (MD-SSL pretraining machinery) | Tasks 1-7, 11, 12 | SQUAT-04-a (Task 2 split), -b/-c (Task 5 loss), -d (Task 4 projector), -e (Task 6 model build), -f (Task 7 schema); colab staging + update_latest (Tasks 11-12) |
| SQUAT-05 (fine-tune + ensemble + TTA machinery) | Tasks 8, 9, 10 | SQUAT-05-a (Task 8 fine-tune model build), -b (Task 9 ensemble), -c (Task 10 TTA recipe) |

## Per-VALIDATION sub-requirement coverage (Wave 0 portion)

| Sub-ID | Task | Test type | Wave |
|---|---|---|---|
| SQUAT-04-a (split_half_cycles contract on synthetic parabola) | Task 2 test_half_cycle_split | unit | 0 |
| SQUAT-04-b (triplet loss known value) | Task 5 test_triplet_loss_known | unit | 0 |
| SQUAT-04-c (triplet loss directionality) | Task 5 test_triplet_loss_direction | unit | 0 |
| SQUAT-04-d (ProjectionHead L2-norm) | Task 4 test_projector_l2norm | unit | 0 |
| SQUAT-04-e (build_md_model fc=Identity, 512-d, projector) | Task 6 test_md_model_build | unit (slow) | 0 |
| SQUAT-04-f (SSL checkpoint schema keys) | Task 7 test_ssl_checkpoint_schema | unit | 0 |
| SQUAT-05-a (build_finetune_model MD backbone + Dropout+Linear) | Task 8 test_finetune_model_build | unit (slow) | 0 |
| SQUAT-05-b (aggregate_sigmoid_mean in [0,1]) | Task 9 test_sigmoid_mean | unit | 0 |
| SQUAT-05-c (select_tta_recipe val-F1-max) | Task 10 test_recipe_selection | unit | 0 |

(SQUAT-04-g/h/i and SQUAT-05-d/e/f are the Colab integration tests in Plans 02/03/04.)
</verification>

<success_criteria>
- All 13 tasks complete with paste-back acceptance from the user
- All 6 production modules + the colab.py edit land with their pure-function cores fully implemented; GPU/Colab-dependent bodies are documented stubs (NotImplementedError naming the finalizing plan/task)
- python -m pytest backend/training/aqa/ -x -q -m "not slow" exits 0 — all new fast Phase 4 tests green PLUS all Phase 3 tests still green (no regression from the colab.py edit)
- Both slow model-build tests (test_md_model_build, test_finetune_model_build) pass on standalone invocation
- split_half_cycles proven on synthetic parabola (descent <= bottom <= ascent, both sign values); md_triplet_loss proven against hand-computed literals (both forms) + directionality; ProjectionHead L2-norm proven; aggregate_sigmoid_mean in [0,1] proven; select_tta_recipe picks val-F1-max; update_latest=False preserves latest.txt
- Every [ASSUMED]/[CITED] value in MDConfig/FinetuneConfig carries an inline citation (RESEARCH § or paper §) — [[feedback_ai_correctness]] satisfied
- One concise threat_model block (proportionate to offline training); T-04-01/02/03 each have a Task-level pytest acceptance proof; T-04-04/SC accept (zero new pip installs)
- git status clean except for the planned files_modified set; Fitness-AQA-Code/ and backend/data/distillation/ remain gitignored
- NO GPU, NO Colab, NO Drive used in this plan — pure local CPU pytest; the 12-24h SSL burn is gated behind Task 13's green suite and authorized only in Plan 02
</success_criteria>

<output>
After Task 13 (Wave 0 gate green), this plan is complete. Plan 02 (Wave 1+2) begins with the gated trajectory-format probe (checkpoint:human-verify) on a fresh L4 Colab, then finalizes squat_ssl.py + md_pretrain.py and runs the SSL pretrain. No SUMMARY.md for this plan individually — the phase SUMMARY is authored at the end of Plan 04 (the phase closeout), per the Phase 3 single-SUMMARY precedent. The orchestrator advances to Plan 02 once Task 13 is green.
</output>

<revision_log>
Phase 4 Plan 01 written 2026-05-21. Wave 0 per RESEARCH §15: new-module scaffolds + unit tests for the reconstructed-from-paper pure-function cores (half-cycle splitter §1, triplet distance-ratio loss §2, projection head §4, ensemble/TTA aggregation §13/§14, checkpoint schemas §10) + the two colab.py modifications (update_latest kwarg + stage_unlabeled_squat_videos). 13 tasks; all CPU-only local pytest (no GPU/Colab/Drive). The GPU-burn gate is Task 13's green suite — no SSL pretrain is authorized until the reconstructed logic is proven on synthetic data.

Key RESEARCH override encoded: the SSL loss is the triplet distance-ratio loss (Eq.1 / official train_test.py:60-71), NOT NT-Xent (RESEARCH §2 CITED, overrides CONTEXT D2's provisional default). Both squared/three_term forms exposed as MDConfig flags for ablation (A4). Stub policy: GPU/Colab-data-dependent bodies (squat_ssl trajectory loading, md_pretrain training loop, md_finetune training loop) are documented stubs finalized in Plans 02/03 once the trajectory-format probe + VRAM probe supply their preconditions.

Security: proportionate offline-training threat model — T-04-01 (triplet loss correctness), T-04-02 (splitter sign), T-04-03 (latest.txt clobber) each have a Task-level pytest proof; T-04-04/SC accept (zero new pip installs per RESEARCH Package Legitimacy Audit).

No revisions yet — initial PLAN.md.
</revision_log>
