---
phase: 06-overhead-press
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - backend/training/aqa/notebooks/07_ohp_md_ssl.py
  - backend/training/aqa/notebooks/07_ohp_md_ssl.ipynb
autonomous: false
requirements: [OHP-01]
must_haves:
  truths:
    - "The MD-SSL pretrain runs on the 5,490 unlabeled OHP clips + barbell trajectories via the UNCHANGED md_pretrain.run_md_pretrain_epoch (the finalized OHPSSLDataset from Plan 02, the v2 strong-aug set, the triplet distance-ratio loss, AdamW lr=1e-4 wd=1e-4, 20-epoch cosine budget) and converges: SSL triplet loss decreases over the first 5 epochs AND embedding effective_rank stays healthy (no representation collapse, the Phase-4 v2 fix) AND linear-probe macro-F1 rises above the frozen-Kinetics baseline by some epoch (OHP-01-f / RESEARCH §4)"
    - "The pretrain is disconnect-safe across the 15-26h multi-session run: it checkpoints every epoch to My Drive/FitNova/checkpoints/phase06/ohp_md_pretrain_v2/ via the Phase 2 atomic-write contract, resumes from latest.txt with map_location='cpu', and the backbone.pt (linear-probe-best epoch) write passes update_latest=False so the epoch-resume pointer is never clobbered (D7)"
    - "A backbone.pt exists on Drive at phase06/ohp_md_pretrain_v2/backbone.pt containing backbone_state_dict (the linear-probe-macro-F1-best epoch's R(2+1)D-18 weights, fc=Identity) — the fork point Plan 04 fine-tunes the 3 seeds from"
    - "The v2 strong-aug set is used by default (translation/zoom/blur/channel-swap; rotation OFF) — NO weak-vs-strong-aug ablation re-run (Phase 4 settled it; CONTEXT D3 carries v2 as the default); the linear-probe + effective_rank collapse guard confirm the representation is not collapsing (the Phase-4 v1 weak-aug collapse must NOT recur)"
    - "The linear-probe trains on the labeled OHP train subset (OHPElbowsKneesDataset, 32-frame deterministic transform) — NOT Squat — so the probe macro-F1 measures OHP-relevant transfer; the staged labeled OHP videos (/content/ohp_videos) feed the probe"
  artifacts:
    - path: "backend/training/aqa/notebooks/07_ohp_md_ssl.py"
      provides: "Phase 6 SSL pretrain jupytext notebook — Cell A + Step 0 env/stage (labeled + unlabeled+traj, reusing Plan 02's staging) + Step 1 VRAM probe (3-branch SSL forward at batch 8) + Step 2 epoch-0 timing gate + Step 3 full SSL pretrain (15-26h, multi-session resume) with linear-probe + collapse monitoring. The fine-tune notebook (08) is Plan 04."
      contains: "run_md_pretrain_epoch"
    - path: "backend/training/aqa/notebooks/07_ohp_md_ssl.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D7)"
      contains: "ohp"
  key_links:
    - from: "backend/training/aqa/notebooks/07_ohp_md_ssl.py"
      to: "backend/training/aqa/harness/md_pretrain.run_md_pretrain_epoch (UNCHANGED) + datasets.ohp_ssl.OHPSSLDataset (finalized Plan 02) + datasets.ohp.OHPElbowsKneesDataset (the linear-probe labeled subset)"
      via: "notebook cell imports"
      pattern: "run_md_pretrain_epoch"
    - from: "backend/training/aqa/notebooks/07_ohp_md_ssl.py"
      to: "backend/training/aqa/harness/colab.py (stage_ohp_videos + stage_unlabeled_ohp_videos from Plan 01; atomic_save_checkpoint update_latest, load_latest_checkpoint)"
      via: "notebook cell imports"
      pattern: "stage_unlabeled_ohp_videos"
    - from: "backend/training/aqa/notebooks/07_ohp_md_ssl.py"
      to: "backbone.pt at phase06/ohp_md_pretrain_v2/backbone.pt (the Plan 04 fork point)"
      via: "run_md_pretrain_epoch writes backbone.pt on linear-probe improvement (update_latest=False)"
      pattern: "backbone.pt"
---

<objective>
Run the Motion-Disentangling SSL pretraining of an R(2+1)D-18 backbone on the 5,490 unlabeled OHP clips + barbell trajectories (OHP-01), producing a backbone.pt (the linear-probe-best epoch) that Plan 04 fine-tunes the 3 seeds from. This is Wave 3 — the 15-26h SSL burn, AFTER the gated trajectory probe (Plan 02 Task 1) confirmed the sign + mapping and AFTER ohp_ssl.py was finalized + the triplet smoke passed. The pretext is the paper's half-cycle contrast with the triplet distance-ratio loss, run via the UNCHANGED md_pretrain.run_md_pretrain_epoch on the finalized OHPSSLDataset.

Purpose: OHP-01 + ROADMAP Phase 6 success criterion 1 ("OHP ... MD-SSL models train on the official OHP splits"). The MD method transfers to OHP with NO method changes (RESEARCH §2 — the paper applies the identical MD-SSL to OHP as to Squat; §5.2). The only OHP-specific input is the trajectory format, already resolved (Plans 01-02). Phase 4 established that STRONG augs (the v2 set) are REQUIRED — the weak-aug v1 collapsed (eff_rank 11.8->3.3, probe peaked at ep5 then declined). CONTEXT D3 carries the v2 set as the default; do NOT re-run the weak-vs-strong ablation. The linear-probe (on the labeled OHP subset) + the effective_rank collapse guard confirm the representation is learning, not collapsing.

Output: the Phase 6 SSL notebook (Cell A + Step 0 staging + VRAM probe + timing gate + the full pretrain with linear-probe + collapse monitoring) delivered as paired .py + .ipynb; backbone.pt + per-epoch checkpoints on Drive at phase06/ohp_md_pretrain_v2/.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on a FRESH L4 Colab notebook ([[feedback_heavy_training_new_notebook]] — prompt the user before the SSL run; this is the longest run in the phase, 15-26h, multiple sessions). Each task is ONE runnable unit; the user runs it and pastes output back. NEVER pre-write task N+1 before task N's paste-back. The SSL pretrain WILL span multiple Colab sessions — its checkpoint/resume must be bulletproof ([[feedback_notebook_disconnect_safe]]). After any git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]) — doubly important here, a stale loss wastes 15-26h.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-overhead-press/06-CONTEXT.md
@.planning/phases/06-overhead-press/06-RESEARCH.md
@.planning/phases/06-overhead-press/06-PATTERNS.md
@.planning/phases/06-overhead-press/06-VALIDATION.md
@.planning/phases/06-overhead-press/06-02-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-SUMMARY.md
@backend/training/aqa/datasets/ohp_ssl.py
@backend/training/aqa/datasets/ohp.py
@backend/training/aqa/datasets/ssl_augs.py
@backend/training/aqa/harness/md_pretrain.py
@backend/training/aqa/harness/colab.py
@backend/training/aqa/notebooks/04_squat_md_ssl.py
@CLAUDE.md

<interfaces>
This plan WIRES the UNCHANGED md_pretrain.run_md_pretrain_epoch on the finalized OHPSSLDataset (Plan 02) — NO new production logic. It writes only notebook cells. Plan 04 consumes the produced backbone.pt.

Reused UNCHANGED (Phase 4):
- md_pretrain.run_md_pretrain_epoch(*, run_name, drive_root, videos_root, trajectories_root, seed=42, config=None, resume=True, max_epochs=None) -> dict — for OHP: run_name="ohp_md_pretrain_v2", videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories". The 3-branch triplet forward, AdamW, cosine, per-epoch embedding collapse metrics, periodic linear-probe, backbone.pt-best write (update_latest=False) are ALL unchanged.
- md_pretrain.MDConfig — the v2 recipe defaults (lr=1e-4, wd=1e-4, batch_size=8, max_epochs=20 baseline extended to linear-probe convergence, frames_per_half=16, linear_probe_cadence=5, strong augs via the OHPSSLDataset strong_augs=True default). The SSL augs are wired in OHPSSLDataset (Plan 01/02), not MDConfig.
- md_pretrain._linear_probe — for OHP, the labeled loader is built from OHPElbowsKneesDataset (the OHP labeled train subset), NOT Squat. The probe macro-F1 measures OHP-relevant transfer. (If run_md_pretrain_epoch builds the linear-probe loader internally with a hardcoded Squat dataset, the notebook passes an OHP labeled loader or the OHP dataset — verify the linear-probe injection path; mirror the dataset_cls seam pattern if needed. NOTE: if _linear_probe needs an OHP-aware loader and the Phase-4 code hardcodes Squat, surface this to the user as a 1-line seam — do NOT silently train the probe on Squat.)
- harness.colab.stage_ohp_videos + stage_unlabeled_ohp_videos (Plan 01).

Produced artifact contract (Plan 04 input):
- Drive path: My Drive/FitNova/checkpoints/phase06/ohp_md_pretrain_v2/backbone.pt
- backbone.pt payload: backbone_state_dict (R(2+1)D-18 with fc=Identity, the linear-probe-macro-F1-best epoch), projector_state_dict, config_hash, config_repr, code_version "phase04-md-pretrain", linear_probe_history, metrics_history.
- Plan 04's build_finetune_model(backbone_path) (Phase 4, tested) loads ONLY backbone_state_dict via load_state_dict(strict=False) into r2plus1d_18(weights=None).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D3-ssl-recipe | MD-SSL pretrain = the Phase-4 v2 recipe UNCHANGED: half-cycle contrast on the OHP barbell trajectories, the v2 STRONG augs (translation/zoom/blur/channel-swap; rotation default-OFF — wired in OHPSSLDataset strong_augs=True), AdamW lr=1e-4 wd=1e-4, triplet distance-ratio loss (squared L2, 3-term), projection head discarded at fine-tune, 20-epoch cosine baseline extended to linear-probe convergence. Backbone = the linear-probe-macro-F1-best epoch. Via run_md_pretrain_epoch (UNCHANGED). | CONTEXT D3 + RESEARCH §4 |
| D3-no-ablation | Do NOT re-run the weak-vs-strong-aug ablation — Phase 4 already established strong augs are required (the v1 weak-aug collapse: eff_rank 11.8->3.3, probe peaked ep5 then declined). Carry the v2 set as the default. Run name is ohp_md_pretrain_v2 (the v1 weak-aug datapoint is not re-collected for OHP). | CONTEXT D3 + 04-02-SUMMARY.md |
| D3-collapse-guard | The linear-probe (every linear_probe_cadence=5 epochs, on the labeled OHP train subset) + the per-epoch effective_rank/embedding_std collapse metric (UNCHANGED from md_pretrain) confirm the representation is learning. Phase 4 v2 converged ~ep5 with eff_rank ~10; expect the same for OHP. If collapse recurs (eff_rank <3 before ep10), the v2 strong augs are already wired — verify the augs are active (OHPSSLDataset strong_augs=True), do NOT add new remediation (Phase 4 settled the recipe). | CONTEXT D3 + RESEARCH §4 |
| D3-probe-OHP | The linear-probe trains on OHPElbowsKneesDataset (the labeled OHP train subset), NOT Squat — so the probe macro-F1 measures OHP transfer. If run_md_pretrain_epoch's internal linear-probe loader is Squat-hardcoded, add a 1-line OHP-aware seam (mirror the dataset_cls pattern) and surface it; do NOT train the probe on Squat. | RESEARCH §4 / §5 |
| D3-walltime | 5,490 OHP clips ~10% more than 4,970 Squat -> ~15-26h SSL (Squat was 12-24h at ep5 convergence). Re-measure epoch-0 timing on L4 (Step 2) before committing. Same convergence pattern expected: probe peaks ~ep3-7. | RESEARCH §2 / §4 / A3 |
| D7 | persistent_workers on the SSL loader (build_ssl_loader, Plan 01); map_location='cpu' on resume; backbone.pt write passes update_latest=False (UNCHANGED md_pretrain — the Phase-4 colab edit); metrics_history/linear_probe_history from latest.txt-pointed checkpoint for viz; .ipynb with Cell A on the FIRST ship; one runnable unit at a time; restart runtime / importlib.reload after git pull; videos_root/trajectories_root point at the OHP staged dirs (Pitfall 5). | CONTEXT D7 + RESEARCH §6 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 2/3/4 + D7)

1. Cell A first: from backend.training.aqa.harness import _envinit is the FIRST executable line of Step 0 (F8 — CUBLAS_WORKSPACE_CONFIG before CUDA).
2. persistent_workers via build_ssl_loader (Plan 01, num_workers>0 guard).
3. map_location='cpu' on load_latest_checkpoint at resume (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume (run_md_pretrain_epoch, unchanged).
5. backbone.pt write passes update_latest=False (UNCHANGED md_pretrain) so latest.txt epoch-resume pointer is never clobbered (RESEARCH §6 / Pitfall 4).
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch.
7. metrics_history + linear_probe_history loaded from the latest.txt-pointed checkpoint for viz (NOT backbone.pt, frozen at the best epoch — [[reference_best_pt_metrics_history_is_stale]]).
8. config_hash via hash_config over the MDConfig recipe keys (recipe change invalidates resume).
9. No AMP (fp32) — paper fidelity + determinism (RESEARCH §4 carry).
10. videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories" (Pitfall 5 — not the Squat defaults); the linear-probe labeled loader uses /content/ohp_videos.
11. Restart the Colab runtime / importlib.reload after git pull (a stale SSL loss wastes 15-26h — [[reference_colab_module_reload_after_git_pull]]).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are the 15-26h-resume pointer, representation collapse (wasted GPU), the linear-probe training on the RIGHT (OHP) dataset, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| Drive OHP unlabeled videos/trajectories -> SSL dataset workers | Reading the staged 5,490 OHP clips + bar_trajectories_raw JSONs; clip IDs from the dataset release, no path traversal. Staged once per session (two release folders — Pitfall 4). |
| In-memory SSL state -> Drive checkpoint (15-26h, multi-session) | epoch_NNN.pt + backbone.pt over Drive FUSE; the 15-26h run depends on the resume pointer never being clobbered (the update_latest=False contract). |
| Linear-probe dataset selection | The probe MUST train on the labeled OHP subset (OHPElbowsKneesDataset), not Squat — else the probe macro-F1 is meaningless for OHP transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Denial of service | Representation collapse — SSL loss decreases but the backbone learns nothing; 15-26h wasted (Phase 4 v1 weak-aug collapse: eff_rank 11.8->3.3) | mitigate | The v2 STRONG augs are wired by default (OHPSSLDataset strong_augs=True — Phase 4's established fix); the per-epoch effective_rank/embedding_std + the linear-probe macro-F1 every 5 epochs (UNCHANGED md_pretrain) monitor it; Step 3 acceptance pastes back the epoch-5 collapse + loss + probe signals before authorizing the full run. MEDIUM — monitored, not silent; the recipe fix is already in place. |
| T-06-10 | Integrity | The linear-probe trains on Squat instead of OHP (if run_md_pretrain_epoch's probe loader is Squat-hardcoded) — a meaningless probe signal | mitigate | The notebook verifies the linear-probe loader is OHPElbowsKneesDataset (Step 0/3); if the Phase-4 code hardcodes Squat, a 1-line OHP-aware seam is added + surfaced (do NOT silently use Squat). The probe macro-F1 paste-back must reflect OHP labels. MEDIUM — verified at run time. |
| T-06-11 | Integrity | 15-26h multi-session resume pointer (latest.txt) corrupted by the backbone.pt write | mitigate | backbone.pt write passes update_latest=False (UNCHANGED md_pretrain — Phase-4 tested); per-epoch atomic_save_checkpoint + prune; resume verified by a paste-back of "Resumed from epoch_NNN.pt" on the first post-disconnect epoch. MEDIUM — the contract is Phase-4-proven. |
| T-06-12 | Denial of service | Disk exhaustion staging 5,490 unlabeled OHP clips + trajectories on Colab /content | accept | The 3-layer resume in stage_unlabeled_ohp_videos handles partial extraction; Step 0 logs the videos.zip size + free disk. Colab /content is ~70-100 GB. LOW — probed at stage time, resume-safe. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe is the guard; PyAV reused from Phase 2/3. No [ASSUMED]/[SUS] packages. |

T-06-09 (collapse) is the highest-cost threat; it is mitigated by the v2 strong-aug default (Phase 4's fix already in place) + the monitored epoch-5 gate before the full run. No web-app threats apply.

</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Notebook Cell A + Step 0 stage + Step 1 VRAM probe + Step 2 epoch-0 timing gate (BLOCKING — user authorizes the 15-26h SSL run)</name>
  <files>
    backend/training/aqa/notebooks/07_ohp_md_ssl.py,
    backend/training/aqa/notebooks/07_ohp_md_ssl.ipynb
  </files>
  <read_first>
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the FULL Squat SSL notebook — Cell A + Step 0 stage + Step 3 VRAM probe + Step 4 epoch-0 timing gate patterns; the max_memory_allocated + estimated_total_h + checkpoint round-trip idioms — copy + swap to OHP),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (the Plan 02 Step 0 OHP staging — reuse the two-drive-root stage calls verbatim),
    backend/training/aqa/harness/md_pretrain.py (run_md_pretrain_epoch + MDConfig + _linear_probe — Step 1 builds the SSL forward, Step 2 calls run_md_pretrain_epoch with max_epochs=1; verify the linear-probe loader dataset),
    backend/training/aqa/datasets/ohp_ssl.py (the finalized OHPSSLDataset from Plan 02) + datasets/ohp.py (OHPElbowsKneesDataset — the linear-probe labeled subset),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — the VRAM re-estimate batch 8, the wall-time 15-26h, the linear-probe protocol on the OHP labeled subset; §2 the method transfers unchanged; A3/A4),
    .planning/phases/04-squat-motion-disentangling-ssl/04-02-SUMMARY.md (the v2 strong-aug fix + the linear-probe-best backbone + the collapse signals — the OHP run expects the same pattern)
  </read_first>
  <decision>
    Should we proceed with the full OHP SSL pretrain at batch_size=8 with the v2 MDConfig, or downscope (batch 5 paper-exact / fewer epochs) before committing to the 15-26h run?
  </decision>
  <context>
    The SSL pretrain is the longest run in the phase (15-26h, multiple Colab sessions). The method + recipe transfer from Phase 4 UNCHANGED (RESEARCH §2). Cell A + Step 0 stage the OHP labeled (2367, for the linear-probe) + unlabeled (5490 + trajectories) via the Plan 02 staging. Two probe cells:

    Step 1 (VRAM probe): build_md_model() to cuda; pull one SSL batch from build_ssl_loader(OHPSSLDataset, MDConfig()); run a forward + backward of the 3-branch triplet (phi_a/p/n + md_triplet_loss + loss.backward()); print torch.cuda.max_memory_allocated()/1e9 (peak backward VRAM at batch 8). Assert it fits L4 (< 22 GB). If OOM or >~20 GB, the user reports and the decision offers batch 5.

    Step 2 (epoch-0 timing gate): run_md_pretrain_epoch(run_name="ohp_md_pretrain_v2_timing", drive_root=MYDRIVE, videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories", seed=42, resume=False, max_epochs=1) — one fresh epoch under the production code path on the 5,490 OHP clips; paste back epoch_0 wall-time (from metrics_history), estimated_total_h = epoch_0_time * MDConfig.max_epochs / 3600, the epoch-0 ssl_loss + embedding_std (sanity: loss finite, embedding_std not already collapsed), GPU util. Also verify the checkpoint round-trip (T-06-11): torch.load(map_location='cpu') the epoch_000.pt, assert the SSL payload keys + code_version "phase04-md-pretrain". CRITICAL VERIFY (T-06-10): confirm the linear-probe loader (if the epoch-0 run triggers a probe, or via inspecting run_md_pretrain_epoch's probe construction) uses OHPElbowsKneesDataset / /content/ohp_videos — NOT Squat. If the Phase-4 code hardcodes the Squat labeled loader for the probe, surface it: add a 1-line OHP-aware seam (mirror the dataset_cls pattern) and note it before the full run. The timing run lands in a separate _timing dir.

    Decision criteria: estimated_total_h <= 24 and VRAM fits at batch 8 -> option-a (proceed batch 8). estimated_total_h > 26 OR VRAM tight -> option-b (batch 5 paper-exact).
  </context>
  <options>
    <option id="option-a">
      <name>Proceed at batch 8, v2 MDConfig (20-epoch baseline extended to linear-probe convergence)</name>
      <pros>Throughput headroom over the paper's batch 5; paper-faithful recipe (triplet loss, 16-frame half-cycles, AdamW, v2 strong augs); fits the 15-26h budget if epoch time is in the Squat range; identical to the proven Phase-4 v2 run.</pros>
      <cons>If epoch-0 is at the high end and convergence needs 40+ epochs, the run approaches the upper budget + spans several sessions (the resume contract carries it).</cons>
    </option>
    <option id="option-b">
      <name>Drop to batch 5 (paper-exact)</name>
      <pros>Exactly the paper's batch size; lowest VRAM; fully paper-faithful. Choose if batch 8 OOMs or VRAM is tight.</pros>
      <cons>Slower per-epoch throughput; marginally longer wall-time.</cons>
    </option>
  </options>
  <action>This is a BLOCKING decision checkpoint. The executor lands Cell A + Step 0 (OHP staging, reused from Plan 02) + Step 1 (VRAM probe) + Step 2 (epoch-0 timing gate + the linear-probe-dataset verify) in the notebook .py AND ships the .ipynb pair (Cell A baked in — D7). The user runs them on the fresh L4 (prompt the user FIRST — the longest run in the phase, [[feedback_heavy_training_new_notebook]]) and pastes back: peak backward VRAM at batch 8 (Step 1), epoch_0 wall-time + estimated_total_h + epoch-0 ssl_loss + embedding_std + GPU util + the checkpoint round-trip + the linear-probe-dataset confirmation (Step 2). The user replies option-a / option-b. No code change unless option-b (MDConfig batch_size=5) or the T-06-10 probe-loader seam is needed. Atomic commit: feat(06): notebook Cell A + Step 0 stage + Step 1 VRAM + Step 2 timing gate (Task 1, D3-walltime / RESEARCH §4).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/07_ohp_md_ssl.py').read()); print('parse ok')" && grep -q "_envinit" backend/training/aqa/notebooks/07_ohp_md_ssl.py && grep -q "max_memory_allocated" backend/training/aqa/notebooks/07_ohp_md_ssl.py && grep -q "estimated_total_h" backend/training/aqa/notebooks/07_ohp_md_ssl.py && grep -q "ohp_md_pretrain_v2_timing" backend/training/aqa/notebooks/07_ohp_md_ssl.py</automated>
  </verify>
  <done>User replies option-a / option-b; Task 2 inherits the chosen batch size; VRAM fit + epoch-0 timing + the checkpoint round-trip + the linear-probe-on-OHP confirmation all verified before the 15-26h burn is authorized</done>
  <resume-signal>Type "option-a" or "option-b". Or paste back the VRAM + epoch_0 time + estimated_total_h + the linear-probe-dataset confirmation and ask for guidance.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Notebook Step 3 — full SSL pretrain (15-26h, multi-session resume) + epoch-5 convergence checkpoint</name>
  <files>backend/training/aqa/notebooks/07_ohp_md_ssl.py</files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (run_md_pretrain_epoch — Step 3 calls it with resume=True; the linear-probe + collapse-metric + backbone.pt-best logic, UNCHANGED),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the Squat Step 5 full-SSL-pretrain pattern — the resume=True single-cell-is-the-whole-run idiom + the staged per-epoch paste-back + the disconnect/resume recovery note + the epoch-5 convergence gate),
    backend/training/aqa/harness/colab.py (load_latest_checkpoint — the "Resumed from epoch_NNN.pt" log line the resume paste-back verifies),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — the convergence criterion: linear-probe rises above frozen-Kinetics; the collapse abort; the backbone = linear-probe-best epoch),
    .planning/phases/06-overhead-press/06-VALIDATION.md (OHP-01-f — MD-SSL linear-probe F1 > Kinetics baseline at some epoch),
    .planning/phases/04-squat-motion-disentangling-ssl/04-02-SUMMARY.md (the v2 run reality — probe peaked ~ep5, eff_rank healthy ~10; the OHP run expects the same pattern)
  </read_first>
  <action>
    Fill in Step 3: the full SSL pretrain. config = MDConfig() (or batch 5 per Task 1's decision); RUN_NAME = "ohp_md_pretrain_v2"; result = run_md_pretrain_epoch(run_name=RUN_NAME, drive_root=MYDRIVE, videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories", seed=42, config=config, resume=True, max_epochs=config.max_epochs). (If Task 1 added the OHP linear-probe seam, pass the OHP labeled loader / dataset_cls as that seam requires.) resume=True so a mid-run Colab disconnect is recoverable (re-run this cell after restart -> resumes from latest.txt). The trainer checkpoints every epoch + writes backbone.pt on linear-probe improvement (update_latest=False), so this single cell IS the whole 15-26h run. Print final epoch reached, backbone path, best linear-probe macro.

    This is one runnable unit but ASYNC + multi-session — acceptance is a STAGED paste-back per [[feedback_interactive_execution]]: (a) FIRST, the epoch-5 convergence gate — paste back the epoch-0..5 ssl_loss (decreasing), embedding_std + effective_rank (eff_rank NOT collapsing toward <3 — the v2 fix holding, contrast with the v1 weak-aug collapse), and the epoch-5 linear-probe macro-F1 vs the frozen-Kinetics baseline (rising = OHP-01-f signal). If eff_rank collapses or the probe stays flat at random, STOP + report (the v2 augs should prevent this — verify OHPSSLDataset strong_augs=True is active, do NOT add new remediation); (b) on any mid-run disconnect: re-run Step 0 + Step 3 -> the first post-resume epoch logs "Resumed from epoch_NNN.pt"; (c) FINAL: the run completes (linear-probe plateau or max_epochs) — paste back the final epoch + the best linear-probe macro + the backbone.pt path on Drive. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Plan 04 until the SSL run completes + the backbone.pt exists.

    Atomic commit: feat(06): notebook Step 3 full OHP SSL pretrain (Task 2, D3-ssl-recipe / OHP-01-f).
  </action>
  <verify>
    <automated>grep -q "run_md_pretrain_epoch" backend/training/aqa/notebooks/07_ohp_md_ssl.py && grep -q "ohp_md_pretrain_v2" backend/training/aqa/notebooks/07_ohp_md_ssl.py && grep -q "resume=True" backend/training/aqa/notebooks/07_ohp_md_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 present; run_md_pretrain_epoch(run_name="ohp_md_pretrain_v2", videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories", resume=True)
    - Runnable unit (Colab, 15-26h, multi-session): epoch-0..5 paste-back shows ssl_loss decreasing + eff_rank healthy (NOT collapsing) + linear-probe macro-F1 rising above frozen-Kinetics (OHP-01-f); on disconnect, the resume paste-back logs "Resumed from epoch_NNN.pt"; FINAL = final epoch + best linear-probe macro + backbone.pt path
    - The v2 strong augs are active (OHPSSLDataset strong_augs=True); NO weak-vs-strong ablation re-run; the linear-probe is on the OHP labeled subset (not Squat)
    - backbone.pt on Drive at phase06/ohp_md_pretrain_v2/backbone.pt (the linear-probe-best epoch); backbone.pt write passed update_latest=False (latest.txt not clobbered)
  </acceptance_criteria>
  <dependencies>Task 1 (VRAM/timing gate — authorizes the run + the batch size)</dependencies>
  <done>The MD-SSL pretrain ran on the 5,490 unlabeled OHP clips + trajectories and converged (linear-probe rose above frozen-Kinetics, OHP-01-f; eff_rank healthy — no v1-style collapse); backbone.pt (the linear-probe-best epoch) is on Drive — the fork point Plan 04 fine-tunes the 3 seeds from</done>
</task>

</tasks>

<verification>
## Mapping to Phase 6 ROADMAP Success Criterion + OHP-01

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (partial): OHP MD-SSL model trains on the official OHP splits | Tasks 1 (gate), 2 (run) | Task 2 epoch-0..5 paste-back: ssl_loss decreasing + linear-probe rising above frozen-Kinetics (OHP-01-f); backbone.pt on Drive |
| The SSL backbone for the 3-seed fine-tune | Task 2 | backbone.pt (linear-probe-best epoch) at phase06/ohp_md_pretrain_v2/ — the Plan 04 fork point |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| OHP-01-f (MD-SSL linear-probe F1 > Kinetics baseline at some epoch) | Task 2 | integration (Colab paste-back) | 3 |

(OHP-01-g final ensemble test F1 is Plan 04.)
</verification>

<success_criteria>
- Both tasks complete with paste-back acceptance from the user on the L4 Colab
- The MD-SSL pretrain ran on the 5,490 unlabeled OHP clips + trajectories via the UNCHANGED md_pretrain.run_md_pretrain_epoch (the finalized OHPSSLDataset, the v2 strong augs) and converged: ssl_loss down, eff_rank healthy (no v1-style collapse), linear-probe macro-F1 above frozen-Kinetics (OHP-01-f)
- The linear-probe trained on the OHP labeled subset (OHPElbowsKneesDataset), NOT Squat (T-06-10 verified; a 1-line OHP-aware seam added if the Phase-4 probe loader was Squat-hardcoded)
- backbone.pt (the linear-probe-best epoch) on Drive at phase06/ohp_md_pretrain_v2/backbone.pt — the Plan 04 fork point; backbone.pt write passed update_latest=False
- Disconnect-safe across the 15-26h multi-session run: the resume paste-back logs "Resumed from epoch_NNN.pt"; videos_root/trajectories_root point at the OHP staged dirs (Pitfall 5)
- NO weak-vs-strong-aug ablation re-run (Phase 4 settled it; v2 carried as default); the run name is ohp_md_pretrain_v2
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D7); every D3 decision carries an inline citation
- One accurate threat_model: offline pipeline, no high-severity threats; T-06-09 (collapse — v2 augs + monitored gate), T-06-10 (probe-on-OHP), T-06-SC accept (zero new installs)
</success_criteria>

<output>
After Task 2 (backbone.pt on Drive), this plan is complete. Plan 04 (Wave 4) loads backbone.pt, runs the 3-seed fine-tune (42/1337/7) + the ensemble + val-tuned threshold + TTA + test eval + results.pkl + the F1-vs-paper comparison. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 04 once Task 2's backbone.pt is confirmed on Drive. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 6 Plan 03 written 2026-05-27. Wave 3: the MD-SSL pretrain on the 5,490 unlabeled OHP clips -> backbone.pt (mirrors Phase 4 Plan 02's SSL run). 2 tasks (a blocking VRAM/timing gate + the 15-26h run). NO new production logic — this plan WIRES the UNCHANGED md_pretrain.run_md_pretrain_epoch on the finalized OHPSSLDataset (Plans 01-02). The v2 strong-aug set is the default (Phase 4 settled weak-vs-strong; the v1 collapse — eff_rank 11.8->3.3 — must not recur); run name ohp_md_pretrain_v2.

The one OHP-specific verification: the linear-probe must train on the OHP labeled subset (OHPElbowsKneesDataset), not Squat (T-06-10). If run_md_pretrain_epoch's internal probe loader is Squat-hardcoded, Task 1 surfaces a 1-line OHP-aware seam (mirror the dataset_cls pattern) — do NOT silently train the probe on Squat. Flagged in <interfaces> + the threat model so the executor checks it at the VRAM/timing gate.

Pitfall 5 honored: videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories" explicit; the probe labeled loader uses /content/ohp_videos. backbone.pt write passes update_latest=False (UNCHANGED md_pretrain — Phase-4 colab edit).

Security: accurate. Offline pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-06-09 (collapse — the highest cost, mitigated by the v2 default + the monitored ep-5 gate), T-06-10 (probe-on-OHP), T-06-11 (resume pointer), T-06-12 accept (disk), T-06-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
