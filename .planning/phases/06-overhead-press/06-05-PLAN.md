---
phase: 06-overhead-press
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - docs/notebooks/05_ohp_eda.py
  - docs/notebooks/05_ohp_eda.ipynb
  - docs/notebooks/06_ohp_data_pipeline.py
  - docs/notebooks/06_ohp_data_pipeline.ipynb
  - docs/notebooks/07_ohp_training.py
  - docs/notebooks/07_ohp_training.ipynb
  - docs/notebooks/08_ohp_evaluation.py
  - docs/notebooks/08_ohp_evaluation.ipynb
  - docs/notebooks/README.md
  - docs/eval/FINDINGS.md
  - docs/eval/ohp_test_scores.csv
  - docs/figures/ohp_class_balance.png
  - docs/figures/ohp_f1_vs_paper.png
  - docs/figures/ohp_pr_curves.png
  - docs/figures/ohp_confusion_matrices.png
  - docs/figures/ohp_training_curves.png
  - .planning/phases/06-overhead-press/06-05-SUMMARY.md
autonomous: false
requirements: [OHP-01]
must_haves:
  truths:
    - "The 4-notebook OHP analysis pack (docs/notebooks/05_ohp_eda, 06_ohp_data_pipeline, 07_ohp_training, 08_ohp_evaluation) exists as paired .py + executed .ipynb mirroring the Squat pack (docs/notebooks/01-04), each pre-executed with figures/tables rendered — built ONLY from real data: results.pkl (Plan 04), the local OHP labeled archive, and the recorded training artifacts; NO fabricated metrics or curves"
    - "The OHP evaluation notebook (08) reports the official-test-split F1 per error (Elbows + Knees) with PR curves + confusion matrices + the per-seed-vs-ensemble + the F1-vs-paper comparison (our baseline -> our MD-SSL -> paper Ours MD 0.4552/0.8452), every number sourced from results.pkl + cross-checked against RESEARCH §1 — the academic deliverable for OHP-01 (F1 per error compared to the published numbers)"
    - "docs/eval/FINDINGS.md has an OHP section (mirroring the Squat section) with the headline result (Elbows/Knees/macro F1 vs paper), the SSL lift over the baseline, the per-seed variance, the D2 near-independence caveat, and the honest framing (no paper Kinetics baseline for OHP; the baseline is the only control); docs/eval/ohp_test_scores.csv has the per-clip ensemble scores + labels (from results.pkl)"
    - "The phase SUMMARY (06-05-SUMMARY.md) declares: the production checkpoint(s) (the 3 ohp_md_finetune seeds + the ensemble protocol + per-error thresholds), the headline F1 vs paper, the SSL lift, deviations, and the D1 descope note (OHP-01 met by model+eval+viz; the ROADMAP SC3 'OHP selectable through the inference API' descoped — frontend cancelled)"
    - "ROADMAP + REQUIREMENTS are reconciled via gsd-sdk (not a direct edit): OHP-01 marked addressed by model+eval+viz; the SC3 descope recorded with the D1 reason; STATE updated to Phase 6 complete"
  artifacts:
    - path: "docs/notebooks/05_ohp_eda.py"
      provides: "OHP EDA notebook — official splits (1582/339/339), class balance (Elbows 25.7% / Knees 34.2%), the near-independence co-occurrence (85/407), clip properties, sample frames per error, barbell-trajectory curves. Mirror docs/notebooks/01_squat_eda. Built from the local OHP labeled archive."
      contains: "ohp"
    - path: "docs/notebooks/06_ohp_data_pipeline.py"
      provides: "OHP data-pipeline notebook — splits used verbatim; imbalance via loss pos_weight (Elbows 2.89x / Knees 1.92x); 32-frame uniform sampling -> 112 crop -> Kinetics norm; the BBox trajectory -> y_center pipeline (the OHP delta vs Squat's flat lists); train-aug vs inference crop. Mirror 02_squat_data_pipeline."
      contains: "ohp"
    - path: "docs/notebooks/07_ohp_training.py"
      provides: "OHP training-diagnostics notebook — SSL pretraining (triplet loss, effective-rank collapse diagnostic v2-vs-v1, linear-probe vs Kinetics), supervised fine-tune train-vs-val loss (overfit onset) + val-F1 early-stop, baseline->MD-SSL lift. Built from results.pkl ssl_epochs + finetune_seeds. Mirror 03_squat_training."
      contains: "ohp"
    - path: "docs/notebooks/08_ohp_evaluation.py"
      provides: "OHP evaluation notebook — official-test-split F1 per error, score distributions, PR curves, confusion matrices, per-seed vs ensemble, TTA ablation, the F1-vs-paper comparison (baseline -> MD-SSL -> paper Ours MD 0.4552/0.8452). Built from results.pkl. Mirror 04_squat_evaluation."
      contains: "ohp"
    - path: "docs/eval/FINDINGS.md"
      provides: "EXTENDED — an OHP section after the Squat section: headline F1 vs paper, the SSL lift, per-seed variance, the D2 near-independence caveat, the no-paper-Kinetics-baseline framing"
      contains: "Overhead Press"
    - path: "docs/eval/ohp_test_scores.csv"
      provides: "Per-clip ensemble test scores + labels + clip_ids for OHP (from results.pkl ensemble_test_scores + test_labels + test_clip_ids) — mirror squat_test_scores.csv"
      contains: "clip_id"
    - path: ".planning/phases/06-overhead-press/06-05-SUMMARY.md"
      provides: "Phase 6 closeout — what shipped, the headline F1 vs paper + the SSL lift, per-seed variance, the chosen TTA recipe, deviations, the D1 descope note (SC3 reinterpreted), the production-checkpoint declaration (3 seeds + ensemble protocol + thresholds)"
      contains: "Phase 6"
  key_links:
    - from: "docs/notebooks/08_ohp_evaluation.py"
      to: ".planning/phases/06-overhead-press/figures/results.pkl (Plan 04 — the source of truth for all numbers + raw scores)"
      via: "the notebook loads results.pkl + plots; NO recomputation, NO fabrication"
      pattern: "results.pkl"
    - from: "docs/notebooks/05_ohp_eda.py"
      to: "the local OHP labeled archive (Labels/error_{elbows,knees}.json + Splits + clips)"
      via: "the EDA notebook reads the local archive (offline) for class balance + co-occurrence + sample frames"
      pattern: "error_elbows"
    - from: ".planning/phases/06-overhead-press/06-05-SUMMARY.md"
      to: "ROADMAP + REQUIREMENTS (OHP-01 addressed by model+eval+viz; SC3 descoped per D1)"
      via: "gsd-sdk reconciliation (not a direct edit)"
      pattern: "OHP-01"
---

<objective>
Build the OHP visualization deliverable — the 4-notebook OHP analysis pack (docs/notebooks/05-08_ohp_*) + figures + the FINDINGS.md OHP section + ohp_test_scores.csv — from results.pkl (Plan 04) + the local OHP data, mirroring the existing Squat pack, and author the phase SUMMARY (OHP-01 + the deliverable per CONTEXT D1). This is Wave 5 — the closeout: no training, only assembling the academic deliverable from the real artifacts produced in Plans 02-04, then reconciling ROADMAP/REQUIREMENTS/STATE.

Purpose: OHP-01 (F1 per error compared to the published numbers) + CONTEXT D1 (the deliverable = the Squat pattern for OHP: results.pkl + F1-vs-paper + the 4-notebook pack + FINDINGS.md, mirroring docs/notebooks/ + docs/eval/FINDINGS.md). The defense (2026-06-03) weights visualizations heavily ([[project_supervisor_visualizations]]); the 4-notebook pack is the OHP analog of the Squat pack the supervisor already has. The docs/notebooks/README.md already anticipates this ("The same notebook set will be produced for Overhead Press"). Every number + plot comes from results.pkl or the local OHP data — NO fabrication ([[feedback_ai_correctness]]).

Output: 4 paired .py + executed .ipynb notebooks under docs/notebooks/ (05-08_ohp_*); the README.md updated with the OHP rows + headline; 5 OHP figures under docs/figures/; the FINDINGS.md OHP section; ohp_test_scores.csv; the phase SUMMARY; ROADMAP/REQUIREMENTS/STATE reconciled (SC3 descope per D1).

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]]. The notebooks are delivered as paired .py + .ipynb ([[feedback_deliver_colab_as_ipynb]]) and pre-executed (open -> figures rendered). Most of this plan runs LOCALLY (results.pkl + the local OHP archive — no GPU, no Drive) except where a notebook needs a sample clip decoded (the local OHP clips suffice). The SUMMARY is the closeout — it declares the production checkpoint + the headline for the defense + the Phase 7 handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-overhead-press/06-CONTEXT.md
@.planning/phases/06-overhead-press/06-RESEARCH.md
@.planning/phases/06-overhead-press/06-PATTERNS.md
@.planning/phases/06-overhead-press/06-VALIDATION.md
@.planning/phases/06-overhead-press/06-04-PLAN.md
@.planning/phases/01-dataset-consolidation-eda/01-DATASET-REPORT.md
@docs/notebooks/README.md
@docs/notebooks/01_squat_eda.py
@docs/notebooks/02_squat_data_pipeline.py
@docs/notebooks/03_squat_training.py
@docs/notebooks/04_squat_evaluation.py
@docs/eval/FINDINGS.md
@CLAUDE.md

<interfaces>
This plan CONSUMES results.pkl (Plan 04) + the local OHP archive. NO new production logic, NO training. It writes notebooks + figures + FINDINGS.md + the CSV + the SUMMARY.

Inputs:
- .planning/phases/06-overhead-press/figures/results.pkl (Plan 04) — the source of truth: ssl_epochs, finetune_seeds {42,1337,7}, ensemble_val/test_scores, val/test_labels, test_clip_ids, best_thresholds, chosen_tta_recipe, test_f1 {elbows,knees,macro} (ensemble + single + per-seed), test_pr_auc, test_confusion, baseline_control {elbows,knees,macro}, ssl_lift, val_test_gap, paper_targets {elbows:0.4552,knees:0.8452}, paper_temporalxform {elbows:0.4138,knees:0.8416}.
- The local OHP labeled archive (Labels/error_{elbows,knees}.json + Splits/{train,val,test}_keys.json + clips) for the EDA notebook (class balance, co-occurrence, sample frames, trajectory curves).
- The Squat pack docs/notebooks/01-04 + docs/eval/FINDINGS.md + docs/figures/squat_*.png — the structure + style to mirror (color palette ["#1B998B","#2E86AB","#D7263D"]).

Deliverable templates (mirror per PATTERNS "OHP deliverable notebooks" section):
- docs/notebooks/01_squat_eda -> 05_ohp_eda; 02_squat_data_pipeline -> 06_ohp_data_pipeline; 03_squat_training -> 07_ohp_training; 04_squat_evaluation -> 08_ohp_evaluation.
- docs/eval/FINDINGS.md (Squat section) -> add an OHP section.
- docs/eval/squat_test_scores.csv -> ohp_test_scores.csv.

gsd-sdk reconciliation (D1 — NOT a direct edit):
- Mark OHP-01 addressed by model+eval+viz; record the SC3 descope (OHP not wired into the inference API — frontend cancelled) with the D1 reason; update STATE to Phase 6 complete. Use the gsd-sdk query/commit handlers per the orchestrator's convention.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D1-deliverable | The deliverable = the Squat pattern, for OHP: the 4-notebook pack (05-08_ohp_*) mirroring docs/notebooks/01-04 + figures + FINDINGS.md (OHP section) + ohp_test_scores.csv, all from results.pkl + the local OHP data. Backend API integration is DESCOPED (frontend cancelled) — NO serving/endpoint notebook. | CONTEXT D1 |
| D1-sc3-descope | ROADMAP Phase-6 SC3 ("OHP selectable through the inference API") is reinterpreted: OHP-01 met by trained/evaluated models + the paper comparison + the visualization pack. Reflect this in ROADMAP/REQUIREMENTS via gsd-sdk (not a direct edit) at plan/close — note OHP-01 met by model+eval+viz, the API criterion descoped with the D1 reason (frontend cancelled). | CONTEXT D1 |
| D-no-fabrication | EVERY number + plot in the notebooks/FINDINGS/CSV comes from results.pkl or the local OHP archive. NO fabricated metrics or curves. The F1-vs-paper numbers are cross-checked against RESEARCH §1 (paper Ours MD 0.4552/0.8452). If results.pkl lacks a field a figure needs, the figure is omitted + noted — never invented. | [[feedback_ai_correctness]] |
| D2-framing | The FINDINGS + the evaluation notebook state the D2 near-independence caveat (OHP Elbows/Knees largely independent — 85/407; the joint head is justified by the shared MD-SSL representation + efficiency, NOT co-occurrence) + the no-paper-Kinetics-baseline framing (the baseline is the only control; the SSL lift = ensemble - baseline). Honest, defensible framing — no overclaiming. | CONTEXT D2 + RESEARCH §1 |
| D-notebooks | Notebooks delivered as paired .py (jupytext percent) + executed .ipynb ([[feedback_deliver_colab_as_ipynb]]); pre-executed (figures rendered on open); mirror the Squat color palette + layout. The EDA + pipeline notebooks read the local OHP archive; the training + evaluation notebooks read results.pkl. | CONTEXT D7 + PATTERNS |
| D7 | Figures saved as PNG to docs/figures/ BEFORE plt.show() with defensive mkdir; one runnable unit per notebook (or per figure subsection) ([[feedback_one_cell_at_a_time_strict]]); commit per notebook (docs(06)). | CONTEXT D7 |

</decisions>

<determinism_checklist>
## Hygiene preconditions

1. NO fabrication: every number/plot from results.pkl or the local OHP archive; cross-check the F1-vs-paper numbers against RESEARCH §1.
2. Figures saved BEFORE plt.show() with Path(out).parent.mkdir(parents=True, exist_ok=True).
3. Notebooks paired .py + executed .ipynb; the .ipynb is the pre-executed artifact (figures rendered).
4. results.pkl is read-only here (the source of truth, written in Plan 04) — this plan does NOT recompute metrics, only visualizes them.
5. The SUMMARY numbers match results.pkl exactly (the orchestrator double-checks macro=(elbows+knees)/2).
6. ROADMAP/REQUIREMENTS/STATE reconciled via gsd-sdk (not direct edits) — the D1 descope recorded with its reason.
7. One runnable unit per notebook; commit per notebook (docs(06)).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE deliverable-assembly task (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are results.pkl (our own artifact) + the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: NO fabricated metrics (academic integrity), the dataset license (non-commercial, don't redistribute media), and not committing dataset media (the notebooks reference local clips, do not embed them). Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| results.pkl -> notebooks/FINDINGS/CSV | The deliverable numbers must come from results.pkl (not hand-typed or invented). A fabricated number misrepresents the academic result — the single integrity risk. |
| Local OHP archive (research-use-only) -> EDA notebook | The EDA notebook reads the local archive for class balance + sample frames. License: non-commercial. Sample frames in the .ipynb are illustrative (a few frames), not a redistribution of the dataset. |
| The notebooks/figures -> git | Commit the .py + .ipynb + the small PNGs + the CSV; NO dataset media, NO weights (gitignored). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | Fabricated metrics/curves in the notebooks or FINDINGS (the academic-integrity risk) | mitigate | EVERY number + plot loads from results.pkl (Plan 04) or the local OHP archive; the F1-vs-paper numbers cross-checked against RESEARCH §1 (paper Ours MD 0.4552/0.8452); if results.pkl lacks a field, the figure is omitted + noted, NEVER invented ([[feedback_ai_correctness]]). The SUMMARY numbers match results.pkl. MEDIUM — the source-of-truth discipline + the cross-check. |
| T-06-19 | Integrity | Committing dataset media (sample-frame .ipynb cells embedding many clips) or the large results.pkl artifacts | accept | The EDA .ipynb embeds only a few illustrative sample frames (not a dataset redistribution); the OHP clips themselves are gitignored / Drive-staged; results.pkl (small — scores + curves, no media) is committed under .planning/ as the Squat one is. Stage files by name only. LOW — the Squat pack set the precedent; license-compliant. |
| T-06-20 | Integrity | The SUMMARY / FINDINGS overclaiming (e.g. claiming a co-occurrence joint lift, or comparing to a non-existent paper Kinetics OHP baseline) | mitigate | The D2 near-independence caveat + the no-paper-Kinetics-baseline framing are stated explicitly (honest, defensible); the comparison is baseline -> MD-SSL -> paper Ours MD only (the CITED rows). MEDIUM — the honest-framing discipline. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). matplotlib/numpy already present. No [ASSUMED]/[SUS] packages. |

T-06-18 (fabrication) is the integrity risk for a deliverable-assembly plan; mitigated by the source-of-truth discipline (results.pkl + the local archive) + the RESEARCH §1 cross-check. No web-app threats apply.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: docs/notebooks/05_ohp_eda + 06_ohp_data_pipeline (.py + executed .ipynb) from the local OHP archive</name>
  <files>
    docs/notebooks/05_ohp_eda.py,
    docs/notebooks/05_ohp_eda.ipynb,
    docs/notebooks/06_ohp_data_pipeline.py,
    docs/notebooks/06_ohp_data_pipeline.ipynb,
    docs/figures/ohp_class_balance.png
  </files>
  <read_first>
    docs/notebooks/01_squat_eda.py (the EDA structure — splits load, class balance, co-occurrence, clip properties, sample frames, trajectory curves; the color palette + layout to mirror),
    docs/notebooks/02_squat_data_pipeline.py (the pipeline notebook — splits-verbatim, pos_weight imbalance, the sampling->crop->norm pipeline, train-aug-vs-inference; mirror for OHP + add the BBox-trajectory->y_center delta),
    .planning/phases/01-dataset-consolidation-eda/01-DATASET-REPORT.md (the EDA method to replicate for OHP),
    .planning/phases/06-overhead-press/06-CONTEXT.md (the verified OHP numbers — 1582/339/339, Elbows 25.7%/Knees 34.2%, co-occurrence 85/407, pos_weight 2.89/1.92),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the "OHP deliverable notebooks" section — the EDA structure + the results.pkl schema + the color palette),
    backend/training/aqa/datasets/ohp_ssl.py (the finalized _load_trajectory — the pipeline notebook documents the BBox->y_center step)
  </read_first>
  <action>
    Create 05_ohp_eda + 06_ohp_data_pipeline (paired .py + executed .ipynb), mirroring 01_squat_eda + 02_squat_data_pipeline.

    05_ohp_eda: load the local OHP archive (Labels/error_{elbows,knees}.json + Splits/{train,val,test}_keys.json). Report + visualize (from the REAL archive, no fabrication): official splits (1582/339/339); class balance (Elbows 25.7% / Knees 34.2% — a grouped bar, save to docs/figures/ohp_class_balance.png); the co-occurrence (both+ 85, Elbow-only 322, Knees-only 456 — the near-independence, contrast with Squat's 192/232 co-occurrence); clip properties (duration/fps/resolution from a sample of the local clips); sample frames per error (a few illustrative frames — not a redistribution); barbell-trajectory curves (a few OHP y_center trajectories via the finalized _load_trajectory — the V-shape, argMIN at overhead). Use the Squat color palette.

    06_ohp_data_pipeline: document the OHP pipeline (mirror 02_squat). Splits used verbatim (no resampling); imbalance via loss pos_weight (Elbows 2.89x / Knees 1.92x — both >1, the moderate-minority profile vs Squat's rare+majority); 32-frame uniform sampling -> 112 center crop -> Kinetics norm; train-aug (random crop) vs inference (center crop); AND the OHP-specific delta — the BBox trajectory -> y_center=(y1+y2)/2 pipeline (region 0 = barbell, empty-frame interpolation) with a worked example (contrast Squat's flat y-lists). Reads the local archive + the finalized ohp_ssl._load_trajectory.

    Execute both notebooks (jupytext --to notebook --execute, or run cells + save) so the .ipynb is pre-rendered. Save figures BEFORE plt.show() with defensive mkdir.

    Single runnable unit — user pastes back: the executed 05_ohp_eda.ipynb (the class-balance + co-occurrence + sample-frame + trajectory figures rendered) + the executed 06_ohp_data_pipeline.ipynb (the pipeline + the BBox->y_center worked example). Verify the counts/balance match CONTEXT D4 (1582/339/339, 25.7%/34.2%).

    Atomic commit: docs(06): OHP EDA + data-pipeline notebooks (Task 1, D1 / OHP-01).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['docs/notebooks/05_ohp_eda.py','docs/notebooks/06_ohp_data_pipeline.py']]; print('parse ok')" && test -f docs/notebooks/05_ohp_eda.ipynb && test -f docs/notebooks/06_ohp_data_pipeline.ipynb && test -f docs/figures/ohp_class_balance.png</automated>
  </verify>
  <acceptance_criteria>
    - 05_ohp_eda + 06_ohp_data_pipeline exist as paired .py + executed .ipynb (figures rendered on open); mirror the Squat structure + palette
    - EDA numbers match CONTEXT D4 (1582/339/339, Elbows 25.7% / Knees 34.2%, co-occurrence 85/407) — from the REAL local archive, no fabrication
    - The pipeline notebook documents the OHP BBox->y_center delta (a worked example) + the pos_weight imbalance (2.89/1.92)
    - ohp_class_balance.png saved before plt.show()
    - Runnable unit: user pastes back the two executed notebooks (figures rendered)
  </acceptance_criteria>
  <dependencies>Plan 04 (the phase's data work done; the local archive + ohp_ssl exist)</dependencies>
  <done>The OHP EDA + data-pipeline deliverable notebooks exist (mirroring the Squat pack), built from the real local archive; the near-independence + the BBox-trajectory delta are documented for the defense</done>
</task>

<task type="auto">
  <name>Task 2: docs/notebooks/07_ohp_training + 08_ohp_evaluation (.py + executed .ipynb) from results.pkl</name>
  <files>
    docs/notebooks/07_ohp_training.py,
    docs/notebooks/07_ohp_training.ipynb,
    docs/notebooks/08_ohp_evaluation.py,
    docs/notebooks/08_ohp_evaluation.ipynb,
    docs/figures/ohp_training_curves.png,
    docs/figures/ohp_pr_curves.png,
    docs/figures/ohp_confusion_matrices.png,
    docs/figures/ohp_f1_vs_paper.png,
    docs/eval/ohp_test_scores.csv
  </files>
  <read_first>
    docs/notebooks/03_squat_training.py (the training-diagnostics structure — SSL loss + effective-rank collapse, linear-probe vs Kinetics, fine-tune train-vs-val overfit + early-stop, baseline->MD lift; mirror for OHP),
    docs/notebooks/04_squat_evaluation.py (the evaluation structure — test F1, score distributions, PR + confusion, per-seed vs ensemble, TTA ablation, the F1-vs-paper comparison; mirror for OHP),
    .planning/phases/06-overhead-press/figures/results.pkl (Plan 04 — the SOURCE OF TRUTH: ssl_epochs, finetune_seeds, ensemble_test_scores, test_labels, test_clip_ids, test_f1, test_pr_auc, test_confusion, baseline_control, ssl_lift, paper_targets — load + plot, NO recomputation),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§1 — the EXACT paper Table 4 numbers to cross-check: Ours MD 0.4552/0.8452, TemporalXform 0.4138/0.8416, OpenPose-TDM 0.4265/0.7131),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the OHP deliverable notebooks section + the results.pkl schema),
    docs/eval/squat_test_scores.csv (the CSV format to mirror for ohp_test_scores.csv)
  </read_first>
  <action>
    Create 07_ohp_training + 08_ohp_evaluation (paired .py + executed .ipynb), built ONLY from results.pkl (Plan 04). NO recomputation, NO fabrication — load + plot.

    07_ohp_training (mirror 03_squat_training): from results.pkl ssl_epochs — the SSL triplet loss curve + the effective_rank collapse diagnostic (the v2 strong-aug run staying healthy — contrast the Phase-4 v1 weak-aug collapse narrative) + the linear-probe macro-F1 vs frozen-Kinetics (the convergence evidence). From finetune_seeds {42,1337,7} — the per-seed train-vs-val loss (overfit onset, the D6 monitor narrative — OHP both pos_weights >1 so better-balanced) + val-F1 early-stop. The baseline->MD-SSL lift (baseline_control vs ensemble macro from results.pkl). Save docs/figures/ohp_training_curves.png.

    08_ohp_evaluation (mirror 04_squat_evaluation): from results.pkl — the official-test-split F1 per error (Elbows + Knees, ensemble + single-model); score distributions; PR curves (docs/figures/ohp_pr_curves.png) with the operating points; confusion matrices (docs/figures/ohp_confusion_matrices.png) at the val-tuned thresholds; per-seed vs ensemble (the variance evidence); the TTA ablation (the chosen recipe; Phase 4 found TTA reversed — report honestly); THE HEADLINE F1-vs-paper comparison (docs/figures/ohp_f1_vs_paper.png — grouped bars: our baseline control, our MD-SSL ensemble, our ensemble+TTA, paper Ours MD 0.4552/0.8452, paper TemporalXform 0.4138/0.8416; annotate the macro bars; cross-check every number against RESEARCH §1). State the D2 near-independence caveat + the no-paper-Kinetics-baseline framing (the baseline is the only control; the SSL lift = ensemble - baseline). Write docs/eval/ohp_test_scores.csv from results.pkl (clip_id, elbows_score, knees_score, elbows_label, knees_label — mirror squat_test_scores.csv).

    Execute both notebooks so the .ipynb is pre-rendered. Save figures BEFORE plt.show() with defensive mkdir.

    Single runnable unit — user pastes back: the executed 07_ohp_training.ipynb (SSL + fine-tune diagnostic figures) + the executed 08_ohp_evaluation.ipynb (the test F1 + PR + confusion + the headline F1-vs-paper comparison) + the ohp_test_scores.csv. Cross-check the F1 numbers against results.pkl + RESEARCH §1.

    Atomic commit: docs(06): OHP training + evaluation notebooks + F1-vs-paper + per-clip CSV (Task 2, D1 / OHP-01 / RESEARCH §1).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['docs/notebooks/07_ohp_training.py','docs/notebooks/08_ohp_evaluation.py']]; print('parse ok')" && test -f docs/notebooks/07_ohp_training.ipynb && test -f docs/notebooks/08_ohp_evaluation.ipynb && test -f docs/figures/ohp_f1_vs_paper.png && test -f docs/eval/ohp_test_scores.csv && grep -q "0.4552" docs/notebooks/08_ohp_evaluation.py</automated>
  </verify>
  <acceptance_criteria>
    - 07_ohp_training + 08_ohp_evaluation exist as paired .py + executed .ipynb; built ONLY from results.pkl (no recomputation, no fabrication)
    - 08 reports official-test-split F1 per error + PR + confusion + per-seed-vs-ensemble + the F1-vs-paper comparison (baseline -> MD-SSL -> paper Ours MD 0.4552/0.8452); numbers cross-checked against results.pkl + RESEARCH §1
    - The D2 near-independence caveat + the no-paper-Kinetics-baseline framing are stated (honest, no overclaiming)
    - 4 figures saved (training_curves, pr_curves, confusion_matrices, f1_vs_paper) + ohp_test_scores.csv written from results.pkl
    - Runnable unit: user pastes back the two executed notebooks + the CSV; the F1 numbers match results.pkl
  </acceptance_criteria>
  <dependencies>Task 1 (the EDA/pipeline notebooks — the pack ordering); Plan 04 (results.pkl)</dependencies>
  <done>The OHP training + evaluation deliverable notebooks exist (mirroring the Squat pack), built from results.pkl; the headline F1-vs-paper comparison + the per-clip CSV are rendered — OHP-01's F1-per-error-compared-to-published deliverable is visualized</done>
</task>

<task type="auto">
  <name>Task 3: README.md OHP rows + FINDINGS.md OHP section + phase SUMMARY + ROADMAP/REQUIREMENTS/STATE reconciliation</name>
  <files>
    docs/notebooks/README.md,
    docs/eval/FINDINGS.md,
    .planning/phases/06-overhead-press/06-05-SUMMARY.md
  </files>
  <read_first>
    docs/notebooks/README.md (the Squat table + headline + the "same notebook set will be produced for Overhead Press" note — add the OHP rows + headline),
    docs/eval/FINDINGS.md (the Squat section — mirror its structure for an OHP section),
    .planning/phases/06-overhead-press/figures/results.pkl (the headline numbers for the SUMMARY + FINDINGS — load, do not hand-type),
    $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template structure),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D1 — the SC3 descope + the reconciliation action; D2 the caveat; the production-checkpoint declaration),
    .planning/phases/04-squat-motion-disentangling-ssl/04-04-PLAN.md (the Phase-4 SUMMARY structure — the production-checkpoint + handoff pattern to mirror)
  </read_first>
  <action>
    Three closeout deliverables + the reconciliation:

    1. docs/notebooks/README.md: add the 4 OHP rows to the table (05_ohp_eda / 06_ohp_data_pipeline / 07_ohp_training / 08_ohp_evaluation with their one-line covers, mirroring the Squat rows); add an OHP headline-result line (Elbows/Knees/macro F1 vs paper Ours MD 0.4552/0.8452, from results.pkl); update the "Other exercises" note (OHP now done; Barbell Row pending). Numbers from results.pkl.

    2. docs/eval/FINDINGS.md: add an OHP section after the Squat section (mirror its structure): the headline result (Elbows/Knees/macro F1 vs paper Ours MD), the SSL lift over the baseline (ensemble - baseline, from results.pkl), the per-seed variance, the D2 near-independence caveat (the joint head justified by the shared representation + efficiency, NOT co-occurrence), the no-paper-Kinetics-baseline framing (the baseline is the only control), the chosen TTA recipe (+ whether it helped or reversed), and the data-source note (results.pkl + local OHP archive, no fabrication). Numbers from results.pkl, cross-checked against RESEARCH §1.

    3. .planning/phases/06-overhead-press/06-05-SUMMARY.md: the phase SUMMARY per the template — what shipped (the 2 dataset modules + the seam + the staging from Plan 01; the baseline from Plan 02; the SSL backbone from Plan 03; the 3-seed ensemble + eval from Plan 04; the 4-notebook pack from Plan 05); the headline F1 vs paper + the SSL lift; the per-seed variance; the chosen TTA recipe + the actual SSL epoch count; deviations; the D1 DESCOPE note (OHP-01 met by model+eval+viz; ROADMAP SC3 "OHP selectable through the inference API" descoped — frontend cancelled, the backend is a Squat-only fallback); the production-checkpoint declaration (the 3 ohp_md_finetune seeds at phase06/ohp_md_finetune_seed{42,1337,7}/best.pt + the ensemble protocol mean-of-sigmoids + the per-error thresholds + the no-TTA headline); the Phase 7 handoff (the next phase = image-based errors CVCSPC, independent of OHP). Numbers from results.pkl.

    4. Reconcile via gsd-sdk (NOT direct edits): mark OHP-01 addressed by model+eval+viz; record the SC3 descope with the D1 reason in ROADMAP/REQUIREMENTS; update STATE to Phase 6 complete. Use the gsd-sdk query/commit handlers (the orchestrator's convention) — e.g. the same pattern Phase 5 used to flip API-01..04 complete.

    Single runnable unit — user pastes back: the updated README.md (OHP rows + headline), the FINDINGS.md OHP section, the SUMMARY, and the gsd-sdk reconciliation output (OHP-01 + SC3 descope recorded; STATE Phase 6 complete). Verify every number matches results.pkl.

    Atomic commit: docs(06): OHP README + FINDINGS section + phase SUMMARY + ROADMAP/REQUIREMENTS/STATE reconciliation (Task 3, D1).
  </action>
  <verify>
    <automated>grep -qi "Overhead Press\|ohp" docs/eval/FINDINGS.md && grep -q "05_ohp_eda\|ohp" docs/notebooks/README.md && test -f .planning/phases/06-overhead-press/06-05-SUMMARY.md && grep -qi "descope\|SC3\|inference API" .planning/phases/06-overhead-press/06-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md has the 4 OHP rows + the OHP headline (F1 vs paper from results.pkl); FINDINGS.md has an OHP section (headline, SSL lift, per-seed variance, the D2 caveat, the no-paper-Kinetics-baseline framing)
    - The SUMMARY declares the production checkpoint(s) (3 seeds + ensemble protocol + thresholds + no-TTA headline), the headline F1 + SSL lift, deviations, and the D1 SC3-descope note (OHP-01 met by model+eval+viz)
    - Every number in README/FINDINGS/SUMMARY matches results.pkl (no fabrication, no hand-typed drift); the F1-vs-paper cross-checked against RESEARCH §1
    - ROADMAP/REQUIREMENTS/STATE reconciled via gsd-sdk (NOT direct edits): OHP-01 addressed, SC3 descoped with the D1 reason, STATE Phase 6 complete
    - Runnable unit: user pastes back the README + FINDINGS section + SUMMARY + the gsd-sdk reconciliation output
  </acceptance_criteria>
  <dependencies>Task 2 (the evaluation notebook + results.pkl-derived numbers)</dependencies>
  <done>The OHP deliverable pack is complete (README + FINDINGS + the 4 notebooks + figures + CSV); the phase SUMMARY declares the production checkpoint + the headline + the D1 descope; ROADMAP/REQUIREMENTS/STATE reconciled — Phase 6 (OHP-01) closed</done>
</task>

</tasks>

<verification>
## Mapping to Phase 6 ROADMAP Success Criteria + OHP-01

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| The visualization deliverable (CONTEXT D1) | Tasks 1, 2 | The 4-notebook OHP pack + figures + ohp_test_scores.csv, built from results.pkl + the local archive |
| SC2 (the published-comparison deliverable) | Task 2 + Task 3 | 08_ohp_evaluation's F1-vs-paper figure + the FINDINGS OHP section (baseline -> MD-SSL -> paper Ours MD 0.4552/0.8452) |
| OHP-01 closeout + the SC3 descope (D1) | Task 3 | The SUMMARY + the gsd-sdk reconciliation (OHP-01 met by model+eval+viz; SC3 descoped) |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| The 4-notebook deliverable + FINDINGS (manual review) | Tasks 1-3 | manual (the supervisor reviews the pack) | 5 |
| The headline F1-vs-paper deliverable | Task 2 | the f1_vs_paper figure + FINDINGS, from results.pkl | 5 |

(All training sub-requirements OHP-01-a..g are covered by Plans 01-04.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user
- The 4-notebook OHP pack (docs/notebooks/05-08_ohp_*) exists as paired .py + executed .ipynb, mirroring the Squat pack, built ONLY from results.pkl + the local OHP archive — NO fabricated metrics or curves
- 08_ohp_evaluation reports the official-test-split F1 per error + PR + confusion + per-seed-vs-ensemble + the F1-vs-paper comparison (baseline -> MD-SSL -> paper Ours MD 0.4552/0.8452, CITED) — the academic deliverable for OHP-01
- docs/eval/FINDINGS.md has an OHP section (headline, SSL lift, per-seed variance, the D2 near-independence caveat, the no-paper-Kinetics-baseline framing); docs/eval/ohp_test_scores.csv has the per-clip scores
- docs/notebooks/README.md has the OHP rows + headline; 5 OHP figures under docs/figures/
- The phase SUMMARY declares the production checkpoint (3 seeds + ensemble protocol + thresholds + no-TTA headline), the headline F1 + SSL lift, deviations, and the D1 SC3-descope note
- ROADMAP/REQUIREMENTS/STATE reconciled via gsd-sdk (NOT direct edits): OHP-01 met by model+eval+viz; SC3 descoped with the D1 reason; STATE Phase 6 complete
- Every number across README/FINDINGS/SUMMARY/notebooks matches results.pkl + cross-checks against RESEARCH §1 — [[feedback_ai_correctness]] satisfied
- One accurate threat_model: offline deliverable-assembly, no high-severity threats; T-06-18 (fabrication — the source-of-truth discipline), T-06-20 (overclaiming — the honest framing), T-06-SC accept
</success_criteria>

<output>
After Task 3 (the pack + SUMMARY + reconciliation), Phase 6 is COMPLETE — OHP-01 met by the trained/evaluated models + the paper comparison + the visualization pack; the ROADMAP SC3 ("OHP selectable through the inference API") is descoped per D1 (frontend cancelled). The orchestrator advances to Phase 7 (Image-Based Errors / CVCSPC — independent of OHP). This is the closing plan; its SUMMARY is the phase SUMMARY.
</output>

<revision_log>
Phase 6 Plan 05 written 2026-05-27. Wave 5: the 4-notebook OHP deliverable pack (docs/notebooks/05-08_ohp_*) + figures + the FINDINGS.md OHP section + ohp_test_scores.csv + the phase SUMMARY + the ROADMAP/REQUIREMENTS/STATE reconciliation. 3 tasks. NO training, NO new production logic — this plan assembles the academic deliverable from results.pkl (Plan 04) + the local OHP archive, mirroring the existing Squat pack (docs/notebooks/01-04, which the README already anticipates extending to OHP). Mostly local (no GPU/Drive) except sample-clip decoding (the local OHP clips suffice).

D1 closeout: OHP-01 met by model+eval+viz; the ROADMAP SC3 ("OHP selectable through the inference API") is descoped (frontend cancelled, the backend is a Squat-only fallback) — reconciled via gsd-sdk (not direct edits), the same pattern Phase 5 used to flip API-01..04. D2 honest framing carried into FINDINGS + the evaluation notebook: OHP errors near-independent (no co-occurrence joint lift); no paper Kinetics baseline for OHP (the Plan-02 baseline is the only control).

Security: accurate. Offline deliverable-assembly (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-06-18 (fabrication — the integrity risk, mitigated by the results.pkl source-of-truth + the RESEARCH §1 cross-check), T-06-19 accept (no dataset media committed — illustrative sample frames only), T-06-20 (overclaiming — the honest D2 + no-Kinetics-baseline framing), T-06-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
