---
phase: 06-overhead-press
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/training/aqa/datasets/ohp.py
  - backend/training/aqa/datasets/ohp_ssl.py
  - backend/training/aqa/datasets/splits.py
  - backend/training/aqa/harness/supervised_train.py
  - backend/training/aqa/harness/md_finetune.py
  - backend/training/aqa/harness/colab.py
  - backend/training/aqa/datasets/test_ohp.py
  - backend/training/aqa/datasets/test_ohp_ssl.py
autonomous: true
requirements: [OHP-01]
must_haves:
  truths:
    - "splits.index_ohp('train', drive_root=..., videos_root=...) returns 1582 OHPClipRecord rows (val 339, test 339), each with label_elbows/label_knees derived from the non-empty-interval rule against OHP/Labeled_Dataset/Labels/error_elbows.json + error_knees.json — proven against the local OHP archive before any Colab work (D4 verified counts)"
    - "_compute_pos_weight over the 1582 train records returns a [2] tensor approx [2.887, 1.924] within 0.05 (Elbows 25.7% / Knees 34.2% — the moderate-minority profile, distinct from Squat's rare+majority [6.10,0.45]) — proving the train-derived class weighting before the baseline GPU run (D2/D3)"
    - "OHPSSLDataset._load_trajectory parses the OHP BBox JSON (per-frame [region_0,region_1,region_2]; region_0 = barbell; y_center=(y1+y2)/2), writes np.nan for empty region-0 frames, linearly interpolates them, and returns a 1-D float array of length == frame count with no NaNs — the ONE genuinely-new logic, verified on a synthetic BBox JSON before the gated Colab probe finalizes it (D5/RESEARCH §3)"
    - "split_half_cycles with bottom_is_argmax=False (argMIN) on a synthetic V-shaped OHP trajectory (decreasing then increasing y) yields descent/ascent index arrays with descent[-1] <= argmin <= ascent[0] — guarding the OHP half-cycle sign wiring (argMIN, same as Squat for the opposite physical reason: overhead = lowest y pixel) before the SSL GPU burn"
    - "_build_dataloaders(..., dataset_cls=OHPElbowsKneesDataset) and run_md_finetune_epoch(..., dataset_cls=OHPElbowsKneesDataset) select the OHP dataset while the default stays SquatKIEKFEDataset (Phase 3/4 Squat tests still green) — the dataset-injection seam that lets the unchanged Phase-3/4 trainers train OHP (D6/RESEARCH §5)"
    - "python -m pytest backend/training/aqa/ -m 'not slow' -q exits 0 with the new OHP unit tests green AND the existing Phase-3/4 suite still green — the Wave 0 GPU-burn gate is locked behind a green pure-function suite + a no-regression proof"
  artifacts:
    - path: "backend/training/aqa/datasets/ohp.py"
      provides: "OHPElbowsKneesDataset (mirror of SquatKIEKFEDataset; label fields label_elbows/label_knees) + _compute_pos_weight + build_loaders convenience factory. __getitem__ reuses the decode->sample->spatial pipeline from transforms.py unchanged."
      contains: "class OHPElbowsKneesDataset"
    - path: "backend/training/aqa/datasets/ohp_ssl.py"
      provides: "OHPSSLDataset (mirror of SquatSSLDataset) + the OHP-specific BBox _load_trajectory + build_ssl_loader. split_half_cycles copied verbatim from squat_ssl (argMIN sign). _load_trajectory finalized against the real format here; the live traj<->frame alignment is confirmed in Plan 02's gated probe."
      contains: "def _load_trajectory"
    - path: "backend/training/aqa/datasets/splits.py"
      provides: "ADD (additive, no edits to index()/ClipRecord) — OHPClipRecord dataclass + index_ohp() with OHP subdir/filenames/counts (1582/339/339). Reuses _load_json + _label_value verbatim."
      contains: "def index_ohp"
    - path: "backend/training/aqa/harness/supervised_train.py"
      provides: "EDIT — dataset_cls kwarg on _build_dataloaders (default SquatKIEKFEDataset; OHP callers pass OHPElbowsKneesDataset). ~3-line change at lines 187-189 + 222-224; no other edits."
      contains: "dataset_cls"
    - path: "backend/training/aqa/harness/md_finetune.py"
      provides: "EDIT — dataset_cls kwarg on run_md_finetune_epoch, forwarded to _build_dataloaders. ~2-line change at the signature (line 134) + the call site (line 193); no other edits."
      contains: "dataset_cls"
    - path: "backend/training/aqa/harness/colab.py"
      provides: "ADD — stage_ohp_videos (labeled, 2367 mp4) + stage_unlabeled_ohp_videos (5490 unlabeled + bar_trajectories_raw.zip, SPLIT across two release folders -3-001/-3-002). Mirror stage_squat_videos / stage_unlabeled_squat_videos; reuse _copy_with_resume_and_progress + _extract_with_resume_and_progress + zipfile.extractall for JSON."
      contains: "def stage_ohp_videos"
    - path: "backend/training/aqa/datasets/test_ohp.py"
      provides: "Unit tests OHP-01-a (dataset shape + pos_weight) + OHP-01-d (index_ohp counts 1582/339/339 against the local archive, slow/skipif) + the dataset_cls signature-seam test"
      contains: "def test_ohp"
    - path: "backend/training/aqa/datasets/test_ohp_ssl.py"
      provides: "Unit tests OHP-01-b (BBox _load_trajectory: y_center + empty-frame interpolation on synthetic JSON) + OHP-01-c (split_half_cycles argMIN on synthetic V-trajectory)"
      contains: "def test_trajectory_load"
  key_links:
    - from: "backend/training/aqa/datasets/ohp.py"
      to: "backend/training/aqa/datasets/splits.py (index_ohp + OHPClipRecord)"
      via: "import + OHPElbowsKneesDataset.__init__ calls splits.index_ohp"
      pattern: "splits\\.index_ohp"
    - from: "backend/training/aqa/datasets/ohp.py"
      to: "backend/training/aqa/datasets/transforms.py (decode_clip, spatial_train/val, uniform_sample_indices — REUSED UNCHANGED)"
      via: "import"
      pattern: "from backend.training.aqa.datasets.transforms import"
    - from: "backend/training/aqa/harness/md_finetune.py"
      to: "backend/training/aqa/harness/supervised_train._build_dataloaders (dataset_cls forwarded)"
      via: "import + call _build_dataloaders(seed, config, drive_root, videos_root, dataset_cls=dataset_cls)"
      pattern: "_build_dataloaders\\(.*dataset_cls"
    - from: "backend/training/aqa/datasets/ohp_ssl.py"
      to: "backend/training/aqa/datasets/ssl_augs.py (the v2 strong-aug set — REUSED UNCHANGED) + transforms.py"
      via: "import + _augment applies ssl_augs per branch"
      pattern: "from backend.training.aqa.datasets import ssl_augs"
---

<objective>
Land all genuinely-new Phase 6 OHP code as scaffolds with their pure-function cores FULLY IMPLEMENTED and unit-tested, plus the three additive/seam edits (splits.index_ohp, the dataset_cls kwarg on _build_dataloaders + run_md_finetune_epoch, the two colab staging functions), and verify everything LOCALLY ON CPU before any Colab GPU burn. This is the Phase-4 Wave-0 pattern (04-01-PLAN): the only genuinely new logic in this phase is the OHP BBox trajectory parser (`ohp_ssl._load_trajectory` — RESEARCH §3, the CRITICAL delta vs Squat's flat-list loader). It has a unit-testable pure-function core that MUST be verified on synthetic data before the gated Colab probe (Plan 02) finalizes the live traj<->frame alignment and before the 15-26h SSL GPU burn (Plan 03). A wrong BBox extraction or a wrong half-cycle sign would waste days of L4 compute. This plan locks that risk behind a green pytest suite.

Purpose: OHP-01 + the Wave-0 precedent. Everything else in Phase 6 reuses Phase-3/4 modules verbatim (transforms.py, ssl_augs.py, eval/metrics.py, eval/ensemble.py, eval/tta.py, the training logic in supervised_train/md_pretrain/md_finetune). The net-new code is: two new dataset modules (ohp.py mirrors squat.py, ohp_ssl.py mirrors squat_ssl.py with the one BBox-parser delta), the additive splits.index_ohp, the dataset_cls injection seam, and the OHP staging functions. This plan implements + tests those WITHOUT GPU, before the data is even staged.

Output: 5 new + 3 edited .py files under backend/training/aqa/{datasets,harness}/ — 2 new dataset modules, 2 new test files, 3 edited modules (splits.py additive, supervised_train.py + md_finetune.py 3-5 line seams, colab.py additive). The Phase-3/4 Squat suite stays green (backward-compatible defaults).

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]]: each task hands the user exactly one runnable unit (a pytest invocation or a python -c smoke) and stops until output is pasted back. NEVER pre-write task N+1 before task N's paste-back is in. These tasks run LOCALLY (no GPU, no Colab, no Drive) — CPU-only pure-function tests, so paste-back is fast. The OHP-01-d splits-count test reads the local OHP archive (offline inspection only, per P2 precedent — training reads from Drive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-overhead-press/06-CONTEXT.md
@.planning/phases/06-overhead-press/06-RESEARCH.md
@.planning/phases/06-overhead-press/06-PATTERNS.md
@.planning/phases/06-overhead-press/06-VALIDATION.md
@backend/training/aqa/datasets/squat.py
@backend/training/aqa/datasets/squat_ssl.py
@backend/training/aqa/datasets/splits.py
@backend/training/aqa/datasets/transforms.py
@backend/training/aqa/harness/supervised_train.py
@backend/training/aqa/harness/md_finetune.py
@backend/training/aqa/harness/colab.py
@CLAUDE.md

<interfaces>
Contracts this plan creates that Plans 02/03/04 consume. The executor implements these EXACT signatures — no codebase scavenger hunt. Full code excerpts live in 06-PATTERNS.md (cited per task in read_first); do NOT re-derive them. All new modules use `logger = logging.getLogger("aqa.phase06")` and start with `from __future__ import annotations` (CLAUDE.md backend convention).

datasets/splits.py (ADD — mirror index()/ClipRecord; do NOT edit the Squat ones):
- @dataclass(frozen=True) OHPClipRecord — fields clip_id: str, video_path: str, label_elbows: int, label_knees: int.
- index_ohp(split: Literal["train","val","test"], *, drive_root: str, videos_root: str) -> list[OHPClipRecord] — mirrors index() exactly; subdir constants _OHP_LABELS_SUBDIR="Fitness-AQA_dataset_release/OHP/Labeled_Dataset/Labels", _OHP_SPLITS_SUBDIR="Fitness-AQA_dataset_release/OHP/Labeled_Dataset/Splits", filenames error_elbows.json / error_knees.json, _OHP_EXPECTED_COUNTS={train:1582,val:339,test:339}. Reuses _load_json + _label_value verbatim.

datasets/ohp.py (mirror squat.py):
- _compute_pos_weight(train_records: list[splits.OHPClipRecord]) -> torch.Tensor — same formula (N-pos)/max(pos,1); returns [2] = [w_elbows, w_knees] approx [2.887, 1.924].
- class OHPElbowsKneesDataset(Dataset) — same __init__ signature as SquatKIEKFEDataset (split, *, drive_root, videos_root, num_frames=32, crop_size=112, train_aug=True, train_jitter_frames=2, seed=42); __getitem__ returns (clip[3,32,112,112] float32, labels[2] float32) where labels=[label_elbows, label_knees]; pos_weight always from TRAIN. Calls splits.index_ohp (NOT splits.index).
- build_loaders(...) — convenience/notebook-smoke factory only (clamps num_workers=0; trainers use _build_dataloaders directly). Mirror squat.build_loaders.

datasets/ohp_ssl.py (mirror squat_ssl.py; ONE delta = _load_trajectory):
- split_half_cycles(...) — COPY VERBATIM from squat_ssl.py (same argMIN sign param). RESEARCH §3 confirms OHP also uses bottom_is_argmax=False.
- class OHPSSLDataset(Dataset) — same __init__ as SquatSSLDataset BUT trajectories are in a FLAT dir: use Path(trajectories_root).glob("*.json") NOT rglob (PATTERNS — OHP trajectory zip extracts flat, unlike Squat's nested). Keep the stem->Path map + the vid_stems & traj_stems intersection.
- _load_trajectory(clip_id) -> np.ndarray — THE NEW LOGIC (RESEARCH §3 / PATTERNS): load the BBox JSON (list of frames; each frame = [region_0, region_1, region_2]; region_0 = list of 0-or-1 [x1,y1,x2,y2,conf]); y_center=(y1+y2)/2 from region_0[0]; empty region_0 -> np.nan; interpolate NaNs; return 1-D float64 length==n_frames. Defensive: frame[0] if len(frame)>0 else [] (A6).
- __getitem__ -> dict {anchor, positive, negative} — copy squat_ssl's body; calls split_half_cycles(..., bottom_is_argmax=False).
- build_ssl_loader(dataset, config, *, seed=42) -> DataLoader — copy verbatim; persistent_workers=(num_workers>0).

harness/supervised_train.py (EDIT — D6/RESEARCH §5):
- _build_dataloaders(seed, config, drive_root, videos_root, *, dataset_cls=SquatKIEKFEDataset) -> dict[str, DataLoader] — ADD the keyword-only dataset_cls (default unchanged); swap the 3 hardcoded SquatKIEKFEDataset(...) constructions (lines 222-224) for dataset_cls(...). No other change; the SquatKIEKFEDataset import stays (it is the default).

harness/md_finetune.py (EDIT — D6/RESEARCH §5):
- run_md_finetune_epoch(*, ..., dataset_cls=SquatKIEKFEDataset) -> dict — ADD the keyword-only dataset_cls to the signature (after max_epochs, line ~143); forward it at the _build_dataloaders call (line 193): _build_dataloaders(seed, config, drive_root, videos_root, dataset_cls=dataset_cls). No other change.

harness/colab.py (ADD — D4/D6/RESEARCH §5/Pitfall 4):
- stage_ohp_videos(drive_root, *, local_root="/content/ohp_videos", expect_count=2367) -> str — mirror stage_squat_videos; src zip = {drive_root}/Fitness-AQA_dataset_release/OHP/Labeled_Dataset/videos.zip. Reuse _copy_with_resume_and_progress + _extract_with_resume_and_progress. NOTE: labeled OHP videos.zip holds 2367 mp4 (2260 official + 107 extra — like Squat's 1739-vs-1623); the count check uses 2367, the split files are the id source of truth.
- stage_unlabeled_ohp_videos(drive_root_3001, drive_root_3002, *, local_videos_root="/content/ohp_unlabeled_videos", local_traj_root="/content/ohp_trajectories", expect_count=5490) -> tuple[str,str] — mirror stage_unlabeled_squat_videos BUT TWO drive roots (Pitfall 4): videos.zip in {drive_root_3002}/.../OHP/Unlabeled_Dataset/, bar_trajectories_raw.zip in {drive_root_3001}/.../OHP/Unlabeled_Dataset/. Videos: byte-resume copy + per-member mp4 extract. Trajectories: byte-resume copy + zipfile.extractall (JSON members, flat layout — NOT _extract_with_resume_and_progress which is mp4-only).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

Locked from CONTEXT.md (D2/D4/D6) + RESEARCH §3/§5 + PATTERNS. The decisive RESEARCH delta: OHP trajectories are per-frame BBox arrays, NOT Squat's flat y-lists (RESEARCH §3, CONFIRMED by ReadMe.md.docx + 20-file inspection) — `_load_trajectory` must compute y_center=(y1+y2)/2 from region 0, NOT `np.asarray(json.load(...))`.

| # | Decision | Source |
|---|----------|--------|
| D2 | OHPElbowsKneesDataset = joint multi-label, label fields label_elbows/label_knees, pos_weight train-derived approx [2.887, 1.924]. NOT a co-occurrence-driven joint lift (OHP errors near-independent: only 85/407 Elbow+ are Knees+) — the joint head is justified by shared MD-SSL representation + efficiency. BCEWithLogitsLoss handles independent multi-label correctly. | CONTEXT D2 + RESEARCH §4 |
| D4 | OHP data contract VERIFIED: labeled 2260 (train 1582 / val 339 / test 339); labels Labels/error_{elbows,knees}.json (non-empty interval => 1); train balance Elbows 25.7% / Knees 34.2%; labeled videos.zip 2367 mp4; unlabeled 5490 + 5490 trajectory JSONs; OHP split across release folders -3-001 (labeled + traj zip) / -3-002 (unlabeled videos). | CONTEXT D4 + RESEARCH §3/§5 |
| D5-format | _load_trajectory parses OHP BBox JSON: region 0 = barbell (widest bbox), y_center=(y1+y2)/2 per ReadMe formula; ~40% of files have empty region-0 frames -> np.nan -> linear interpolate (Squat's np.isnan path REUSED; only the extraction differs). The 1:1 traj<->frame alignment + the argMIN sign are CONFIRMED by Plan 02's gated Colab probe before finalization; this plan implements against the offline-confirmed structure (RESEARCH §3 inspected 20 files). | CONTEXT D5 + RESEARCH §3 |
| D6-factoring | splits: ADD index_ohp + OHPClipRecord (additive, no edits to the Squat index()/ClipRecord — backward-safe). dataloaders: dataset_cls kwarg on _build_dataloaders (default SquatKIEKFEDataset) forwarded from run_md_finetune_epoch (the cleanest factoring per RESEARCH §5 — backward-compatible). staging: NEW stage_ohp_videos + stage_unlabeled_ohp_videos (additive). transforms/ssl_augs/eval/md_pretrain/the trainer epoch loops REUSED UNCHANGED. | CONTEXT D6 + RESEARCH §5 |
| D6-reuse | split_half_cycles COPIED VERBATIM into ohp_ssl (argMIN, bottom_is_argmax=False — same as Squat; RESEARCH §3 confirms via 11681_3.json argMIN at frame 47 mid-rep). ssl_augs.py (v2 strong set) reused unchanged — NO weak-vs-strong ablation re-run (Phase 4 settled it). | CONTEXT D3/D6 + RESEARCH §4 |
| D7-landmines | persistent_workers=(num_workers>0) on every OHP DataLoader-constructing helper (ohp.build_loaders, build_ssl_loader); from __future__ import annotations first line; logger aqa.phase06; trajectory staging uses zipfile.extractall (JSON), NOT the mp4-only extractor (Pitfall 4/§6). | CONTEXT D7 + RESEARCH §6 |

</decisions>

<determinism_checklist>
## Determinism / hygiene preconditions (carry Phase 2/3/4 + D7 landmines)

1. logger = logging.getLogger("aqa.phase06") in every new module (PATTERNS Shared Patterns — phase06, NOT phase04).
2. from __future__ import annotations as the first import in every new file (CLAUDE.md; every existing backend/training/aqa/ file has it).
3. persistent_workers = config.num_workers > 0 guard in build_ssl_loader (ohp.build_loaders keeps the num_workers=0 convenience clamp — it is a smoke factory, not on the training path). The trainers' _build_dataloaders already wires persistent_workers (unchanged).
4. OHP trajectories are a FLAT directory: glob("*.json"), NOT rglob — opposite of Squat (whose extractall nested the JSONs, forcing rglob). PATTERNS verified the OHP zip extracts flat (bar_trajectories_raw/{clip_id}.json one level).
5. _load_trajectory empty-detection sentinel is an EMPTY LIST [] -> np.nan (NOT null/NaN in-file — OHP bboxes are integers; "missing" = empty region list). The Squat np.isnan() interpolation is reused on the post-extraction y array.
6. dataset_cls default = SquatKIEKFEDataset so the Phase 3/4 Squat call sites (supervised_train, md_finetune's existing Squat callers) are byte-for-byte unaffected. Verified by the no-regression pytest run.
7. test files: torch = pytest.importorskip("torch") at module top (PATTERNS — they need torch); the index_ohp count test is @pytest.mark.slow or skipif on a missing local archive (reads real files).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training + evaluation pipeline (D1 descopes serving/API entirely): NO network surface, NO authentication, NO untrusted user input at any point, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. There are NO high-severity threats — injection/XSS/authz do not apply to a CPU-only dataset-module test suite. The only genuine constraints are dataset-license compliance and not committing large data/weight artifacts. Per the planning brief: state this honestly rather than inventing web-app threats.

| Boundary | Description |
|----------|-------------|
| OHP BBox JSON -> y_center array | _load_trajectory is the ONE new parser. A wrong region index, wrong y_center formula, or mishandled empty frame silently produces meaningless half-cycle splits across the 15-26h SSL run. Caught by the synthetic-JSON unit test (this plan) + the gated Colab probe (Plan 02). |
| Local OHP archive (research-use-only) -> code | Reading the local Fitness-AQA OHP archive for the index_ohp count test (offline inspection only). License: non-commercial, do not redistribute. No dataset media committed (gitignored / Drive-staged). |
| dataset_cls default -> Phase 3/4 Squat behavior | The dataset_cls=SquatKIEKFEDataset default must keep the existing Squat trainers byte-for-byte unchanged. A wrong default silently breaks Squat. Caught by the no-regression pytest run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | ohp_ssl._load_trajectory BBox parser (the one new logic) — wrong region/formula/empty-frame handling produces meaningless triplets across the SSL burn | mitigate | Task 3's test_trajectory_load asserts y_center=(y1+y2)/2 on a synthetic 5-frame BBox JSON with 2 empty region-0 frames, verifies interpolation lands between neighbors + no residual NaN (RESEARCH §3 / Pitfall 1+3). The live traj<->frame alignment is Plan 02's gated checkpoint:human-verify probe BEFORE the GPU burn. MEDIUM-cost-if-wrong, caught at the cheapest point (a CPU unit test). |
| T-06-02 | Tampering | split_half_cycles argMIN sign for OHP — wrong sign (argMAX) lands the split at the standing endpoint, collapsing one half-cycle to 0-1 frames | mitigate | Task 4's test_split_half_cycles_ohp asserts descent[-1] <= argmin <= ascent[0] on a synthetic V-trajectory with bottom_is_argmax=False (RESEARCH §3 / Pitfall 2). The real-data sign is re-confirmed visually in Plan 02's probe. LOW — the function is copied verbatim from the Squat-proven splitter; the test guards the OHP wiring. |
| T-06-03 | Integrity | dataset_cls default change silently breaks the Phase 3/4 Squat trainers | mitigate | dataset_cls defaults to SquatKIEKFEDataset (keyword-only, backward-compatible); the existing Squat call sites pass no dataset_cls and are unaffected. Task 5's verify runs the FULL existing suite (-m "not slow") and asserts it stays green. LOW — a default-value contract, regression-tested. |
| T-06-04 | Integrity | Committing dataset media / weights (5.8 GB Fitness-AQA, extracted videos) — license + repo-bloat | accept | All dataset media + weights are gitignored / Drive-staged (Phase 1-5 contract). This plan commits only .py source + tests; the index_ohp test reads (does not copy) the local archive. Stage files by name only. LOW — the gitignore contract is established; no media touched. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs this phase (RESEARCH Package Legitimacy Audit: "(none added)"; all libs Colab-provided or in backend/requirements.txt). No [ASSUMED]/[SUS] packages exist; the legitimacy checkpoint is not applicable. |

All threats MEDIUM at worst; none block execution. T-06-01 is the one to catch — its CPU unit test is the whole point of Wave 0 (verify the new parser before the GPU burn). No web-app threats apply (offline pipeline).

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add splits.index_ohp + OHPClipRecord (additive) + OHP-01-d count test</name>
  <files>
    backend/training/aqa/datasets/splits.py,
    backend/training/aqa/datasets/test_ohp.py
  </files>
  <read_first>
    backend/training/aqa/datasets/splits.py (the FULL Squat index() lines 71-144 + ClipRecord lines 29-43 + the reused _load_json line 46 + _label_value line 52 — index_ohp mirrors index() exactly; do NOT edit any existing code),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the "datasets/splits.py — MODIFY" section — the exact OHPClipRecord dataclass, the _OHP_* constants, and the full index_ohp() body to copy),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§5 item 1 — the Squat-hardcoded constants + the OHP variant; D4 verified counts 1582/339/339),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D4 — label paths Labels/error_{elbows,knees}.json, non-empty interval => positive, same id format as Squat)
  </read_first>
  <action>
    ADD to splits.py (additive — do NOT touch the existing index()/ClipRecord/expected_counts/__main__): a clearly-separated OHP section after the Squat code (e.g. after expected_counts(), before __main__). Per PATTERNS:

    1. New constants: _OHP_EXPECTED_COUNTS={"train":1582,"val":339,"test":339}; _OHP_LABELS_SUBDIR="Fitness-AQA_dataset_release/OHP/Labeled_Dataset/Labels"; _OHP_SPLITS_SUBDIR="Fitness-AQA_dataset_release/OHP/Labeled_Dataset/Splits"; _OHP_LABEL_ELBOWS_FILENAME="error_elbows.json"; _OHP_LABEL_KNEES_FILENAME="error_knees.json".
    2. @dataclass(frozen=True) OHPClipRecord with fields clip_id: str, video_path: str, label_elbows: int, label_knees: int (mirror ClipRecord; docstring per PATTERNS).
    3. index_ohp(split, *, drive_root, videos_root) -> list[OHPClipRecord] — copy the index() body verbatim, swapping: the constants (_OHP_*), the dataclass (OHPClipRecord), the label fields (label_elbows/label_knees), and the two label filenames. REUSE _load_json + _label_value directly (they are generic — call them, do not copy). Keep the count-drift logger.warning (compares against _OHP_EXPECTED_COUNTS). The interval->binary logic is identical to Squat.

    Create test_ohp.py with the OHP-01-d count test FIRST (this task's deliverable): test_ohp_splits_count — call splits.index_ohp for each split against the LOCAL OHP archive (the offline path under Fitness-AQA/...release...-3-001/.../OHP). Mark it @pytest.mark.slow OR @pytest.mark.skipif(not <local archive path>.exists(), reason="local OHP archive required"). Assert len == 1582/339/339; assert each record is an OHPClipRecord with int label_elbows/label_knees in {0,1}; assert the train Elbows+ count is ~407 and Knees+ ~541 (the D4-verified balance). Module top: from __future__ import annotations; import pytest; torch = pytest.importorskip("torch") (consistency with the sibling test, even though splits needs no torch). Resolve the local OHP drive-root path from the canonical_refs path (Fitness-AQA/Fitness-AQA_dataset_release-20260518T073812Z-3-001/Fitness-AQA_dataset_release) — note index_ohp prepends "Fitness-AQA_dataset_release/OHP/..." so drive_root is the folder ABOVE that.

    Single runnable unit — user pastes back: (1) python -c AST-parse of splits.py prints "parse ok"; (2) python -m pytest backend/training/aqa/datasets/test_ohp.py::test_ohp_splits_count -x -v (passes against the local archive, or skips cleanly if the path differs — then the user supplies the correct local OHP root and re-runs).

    Atomic commit: feat(06): add splits.index_ohp + OHPClipRecord + OHP-01-d count test (Task 1, D4/D6).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/datasets/splits.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_ohp.py::test_ohp_splits_count -x -v</automated>
  </verify>
  <acceptance_criteria>
    - splits.py parses; index_ohp + OHPClipRecord added; the existing index()/ClipRecord/expected_counts UNCHANGED (git diff is purely additive)
    - index_ohp('train') returns 1582 OHPClipRecord rows, val/test 339/339 (against the local archive); train Elbows+ ~407, Knees+ ~541
    - _load_json + _label_value are CALLED (not re-copied); the interval->binary rule matches Squat
    - test_ohp_splits_count passes (or skips cleanly with a clear reason if the local archive path differs)
    - Runnable unit: user pastes back the AST-parse + the count test passing
  </acceptance_criteria>
  <dependencies>None — Wave 0 entry task</dependencies>
  <done>The official OHP split loader exists + reconciles to the D4-verified 1582/339/339; the Squat splits are untouched; ohp.py has a split source to consume</done>
</task>

<task type="auto">
  <name>Task 2: Implement datasets/ohp.py (OHPElbowsKneesDataset + pos_weight) + OHP-01-a test</name>
  <files>
    backend/training/aqa/datasets/ohp.py,
    backend/training/aqa/datasets/test_ohp.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat.py (the FULL file, 204 lines — the template: imports, module docstring, _compute_pos_weight lines 39-56, SquatKIEKFEDataset lines 59-156, build_loaders lines 159-203),
    backend/training/aqa/datasets/splits.py (index_ohp + OHPClipRecord from Task 1),
    backend/training/aqa/datasets/transforms.py (decode_clip, spatial_train, spatial_val, uniform_sample_indices — REUSED UNCHANGED; the output-shape contract),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the "datasets/ohp.py — NEW" section — the exact imports block, _compute_pos_weight rename, the OHPElbowsKneesDataset skeleton, the build_loaders note + the Shared module-docstring/logger/persistent_workers patterns),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D2 — joint multi-label, label fields, the pos_weight ~2.887/1.924 + the near-independence caveat)
  </read_first>
  <action>
    Create ohp.py as a direct mirror of squat.py (PATTERNS "datasets/ohp.py" section). Changes vs squat.py: (1) module docstring documents OHP numbers (Elbows+ 25.7% w~2.887, Knees+ 34.2% w~1.924; joint head justified by shared MD-SSL representation + efficiency, NOT co-occurrence — errors largely independent, D2); (2) logger = logging.getLogger("aqa.phase06"); (3) _compute_pos_weight takes list[splits.OHPClipRecord], sums r.label_elbows / r.label_knees, returns torch.tensor([w_elbows, w_knees]) — same (N-pos)/max(pos,1) formula; (4) class OHPElbowsKneesDataset(Dataset) with the IDENTICAL __init__ signature as SquatKIEKFEDataset, but self.records = splits.index_ohp(...) (NOT splits.index), and pos_weight always from the train split via index_ohp("train", ...); (5) __getitem__ identical decode->sample->spatial body, label = torch.tensor([rec.label_elbows, rec.label_knees], dtype=torch.float32); (6) build_loaders mirrors squat.build_loaders (rename the class; the num_workers=0 clamp comment becomes "convenience/notebook-smoke factory only; trainers use _build_dataloaders directly with num_workers>0"). REUSE decode_clip/spatial_train/spatial_val/uniform_sample_indices from transforms.py unchanged.

    In test_ohp.py, add test_ohp_dataset_shape + test_ohp_pos_weight (OHP-01-a). For the shape test, build a tmp_path fixture with 2 tiny synthetic mp4s + mock split/label JSONs (a Splits/ dir with {train,val,test}_keys.json each listing the 2 clip ids, a Labels/ dir with error_elbows.json + error_knees.json mapping ids to interval lists). Write the synthetic mp4s via torchvision.io.write_video of a small random uint8 [T,H,W,3] tensor. Instantiate OHPElbowsKneesDataset(split="train", drive_root=tmp_root, videos_root=<mp4 dir>, num_frames=8, crop_size=112); assert ds[0] returns (clip with shape (3,8,112,112) float32, labels shape (2,) float32); assert ds.pos_weight.shape == (2,). For test_ohp_pos_weight, build a synthetic train record list with a known positive count (e.g. 1 of 4 Elbows+, 2 of 4 Knees+) and assert _compute_pos_weight returns the hand-computed [3.0, 1.0] within 1e-6 (proving the formula; the real 2.887/1.924 is asserted in Task 1's archive test). Mark the mp4-decoding shape test @pytest.mark.slow if write_video/decode is slow; keep test_ohp_pos_weight fast (no I/O).

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/datasets/test_ohp.py -k "dataset or pos_weight" -x -v (both pass; the shape test may need torchvision's video writer — skip cleanly if the codec is unavailable, but pos_weight must pass).

    Atomic commit: feat(06): implement ohp.py OHPElbowsKneesDataset + pos_weight + OHP-01-a test (Task 2, D2/D3).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/datasets/ohp.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_ohp.py -k "dataset or pos_weight" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - ohp.py parses; defines class OHPElbowsKneesDataset + _compute_pos_weight + build_loaders; logger is aqa.phase06; from __future__ import annotations first line
    - OHPElbowsKneesDataset.__init__ calls splits.index_ohp (NOT splits.index); __getitem__ returns (clip[3,T,112,112] float32, labels[2] float32) with labels=[label_elbows,label_knees]
    - _compute_pos_weight returns the hand-computed [3.0,1.0] on the synthetic 1/4-Elbows 2/4-Knees record list (formula proven)
    - decode_clip/spatial_*/uniform_sample_indices imported from transforms.py UNCHANGED (no copy)
    - Runnable unit: user pastes back the dataset + pos_weight tests passing
  </acceptance_criteria>
  <dependencies>Task 1 (index_ohp + OHPClipRecord)</dependencies>
  <done>OHPElbowsKneesDataset mirrors the Squat dataset with elbows/knees labels + train-derived pos_weight; the labeled OHP pipeline the baseline + fine-tune consume exists + is shape-proven</done>
</task>

<task type="auto">
  <name>Task 3: Implement ohp_ssl.py BBox _load_trajectory + OHP-01-b test (the one new logic)</name>
  <files>
    backend/training/aqa/datasets/ohp_ssl.py,
    backend/training/aqa/datasets/test_ohp_ssl.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat_ssl.py (the FULL file, 269 lines — the template: imports, split_half_cycles lines 51-96 COPY VERBATIM, SquatSSLDataset, _load_trajectory lines 146-164 which is the Squat flat-list version to REPLACE, _augment, __getitem__, build_ssl_loader),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§3 — the FULL OHP trajectory format: the ReadMe formula y_center=(y1+y2)/2, the per-frame [region_0,region_1,region_2] structure, region 0 = barbell, ~40% empty-region-0 frames, the recommended _load_trajectory implementation; Pitfalls 1+2+3),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the "datasets/ohp_ssl.py — NEW" section — the imports, the FLAT-dir glob (NOT rglob), the full _load_trajectory BBox parser to copy, the _augment/__getitem__ verbatim-copy note, the test_trajectory_load example),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D5 — gated probe before SSL; D6 — split_half_cycles verbatim, argMIN)
  </read_first>
  <action>
    Create ohp_ssl.py as a mirror of squat_ssl.py with the ONE delta = _load_trajectory (PATTERNS "datasets/ohp_ssl.py" section). Per PATTERNS:

    1. Imports = squat_ssl.py verbatim; logger = logging.getLogger("aqa.phase06"); module docstring documents the OHP BBox format + the gated-probe note (the live 1:1 traj<->frame alignment is confirmed in Plan 02 before the SSL burn).
    2. split_half_cycles — COPY VERBATIM from squat_ssl.py (lines 51-96; same argMIN sign param). RESEARCH §3 confirms OHP also uses bottom_is_argmax=False (overhead = lowest y pixel).
    3. class OHPSSLDataset(Dataset).__init__ = SquatSSLDataset.__init__ verbatim EXCEPT the trajectory glob: self._traj_paths = {p.stem: p for p in Path(trajectories_root).glob("*.json")} — FLAT dir, glob NOT rglob (PATTERNS: OHP zip extracts flat; Squat's nested forced rglob). Keep vid_stems & set(self._traj_paths) intersection + the logger.info.
    4. _load_trajectory(clip_id) -> np.ndarray — REPLACE the Squat flat-list body ENTIRELY with the BBox parser (RESEARCH §3 / PATTERNS code): open the JSON (list of frames); for each frame, region_0 = frame[0] if len(frame)>0 else [] (defensive, A6); if region_0: x1,y1,x2,y2,conf = region_0[0]; y[i]=(y1+y2)/2.0; else y[i]=np.nan; then the SAME np.isnan interpolation as Squat (valid<2 -> ValueError; np.interp). Return 1-D float length==n_frames. Inline-cite the ReadMe formula + RESEARCH §3 + Pitfall 1/3.
    5. _augment + __getitem__ + seed_worker + build_ssl_loader — copy verbatim from squat_ssl.py (rename SquatSSLDataset->OHPSSLDataset in annotations). __getitem__ already calls split_half_cycles(..., bottom_is_argmax=False). build_ssl_loader keeps persistent_workers=(num_workers>0).

    Create test_ohp_ssl.py with test_trajectory_load (OHP-01-b) per PATTERNS: build a synthetic 5-frame BBox JSON (region_0 present on frames 0/2/4 with bboxes [10,100,200,200,90]/[10,60,200,160,88]/[10,140,200,260,91], empty on 1/3); write to tmp_path; ds = OHPSSLDataset.__new__(OHPSSLDataset); ds._traj_paths = {"00001_0": traj_path}; y = ds._load_trajectory("00001_0"); assert y.shape==(5,); assert not np.isnan(y).any(); assert y[0]==pytest.approx(150.0), y[2]==pytest.approx(110.0), y[4]==pytest.approx(200.0) (the (y1+y2)/2 values); assert the interpolated frames 1,3 lie between their neighbors. Module top: from __future__ import annotations; import json; import numpy as np; import pytest; torch = pytest.importorskip("torch"). This is the T-06-01 mitigation — the one-new-logic verified on synthetic data before the gated probe + GPU burn.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py::test_trajectory_load -x -v (passes; pure CPU, sub-second).

    Atomic commit: feat(06): implement ohp_ssl.py BBox _load_trajectory + OHP-01-b test (Task 3, D5 / RESEARCH §3).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/datasets/ohp_ssl.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py::test_trajectory_load -x -v</automated>
  </verify>
  <acceptance_criteria>
    - ohp_ssl.py parses; defines OHPSSLDataset + the BBox _load_trajectory + build_ssl_loader; split_half_cycles copied verbatim; logger aqa.phase06
    - _load_trajectory extracts y_center=(y1+y2)/2 from region 0, writes np.nan for empty region-0 frames, interpolates them; returns 1-D float length==n_frames with no residual NaN
    - test_trajectory_load passes: y[0]==150, y[2]==110, y[4]==200; interpolated frames 1,3 between neighbors; no NaN
    - The trajectory glob is glob("*.json") (FLAT), NOT rglob (the OHP delta vs Squat); __getitem__ calls split_half_cycles(bottom_is_argmax=False)
    - Runnable unit: user pastes back test_trajectory_load passing (1 passed)
  </acceptance_criteria>
  <dependencies>Task 1 (the splits module is imported by the SSL test's sibling); structurally independent of Task 2</dependencies>
  <done>The ONE genuinely-new logic (OHP BBox trajectory parser) is implemented + proven on synthetic data; the SSL dataset triplet pipeline is wired; the live format/alignment is gated to Plan 02's probe before the GPU burn</done>
</task>

<task type="auto">
  <name>Task 4: split_half_cycles argMIN guard (OHP-01-c) — synthetic V-trajectory test</name>
  <files>backend/training/aqa/datasets/test_ohp_ssl.py</files>
  <read_first>
    backend/training/aqa/datasets/ohp_ssl.py (the split_half_cycles copied in Task 3 — the argMIN sign + the descent[-1] <= bottom <= ascent[0] contract),
    backend/training/aqa/datasets/transforms.py (uniform_sample_indices — split_half_cycles calls it; the return type),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§3 — the argMIN inference: overhead = barbell at top of image = lowest y pixel = argMIN, the 11681_3.json mid-rep example; Pitfall 2 — argMAX collapses a half-cycle),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the test_split_half_cycles_ohp example — the V-shape synthetic trajectory)
  </read_first>
  <action>
    Add test_split_half_cycles_ohp (OHP-01-c) to test_ohp_ssl.py. Build a synthetic V-shaped OHP trajectory: traj = (np.linspace(-1,1,80)**2) * 100 + 50 is a parabola with the MINIMUM at the middle (index ~40) — this models the OHP barbell y_center: starts high (shoulder), decreases to the overhead minimum mid-rep, increases back. Call descent_idx, ascent_idx = split_half_cycles(traj, frames_per_half=16, bottom_is_argmax=False). Assert: both arrays length 16; argmin = int(np.argmin(scipy.ndimage.gaussian_filter1d(traj, 2.0))) is in the middle (not 0 or 79); descent_idx[-1] <= argmin <= ascent_idx[0] (the half-cycle contract); descent_idx[0] near 0, ascent_idx[-1] near 79; indices sorted + within [0,80). ALSO assert that calling with bottom_is_argmax=True (argMAX) on this V-trajectory moves the detected split to an endpoint (argmax of a V is at index 0 or 79), degenerating one half-cycle — proving the argMIN sign is the correct OHP choice and the sign param is wired (Pitfall 2 / T-06-02). This is the same logic as Squat (argMIN) but the test documents the OHP-specific physical reasoning (overhead = lowest y pixel) inline.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py::test_split_half_cycles_ohp -x -v (passes; pure CPU, sub-second).

    Atomic commit: test(06): split_half_cycles argMIN guard for OHP (Task 4, D6 / RESEARCH §3 / Pitfall 2).
  </action>
  <verify>
    <automated>python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py::test_split_half_cycles_ohp -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test_split_half_cycles_ohp passes; descent[-1] <= argmin <= ascent[0] on the synthetic V-trajectory with bottom_is_argmax=False
    - argMIN lands mid-trajectory (not an endpoint); argMAX on the same V degenerates a half-cycle (the sign param is wired + argMIN is the correct OHP choice)
    - Both index arrays length 16, sorted, within [0,80)
    - Runnable unit: user pastes back the pytest output ending with 1 passed
  </acceptance_criteria>
  <dependencies>Task 3 (split_half_cycles copied into ohp_ssl.py)</dependencies>
  <done>The OHP half-cycle sign (argMIN) is regression-guarded on synthetic data before the SSL burn; the descent/ascent contract holds; Plan 02's probe re-confirms it visually on real clips</done>
</task>

<task type="auto">
  <name>Task 5: dataset_cls seam on _build_dataloaders + run_md_finetune_epoch + colab OHP staging + no-regression gate</name>
  <files>
    backend/training/aqa/harness/supervised_train.py,
    backend/training/aqa/harness/md_finetune.py,
    backend/training/aqa/harness/colab.py,
    backend/training/aqa/datasets/test_ohp.py
  </files>
  <read_first>
    backend/training/aqa/harness/supervised_train.py (lines 187-189 the _build_dataloaders signature + 222-224 the SquatKIEKFEDataset constructions — the EXACT edit sites; the SquatKIEKFEDataset import at the top stays as the default),
    backend/training/aqa/harness/md_finetune.py (the run_md_finetune_epoch signature at line 134 + the _build_dataloaders call at line 193 — the EXACT edit sites; the import block lines 37-55; the existing _d6_should_abort at line ~118 uses val/train ratio — do NOT touch it),
    backend/training/aqa/harness/colab.py (stage_squat_videos lines 317-443 + stage_unlabeled_squat_videos lines 449-538 — the staging templates; _copy_with_resume_and_progress line 93 + _extract_with_resume_and_progress line 168 + the _SQUAT_*_EXPECT_COUNT constants to mirror),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the "supervised_train.py", "md_finetune.py", and "colab.py" MODIFY sections — the exact 3-5 line edits + the two stage_ohp signatures + the two-drive-root note),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§5 items 2/3/4 — the dataset_cls factoring + the two-drive-root staging + Pitfall 4/5)
  </read_first>
  <action>
    Three small additive/seam edits + the OHP staging functions + the no-regression test. Per PATTERNS:

    1. supervised_train._build_dataloaders (lines 187-189): ADD a keyword-only param `*, dataset_cls=SquatKIEKFEDataset` to the signature. At lines 222-224, replace the 3 hardcoded SquatKIEKFEDataset(...) with dataset_cls(...). NO other change — common_kwargs, the generator, the persistent_workers guard, the 3 DataLoaders are all unchanged. The SquatKIEKFEDataset import stays (it is the default value).

    2. md_finetune.run_md_finetune_epoch (line 134): ADD `dataset_cls=SquatKIEKFEDataset` as a keyword-only arg after max_epochs. At the _build_dataloaders call (line 193): change to `loaders = _build_dataloaders(seed, config, drive_root, videos_root, dataset_cls=dataset_cls)`. NO other change. Leave the D6 monitor (_d6_should_abort, val/train ratio) untouched.

    3. colab.py: ADD after stage_unlabeled_squat_videos (after line 538). New constants _OHP_LABELED_VIDEOS_EXPECT_COUNT=2367, _OHP_UNLABELED_VIDEOS_EXPECT_COUNT=5490. stage_ohp_videos(drive_root, *, local_root="/content/ohp_videos", expect_count=_OHP_LABELED_VIDEOS_EXPECT_COUNT) -> str — copy stage_squat_videos's body, change ONLY: src_zip = Path(drive_root)/"Fitness-AQA_dataset_release/OHP/Labeled_Dataset/videos.zip"; local_zip = Path("/content/ohp_videos.zip"); the cache-hit/migrate/copy/extract/verify/cleanup logic is identical. stage_unlabeled_ohp_videos(drive_root_3001, drive_root_3002, *, local_videos_root="/content/ohp_unlabeled_videos", local_traj_root="/content/ohp_trajectories", expect_count=_OHP_UNLABELED_VIDEOS_EXPECT_COUNT) -> tuple[str,str] — copy stage_unlabeled_squat_videos's body but with TWO drive roots (Pitfall 4): src_videos = Path(drive_root_3002)/"Fitness-AQA_dataset_release/OHP/Unlabeled_Dataset/videos.zip"; src_traj = Path(drive_root_3001)/"Fitness-AQA_dataset_release/OHP/Unlabeled_Dataset/bar_trajectories_raw.zip". Videos use byte-resume copy + _extract_with_resume_and_progress; trajectories use byte-resume copy + zipfile.extractall (JSON, flat). Mirror the cache-hit + cleanup.

    4. In test_ohp.py, add test_dataset_cls_injection (no GPU, no data): import _build_dataloaders + inspect.signature, assert "dataset_cls" is a keyword-only parameter with default SquatKIEKFEDataset; import run_md_finetune_epoch, assert its signature also has dataset_cls defaulting to SquatKIEKFEDataset. (A signature-level test proves the backward-compatible seam without staging data — the real OHP dataset injection is exercised in Plan 02/04's Colab runs.) Mark fast.

    Single runnable unit — user pastes back the no-regression + seam gate: python -m pytest backend/training/aqa/ -m "not slow" -q (ALL tests green — the new OHP tests + the existing Phase 3/4 Squat suite; the dataset_cls default kept Squat unchanged).

    Atomic commit: feat(06): dataset_cls seam + OHP colab staging + no-regression gate (Task 5, D6 / RESEARCH §5).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/training/aqa/harness/supervised_train.py','backend/training/aqa/harness/md_finetune.py','backend/training/aqa/harness/colab.py']]; print('parse ok')" && python -m pytest backend/training/aqa/ -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - All 3 harness files parse; _build_dataloaders + run_md_finetune_epoch have a keyword-only dataset_cls defaulting to SquatKIEKFEDataset; stage_ohp_videos + stage_unlabeled_ohp_videos added to colab.py
    - The seam edits are MINIMAL: git diff on supervised_train.py touches only the signature (187-189) + lines 222-224; on md_finetune.py only the signature (134) + the call (193); the D6 monitor + everything else UNCHANGED
    - stage_unlabeled_ohp_videos takes TWO drive roots (3001 for the trajectory zip, 3002 for the unlabeled videos — Pitfall 4); trajectory extract uses zipfile.extractall (JSON), videos use _extract_with_resume_and_progress (mp4)
    - test_dataset_cls_injection passes (signature-level seam proof); the FULL existing Phase 3/4 Squat suite stays green under -m "not slow" (no regression from the default-value change)
    - Runnable unit: user pastes back the full -m "not slow" suite green (new OHP + existing Squat)
  </acceptance_criteria>
  <dependencies>Task 2 (OHPElbowsKneesDataset — the dataset_cls the seam injects), Task 3 (ohp_ssl exists so the full suite collects), Task 4 (the SSL tests are in the suite)</dependencies>
  <done>The dataset-injection seam (dataset_cls) lets the UNCHANGED Phase-3/4 trainers train OHP; the OHP staging functions handle the two-release-folder split; the existing Squat suite stays green — Wave 0 is complete and the GPU-burn gate is locked behind a green pure-function suite</done>
</task>

</tasks>

<verification>
## Mapping to OHP-01 + the Wave-0 gate

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| OHP-01-a: OHPElbowsKneesDataset shape + pos_weight ~[2.89,1.92] | Task 2 | test_ohp_dataset_shape + test_ohp_pos_weight pass; pos_weight formula proven on synthetic records (the real 2.887/1.924 is Task 1's archive test) |
| OHP-01-b: BBox _load_trajectory (y_center + empty-frame interpolation) | Task 3 | test_trajectory_load passes (y_center=(y1+y2)/2, interpolation, no NaN) |
| OHP-01-c: split_half_cycles argMIN on synthetic OHP trajectory | Task 4 | test_split_half_cycles_ohp passes (descent[-1] <= argmin <= ascent[0]; argMAX degenerates) |
| OHP-01-d: index_ohp counts 1582/339/339 | Task 1 | test_ohp_splits_count passes against the local archive |
| The dataset_cls seam (lets the unchanged trainers train OHP) | Task 5 | test_dataset_cls_injection + the full no-regression suite green |
| Wave-0 GPU-burn gate | Task 5 | `python -m pytest backend/training/aqa/ -m "not slow" -q` exits 0 (new OHP + existing Squat) |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| OHP-01-a (dataset shape + pos_weight) | Task 2 | unit | 0 |
| OHP-01-b (BBox trajectory load) | Task 3 | unit | 0 |
| OHP-01-c (half-cycle argMIN) | Task 4 | unit | 0 |
| OHP-01-d (index_ohp counts) | Task 1 | unit (offline, local archive) | 0 |

(OHP-01-e baseline convergence is Plan 02; OHP-01-f SSL linear-probe is Plan 03; OHP-01-g final test F1 is Plan 04.)
</verification>

<success_criteria>
- All 5 tasks complete with paste-back acceptance from the user (LOCAL CPU — fast paste-back)
- splits.index_ohp + OHPClipRecord added (additive; the Squat index()/ClipRecord UNCHANGED); reconciles to 1582/339/339
- ohp.py = OHPElbowsKneesDataset mirror of squat.py (elbows/knees labels, train pos_weight ~[2.887,1.924]); transforms.py reused unchanged
- ohp_ssl.py = OHPSSLDataset mirror of squat_ssl.py with the ONE BBox _load_trajectory delta (y_center=(y1+y2)/2 from region 0, empty-frame interpolation); split_half_cycles copied verbatim (argMIN); ssl_augs.py reused unchanged
- dataset_cls seam on _build_dataloaders + run_md_finetune_epoch (default SquatKIEKFEDataset — backward-compatible); the existing Phase 3/4 Squat suite stays green
- stage_ohp_videos + stage_unlabeled_ohp_videos added (the two-release-folder split handled; JSON via extractall)
- The 4 new OHP unit tests green (shape/pos_weight, BBox trajectory, argMIN split, index_ohp counts) + the seam test + no Squat regression
- Every D2/D4/D5/D6 decision carries an inline citation — [[feedback_ai_correctness]] satisfied
- One accurate threat_model: offline pipeline, no high-severity threats; T-06-01 (the new parser) caught by a CPU unit test; T-06-SC accept (zero new installs)
- NO GPU, NO Colab, NO Drive touched — this is the pure-function Wave-0 gate before the gated probe (Plan 02) + the SSL burn (Plan 03)
</success_criteria>

<output>
After Task 5 (green Wave-0 suite + no Squat regression), this plan is complete. Plan 02 (Wave 1+2) runs the gated trajectory-format + half-cycle-sign probe (BLOCKING — finalizes the live traj<->frame alignment + re-confirms argMIN before any GPU) then trains the supervised baseline. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 02 once Task 5's no-regression suite is green. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 6 Plan 01 written 2026-05-27. Wave 0: pure-function scaffolds + unit tests, CPU-verified before any GPU burn (the Phase-4 Wave-0 precedent, 04-01-PLAN). 5 tasks. The net-new code is two dataset modules (ohp.py mirrors squat.py; ohp_ssl.py mirrors squat_ssl.py with the ONE BBox _load_trajectory delta — RESEARCH §3, the CRITICAL delta vs Squat's flat-list loader), the additive splits.index_ohp + OHPClipRecord, the dataset_cls injection seam on _build_dataloaders + run_md_finetune_epoch (RESEARCH §5, backward-compatible), and the two OHP staging functions (two-release-folder split, Pitfall 4). Everything else (transforms, ssl_augs, eval/*, the trainer epoch loops) reused UNCHANGED.

Correction vs the handoff/RESEARCH cross-check: the actual md_finetune._d6_should_abort (line 130) uses val/train ratio (the academically-correct overfit direction), not the Plan-03-spec train/val — Task 5 explicitly leaves the D6 monitor untouched. Line numbers verified against source: _build_dataloaders 187-189 + 222-224, run_md_finetune_epoch 134 + call 193, stage_squat_videos 317, stage_unlabeled_squat_videos 449.

Security: accurate, not boilerplate. Offline ML pipeline (D1 descopes serving): no network/auth/untrusted-input/PII surface; no high-severity threats. Genuine items only: T-06-01 (the new BBox parser — caught by a CPU unit test before the GPU burn), T-06-03 (dataset_cls default — no Squat regression), T-06-04 (no dataset media/weights committed — gitignored), T-06-SC accept (zero new installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
