---
phase: 06-overhead-press
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - backend/training/aqa/notebooks/08_ohp_md_finetune.py
  - backend/training/aqa/notebooks/08_ohp_md_finetune.ipynb
  - .planning/phases/06-overhead-press/figures/results.pkl
autonomous: false
requirements: [OHP-01]
must_haves:
  truths:
    - "Three fine-tune seeds (42, 1337, 7) run sequentially on one L4 from the SHARED Plan 03 backbone.pt via the UNCHANGED md_finetune.run_md_finetune_epoch(dataset_cls=OHPElbowsKneesDataset) — each loads the MD backbone (NOT Kinetics) with a fresh Dropout(0.2)+Linear(512,2) head, AdamW (lr=1e-4, wd=1e-4), BCEWithLogitsLoss(pos_weight ~[2.887,1.924]), 50ep/8-patience cosine + the D6 val/train overfit monitor — producing best.pt at phase06/ohp_md_finetune_seed{42,1337,7}/best.pt; per-seed val macro-F1 recorded (D4 variance evidence)"
    - "The 3 seeds are aggregated by mean-of-sigmoids per error head (aggregate_sigmoid_mean, output in [0,1]) and a SINGLE per-error decision threshold is tuned on the ENSEMBLE val scores via threshold_sweep (NOT per-seed thresholds then vote, D4); the TTA recipe is val-tuned (select_tta_recipe over temporal_jitter/spatial_5crop/flip; flip OOD-validated; Phase 4 found TTA reversed on test — expect the same, report both numbers)"
    - "Final test F1 per error is reported on the official 339-clip OHP test split at the val-tuned thresholds, with BOTH the ensemble (+TTA) headline AND a single-model number; the comparison places our baseline (Plan 02 control) -> our MD-SSL ensemble -> paper Ours MD (Elbow 0.4552 / Knees 0.8452, CITED RESEARCH §1 Table 4) on identical metrics — the SSL lift = (our ensemble) - (our baseline), every number arithmetic-double-checked (macro=(elbows+knees)/2 computed in code)"
    - "results.pkl is written (BEFORE any figure) to .planning/phases/06-overhead-press/figures/ holding: per-epoch SSL curves (from ohp_md_pretrain_v2's latest.txt-pointed checkpoint), per-seed fine-tune curves, ensemble val+test scores + labels + clip_ids, the val-tuned thresholds + chosen TTA recipe, test F1 per error (ensemble + single-model + per-seed), the baseline control numbers (Plan 02), and the paper_targets {elbows:0.4552, knees:0.8452} — the source of truth for the Plan 05 notebook pack"
    - "Each fine-tune is disconnect-safe (per-epoch atomic checkpoint, map_location='cpu' resume, best.pt write does not clobber latest.txt); the D6 reg-bump policy is honored (seed 42 first; on a trip bump wd=5e-4/dropout=0.3 + lock for 1337/7; a later-seed trip at the locked recipe documented as variance, not auto-bumped)"
  artifacts:
    - path: "backend/training/aqa/notebooks/08_ohp_md_finetune.py"
      provides: "Phase 6 fine-tune + ensemble + eval jupytext notebook — Cell A + Step 0 stage (labeled OHP) + Step 1 backbone-path assert + Step 2 3-seed fine-tune (42 first + D6 gate, then 1337/7) + Step 3 ensemble + val threshold + Step 4 val-tuned TTA + Step 5 test eval + D6 post-hoc monitors + results.pkl. Figures are Plan 05 (the deliverable pack)."
      contains: "run_md_finetune_epoch"
    - path: "backend/training/aqa/notebooks/08_ohp_md_finetune.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D7)"
      contains: "ohp"
    - path: ".planning/phases/06-overhead-press/figures/results.pkl"
      provides: "Pickled dict — per-epoch SSL + per-seed fine-tune curves, ensemble val+test scores/labels/clip_ids, val-tuned thresholds, chosen TTA recipe, test F1 per error (ensemble + single-model + per-seed), the Plan-02 baseline control row, paper_targets {elbows:0.4552, knees:0.8452}. The source of truth for the Plan 05 notebook pack + FINDINGS.md."
      contains: "paper_targets"
  key_links:
    - from: "backend/training/aqa/notebooks/08_ohp_md_finetune.py"
      to: "backend/training/aqa/harness/md_finetune.run_md_finetune_epoch (dataset_cls=OHPElbowsKneesDataset) + the Plan 03 backbone.pt"
      via: "notebook cell imports; build_finetune_model(backbone_path) loads backbone_state_dict (strict=False), NOT Kinetics"
      pattern: "dataset_cls=OHPElbowsKneesDataset"
    - from: "backend/training/aqa/notebooks/08_ohp_md_finetune.py"
      to: "backend/training/aqa/eval/{ensemble.aggregate_sigmoid_mean, tta.select_tta_recipe/tta_forward, metrics.threshold_sweep/f1_per_error/pr_auc_per_error/confusion_matrix_per_error} — REUSED UNCHANGED"
      via: "notebook cell imports"
      pattern: "from backend.training.aqa.eval"
    - from: ".planning/phases/06-overhead-press/figures/results.pkl"
      to: "the 3 best.pt (phase06/ohp_md_finetune_seed{42,1337,7}) + the Plan 03 ohp_md_pretrain_v2 metrics_history + the Plan 02 baseline control"
      via: "the notebook gathers per-seed scores -> ensemble -> threshold -> test eval -> pickle"
      pattern: "results.pkl"
---

<objective>
Fine-tune the MD-pretrained OHP backbone (Plan 03's backbone.pt) for joint Elbows/Knees detection across 3 seeds (42, 1337, 7), ensemble (mean-of-sigmoids) + val-tuned threshold + val-tuned TTA, evaluate on the official OHP test split, and write results.pkl — the headline Phase 6 result + the source of truth for the Plan 05 deliverable pack (OHP-01). This is Wave 4 — it consumes the shared Plan 03 backbone + the UNCHANGED Phase-4 fine-tune/ensemble/TTA/eval code, and produces the F1-vs-paper comparison.

Purpose: OHP-01 + ROADMAP Phase 6 success criteria 1 ("MD-SSL models train on the official OHP splits") + 2 ("F1 per error reported and compared to the published numbers"). The fine-tune recipe is the Phase-4 D3 recipe UNCHANGED (AdamW wd=1e-4, dropout=0.2 head-only, lr=1e-4, 50ep/8-patience + the D6 overfit monitor); the 3-seed ensemble is the Phase-4 D4 recipe (mean-of-sigmoids, single per-head threshold on the ensemble val scores); TTA is the Phase-4 D5 recipe (val-tuned; Phase 4 found it reversed on test — expect the same, report both). The comparison is our baseline (Plan 02 control) -> our MD-SSL ensemble -> paper Ours MD (Elbow 0.4552 / Knees 0.8452, CITED Table 4 RESEARCH §1). The SSL lift = (our ensemble) - (our baseline). NOTE (defense framing, D2): OHP errors are near-independent (only 85/407 Elbow+ are Knees+), so do NOT expect a co-occurrence-driven joint lift — the contribution is the shared MD-SSL representation, not the joint head.

Output: the Phase 6 fine-tune notebook (Cell A + 3-seed fine-tune + ensemble + TTA + test eval + D6 monitors + results.pkl) delivered as paired .py + .ipynb; 3 best.pt on Drive at phase06/ohp_md_finetune_seed{42,1337,7}/; results.pkl under figures/ — the headline F1 + raw scores + the comparison numbers.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on the L4 Colab. The 3 fine-tunes run SEQUENTIALLY (D6 ensemble-homogeneity — seed 42 first, lock the recipe, then 1337 + 7). Each seed is a resume=True cell (disconnect-safe). Per the Squat anchor, fine-tune was ~6.1 min/epoch on L4; 50ep x 3 seeds is ~13-16h with early-stop bounding it. After git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]). No fabricated metrics — every number from a real run ([[feedback_ai_correctness]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-overhead-press/06-CONTEXT.md
@.planning/phases/06-overhead-press/06-RESEARCH.md
@.planning/phases/06-overhead-press/06-PATTERNS.md
@.planning/phases/06-overhead-press/06-VALIDATION.md
@.planning/phases/06-overhead-press/06-02-PLAN.md
@.planning/phases/06-overhead-press/06-03-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-03-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-04-PLAN.md
@backend/training/aqa/harness/md_finetune.py
@backend/training/aqa/datasets/ohp.py
@backend/training/aqa/eval/ensemble.py
@backend/training/aqa/eval/tta.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/notebooks/04_squat_md_ssl.py
@CLAUDE.md

<interfaces>
This plan CONSUMES the Plan 03 backbone.pt + the UNCHANGED Phase-4 fine-tune/ensemble/TTA/eval code (all implemented + unit-tested in Phase 4). It writes only notebook cells + results.pkl. NO new production logic.

Reused UNCHANGED (Phase 4 + the Plan 01 seam):
- md_finetune.run_md_finetune_epoch(*, run_name, md_backbone_path, drive_root, videos_root, seed=42, config=None, resume=True, max_epochs=None, dataset_cls=SquatKIEKFEDataset) -> dict — OHP callers pass dataset_cls=OHPElbowsKneesDataset, videos_root="/content/ohp_videos", md_backbone_path=<phase06/ohp_md_pretrain_v2/backbone.pt>. Returns epoch, metrics_history, best_f1_val, best_thresholds (None), checkpoint_path, best_checkpoint_path, config_hash, aborted_overfit. The D6 monitor (_d6_should_abort, val/train ratio > 10x before epoch 10) is UNCHANGED.
- md_finetune.build_finetune_model(backbone_path, dropout=0.2) — loads backbone_state_dict (strict=False) into r2plus1d_18(weights=None) + a fresh Dropout(0.2)+Linear(512,2) head. Exercise-agnostic — NO OHP change (the head is always a 2-output linear). Used to reconstruct each seed's model for the val/test passes.
- md_finetune.FinetuneConfig — the Phase-4 recipe (AdamW lr=1e-4 wd=1e-4, dropout=0.2, batch 16, 50ep, 8-patience cosine, num_frames=32, crop 112, flip OFF).
- eval/ensemble.aggregate_sigmoid_mean(per_seed_logits) -> mean-of-sigmoids in [0,1].
- eval/tta.select_tta_recipe(val_logits_per_recipe, val_labels) + tta_forward(model, clip, recipe, device) -> mean logits.
- eval/metrics.threshold_sweep / f1_per_error / pr_auc_per_error / confusion_matrix_per_error.

D6 reg-bump policy (notebook Step 2 orchestrates, NOT the trainer):
- Run seed 42 first with FinetuneConfig() (wd=1e-4, dropout=0.2).
- If seed 42 returns aborted_overfit=True: bump FinetuneConfig(weight_decay=5e-4, dropout=0.3); restart seed 42 until clean; LOCK that recipe for 1337 + 7.
- If seed 42 is clean: run 1337 + 7 at the base recipe. A later-seed trip at the LOCKED recipe is documented as seed variance, NOT auto-bumped.
NOTE (D2 caveat): OHP both pos_weights >1 (Elbows 2.887, Knees 1.924) — better-balanced per-head than Squat's [6.10, 0.45], so the D6 monitor is even less likely to fire than for Squat. Still mandatory.

Comparison numbers (RESEARCH §1, CITED from paper Table 4 — use EXACTLY these):
- Paper Ours MD (the Phase-6 target row): Elbow 0.4552, Knees 0.8452.
- Paper TemporalXform (best general video): Elbow 0.4138, Knees 0.8416 (secondary comparison).
- Paper OpenPose-TDM: Elbow 0.4265, Knees 0.7131.
- NO paper Kinetics row for OHP (RESEARCH §1) — our Plan-02 baseline is the only supervised control. The SSL lift = (our ensemble) - (our baseline).

Aggregation order (D5): per-seed -> average its TTA copies (tta_forward returns mean logits) -> sigmoid -> mean-of-sigmoids across seeds (aggregate_sigmoid_mean) -> single per-error threshold tuned on the ensemble VAL scores (threshold_sweep) -> apply to test.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D3-finetune | FinetuneConfig (Phase-4 D3, UNCHANGED): AdamW lr=1e-4 wd=1e-4, dropout=0.2 head-only, batch 16, 50ep, 8-patience cosine, num_frames=32, crop 112, flip OFF. build_finetune_model loads the Plan 03 MD backbone (strict=False), NOT Kinetics. The labeled OHP pipeline (OHPElbowsKneesDataset) reused via the dataset_cls seam — 32-frame sampling, NOT 16-frame half-cycles. | CONTEXT D3 + RESEARCH §4 |
| D6-monitor | The runtime val/train BCE loss ratio per epoch; > 10x before epoch 10 -> abort that seed (aborted_overfit=True) + bump reg (wd=5e-4, dropout=0.3). UNCHANGED md_finetune._d6_should_abort (val/train direction — the academically-correct overfit signal). OHP both pos_weights >1 -> even less likely to fire than Squat, but mandatory. | CONTEXT D6 |
| D6-homogeneity | Seed 42 runs FIRST; on a trip, bump + restart 42, then LOCK that recipe for 1337 + 7 (ensemble homogeneity — 3 seeds, 1 recipe). A later-seed trip at the locked recipe is documented as seed variance, not auto-bumped. | CONTEXT D6 |
| D4-ensemble | Mean of sigmoid scores across the 3 seeds (aggregate_sigmoid_mean, output in [0,1]); a SINGLE per-error threshold tuned on the ensemble VAL scores via threshold_sweep (NOT per-seed thresholds then vote). Per-seed test F1 also reported (variance evidence). The joint head is justified by the shared MD-SSL representation + efficiency, NOT co-occurrence (D2 — OHP errors near-independent). | CONTEXT D4 + RESEARCH §4/§13(Squat) |
| D5-tta | TTA recipe val-tuned via select_tta_recipe over {temporal_jitter, spatial_5crop, flip}; flip is OOD (trained flip OFF) and kept only if it raises ensemble val macro-F1. Phase 4 found TTA reversed on test + was not adopted — report BOTH the ensemble+TTA AND the single-model/no-TTA number, expect the no-TTA headline. | CONTEXT D3(TTA) + RESEARCH §4 |
| D3-eval | eval/metrics.py + ensemble.py + tta.py reused UNCHANGED (Phase-4 unit-tested). Per-error threshold sweep on val, applied to test. PR-AUC threshold-free. macro = (elbows + knees)/2 computed in code (NOT hand-typed). | CONTEXT D3 + RESEARCH §4 |
| D-compare | Comparison = our baseline (Plan 02 control) -> our MD-SSL ensemble (+TTA) -> paper Ours MD (Elbow 0.4552 / Knees 0.8452, CITED Table 4). NO paper Kinetics row for OHP (RESEARCH §1) — the baseline is the only control. The SSL lift = (our ensemble) - (our baseline). NO fabricated numbers. | RESEARCH §1 + [[feedback_ai_correctness]] |
| D8-resultspkl | results.pkl written BEFORE any figure (the source of truth) — per-epoch SSL + per-seed fine-tune curves, ensemble val+test scores/labels/clip_ids, val-tuned thresholds, chosen TTA recipe, test F1 (ensemble + single-model + per-seed), the Plan-02 baseline control row, paper_targets {elbows:0.4552, knees:0.8452}. Figures are Plan 05. | CONTEXT D1 (deliverable) + [[project_supervisor_visualizations]] |
| D7 | persistent_workers (via _build_dataloaders, unchanged); map_location='cpu' on resume + the backbone load; best.pt write does not clobber latest.txt (update_latest=False kwarg); metrics_history from latest.txt-pointed checkpoint for viz; each seed its own run_dir; one runnable unit at a time; restart runtime / importlib.reload after git pull; videos_root="/content/ohp_videos" explicit (Pitfall 5). | CONTEXT D7 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 3/4 + D7)

1. _set_global_seed(seed) per seed (42, 1337, 7) — covers Python/NumPy/torch-CPU/torch-CUDA RNG (run_md_finetune_epoch, unchanged).
2. persistent_workers via _build_dataloaders (unchanged, num_workers>0 guard).
3. map_location='cpu' on load_latest_checkpoint resume AND on the backbone.pt load in build_finetune_model (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume.
5. best.pt write passes update_latest=False (the Phase-4 kwarg) so latest.txt keeps pointing at the latest epoch_NNN.pt.
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch per seed.
7. metrics_history loaded from the latest.txt-pointed checkpoint for the per-seed curves (NOT best.pt — [[reference_best_pt_metrics_history_is_stale]]); the SSL curves from ohp_md_pretrain_v2's latest.txt-pointed checkpoint.
8. config_hash via hash_config (FinetuneConfig recipe keys + md_backbone_path); each seed its OWN run_dir (ohp_md_finetune_seed42/_seed1337/_seed7) so resume never crosses seeds.
9. No AMP (fp32) — Phase 3/4 contract.
10. videos_root="/content/ohp_videos" passed explicitly to run_md_finetune_epoch (Pitfall 5).
11. All val/test scores gathered on CPU then a single sklearn call (no per-batch F1 — aliasing on the moderate-minority rates). The ensemble + TTA produce sigmoid means in [0,1] so pr_auc_per_error's [0,1] assertion holds.
12. results.pkl written BEFORE any figure; restart runtime / importlib.reload after git pull.

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training + evaluation pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: the headline F1 arithmetic correctness, val-test leakage (the held-out test stays held out), the MD-not-Kinetics backbone load, ensemble homogeneity, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| Plan 03 backbone.pt -> fine-tune init | build_finetune_model loads backbone_state_dict (strict=False) into r2plus1d_18(weights=None). A wrong load (Kinetics, or a key mismatch) silently discards the SSL contribution. |
| Ensemble/TTA tuning -> the held-out OHP test split | TTA recipe + threshold tuned on VAL only; test stays held out. The val-test-gap monitor guards over-tuning. |
| Computed metrics -> the comparison + results.pkl | The comparison numbers must be arithmetically correct + match the CITED paper Table 4 (Elbow 0.4552 / Knees 0.8452); the baseline control loaded from Plan 02 (not hand-typed); mean-of-sigmoids (not logits). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | build_finetune_model could load Kinetics instead of the MD backbone (silently discarding the SSL contribution) | mitigate | build_finetune_model uses r2plus1d_18(weights=None) + explicit load_state_dict(ckpt["backbone_state_dict"], strict=False) — proven in Phase 4. Step 1 asserts md_backbone_path points to phase06/ohp_md_pretrain_v2/backbone.pt (NOT a Kinetics path). MEDIUM — tested in Phase 4 + path-asserted. |
| T-06-14 | Tampering | Headline-number arithmetic error (Phase 3's plan had wrong F1 literals the user caught) | mitigate | macro = (elbows + knees)/2 computed in code, NOT hand-typed; the comparison uses the CITED paper Table 4 numbers (0.4552/0.8452) + the Plan-02 baseline loaded from the notebook namespace/Drive (not hand-typed); Step 5 recomputes macro from the per-error F1 + asserts it matches the printed macro. MEDIUM — double-checked + recomputed in code. |
| T-06-15 | Information disclosure | Val-test leakage — the TTA recipe + threshold over-tuned to val, inflating the test number | mitigate | TTA recipe (select_tta_recipe) + threshold (threshold_sweep) tuned on VAL only; the val-test macro-F1 gap reported + flagged if large. MEDIUM — the gap monitor is the leakage guard. |
| T-06-16 | Tampering | Ensemble inhomogeneity (seeds on different recipes) or mean-of-logits instead of mean-of-sigmoids | mitigate | The D6 reg-bump policy locks the recipe on seed 42 (1 recipe, 3 seeds); aggregate_sigmoid_mean (Phase-4 unit-tested) applies sigmoid BEFORE the mean + asserts [0,1]. Per-seed val/test F1 recorded (D4 variance). MEDIUM — policy + the tested aggregation function. |
| T-06-17 | Integrity | Committing dataset media / weights (the 3 best.pt ~360 MB each) — license + repo-bloat | accept | All weights gitignored / Drive-staged. This plan commits only the notebook .py/.ipynb + results.pkl (small, no media). The best.pt live on Drive, never git. Stage files by name only. LOW — gitignore contract established. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe is the guard. No [ASSUMED]/[SUS] packages. |

T-06-14 (headline arithmetic) is the highest-priority threat given the Phase 3 precedent (the user caught wrong F1 literals); mitigated by computing macro in code + the CITED Table 4 numbers + loading the baseline from Plan 02 rather than hand-typing. No web-app threats apply.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Notebook Cell A + Step 0 stage + Step 1 backbone-path assert + Step 2 3-seed fine-tune (42 first + D6 gate, then 1337/7)</name>
  <files>
    backend/training/aqa/notebooks/08_ohp_md_finetune.py,
    backend/training/aqa/notebooks/08_ohp_md_finetune.ipynb
  </files>
  <read_first>
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the Squat Step 6 3-seed fine-tune pattern — the per-seed resume=True cells + the D6 reg-bump orchestration + the per-seed val-F1 table; the Cell A + Step 0 stage to copy + swap to OHP),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (the Plan 02 Step 0 OHP labeled staging — reuse the stage_ohp_videos call),
    backend/training/aqa/harness/md_finetune.py (run_md_finetune_epoch + FinetuneConfig + build_finetune_model + _d6_should_abort — Step 2 calls run_md_finetune_epoch per seed with dataset_cls=OHPElbowsKneesDataset),
    .planning/phases/04-squat-motion-disentangling-ssl/04-03-PLAN.md (the Squat 3-seed Step 6 — the D6 reg-bump policy, seed 42 first, lock for 1337/7, the per-seed variance recording),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — the fine-tune recipe; the OHP pos_weights >1 making the D6 monitor less likely to fire; §1 the paper targets),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D6 reg-bump policy + D4 per-seed F1 recording + D2 the near-independence caveat)
  </read_first>
  <action>
    Land Cell A + Step 0 + Step 1 + Step 2 in the fine-tune notebook (08_ohp_md_finetune.py + .ipynb pair, Cell A baked in — D7). Cell A: clone-or-pull + sys.path (verbatim from the Squat SSL notebook; OHP title). Step 0: _envinit first; PyAV; dep-version probe; L4 assert; Drive mount; stage_ohp_videos(DRIVE_ROOT, local_root="/content/ohp_videos") (labeled 2367 — the fine-tune trains on these). Step 1: BACKBONE_PATH = os.path.join(MYDRIVE, "FitNova/checkpoints/phase06/ohp_md_pretrain_v2/backbone.pt"); assert os.path.exists(BACKBONE_PATH), "Plan 03 backbone.pt must exist" (T-06-13 path assertion — NOT a Kinetics path).

    Step 2 = the 3-seed fine-tune, orchestrating the D6 reg-bump policy. Structure as SEQUENTIAL per-seed cells (one runnable unit each, D6 homogeneity):
    - Step 2a (seed 42, the recipe-locking seed): base_config = FinetuneConfig(); result42 = run_md_finetune_epoch(run_name="ohp_md_finetune_seed42", md_backbone_path=BACKBONE_PATH, drive_root=MYDRIVE, videos_root="/content/ohp_videos", seed=42, config=base_config, resume=True, dataset_cls=OHPElbowsKneesDataset). Paste-back: result42["aborted_overfit"], result42["best_f1_val"], final epoch. IF aborted_overfit: bumped_config = FinetuneConfig(weight_decay=5e-4, dropout=0.3); restart seed 42 until clean; LOCK bumped_config for 1337/7. ELSE locked_config = base_config. Record locked_config + result42 best_f1_val (D4). The D6 monitor likely will NOT fire (OHP pos_weights >1, better-balanced than Squat) — but the gate is mandatory.
    - Step 2b (seed 1337, locked recipe): result1337 = run_md_finetune_epoch(run_name="ohp_md_finetune_seed1337", ..., seed=1337, config=locked_config, resume=True, dataset_cls=OHPElbowsKneesDataset). Paste-back best_f1_val + aborted_overfit. A trip here at the LOCKED recipe is documented as variance, NOT auto-bumped (D6).
    - Step 2c (seed 7, locked recipe): result7 = run_md_finetune_epoch(run_name="ohp_md_finetune_seed7", ..., seed=7, config=locked_config, resume=True, dataset_cls=OHPElbowsKneesDataset). Paste-back best_f1_val + aborted_overfit.
    After all 3: print the per-seed val macro-F1 table (42/1337/7 best_f1_val + best epoch) — the D4 variance evidence. Each seed cell is resume=True (disconnect-safe — re-run after a restart resumes that seed from its latest.txt; the first post-resume epoch logs "Resumed from epoch_NNN.pt").

    Single runnable unit per seed (ASYNC, ~13-16h across the 3, multi-session) — per-seed paste-back of best_f1_val + aborted_overfit + the resume log on any disconnect; the per-seed val-F1 table at the end. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Step 3 until all 3 best.pt exist + the table is pasted back.

    Atomic commit (per seed completion): feat(06): notebook Step 2 fine-tune seed{N} (Task 1, D3/D6/D4).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/08_ohp_md_finetune.py').read()); print('parse ok')" && grep -q "run_md_finetune_epoch" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "ohp_md_finetune_seed42" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "ohp_md_finetune_seed1337" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "ohp_md_finetune_seed7" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "dataset_cls=OHPElbowsKneesDataset" backend/training/aqa/notebooks/08_ohp_md_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook .py parses; Cell A + Step 0 + Step 1 + Step 2 present; .ipynb pair shipped; 3 sequential per-seed cells (42 first, then 1337, 7)
    - BACKBONE_PATH asserted to be phase06/ohp_md_pretrain_v2/backbone.pt (T-06-13); run_md_finetune_epoch called with dataset_cls=OHPElbowsKneesDataset + videos_root="/content/ohp_videos" (Pitfall 5)
    - Runnable unit (Colab, ~13-16h, multi-session): per-seed paste-back of best_f1_val + aborted_overfit; the per-seed val-F1 table (D4 variance); on disconnect, "Resumed from epoch_NNN.pt"
    - D6 reg-bump policy honored: if seed 42 trips, bump + lock for 1337/7; a later-seed trip at the locked recipe documented as variance, not auto-bumped
    - 3 best.pt on Drive at phase06/ohp_md_finetune_seed{42,1337,7}/
  </acceptance_criteria>
  <dependencies>Plan 03 (backbone.pt on Drive)</dependencies>
  <done>3 fine-tune best.pt checkpoints on Drive from the shared MD backbone via the UNCHANGED md_finetune trainer (dataset_cls=OHPElbowsKneesDataset); per-seed val macro-F1 recorded (D4 variance); the ensemble homogeneity policy honored; the 3 inputs for the ensemble exist</done>
</task>

<task type="auto">
  <name>Task 2: Notebook Step 3 + Step 4 — mean-of-sigmoids ensemble + val threshold + val-tuned TTA</name>
  <files>backend/training/aqa/notebooks/08_ohp_md_finetune.py</files>
  <read_first>
    backend/training/aqa/eval/ensemble.py (aggregate_sigmoid_mean — Phase-4 tested; mean-of-sigmoids in [0,1]),
    backend/training/aqa/eval/tta.py (select_tta_recipe + tta_forward — Phase-4 tested; the 5-crop inline; mean-logits return),
    backend/training/aqa/eval/metrics.py (threshold_sweep — single per-error threshold on ensemble val scores; f1_per_error),
    backend/training/aqa/harness/md_finetune.py (build_finetune_model — reconstruct each seed's model for the val/test passes; the labeled loaders),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the Squat Step 7 ensemble + Step 8 val-tuned TTA patterns — the per-seed score gather, the aggregation order, the per-recipe val table),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — the ensemble + TTA recipe; Phase 4 found TTA reversed on test — expect the same),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D4 mean-of-sigmoids + single ensemble-val threshold; the TTA val-tuning + report-both)
  </read_first>
  <action>
    Append Step 3 (ensemble + val threshold) + Step 4 (val-tuned TTA). Step 3: for seed in (42,1337,7): ckpt = torch.load(phase06/ohp_md_finetune_seed{seed}/best.pt, map_location='cuda'); model = build_finetune_model(BACKBONE_PATH, dropout=locked_config.dropout); model.load_state_dict(ckpt["model_state_dict"]); model.eval(); run a val pass + a test pass on the OHP val/test loaders (dataset_cls=OHPElbowsKneesDataset; gather logits on CPU, no per-batch F1) -> per_seed_val_logits[seed] (339,2), per_seed_test_logits[seed] (339,2). Capture test_clip_ids once. Ensemble (NO TTA yet): ensemble_val = aggregate_sigmoid_mean([val_logits 42/1337/7]) (339,2) in [0,1]; ensemble_test similarly. Tune the single per-error threshold on the ensemble VAL scores: t_elbows, f1_elbows_val = threshold_sweep(val_labels[:,0], ensemble_val[:,0]); t_knees, f1_knees_val = threshold_sweep(val_labels[:,1], ensemble_val[:,1]) (D4 — single threshold on the ensemble, NOT per-seed-then-vote). Print: per-seed val macro-F1 (each at 0.5, D4 variance), the ensemble val F1 + the two thresholds. Store ensemble_val/test, per_seed_*, thresholds, test_clip_ids for Steps 4-5.

    Step 4: val-tune the TTA recipe. Candidates: the combos over {temporal_jitter, spatial_5crop, flip} (e.g. (), (temporal_jitter,), (spatial_5crop,), (temporal_jitter,spatial_5crop), and +flip). For each candidate: per seed, per val clip, tta_forward (mean logits over the TTA copies); sigmoid; aggregate_sigmoid_mean across seeds -> ensemble val scores for that recipe. select_tta_recipe(val_logits_per_recipe, val_labels) returns the val-macro-F1-maximizing recipe (D5 — measure; flip kept only if it raises val macro-F1). Print the per-recipe val table + the chosen recipe + whether flip was selected. Then compute the FINAL ensemble+TTA scores on VAL and TEST using the chosen recipe + the aggregation order (per-seed -> avg TTA copies -> sigmoid -> mean-of-sigmoids across seeds -> re-tune the per-error threshold on the ensemble+TTA val scores). Keep BOTH the single-model/no-TTA number (Step 3's ensemble) AND the ensemble+TTA number. Print the TTA forward-pass cost. NOTE: Phase 4 found TTA reversed on test — expect the no-TTA headline; report both honestly.

    Single runnable unit (Colab) — user pastes back: per-seed val macro-F1 (3 numbers) + ensemble val F1 + the two thresholds (Step 3); the per-recipe val table + the chosen TTA recipe + the TTA cost (Step 4).

    Atomic commit: feat(06): notebook Step 3 ensemble + threshold + Step 4 val-tuned TTA (Task 2, D4/D5 / RESEARCH §4).
  </action>
  <verify>
    <automated>grep -q "aggregate_sigmoid_mean" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "threshold_sweep" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "select_tta_recipe" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "tta_forward" backend/training/aqa/notebooks/08_ohp_md_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 + Step 4 present; loads the 3 best.pt; gathers per-seed val+test logits; ensembles via aggregate_sigmoid_mean (mean-of-sigmoids in [0,1], NOT logits — T-06-16); single per-error threshold on the ensemble VAL scores (NOT per-seed then vote)
    - select_tta_recipe picks the val-macro-F1-maximizing recipe; flip kept ONLY if it raised val macro-F1; the aggregation order is per-seed -> avg TTA -> mean-of-sigmoids -> val threshold (D5)
    - BOTH the ensemble+TTA val F1 AND the single-model/no-TTA number retained (report-both)
    - Runnable unit (Colab): user pastes back per-seed val macro-F1 (3) + ensemble val F1 + thresholds + the per-recipe TTA table + chosen recipe + TTA cost
  </acceptance_criteria>
  <dependencies>Task 1 (the 3 best.pt)</dependencies>
  <done>The 3 seeds are ensembled by mean-of-sigmoids; the single per-error threshold is tuned on the ensemble val scores; the TTA recipe is val-tuned (flip OOD-validated); both the ensemble+TTA and single-model numbers are ready for the test eval</done>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 5 — final OHP test eval + D6 post-hoc monitors + F1-vs-paper + results.pkl</name>
  <files>
    backend/training/aqa/notebooks/08_ohp_md_finetune.py,
    .planning/phases/06-overhead-press/figures/results.pkl
  </files>
  <read_first>
    backend/training/aqa/eval/metrics.py (f1_per_error, pr_auc_per_error, confusion_matrix_per_error — the final test metrics),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (the Squat Step 9 test-eval + results.pkl pattern — the summary table + the pickle structure to mirror; the D6 post-hoc monitors),
    .planning/phases/06-overhead-press/06-02-PLAN.md (the Plan 02 baseline control numbers — the SSL-lift control row; how they were recorded in the baseline notebook namespace),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§1 — the EXACT paper Table 4 numbers: Ours MD Elbow 0.4552 / Knees 0.8452, TemporalXform 0.4138/0.8416, OpenPose-TDM 0.4265/0.7131; NO Kinetics row; double-check every value),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D1 the results.pkl deliverable + the comparison; D6 the val-test-gap / no-regress monitors)
  </read_first>
  <action>
    Append Step 5: final test eval at the val-tuned thresholds + the D6 post-hoc monitors + the F1-vs-paper comparison + results.pkl. Apply the val-tuned thresholds to the ensemble(+TTA) TEST scores: test_pred_elbows = (ensemble_test[:,0] > t_elbows).astype(int); same Knees; test_f1_elbows = f1_per_error(test_labels[:,0].astype(int), test_pred_elbows); test_f1_knees similarly; test_macro = (test_f1_elbows + test_f1_knees)/2 (computed in code, NOT hand-typed — T-06-14); test_pr_auc_elbows/knees = pr_auc_per_error(...); test_cm_elbows/knees = confusion_matrix_per_error(...). ALSO compute the single-model/no-TTA test F1 (Step 3's ensemble, no TTA) + the per-seed test F1 (D4 variance). Load the Plan 02 baseline control numbers (from the baseline notebook's recorded values, or re-load ohp_supervised_v1/best.pt + re-run a test pass if not cached). Print the summary table: our baseline (control) Elbows/Knees/macro; our MD-SSL ensemble Elbows/Knees/macro; our ensemble+TTA; paper Ours MD 0.4552/0.8452; paper TemporalXform 0.4138/0.8416 (use EXACTLY these — RESEARCH §1, arithmetic pre-verified). Compute + print the SSL LIFT = (our ensemble macro) - (our baseline macro).

    D6 POST-HOC MONITORS (print + flag, not hard-fail): (a) val_test_gap = abs(ensemble_val_macro - test_macro); flag if > 0.05 ("over-tuned to val"); (b) report whether TTA helped or reversed on test (Phase-4 precedent: expect reversal — report both numbers, headline the better one, expect no-TTA). NO-LIFT NOTE: if test_macro <= baseline_macro (the MD-SSL did not beat the supervised baseline), record it honestly for the SUMMARY (a valid result — "faithfully implemented MD-SSL on OHP; lift over the baseline was X"); the one-remediation policy applies at the phase level (Plan 05 SUMMARY) — do NOT open-ended iterate here.

    Write results.pkl (BEFORE any figure — the source of truth, T-06-14) to .planning/phases/06-overhead-press/figures/ (mkdir parents first): the dict per PATTERNS results.pkl schema — ssl_epochs (per-epoch SSL loss + linear-probe from ohp_md_pretrain_v2's latest.txt-pointed checkpoint), finetune_seeds {42:curves,1337:...,7:...} (per-seed train/val loss + val macro-F1 from each seed's latest.txt-pointed checkpoint), ensemble_val_scores (339,2), ensemble_test_scores (339,2), val_labels, test_labels, test_clip_ids, best_thresholds {elbows,knees}, chosen_tta_recipe, test_f1 {elbows,knees,macro} (ensemble), test_f1_single_model, test_f1_per_seed, test_pr_auc {elbows,knees}, test_confusion {elbows,knees}, baseline_control {elbows,knees,macro} (Plan 02), ssl_lift, val_test_gap, paper_targets {elbows:0.4552, knees:0.8452}, paper_temporalxform {elbows:0.4138, knees:0.8416}, run provenance.

    Single runnable unit (Colab) — user pastes back: the full summary table (our baseline -> ensemble -> ensemble+TTA -> paper rows) + the SSL lift + the D6 monitor outcomes (val-test gap, TTA-helped-or-reversed) + the results.pkl write confirmation. The orchestrator double-checks macro=(elbows+knees)/2 against the printed values + the §1 paper numbers.

    Atomic commit: feat(06): notebook Step 5 test eval + D6 monitors + F1-vs-paper + results.pkl (Task 3, D-compare / RESEARCH §1).
  </action>
  <verify>
    <automated>grep -q "results.pkl" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "test_f1_elbows" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "0.4552" backend/training/aqa/notebooks/08_ohp_md_finetune.py && grep -q "paper_targets" backend/training/aqa/notebooks/08_ohp_md_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 present; test eval at the val-tuned thresholds; test_macro computed in code as (test_f1_elbows + test_f1_knees)/2 (NOT hand-typed — T-06-14)
    - The comparison rows match RESEARCH §1 EXACTLY: paper Ours MD 0.4552/0.8452, TemporalXform 0.4138/0.8416; the baseline control loaded from Plan 02 (not hand-typed); the SSL lift = (ensemble macro) - (baseline macro) computed + printed
    - D6 monitors reported: val-test gap (flag if >0.05); TTA helped-or-reversed (expect reversal per Phase 4 — report both, headline the better)
    - results.pkl written (BEFORE figures) with the full schema incl. ssl_epochs, finetune_seeds, ensemble val+test scores/labels/clip_ids, thresholds, chosen TTA recipe, test F1 (ensemble+single+per-seed), baseline_control, ssl_lift, paper_targets {elbows:0.4552,knees:0.8452}
    - Runnable unit (Colab): user pastes back the summary table + the SSL lift + the D6 outcomes + the results.pkl write confirmation
  </acceptance_criteria>
  <dependencies>Task 2 (the ensemble+TTA scores)</dependencies>
  <done>The headline Phase 6 OHP test F1 is computed (ensemble + single-model) at the val-tuned thresholds; the comparison places it against the CITED paper Ours MD (0.4552/0.8452) + the Plan-02 baseline control; the SSL lift is measured; results.pkl (the source of truth for Plan 05) is on disk — OHP-01's F1-per-error-compared-to-published deliverable is computed</done>
</task>

</tasks>

<verification>
## Mapping to Phase 6 ROADMAP Success Criteria + OHP-01

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (complete): OHP MD-SSL models train on the official OHP splits | Task 1 (3-seed fine-tune from the SSL backbone) | 3 best.pt on Drive; per-seed val macro-F1 table |
| SC2 (complete): F1 per error reported + compared to the published numbers | Task 3 | Test F1 per error at val-tuned thresholds vs paper Ours MD 0.4552/0.8452 (CITED); the SSL lift over the baseline |
| OHP-01-g: final OHP test F1 (Elbows + Knees reported) | Task 3 | The summary table + results.pkl |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| OHP-01-g (final OHP test F1 on the official split) | Task 3 | integration (Colab paste-back) | 4 |
| (3-seed ensemble + per-seed variance — D4) | Task 1 + Task 2 | integration | 4 |

(The notebook deliverable pack + FINDINGS.md + the phase SUMMARY are Plan 05.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user on the L4 Colab
- 3 fine-tune seeds (42, 1337, 7) ran SEQUENTIALLY from the shared Plan 03 backbone via the UNCHANGED md_finetune.run_md_finetune_epoch(dataset_cls=OHPElbowsKneesDataset); the D6 reg-bump policy honored; 3 best.pt on Drive
- The ensemble is mean-of-sigmoids (in [0,1]) with a single per-error threshold tuned on the ensemble val scores (D4); the TTA recipe is val-tuned (flip OOD-validated; Phase 4 found TTA reversed on test — both numbers reported)
- Final OHP test F1 per error reported at val-tuned thresholds (ensemble + single-model + per-seed); the comparison vs paper Ours MD (0.4552/0.8452, CITED) + the Plan-02 baseline control; the SSL lift = (ensemble) - (baseline) computed in code
- macro = (elbows + knees)/2 computed in code (NOT hand-typed — T-06-14); the comparison numbers match RESEARCH §1 exactly
- results.pkl written (BEFORE any figure) with the full schema — the source of truth for the Plan 05 notebook pack + FINDINGS.md
- Disconnect-safe: each seed resumes cleanly (resume paste-back logs "Resumed from epoch_NNN.pt"); build_finetune_model loads the MD backbone (NOT Kinetics — path-asserted, T-06-13); videos_root="/content/ohp_videos" explicit (Pitfall 5)
- D2 defense framing honored: NO co-occurrence-driven joint lift expected (OHP errors near-independent); the contribution is the shared MD-SSL representation
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D7); every D3/D4/D5 decision carries an inline citation; NO fabricated numbers
- One accurate threat_model: offline pipeline, no high-severity threats; T-06-14 (headline arithmetic — computed in code + CITED numbers), T-06-SC accept
</success_criteria>

<output>
After Task 3 (results.pkl on disk + the headline F1-vs-paper computed), this plan is complete. Plan 05 (Wave 5) builds the 4-notebook OHP deliverable pack (docs/notebooks/05-08_ohp_*) + figures + the FINDINGS.md OHP section from results.pkl + local OHP data, and authors the phase SUMMARY. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 05 once Task 3's results.pkl + the headline numbers are confirmed. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 6 Plan 04 written 2026-05-27. Wave 4: the 3-seed fine-tune + ensemble + val-tuned threshold + TTA + test eval + results.pkl (mirrors Phase 4 Plans 03+04 combined). 3 tasks. NO new production logic — this plan WIRES the UNCHANGED md_finetune.run_md_finetune_epoch (the Plan-01 dataset_cls seam) + eval/ensemble.py + eval/tta.py + eval/metrics.py on the OHP data + the Plan 03 backbone. The fine-tune recipe (D3), the 3-seed ensemble (D4), the val-tuned TTA (D5), and the D6 reg-bump policy are all the Phase-4 recipes carried unchanged.

Key OHP framing: there is NO paper Kinetics row for OHP (RESEARCH §1) — the Plan-02 baseline is the only supervised control; the SSL lift = (our ensemble) - (our baseline). The comparison target is paper Ours MD (Elbow 0.4552 / Knees 0.8452, CITED Table 4). D2 defense caveat surfaced: OHP errors are near-independent (85/407 Elbow+ are Knees+) — NO co-occurrence-driven joint lift expected; the contribution is the shared MD-SSL representation. The D6 monitor uses the actual md_finetune._d6_should_abort (val/train ratio) and is even less likely to fire than Squat (OHP both pos_weights >1, better-balanced).

Pitfall 5 honored: videos_root="/content/ohp_videos" explicit. T-06-13: BACKBONE_PATH asserted to be the MD backbone (not Kinetics). T-06-14 (headline arithmetic — the Phase 3 precedent the user caught): macro computed in code + CITED §1 numbers + baseline loaded from Plan 02, not hand-typed.

Security: accurate. Offline pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-06-13 (MD-not-Kinetics — Phase-4-tested + path-asserted), T-06-14 (arithmetic — highest priority, computed in code), T-06-15 (val-test leakage — the gap monitor), T-06-16 (ensemble homogeneity + mean-of-sigmoids), T-06-17 accept (no weights committed), T-06-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
