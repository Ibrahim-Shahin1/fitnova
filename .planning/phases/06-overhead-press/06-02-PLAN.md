---
phase: 06-overhead-press
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - backend/training/aqa/notebooks/06_ohp_supervised_baseline.py
  - backend/training/aqa/notebooks/06_ohp_supervised_baseline.ipynb
  - backend/training/aqa/datasets/ohp_ssl.py
  - .planning/phases/06-overhead-press/figures/.gitkeep
  - .planning/phases/06-overhead-press/figures/ohp_traj_probe.png
autonomous: false
requirements: [OHP-01]
must_haves:
  truths:
    - "On 2-5 real OHP unlabeled clips in Colab, the BBox trajectory format is confirmed live (region 0 = barbell; y_center=(y1+y2)/2), the trajectory-length to video-frame-count mapping is measured (1:1 confirmed or the rescale formula recorded), the empty-region-0 frequency is reported, and the half-cycle bottom SIGN is resolved by a human looking at a y_center plot with the argMIN split overlaid AND a descent/ascent frame grid — BEFORE ohp_ssl._load_trajectory is finalized and BEFORE any SSL GPU pretraining (D5 / RESEARCH §3 Open Question 1)"
    - "ohp_ssl.py is finalized against the probe-confirmed format: _load_trajectory applies the confirmed traj<->frame mapping, __getitem__ builds {anchor,positive,negative} triplets with split_half_cycles(bottom_is_argmax=False), and one Colab batch's three tensor shapes [3,16,112,112] float32 + a decoded descent/ascent sanity grid are pasted back — the live re-confirmation of the Plan-01 synthetic-data implementation"
    - "An R(2+1)D-18 (Kinetics-400-V1 init) joint Elbows/Knees baseline (nn.Linear(512,2) head, end-to-end fine-tune, BCEWithLogitsLoss with the train pos_weight ~[2.887,1.924], Adam lr 1e-4, 50ep/8-patience cosine, batch 16) trains to convergence on the official 1582-clip OHP train split via the UNCHANGED supervised_train.run_supervised_epoch (dataset_cls=OHPElbowsKneesDataset) — val macro-F1 strictly improves over the first 5 epochs (OHP-01-e)"
    - "F1 per error is reported on the official 339-clip OHP test split at per-error thresholds tuned independently on the official 339-clip val split (eval/metrics.threshold_sweep), and recorded as the SSL-lift CONTROL — there is NO paper Kinetics baseline for OHP (RESEARCH §1), so this measured baseline is the only control the Plan 04 MD-SSL lift is computed against"
    - "The baseline is disconnect-safe: it auto-resumes from My Drive/FitNova/checkpoints/phase06/ohp_supervised_v1/latest.txt with map_location='cpu', checkpoints every epoch via the Phase 2 atomic-write contract, and a mid-run Colab disconnect resumes cleanly without restarting"
  artifacts:
    - path: "backend/training/aqa/notebooks/06_ohp_supervised_baseline.py"
      provides: "Phase 6 baseline jupytext notebook — Cell A bootstrap + Step 0 env/stage(ohp labeled + unlabeled+traj) + Step 1 trajectory probe (GATED) + Step 2 SSL dataset smoke + Step 3 model+VRAM probe + Step 4 epoch-0 timing gate + Step 5 baseline training + Step 6 threshold sweep + Step 7 test eval. The SSL pretrain notebook (07) is Plan 03; the fine-tune notebook (08) is Plan 04."
      contains: "stage_ohp_videos"
    - path: "backend/training/aqa/notebooks/06_ohp_supervised_baseline.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D7 landmine)"
      contains: "ohp"
    - path: "backend/training/aqa/datasets/ohp_ssl.py"
      provides: "FINALIZED — _load_trajectory applies the probe-confirmed traj<->frame mapping (1:1 or the recorded rescale); __getitem__ wired against real clips; the Plan-01 synthetic implementation re-confirmed on a real Colab batch. (If the probe reveals a format deviation from RESEARCH §3, _load_trajectory is corrected here.)"
      contains: "def _load_trajectory"
    - path: ".planning/phases/06-overhead-press/figures/ohp_traj_probe.png"
      provides: "The probe figure — 2-5 OHP y_center trajectories with the argMIN split overlaid + descent/ascent frame grids — the human-verify evidence for the sign + format"
  key_links:
    - from: "backend/training/aqa/notebooks/06_ohp_supervised_baseline.py"
      to: "backend/training/aqa/harness/supervised_train.run_supervised_epoch (dataset_cls=OHPElbowsKneesDataset) + harness.colab.stage_ohp_videos"
      via: "notebook cell imports"
      pattern: "dataset_cls=OHPElbowsKneesDataset"
    - from: "backend/training/aqa/datasets/ohp_ssl.py"
      to: "backend/training/aqa/datasets/transforms.py (decode_clip, uniform_sample_indices, spatial_train) + ssl_augs.py"
      via: "import + __getitem__ reuses the Phase 2 decode/spatial pipeline + per-branch augs"
      pattern: "from backend.training.aqa.datasets.transforms import"
    - from: "backend/training/aqa/notebooks/06_ohp_supervised_baseline.py"
      to: "backend/training/aqa/eval/metrics.py (threshold_sweep, f1_per_error, pr_auc_per_error — REUSED UNCHANGED)"
      via: "notebook cell imports for Step 6/7"
      pattern: "from backend.training.aqa.eval.metrics import"
---

<objective>
Run the gated trajectory-format + half-cycle-sign probe (the single highest-leverage sequencing gate in the phase, RESEARCH §3 Open Question 1) on 2-5 real OHP clips in Colab, finalize ohp_ssl.py against the probe-confirmed format, then train the supervised OHP Elbows/Knees baseline (R(2+1)D-18 Kinetics-V1, the Phase-3 recipe) and report F1 on the official OHP test split — the SSL-lift CONTROL. This is Wave 1 (the probe — the FIRST GPU-touching task, which MUST pass before the SSL burn in Plan 03) and the supervised baseline (OHP-01-e).

Purpose: OHP-01 + ROADMAP Phase 6 success criterion 1 ("OHP baseline ... trains on the official OHP splits"). The probe is Task 1 (BLOCKING checkpoint:human-verify) because the OHP trajectory format is the CRITICAL delta vs Squat (per-frame BBox arrays, not flat y-lists — RESEARCH §3). Plan 01 implemented _load_trajectory against the offline-inspected structure (20 files) + the ReadMe formula, and unit-tested it on synthetic data; but the live 1:1 traj<->frame alignment is [ASSUMED until the Colab probe] (RESEARCH A1) and the argMIN sign, though strongly inferred (overhead = lowest y pixel), MUST be visually confirmed on real clips before the 15-26h SSL GPU burn. A wrong mapping or sign would silently waste days of L4 compute. The supervised baseline is the SSL-lift control: unlike Squat (where the paper gave a Kinetics row), the paper has NO OHP Kinetics baseline (RESEARCH §1) — our measured baseline is the only control for the Plan 04 MD lift.

Output: the Phase 6 baseline notebook (Cell A + Step 0 staging + the gated probe + SSL dataset smoke + VRAM/timing gates + baseline training + threshold sweep + test eval) delivered as paired .py + .ipynb; the finalized ohp_ssl.py; the ohp_supervised_v1 best.pt + per-epoch checkpoints on Drive; the baseline F1 per error on the official OHP test split.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on a FRESH L4 Colab notebook ([[feedback_heavy_training_new_notebook]] — prompt the user before the first training cell). Each task is ONE runnable unit; the user runs it and pastes output back. NEVER pre-write task N+1 before task N's paste-back. After any git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]). The baseline training cell (Step 5) is async + may span sessions — its checkpoint/resume must be bulletproof ([[feedback_notebook_disconnect_safe]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-overhead-press/06-CONTEXT.md
@.planning/phases/06-overhead-press/06-RESEARCH.md
@.planning/phases/06-overhead-press/06-PATTERNS.md
@.planning/phases/06-overhead-press/06-VALIDATION.md
@.planning/phases/06-overhead-press/06-01-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-PLAN.md
@.planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md
@backend/training/aqa/datasets/ohp.py
@backend/training/aqa/datasets/ohp_ssl.py
@backend/training/aqa/datasets/squat_ssl.py
@backend/training/aqa/datasets/transforms.py
@backend/training/aqa/harness/supervised_train.py
@backend/training/aqa/harness/colab.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/notebooks/03_squat_supervised_baseline.py
@backend/training/aqa/notebooks/04_squat_md_ssl.py
@CLAUDE.md

<interfaces>
This plan FINALIZES ohp_ssl.py (the Plan 01 stubs are implemented + synthetic-tested; here the body is re-confirmed/corrected against the LIVE probe). It REUSES supervised_train.run_supervised_epoch UNCHANGED (the dataset_cls seam from Plan 01) + eval/metrics.py UNCHANGED. Plan 03 consumes the finalized ohp_ssl.py.

Reused UNCHANGED (Plan 01 + Phase 3):
- supervised_train.run_supervised_epoch(*, run_name, drive_root, videos_root, seed=42, config=None, resume=True, max_epochs=None, dataset_cls=SquatKIEKFEDataset) -> dict — OHP callers pass dataset_cls=OHPElbowsKneesDataset, videos_root="/content/ohp_videos", run_name="ohp_supervised_v1". SupervisedConfig defaults are the Phase-3 recipe (Adam lr=1e-4, batch 16, 50ep, 8-patience cosine, num_frames=32, crop 112, flip OFF).
- harness.colab.stage_ohp_videos(drive_root, ...) + stage_unlabeled_ohp_videos(drive_root_3001, drive_root_3002, ...) (Plan 01).
- eval/metrics.threshold_sweep / f1_per_error / pr_auc_per_error / confusion_matrix_per_error.

Produced artifact contract (Plan 03/04 input):
- Finalized ohp_ssl.py — _load_trajectory + __getitem__ confirmed on real clips (the SSL pretrain in Plan 03 consumes OHPSSLDataset + build_ssl_loader).
- Drive: My Drive/FitNova/checkpoints/phase06/ohp_supervised_v1/best.pt — the baseline checkpoint + best_thresholds; its test F1 is the SSL-lift control (recorded in the notebook + later in results.pkl).

Probe contract (Task 1 reports, RESEARCH §3 / Open Question 1):
- (1) per-clip JSON BBox structure live (list of frames; [region_0,region_1,region_2]; region_0 = barbell);
- (2) y_center=(y1+y2)/2 from region 0 matches the ReadMe formula;
- (3) trajectory length vs read_video_timestamps frame count for the SAME clip (1:1? or the rescale);
- (4) empty-region-0 frequency across the sampled clips;
- (5) the argMIN-vs-argMAX SIGN resolved by the human via the y_center plot + descent/ascent frame grid.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D5-probe | The trajectory-format + half-cycle-sign probe is Task 1, GATED checkpoint:human-verify, BEFORE ohp_ssl.py is finalized and BEFORE any GPU pretrain (Plan 03). It probes the 5 §3 items + resolves the argMIN sign by a human viewing the y_center plot with the split overlaid + a descent/ascent frame grid. Mirrors Phase 4 Wave-1 Task 1 (which found Squat argMIN + rglob nesting). Do NOT assume OHP equals Squat — confirm live. | CONTEXT D5 + RESEARCH §3 / Open Question 1 |
| D3-baseline | Supervised baseline = R(2+1)D-18 Kinetics-V1 init, end-to-end fine-tune, nn.Linear(512,2) head (NO dropout for the baseline — the Phase-3 recipe), BCEWithLogitsLoss(pos_weight=dataset.pos_weight ~[2.887,1.924]), Adam lr=1e-4 wd=0, CosineAnnealingLR, 50ep/8-patience, batch 16, num_frames=32, crop 112, flip OFF. Via run_supervised_epoch (UNCHANGED) with dataset_cls=OHPElbowsKneesDataset. | CONTEXT D3 + RESEARCH §4 |
| D3-baseline-control | There is NO paper Kinetics baseline for OHP (RESEARCH §1 — Table 4 has no Kinetics row, unlike Squat's Table 2). The measured baseline is the ONLY SSL-lift control; the headline is our baseline -> our MD-SSL -> paper Ours MD (Elbow 0.4552 / Knees 0.8452). Set defense expectations: the baseline is the control, not a paper-comparison number. | RESEARCH §1 / §4 |
| D3-threshold | Per-error independent threshold sweep on val (eval/metrics.threshold_sweep), applied to test. PR-AUC threshold-free. eval/metrics.py reused UNCHANGED (Phase 3 contract). | CONTEXT D3 + RESEARCH §4 |
| D5-finalize | ohp_ssl.py is finalized against the probe: if the probe confirms 1:1 traj<->frame + argMIN (the expected case per RESEARCH §3), _load_trajectory needs only the live-confirmation re-run (the Plan-01 body stands). If the probe reveals a non-1:1 mapping, _load_trajectory adds the recorded rescale (frame = round(traj_idx * n_frames / n_traj)). The sign in __getitem__ stays bottom_is_argmax=False unless the probe contradicts it. | CONTEXT D5 + RESEARCH §3 / A1 |
| D7 | persistent_workers on the loaders (already in _build_dataloaders + build_ssl_loader); map_location='cpu' on resume; metrics_history from latest.txt-pointed checkpoint for viz; .ipynb with Cell A on the FIRST ship; one runnable unit at a time; restart runtime / importlib.reload after git pull; videos_root="/content/ohp_videos" passed explicitly (Pitfall 5). | CONTEXT D7 + RESEARCH §6 |
| D8-VRAM | VRAM re-estimated in-notebook (Step 3) before committing — Phase 3 measured 15.22 GB at batch 16/32-frame; OHP same model + input shape -> same VRAM (RESEARCH §4 / A4). If OHP videos are higher-res, the probe catches it; batch fallback documented. | RESEARCH §4 / A4 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 2/3/4 + D7)

1. Cell A first: from backend.training.aqa.harness import _envinit is the FIRST executable line of Step 0 (F8 — sets CUBLAS_WORKSPACE_CONFIG before any CUDA context).
2. persistent_workers via _build_dataloaders (unchanged) + build_ssl_loader (Plan 01).
3. map_location='cpu' on load_latest_checkpoint at resume (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume (run_supervised_epoch, unchanged).
5. videos_root="/content/ohp_videos" passed explicitly to run_supervised_epoch (Pitfall 5 — the SupervisedConfig/FinetuneConfig default points at /content/squat_videos).
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch.
7. metrics_history loaded from the latest.txt-pointed checkpoint for the training-curve viz (NOT best.pt — [[reference_best_pt_metrics_history_is_stale]]).
8. config_hash via hash_config over the SupervisedConfig recipe keys (Phase 3 D12 key set — recipe change invalidates resume).
9. No AMP (fp32) — Phase 3 contract carried.
10. Restart the Colab runtime / importlib.reload after git pull (a stale baseline loss wastes the run — RESEARCH Pitfall; [[reference_colab_module_reload_after_git_pull]]).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are the trajectory-format correctness (a wrong sign/mapping wastes GPU), the multi-session resume pointer, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate — injection/XSS/authz do not apply to a Colab training notebook.

| Boundary | Description |
|----------|-------------|
| Drive OHP trajectory/video files -> SSL dataset + the probe | Reading the staged OHP unlabeled videos + bar_trajectories_raw JSONs; clip IDs from the dataset release, no path traversal. Staged once per session by stage_unlabeled_ohp_videos (two release folders — Pitfall 4). |
| Reconstructed traj<->frame mapping + half-cycle sign -> the SSL burn (Plan 03) | A wrong live mapping/sign silently produces meaningless triplets across the 15-26h SSL run — the single highest-cost failure in the phase. The gated probe (Task 1) catches it at the cheapest point. |
| In-memory baseline state -> Drive checkpoint (multi-session) | epoch_NNN.pt + best.pt over Drive FUSE; the resume pointer (latest.txt) must survive a disconnect. Phase 2 atomic-write contract mitigates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | OHP traj<->frame mapping + half-cycle sign [ASSUMED until the live probe] — a wrong mapping/sign silently produces meaningless triplets across the 15-26h SSL burn (Plan 03) | mitigate | Task 1 is a BLOCKING checkpoint:human-verify probe on 2-5 real clips: the y_center plot + argMIN split overlay + descent/ascent frame grid + the read_video_timestamps-vs-traj-length mapping; the human confirms the sign + mapping BEFORE ohp_ssl.py is finalized + BEFORE any pretrain. HIGH-cost-if-wrong, caught at the cheapest possible point (RESEARCH §3 / Open Question 1 / Pitfall 1-3). |
| T-06-06 | Integrity | Multi-session resume pointer (latest.txt) corrupted, or the wrong videos_root (Squat clips) silently used | mitigate | run_supervised_epoch uses the Phase 2 atomic-write contract (latest.txt last) + map_location='cpu' resume; videos_root="/content/ohp_videos" passed explicitly (Pitfall 5); Step 1 stage logs the OHP labeled count (2367). Resume verified by a paste-back of "Resumed from epoch_NNN.pt" on the first post-disconnect epoch. MEDIUM — the contract is Phase-2-proven; the OHP-path guard is the new bit. |
| T-06-07 | Denial of service | Disk exhaustion staging 2367 labeled + 5490 unlabeled OHP clips on Colab /content | accept | The 3-layer resume in stage_ohp_videos / stage_unlabeled_ohp_videos handles partial extraction; Step 0 logs the videos.zip sizes + free disk. Colab /content is ~70-100 GB. LOW — probed at stage time, resume-safe. |
| T-06-08 | Integrity | Committing dataset media / weights (the OHP clips, the baseline best.pt ~360 MB) | accept | All media + weights gitignored / Drive-staged (Phase 1-5 contract). This plan commits only the notebook .py/.ipynb + the finalized ohp_ssl.py + the small probe PNG; the best.pt lives on Drive, never git. Stage files by name only. LOW — gitignore contract established. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe (carried from Phase 3, scipy added) is the supply-chain guard; PyAV is the only install, reused from Phase 2/3. No [ASSUMED]/[SUS] packages. |

T-06-05 is the highest-cost threat; the entire sequencing of this plan (probe = Task 1, gated, before any GPU) exists to catch it before the SSL burn. No web-app threats apply.

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Notebook Cell A + Step 0 stage + Step 1 trajectory-format + half-cycle-sign probe (BLOCKING — human confirms sign + mapping before any GPU)</name>
  <files>
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py,
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.ipynb,
    .planning/phases/06-overhead-press/figures/.gitkeep,
    .planning/phases/06-overhead-press/figures/ohp_traj_probe.png
  </files>
  <read_first>
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Cell A bootstrap lines 30-61 + Step 0 env/PyAV/Drive/stage pattern — copy verbatim, change branch comment + swap to OHP staging),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (Step 1 the Squat trajectory probe — the §8 6-item report + the split-overlay + frame-grid + find_peaks pattern to mirror for OHP),
    backend/training/aqa/harness/colab.py (stage_ohp_videos + stage_unlabeled_ohp_videos from Plan 01 — the staging calls; mount_drive),
    backend/training/aqa/datasets/ohp_ssl.py (the Plan-01 _load_trajectory + split_half_cycles — the probe plots the split with bottom_is_argmax=False AND True for the visual comparison),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§3 — the 5 things the probe must confirm: BBox structure, y_center formula, 1:1 mapping, empty-frame frequency, the argMIN sign; Open Question 1+2; Pitfall 1-4),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the OHP notebook Cell A + Step 0 patterns; the importlib.reload note; the two-drive-root staging call)
  </read_first>
  <what-built>
    The Phase 6 baseline notebook scaffold (06_ohp_supervised_baseline.py + .ipynb pair, Cell A baked into the .ipynb per [[feedback_deliver_colab_as_ipynb]] D7) with:
    - Cell A: clone-or-pull the Ibrahim-Shahin1/fitnova repo (branch fresh-start) + sys.path.insert (verbatim from 03/04's Cell A; change only the title comment to OHP).
    - Step 0: from backend.training.aqa.harness import _envinit as the FIRST executable line; PyAV-before-torch install; the Phase 3 dep-version probe (scipy in the list — supply-chain guard); L4 GPU assert (>=22 GB); Drive mount; stage_ohp_videos(DRIVE_ROOT, local_root="/content/ohp_videos") (labeled 2367) AND stage_unlabeled_ohp_videos(DRIVE_ROOT_3001, DRIVE_ROOT_3002, ...) (5490 unlabeled + trajectories — TWO drive roots, Pitfall 4; the user supplies both Drive paths as notebook vars). Probe + log the videos.zip sizes + free /content disk (T-06-07).
    - Step 1 (THE PROBE): ls the staged ohp_trajectories dir; load 2-5 trajectory entries and report (RESEARCH §3): (1) the per-frame BBox structure live ([region_0,region_1,region_2]; region_0 bbox list); (2) y_center=(y1+y2)/2 from region 0 for a sample frame; (3) trajectory length (len(json)) vs read_video_timestamps frame count for the SAME clip ID -> the traj<->frame mapping (1:1 expected per RESEARCH §3); (4) the empty-region-0 frequency across the sampled clips; (5) ID alignment (traj stems match unlabeled clip ids). For 2-5 sample clips: compute the region-0 y_center array via the finalized-or-Plan01 _load_trajectory; plot it; overlay the split point from split_half_cycles with bottom_is_argmax=False AND =True; decode an 8-frame strip of the descent half + the ascent half for each sign; render a frame grid. ALSO run scipy.signal.find_peaks across a sample (or all) trajectories and report the multi-rep frequency (RESEARCH §3 — paper is nominally single-rep). Save the probe figure to .planning/phases/06-overhead-press/figures/ohp_traj_probe.png BEFORE plt.show() with defensive mkdir.
  </what-built>
  <how-to-verify>
    1. Confirm Cell A runs: repo cloned/pulled at HEAD, no ModuleNotFoundError for backend.
    2. Confirm Step 0: scipy + the other deps print versions at/above requirements; GPU is L4 (>=22 GB); Drive mounted; BOTH stage functions report cache-hit or fresh-stage with the expected counts (2367 labeled, 5490 unlabeled); the videos.zip sizes + free disk logged (T-06-07). If stage_unlabeled_ohp_videos raises FileNotFoundError, the two Drive roots (-3-001 / -3-002) are wrong — the user supplies the correct paths (Pitfall 4).
    3. Read the Step 1 probe output: confirm the BBox structure matches RESEARCH §3 (region 0 = barbell), the y_center formula gives sensible pixel values, the traj-length-vs-frame-count mapping is reported (1:1? — RESEARCH A1), and the empty-region-0 + multi-rep frequencies are reported.
    4. THE SIGN DECISION: look at the y_center plot for the 2-5 clips. The split point is overlaid for BOTH argMIN and argMAX. Look at the decoded descent/ascent frame grids. Confirm which sign puts the split at the visual rep turning point (overhead = barbell highest in frame = lowest y pixel) AND makes the "descent" frames show the barbell pressing UP / "ascent" lowering DOWN (or the convention the data shows). Reply with the resolved sign (expected: "bottom_is_argmax=False" per RESEARCH §3).
    5. If the format/mapping is NOT as RESEARCH §3 expects (e.g. non-1:1 mapping, or region 0 is not the barbell), describe what it actually is so Task 2 corrects _load_trajectory.
  </how-to-verify>
  <action>
    This is a BLOCKING human-verify probe — the single highest-leverage sequencing decision in the phase (RESEARCH §3 Open Question 1). The executor lands Cell A + Step 0 (the two-drive-root staging) + Step 1 (the probe) in the notebook .py AND ships the .ipynb pair (Cell A baked in — D7 / [[feedback_deliver_colab_as_ipynb]]). The user runs Cell A + Step 0 + Step 1 on the fresh L4 Colab (prompt the user FIRST — this is the first GPU session, [[feedback_heavy_training_new_notebook]]) and pastes back: the probe report (the §3 items + the multi-rep count), the ohp_traj_probe.png figure (or screenshot), and the RESOLVED SIGN + the confirmed traj<->frame mapping. The executor does NOT finalize ohp_ssl.py's _load_trajectory until this paste-back confirms the format + sign + mapping (Task 2 inherits all three). One runnable unit; this is the [[feedback_interactive_execution]] "understand the data before building" gate. Atomic commit: feat(06): notebook Cell A + Step 0 OHP staging + Step 1 trajectory probe (Task 1, D5 / RESEARCH §3).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/06_ohp_supervised_baseline.py').read()); print('parse ok')" && grep -q "_envinit" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "stage_ohp_videos" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "stage_unlabeled_ohp_videos" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "find_peaks" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook .py parses; Cell A + Step 0 + Step 1 present; .ipynb pair shipped (Cell A baked in)
    - Step 0 calls stage_ohp_videos (labeled) AND stage_unlabeled_ohp_videos (two drive roots — Pitfall 4); logs both counts + free disk
    - Step 1 reports the 5 §3 items + the multi-rep count; saves ohp_traj_probe.png before plt.show()
    - User pastes back the probe report + the figure + the RESOLVED SIGN (expected bottom_is_argmax=False) + the confirmed traj<->frame mapping (expected 1:1)
    - NO GPU pretraining yet; ohp_ssl.py NOT finalized until the paste-back confirms format + sign + mapping
  </acceptance_criteria>
  <dependencies>Plan 01 (ohp_ssl.py + stage_ohp_videos/stage_unlabeled_ohp_videos exist)</dependencies>
  <done>User pastes back the probe report + ohp_traj_probe.png + the resolved bottom_is_argmax sign + the confirmed traj<->frame mapping; Task 2 (finalize ohp_ssl.py) is authorized with the confirmed sign/format/mapping. NO GPU pretraining yet.</done>
  <resume-signal>Reply with the resolved sign ("bottom_is_argmax=False" confirmed, or "...=True" if the data contradicts the §3 inference) + the traj<->frame mapping (1:1, or the rescale formula) + the multi-rep count. Or paste the probe output and ask for guidance on the sign.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Finalize ohp_ssl.py against the probe + Step 2 SSL dataset smoke (triplet shapes + sanity grid)</name>
  <files>
    backend/training/aqa/datasets/ohp_ssl.py,
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py
  </files>
  <read_first>
    backend/training/aqa/datasets/ohp_ssl.py (the Plan-01 _load_trajectory + __getitem__ + build_ssl_loader — finalize/correct against the Task-1-confirmed mapping),
    backend/training/aqa/datasets/squat_ssl.py (the analog — the __getitem__ recipe + the build_ssl_loader persistent_workers guard already mirrored in Plan 01),
    backend/training/aqa/notebooks/04_squat_md_ssl.py (Step 2/3 the Squat SSL dataset-smoke + sanity-grid pattern — counts + batch shape + the decode/denorm-for-viz idiom),
    backend/training/aqa/datasets/transforms.py (KINETICS_MEAN/STD lines 24-25 — for denormalizing the sanity grid),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§3 — the traj<->frame mapping + A1; the recommended _load_trajectory; Pitfall 1-3),
    .planning/phases/06-overhead-press/06-PATTERNS.md (the ohp_ssl section + the notebook save-before-show + mkdir defensive pattern)
  </read_first>
  <action>
    Finalize ohp_ssl.py using the Task-1-confirmed format/mapping/sign:
    - If the probe confirmed 1:1 traj<->frame + argMIN (the RESEARCH §3 expected case): the Plan-01 _load_trajectory body stands; add an inline comment recording the Task-1 confirmation (the trajectory index space IS the video-frame space; bottom_is_argmax=False confirmed at the probe on N clips). Re-confirm __getitem__'s split_half_cycles(bottom_is_argmax=False) call.
    - If the probe found a non-1:1 mapping: add the recorded rescale to _load_trajectory (frame = round(traj_idx * n_video_frames / n_traj_samples)) and document it inline.
    - If the probe contradicted the argMIN sign: flip __getitem__'s bottom_is_argmax. (Expected: no flip.)
    - If the probe found a non-trivial multi-rep tail: add first-rep extraction (find_peaks prominence + take the first descent+ascent), mirroring the deferred Squat handling; else note inline that the global-extremum split is sufficient.

    Then fill in Step 2 of the notebook (SSL dataset smoke — the live re-confirmation): import OHPSSLDataset + build_ssl_loader + MDConfig (MDConfig from md_pretrain, reused); ds = OHPSSLDataset(videos_root="/content/ohp_unlabeled_videos", trajectories_root="/content/ohp_trajectories", frames_per_half=16, crop_size=112); print len(ds) (expect ~5490); loader = build_ssl_loader(ds, MDConfig(), seed=42); batch = next(iter(loader)); assert each of batch anchor/positive/negative has shape (batch_size, 3, 16, 112, 112) float32; print the three shapes. Sanity grid: take batch["anchor"][0] + batch["negative"][0], denormalize (frame*KINETICS_STD + KINETICS_MEAN, clamp [0,1]), render an 8-frame strip of each so the user visually confirms the descent (anchor) + ascent (negative) split on a REAL batch (the second confirmation of the Task-1 sign, through the full dataset). Save to .planning/phases/06-overhead-press/figures/ohp_ssl_triplet_sanity.png BEFORE plt.show() with defensive mkdir.

    Single runnable unit (LOCAL then Colab): user pastes back (1) AST-parse of ohp_ssl.py prints "parse ok" + python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py -x -q (the Plan-01 synthetic tests STILL green — the finalization did not regress them); (2) in Colab after git pull + importlib.reload: len(ds) ~5490 + ds[0]/one batch shapes [3,16,112,112] float32 + the sanity grid.

    Atomic commit: feat(06): finalize ohp_ssl.py against the probe + Step 2 SSL smoke (Task 2, D5 / RESEARCH §3).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/datasets/ohp_ssl.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_ohp_ssl.py -x -q && grep -q "OHPSSLDataset" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "ohp_ssl_triplet_sanity" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - ohp_ssl.py parses; _load_trajectory finalized per the Task-1 mapping (inline comment records the confirmation); __getitem__ uses the Task-1-resolved bottom_is_argmax sign
    - The Plan-01 synthetic tests (test_trajectory_load, test_split_half_cycles_ohp) STILL pass — the finalization did not regress them
    - Step 2 present; instantiates OHPSSLDataset + build_ssl_loader; asserts the three triplet shapes
    - Runnable unit (Colab): user pastes back len(ds) ~5490, three shapes (B,3,16,112,112) float32, and the sanity grid (anchor=descent, negative=ascent visually correct)
  </acceptance_criteria>
  <dependencies>Task 1 (the gated probe — confirms format + sign + mapping)</dependencies>
  <done>OHPSSLDataset is finalized against the confirmed real-data format; the triplet pipeline is re-confirmed end-to-end on a real Colab batch; Plan 03's SSL trainer is authorized to consume this loader</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Notebook Step 3 + Step 4 — model+VRAM probe + epoch-0 timing gate (BLOCKING — user authorizes the baseline run)</name>
  <files>backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</files>
  <read_first>
    backend/training/aqa/harness/supervised_train.py (build_model + run_supervised_epoch — Step 3 builds the model, Step 4 calls run_supervised_epoch with max_epochs=1, dataset_cls=OHPElbowsKneesDataset),
    backend/training/aqa/datasets/ohp.py (OHPElbowsKneesDataset — Step 3/4 inject it),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 2 VRAM probe + Step 3 epoch-0 timing gate patterns — the max_memory_allocated idiom + estimated_total_h + the checkpoint round-trip verify),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — VRAM re-estimate: Phase 3 measured 15.22 GB at batch 16/32-frame, OHP same model+shape -> same VRAM; A4; the wall-time anchor ~6.1 min/epoch on L4),
    .planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md (the measured 15.22 GB at batch 16 + the timing-probe-was-conservative finding — the calibration anchor)
  </read_first>
  <decision>
    Should we proceed with the full OHP baseline at batch_size=16 with the locked SupervisedConfig (Phase-3 recipe), or downscope (smaller batch / fewer epochs) before committing to the run?
  </decision>
  <context>
    The OHP baseline reuses the Phase-3 recipe + model exactly (RESEARCH §4 — same R(2+1)D-18, same 32-frame/112 input, so the same VRAM envelope). Two probe cells:

    Step 3 (model + VRAM probe): build_model() to cuda; pull one OHP batch from a dataset_cls=OHPElbowsKneesDataset loader (via _build_dataloaders or a direct OHPElbowsKneesDataset); run a forward + backward (BCEWithLogitsLoss(pos_weight) + loss.backward()); print torch.cuda.max_memory_allocated()/1e9 (peak backward VRAM at batch 16). Assert it fits L4 (< 22 GB). ALSO assert the model head is nn.Linear(512,2) and a forward on zeros[2,3,32,112,112] gives [2,2] (the OHP joint head). If OOM or >~20 GB, the user reports and the decision offers a smaller batch.

    Step 4 (epoch-0 timing gate): run_supervised_epoch(run_name="ohp_supervised_v1_timing", drive_root=MYDRIVE, videos_root="/content/ohp_videos", seed=42, config=SupervisedConfig(), resume=False, max_epochs=1, dataset_cls=OHPElbowsKneesDataset) — one fresh epoch under the production code path on the 1582-clip OHP train split; paste back epoch_0 wall-time (from metrics_history), estimated_total_h = epoch_0_time * config.max_epochs / 3600, the epoch-0 train loss + val macro-F1 (sanity: loss finite, val F1 not degenerate), GPU util. Also verify the checkpoint round-trip (T-06-06): os.path.getsize(epoch_000.pt), torch.load(map_location='cpu') round-trip, assert the Phase-3 payload keys + code_version "phase03-supervised-baseline". The timing run lands in a separate _timing run dir (pruned when the production run starts under ohp_supervised_v1).

    Decision criteria: estimated_total_h <= 8 and VRAM fits at batch 16 -> option-a (proceed batch 16). estimated_total_h > 8 OR VRAM tight -> option-b (smaller batch / fewer epochs).
  </context>
  <options>
    <option id="option-a">
      <name>Proceed at batch 16, locked SupervisedConfig (the Phase-3 recipe, 50ep + 8-patience)</name>
      <pros>Identical recipe to the proven Squat baseline (clean cross-exercise comparison); Phase 3 was ~6.1 min/epoch on L4 so ~5h for 50 epochs (early-stop bounds it lower); VRAM fits (Phase 3 measured 15.22 GB).</pros>
      <cons>If OHP videos are higher-res than Squat, VRAM/epoch-time rise — caught by the probe; batch fallback available.</cons>
    </option>
    <option id="option-b">
      <name>Drop batch to 8 (or fewer epochs) if VRAM tight or epoch-0 slow</name>
      <pros>Lower VRAM headroom margin; choose if Step 3 OOMs or Step 4 estimated_total_h > 8.</pros>
      <cons>Slightly slower throughput / a recipe deviation from Squat — document it in the SUMMARY.</cons>
    </option>
  </options>
  <action>This is a BLOCKING decision checkpoint. The executor lands Step 3 (model+VRAM probe) + Step 4 (epoch-0 timing gate) in the notebook. The user runs both on the L4 and pastes back: the head-shape + peak backward VRAM at batch 16 (Step 3), epoch_0 wall-time + estimated_total_h + epoch-0 train loss + val macro-F1 + GPU util + the checkpoint round-trip verify (Step 4). The user replies option-a / option-b per the criteria. No code change by the executor unless option-b (set SupervisedConfig(batch_size=8) or a lower max_epochs). Atomic commit: feat(06): notebook Step 3 VRAM probe + Step 4 epoch-0 timing gate (Task 3, D8-VRAM / RESEARCH §4).</action>
  <verify>
    <automated>grep -q "max_memory_allocated" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "estimated_total_h" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "ohp_supervised_v1_timing" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "dataset_cls=OHPElbowsKneesDataset" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</automated>
  </verify>
  <done>User replies option-a / option-b; Task 4 inherits the chosen batch/epochs; VRAM fit + epoch-0 timing + the OHP joint-head shape + checkpoint round-trip all confirmed before the baseline run is authorized</done>
  <resume-signal>Type "option-a" or "option-b". Or paste back the VRAM + epoch_0 time + estimated_total_h + the head shape and ask for guidance.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Notebook Step 5 — full OHP baseline training (multi-session resume) + epoch-5 convergence check</name>
  <files>backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</files>
  <read_first>
    backend/training/aqa/harness/supervised_train.py (run_supervised_epoch — Step 5 calls it with resume=True, dataset_cls=OHPElbowsKneesDataset),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 4 full-training-run pattern — the resume=True single-cell-is-the-whole-run idiom + per-epoch paste-back + disconnect/resume recovery),
    backend/training/aqa/harness/colab.py (load_latest_checkpoint — the "Resumed from epoch_NNN.pt" log line the resume paste-back verifies),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§4 — the baseline recipe; the wall-time anchor; A4),
    .planning/phases/06-overhead-press/06-VALIDATION.md (OHP-01-e — baseline val macro-F1 improves over 5 epochs)
  </read_first>
  <action>
    Fill in Step 5: the full OHP baseline run. config = SupervisedConfig() (or batch 8 / fewer epochs per Task 3's decision); RUN_NAME = "ohp_supervised_v1"; result = run_supervised_epoch(run_name=RUN_NAME, drive_root=MYDRIVE, videos_root="/content/ohp_videos", seed=42, config=config, resume=True, max_epochs=config.max_epochs, dataset_cls=OHPElbowsKneesDataset). resume=True so a mid-run Colab disconnect is recoverable (re-run this cell after restart -> resumes from latest.txt). The trainer checkpoints every epoch + writes best.pt on val-macro-F1 improvement, so this single cell IS the whole baseline run. Print final epoch reached, best.pt path, best val macro-F1.

    This is one runnable unit but ASYNC + may span sessions — acceptance is a STAGED paste-back per [[feedback_interactive_execution]]: (a) FIRST: the epoch-0..5 paste-back — val macro-F1 strictly improving over the first 5 epochs (OHP-01-e convergence signal: loss decreasing, val F1 rising, the OHP joint head learning both Elbows + Knees); (b) on any mid-run disconnect: re-run Step 0 + Step 5 -> the first post-resume epoch logs "Resumed from epoch_NNN.pt" (the disconnect-safety proof); (c) FINAL: the run completes (early-stop or max_epochs) — paste back the final epoch + best val macro-F1 + the best.pt path on Drive. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Step 6 until the training completes + the user pastes back the convergence + final numbers.

    Atomic commit: feat(06): notebook Step 5 full OHP baseline run (Task 4, D3-baseline / OHP-01-e).
  </action>
  <verify>
    <automated>grep -q "run_supervised_epoch" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "ohp_supervised_v1" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "resume=True" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 present; run_supervised_epoch(run_name="ohp_supervised_v1", videos_root="/content/ohp_videos", dataset_cls=OHPElbowsKneesDataset, resume=True)
    - Runnable unit (Colab, ~5h, possibly multi-session): epoch-0..5 paste-back shows val macro-F1 strictly improving (OHP-01-e); on disconnect, the resume paste-back logs "Resumed from epoch_NNN.pt"; FINAL paste-back = final epoch + best val macro-F1 + best.pt path
    - videos_root is "/content/ohp_videos" (Pitfall 5 — not the Squat default); the run_dir is phase06/ohp_supervised_v1
    - best.pt on Drive at phase06/ohp_supervised_v1/best.pt; per-epoch atomic checkpoint + prune
  </acceptance_criteria>
  <dependencies>Task 3 (VRAM/timing gate — authorizes the run + the batch/epochs)</dependencies>
  <done>The OHP supervised baseline trains to convergence on the official 1582-clip train split via the UNCHANGED Phase-3 trainer (dataset_cls=OHPElbowsKneesDataset); val macro-F1 improves (OHP-01-e); the ohp_supervised_v1 best.pt is on Drive — the SSL-lift control checkpoint</done>
</task>

<task type="auto">
  <name>Task 5: Notebook Step 6 + Step 7 — val threshold sweep + test eval (the SSL-lift control F1)</name>
  <files>backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</files>
  <read_first>
    backend/training/aqa/eval/metrics.py (threshold_sweep + f1_per_error + pr_auc_per_error + confusion_matrix_per_error — REUSED UNCHANGED),
    backend/training/aqa/harness/supervised_train.py (build_model — to reload the best.pt for the val/test passes),
    backend/training/aqa/notebooks/03_squat_supervised_baseline.py (Step 5 threshold-sweep + Step 6 test-eval patterns — gather val scores, threshold_sweep per head, apply to test, the summary table),
    .planning/phases/06-overhead-press/06-RESEARCH.md (§1 — NO paper Kinetics baseline for OHP; the headline is baseline (control) -> MD-SSL -> paper Ours MD 0.4552/0.8452; §4 the threshold protocol),
    .planning/phases/06-overhead-press/06-CONTEXT.md (D3 — per-error threshold sweep on val, applied to test; the baseline is the SSL-lift control)
  </read_first>
  <action>
    Append Step 6 (val threshold sweep) + Step 7 (test eval). Step 6: load ohp_supervised_v1/best.pt (map_location='cuda'); model = build_model(); model.load_state_dict(ckpt["model_state_dict"]); model.eval(); run a val pass (gather per-clip sigmoid scores on CPU, no per-batch F1) on the 339-clip OHP val split via a dataset_cls=OHPElbowsKneesDataset val loader; t_elbows, f1_elbows_val = threshold_sweep(val_labels[:,0], val_scores[:,0]); t_knees, f1_knees_val = threshold_sweep(val_labels[:,1], val_scores[:,1]) (per-error independent sweep, D3). Print the two thresholds + val F1.

    Step 7: run a test pass on the 339-clip OHP test split; apply the val-tuned thresholds: test_pred_elbows = (test_scores[:,0] > t_elbows).astype(int); same Knees; test_f1_elbows = f1_per_error(test_labels[:,0], test_pred_elbows); test_f1_knees similarly; test_macro = (test_f1_elbows + test_f1_knees)/2 (computed in code); test_pr_auc_elbows/knees = pr_auc_per_error(...); test_cm_elbows/knees = confusion_matrix_per_error(...). Print the summary table: our baseline Elbows/Knees/macro F1 + PR-AUC, framed as the SSL-LIFT CONTROL (RESEARCH §1 — there is NO paper Kinetics baseline for OHP, so this is the only control; the paper Ours MD target 0.4552 Elbow / 0.8452 Knees is the Plan-04 fine-tune target, NOT a baseline comparison). Store the baseline numbers in the notebook namespace + note them for the Plan 04 results.pkl (the baseline row of the comparison). Do NOT write results.pkl here — that is Plan 04's source of truth (the baseline numbers get folded in there).

    Single runnable unit (Colab) — user pastes back: the two val-tuned thresholds + val F1 (Step 6), and the test summary table (Elbows/Knees/macro F1 + PR-AUC) framed as the SSL-lift control (Step 7).

    Atomic commit: feat(06): notebook Step 6 threshold sweep + Step 7 test eval — baseline control F1 (Task 5, D3 / RESEARCH §1).
  </action>
  <verify>
    <automated>grep -q "threshold_sweep" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "f1_per_error" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py && grep -q "pr_auc_per_error" backend/training/aqa/notebooks/06_ohp_supervised_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 6 + Step 7 present; per-error threshold sweep on the OHP val split (threshold_sweep), applied to the OHP test split; eval/metrics.py reused UNCHANGED
    - test_macro computed in code as (test_f1_elbows + test_f1_knees)/2 (NOT hand-typed)
    - The baseline F1 is framed as the SSL-LIFT CONTROL (no paper Kinetics row for OHP — RESEARCH §1); the paper Ours MD 0.4552/0.8452 is noted as the Plan-04 target, not a baseline comparison
    - Runnable unit (Colab): user pastes back the two val-tuned thresholds + val F1 + the test summary table (Elbows/Knees/macro F1 + PR-AUC)
  </acceptance_criteria>
  <dependencies>Task 4 (ohp_supervised_v1/best.pt on Drive)</dependencies>
  <done>The OHP supervised baseline F1 per error is reported on the official test split at val-tuned thresholds — the SSL-lift control the Plan 04 MD-SSL ensemble is measured against; the baseline numbers are recorded for the Plan 04 comparison + results.pkl</done>
</task>

</tasks>

<verification>
## Mapping to Phase 6 ROADMAP Success Criterion + OHP-01

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (partial): OHP baseline trains on the official OHP splits | Tasks 3 (gate), 4 (run) | Task 4 epoch-0..5 paste-back: val macro-F1 improving on the 1582-clip train split (OHP-01-e); best.pt on Drive |
| SC2 (partial): F1 per error reported on the official OHP test split | Task 5 | Task 5 test summary table: baseline Elbows/Knees/macro F1 at val-tuned thresholds (the SSL-lift control) |
| The trajectory format/sign gate (before the SSL burn) | Task 1 (blocking) + Task 2 (finalize) | Task 1 probe paste-back resolves the sign + mapping; Task 2 finalizes ohp_ssl.py + the live triplet smoke |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| Trajectory format + half-cycle sign (manual probe) | Task 1 | manual (gated Colab) | 1 |
| OHP-01-e (baseline val macro-F1 improves over 5 epochs) | Task 4 | integration (Colab paste-back) | 1 |
| Baseline test F1 per error (SSL-lift control) | Task 5 | integration (Colab paste-back) | 1 |

(OHP-01-f SSL linear-probe is Plan 03; OHP-01-g final ensemble test F1 is Plan 04.)
</verification>

<success_criteria>
- All 5 tasks complete with paste-back acceptance from the user on the L4 Colab
- Task 1 (BLOCKING probe): the OHP trajectory BBox format + the traj<->frame mapping + the argMIN sign confirmed live on 2-5 real clips BEFORE ohp_ssl.py is finalized + BEFORE any SSL GPU burn; ohp_traj_probe.png pasted back
- ohp_ssl.py finalized against the probe (the Plan-01 synthetic tests still green); the live triplet smoke confirms [3,16,112,112] shapes + the descent/ascent sanity grid
- The OHP supervised baseline trains to convergence (val macro-F1 improving, OHP-01-e) via the UNCHANGED supervised_train.run_supervised_epoch (dataset_cls=OHPElbowsKneesDataset); ohp_supervised_v1/best.pt on Drive
- Baseline F1 per error reported on the official 339-clip OHP test split at val-tuned thresholds — the SSL-lift CONTROL (no paper Kinetics row for OHP — RESEARCH §1); eval/metrics.py reused unchanged
- Disconnect-safe: the baseline run resumes cleanly (the resume paste-back logs "Resumed from epoch_NNN.pt"); videos_root="/content/ohp_videos" explicit (Pitfall 5)
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D7); every D3/D5 decision carries an inline citation
- One accurate threat_model: offline pipeline, no high-severity threats; T-06-05 (the trajectory sign/mapping) is the gated probe; T-06-SC accept (zero new installs)
</success_criteria>

<output>
After Task 5 (baseline test F1 recorded + ohp_ssl.py finalized), this plan is complete. Plan 03 (Wave 3) runs the MD-SSL pretrain on the 5,490 unlabeled OHP clips (the finalized OHPSSLDataset) -> backbone.pt. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 03 once Task 5's baseline numbers + the finalized ohp_ssl.py are confirmed. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 6 Plan 02 written 2026-05-27. Wave 1: the gated trajectory probe (Task 1, blocking human-verify — the highest-leverage gate, mirrors Phase 4 Wave-1 Task 1) + the supervised baseline (the SSL-lift control). 5 tasks. The probe confirms the live BBox format + traj<->frame mapping + the argMIN sign on real OHP clips BEFORE ohp_ssl.py is finalized + BEFORE the SSL GPU burn (Plan 03) — Plan 01 implemented _load_trajectory against the offline structure + synthetic tests, but the live 1:1 alignment is [ASSUMED until probed] (RESEARCH A1). The baseline reuses supervised_train.run_supervised_epoch UNCHANGED (the Plan-01 dataset_cls seam) + eval/metrics.py UNCHANGED — the only new code is the notebook + the ohp_ssl.py finalization.

Key framing: there is NO paper Kinetics baseline for OHP (RESEARCH §1 — Table 4 has no Kinetics row). The measured baseline is the ONLY SSL-lift control; the paper Ours MD (Elbow 0.4552 / Knees 0.8452) is the Plan-04 fine-tune target, not a baseline comparison. Set defense expectations accordingly.

Pitfall 5 honored: videos_root="/content/ohp_videos" passed explicitly everywhere (the config default points at /content/squat_videos). Pitfall 4: stage_unlabeled_ohp_videos takes two Drive roots (-3-001 traj / -3-002 videos).

Security: accurate. Offline pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-06-05 (the trajectory sign/mapping — the gated probe, highest cost-if-wrong), T-06-06 (resume pointer + the OHP-path guard), T-06-07/08 accept (disk + no-media-committed), T-06-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
