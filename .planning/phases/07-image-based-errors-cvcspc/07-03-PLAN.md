---
phase: 07-image-based-errors-cvcspc
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py
  - backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.ipynb
autonomous: false
requirements: [IMG-01]
must_haves:
  truths:
    - "A Step-0 frame-extraction cell decodes the 4,970 unlabeled Squat mp4s -> per-clip JPEG frame dirs at /content/squat_ssl_frames/{video_id}/frame_{:06d}.jpg via extract_frames_for_ssl (cv2 decode, skip-existing, tqdm), the official traj_nan.json is STAGED alongside the trajectories (the NaN-trajectory exclusion list, CITED dataloader.py:67-68), AND a human confirms on 2-5 real clips that the bar-trajectory format is live (the trajectory length vs the extracted-frame count for the same clip is reported — 1:1 or the recorded mapping) and the phase-matched triplet selection looks correct on a y_center/phase plot + a decoded anchor/positive/negative frame grid — BEFORE the SSL pretrain (D2a / RESEARCH §SSL Dataloader Open Q)"
    - "The faithful CVCSPC SSL pretrain runs on the unlabeled Squat frame dirs + bar-trajectory JSONs (with the traj_nan.json clips EXCLUDED) via the Plan-01 cvcspc_pretrain.run_cvcspc_pretrain_epoch (the Plan-01 ShallowSquatSSLDataset, the 3-term loss, AdamW lr=1e-4, batch 25, the IMPLEMENTED phase-gap anneal 30°-> per train_test.py:172-179) and converges: SSL triplet loss decreases over the first epochs AND the triplet-accuracy (fraction with AP<AN) rises above 50% chance (IMG-01 / RESEARCH §SSL Recipe)"
    - "The pretrain is disconnect-safe across the multi-session run: it checkpoints every epoch to My Drive/FitNova/checkpoints/phase07/shallow_squat_cvcspc_v1/ via the Phase-2 atomic-write contract, resumes from latest.txt with map_location='cpu' (re-applying the annealed phase_gap from the resumed epoch), and the backbone.pt (triplet-accuracy-best epoch) write passes update_latest=False so the epoch-resume pointer is never clobbered"
    - "A backbone.pt exists on Drive at phase07/shallow_squat_cvcspc_v1/backbone.pt containing backbone_state_dict (the triplet-accuracy-best epoch's ResNet-18 weights, fc=Identity) — the fork point Plan 04 fine-tunes the multiple seeds from"
    - "The frame-extraction is disconnect-safe (skip-existing: a {video_id}/ dir that already has frames is skipped) and the trajectory JSONs + traj_nan.json are the Phase-4 SSL set staged via stage_unlabeled_squat_videos (the unlabeled Squat videos.zip + bar_trajectories + traj_nan.json) — NO BarbellRow, NO labeled crops in the SSL training set"
  artifacts:
    - path: "backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py"
      provides: "Phase 7 CVCSPC SSL pretrain jupytext notebook — Cell A + Step 0 stage(unlabeled videos+traj+traj_nan) + frame-extract + the trajectory/phase-matching PROBE (BLOCKING human-verify) + Step 1 SSL dataset smoke (triplet shapes + sanity grid + traj_nan exclusion count) + Step 2 VRAM probe + Step 3 epoch-0 timing gate (BLOCKING decision) + Step 4 full SSL pretrain (multi-session resume) with the triplet-accuracy monitor + the phase-gap anneal. The fine-tune notebook (11) is Plan 04."
      contains: "run_cvcspc_pretrain_epoch"
    - path: "backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D6)"
      contains: "cvcspc"
  key_links:
    - from: "backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py"
      to: "backend/training/aqa/harness/cvcspc_pretrain.run_cvcspc_pretrain_epoch (UNCHANGED, with traj_nan_path threaded) + datasets.cvcspc_ssl.ShallowSquatSSLDataset (Plan 01)"
      via: "notebook cell imports"
      pattern: "run_cvcspc_pretrain_epoch"
    - from: "backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py"
      to: "backend/training/aqa/harness/colab.{stage_unlabeled_squat_videos, extract_frames_for_ssl} (Plan 01 / reused) + the staged traj_nan.json"
      via: "notebook cell imports — Step 0 stages the unlabeled videos+traj+traj_nan then frame-extracts"
      pattern: "extract_frames_for_ssl"
    - from: "backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py"
      to: "backbone.pt at phase07/shallow_squat_cvcspc_v1/backbone.pt (the Plan 04 fork point)"
      via: "run_cvcspc_pretrain_epoch writes backbone.pt on triplet-accuracy improvement (update_latest=False)"
      pattern: "backbone.pt"
---

<objective>
Run the faithful CVCSPC pose-contrastive SSL pretraining of a ResNet-18 backbone on the 4,970 unlabeled Squat clips + bar-trajectory JSONs (IMG-01), producing a backbone.pt (the triplet-accuracy-best epoch) that Plan 04 fine-tunes the multiple seeds from. This is Wave 2 — it depends only on the Plan-01 Wave-0 scaffold (the CVCSPC dataset + the NEW self-contained cvcspc_pretrain trainer + the colab frame-extraction), so it can run in PARALLEL with the Plan-02 supervised baseline (different checkpoint dirs, no file overlap). The pretext is the paper's phase-matched cross-rep contrast with the 3-term distance-ratio loss, run via the Plan-01 cvcspc_pretrain.run_cvcspc_pretrain_epoch (a NEW module that mirrors md_pretrain's contract but does NOT import its R(2+1)D epoch loop) on the Plan-01 ShallowSquatSSLDataset. A Step-0 frame-extraction (D2a) + the traj_nan.json exclusion staging + a human probe of the trajectory format + the phase-matching gate the SSL burn (RESEARCH §SSL Dataloader Open Q — the live frame layout + the traj<->frame mapping are confirmed before the GPU compute).

Purpose: IMG-01 (the CVCSPC ResNet-18 image pipeline, self-supervised half) + ROADMAP Phase 7 success criterion 1 ("a CVCSPC ResNet-18 image pipeline trains baseline AND self-supervised models"). The decisive RESEARCH finding (07-RESEARCH §CVCSPC-Standalone Verdict): CVCSPC is FEASIBLE on the BackSquat unlabeled set ALONE — the paper pretrained CVCSPC on the unlabeled BackSquat set, then fine-tuned on the labeled Shallow-Squat crops; BarbellRow is NOT required. The unlabeled Squat videos.zip (4,970 mp4s) + bar_trajectories + traj_nan.json (the Phase-4 SSL set) are on disk/Drive. The ONE caveat: the dataloader expects per-video frame dirs, not mp4s — hence the Step-0 frame-extraction. The official dataloader.py:67-68 also excludes the traj_nan.json clips (NaN bar-trajectories) before phase-matching — the Plan-01 ShallowSquatSSLDataset applies that exclusion, so traj_nan.json must be staged. The triplet-accuracy monitor (AP<AN, CITED train_test.py:130-134) is the cheap convergence signal before fine-tune.

Output: the Phase 7 CVCSPC SSL notebook (Cell A + Step 0 stage + frame-extract + traj_nan staging + the trajectory/phase probe + the SSL dataset smoke + VRAM/timing gates + the full pretrain with the triplet-accuracy monitor + the phase-gap anneal) delivered as paired .py + .ipynb; backbone.pt + per-epoch checkpoints on Drive at phase07/shallow_squat_cvcspc_v1/.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on a FRESH L4 Colab notebook ([[feedback_heavy_training_new_notebook]] — prompt the user before the SSL run). Each task is ONE runnable unit; the user runs it and pastes output back. NEVER pre-write task N+1 before task N's paste-back. The SSL pretrain may span multiple Colab sessions — its checkpoint/resume must be bulletproof ([[feedback_notebook_disconnect_safe]]). After any git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]) — doubly important here, a stale loss wastes compute. tqdm/in-loop progress on any cell >2 min (the frame-extract + every SSL epoch) ([[feedback_progress_bars_required]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md
@.planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md
@.planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md
@.planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md
@.planning/phases/07-image-based-errors-cvcspc/07-01-PLAN.md
@.planning/phases/06-overhead-press/06-03-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-02-SUMMARY.md
@backend/training/aqa/datasets/cvcspc_ssl.py
@backend/training/aqa/harness/cvcspc_pretrain.py
@backend/training/aqa/harness/colab.py
@Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/dataloader.py
@Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/train_test.py
@backend/training/aqa/notebooks/06_ohp_supervised_baseline.py
@backend/training/aqa/notebooks/07_ohp_md_ssl.py
@CLAUDE.md

<interfaces>
This plan WIRES the Plan-01 cvcspc_pretrain.run_cvcspc_pretrain_epoch (a NEW self-contained CVCSPC SSL trainer, NOT a reuse of md_pretrain's R(2+1)D loop) on the Plan-01 ShallowSquatSSLDataset — NO new production logic. It writes only notebook cells. Plan 04 consumes the produced backbone.pt.

Reused UNCHANGED (Plan 01):
- cvcspc_pretrain.run_cvcspc_pretrain_epoch(*, run_name, drive_root="/content/drive/MyDrive", frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=None, seed=42, config=None, resume=True, max_epochs=None, ssl_dataset_cls=ShallowSquatSSLDataset, checkpoint_phase="phase07") -> {epoch, metrics_history, triplet_acc_history, backbone_path, checkpoint_path, config_hash} — for the SSL run: run_name="shallow_squat_cvcspc_v1", traj_nan_path pointing at the staged traj_nan.json. The 3-branch triplet forward, AdamW, cosine, per-epoch triplet-accuracy monitor, backbone.pt-best write (update_latest=False), the phase-gap anneal are ALL implemented in Plan 01 + unchanged here.
- cvcspc_pretrain.CVCSPCConfig — the recipe defaults (lr=1e-4 wd=0, batch_size=25, max_epochs=100, projector 512->128->128 no-BN [config knobs], phase_gap_start=30, phase_gap_decay_every=4, mask_prob=0.5/0.4-0.5, triplet_acc_cadence=5). The masking aug + the phase signal + the traj_nan exclusion are wired in ShallowSquatSSLDataset (Plan 01), not CVCSPCConfig.
- cvcspc_pretrain.build_cvcspc_model + cvcspc_triplet_loss + _triplet_accuracy — used by the VRAM probe + the run loop.
- datasets.cvcspc_ssl.ShallowSquatSSLDataset(*, frames_root, trajectories_root, traj_nan_path=None, ...) + build_cvcspc_loader (Plan 01, synthetic-tested incl. the traj_nan exclusion + the degenerate-trajectory guard).
- harness.colab.stage_unlabeled_squat_videos(drive_root, ...) -> (videos_root, traj_root) (the Phase-4 SSL set — UNCHANGED; it also stages traj_nan.json if present in the release) + extract_frames_for_ssl(videos_root, frames_root, *, skip_existing=True) -> int (Plan 01).

Produced artifact contract (Plan 04 input):
- Drive path: My Drive/FitNova/checkpoints/phase07/shallow_squat_cvcspc_v1/backbone.pt
- backbone.pt payload: backbone_state_dict (ResNet-18 with fc=Identity, the triplet-accuracy-best epoch), projector_state_dict, config_hash, config_repr, code_version "phase07-cvcspc-pretrain", triplet_acc_history, metrics_history.
- Plan 04's fine-tune model builder loads ONLY backbone_state_dict via load_state_dict(strict=False) into resnet18(weights=None) + a fresh nn.Linear(512,1) head.

Probe contract (Task 1 reports, RESEARCH §SSL Dataloader Open Q / Pitfall 2):
- (1) extract_frames_for_ssl produced {video_id}/frame_{:06d}.jpg dirs; the per-clip frame count for 2-5 sample clips;
- (2) the trajectory length (len(json)) vs the extracted-frame count for the SAME clip -> the traj<->frame mapping (1:1 expected — the trajectory is 1:1 with video frames);
- (3) traj_nan.json is staged + loaded; the dataset reports its clip count AFTER the traj_nan exclusion (frame_ids ∩ traj − traj_nan);
- (4) the _traj2phase output looks sane (a monotone-ish ramp -> 0..360°) on a sample;
- (5) the phase-matched triplet selection (anchor@v0 + positive@v1 at a shared phase + negative@v1 gap-away) on a y_center/phase plot + a decoded anchor/positive/negative frame grid — the human confirms anchor & positive are at a similar pose, negative is at a different pose.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D2-ssl | Faithful CVCSPC SSL = the phase-matched cross-rep contrast on the unlabeled Squat bar-trajectories, the 3-term distance-ratio loss (squared L2), masking-only aug (top 40-50% blackout p~0.5), AdamW lr=1e-4 wd=0, batch 25, 100-epoch cosine baseline (run to triplet-accuracy plateau), the IMPLEMENTED phase-gap anneal 30°-> (CITED train_test.py:172-179). Backbone = the triplet-accuracy-best epoch. Via run_cvcspc_pretrain_epoch (the Plan-01 NEW self-contained trainer, UNCHANGED). | CONTEXT D2 + RESEARCH §SSL Recipe |
| D2a-frames | A Step-0 frame-extraction (extract_frames_for_ssl) decodes the 4,970 unlabeled Squat mp4s -> {frames_root}/{video_id}/frame_{:06d}.jpg on /content before SSL (the dataloader indexes extracted frames, NOT mp4s). ~10-20 min one-time on L4, skip-existing/disconnect-safe (cv2 decode — tv 0.26 removed read_video). The bar-trajectory JSONs give the phase signal. | CONTEXT D2a + RESEARCH §Frame Extraction |
| D2-trajnan | The official dataloader.py:67-68 loads traj_nan.json and excludes those clips (NaN bar-trajectories) before phase-matching. The Plan-01 ShallowSquatSSLDataset applies that exclusion via traj_nan_path; this notebook STAGES traj_nan.json alongside the trajectories (stage_unlabeled_squat_videos stages it from the release; if absent, the dataset logs a warning + proceeds with the degenerate-trajectory guard as the fallback) and passes traj_nan_path to run_cvcspc_pretrain_epoch. Step 1 reports the clip count AFTER exclusion. | CONTEXT D2a + RESEARCH §SSL Dataloader + checker BLOCKER 2 |
| D2-probe | The trajectory-format + phase-matching probe is a BLOCKING human-verify in Task 1 (after the frame-extract, before the SSL burn): confirm the {video_id}/ frame layout, the traj<->frame mapping (1:1 expected), the traj_nan exclusion count, _traj2phase sanity, and the phase-matched triplet selection on a plot + frame grid. Mirrors the Phase-4/6 Wave-1 probe. The Plan-01 synthetic tests proved the LOGIC; this confirms the LIVE format. | CONTEXT D2a + RESEARCH §SSL Dataloader Open Q / Pitfall 2 |
| D-no-collapse-abort | NO collapse-abort (ResNet-18 ImageNet-init is far more stable than R(2+1)D-18 from Kinetics on a small unlabeled set — the Phase-4 collapse guard is NOT carried). The triplet-accuracy monitor (AP<AN every triplet_acc_cadence=5 epochs) is the convergence signal: rising above 50% chance = learning; flat-at-50% = a problem (verify the augs/phase-gap, do not add new remediation). | CONTEXT D2 + RESEARCH §Risk Summary |
| D-proj-ablation | If the triplet-accuracy stays flat (~50%) after the first monitor checks, the [ASSUMED] 512->128->128 projection head may be the cause — the user may ablate to 512->512 via the CVCSPCConfig projector_hidden/projector_out_dim knobs (Plan-01 made them configurable — Discretion); this is the ONE allowed in-flight recipe change, surfaced as a decision, not silent. | CONTEXT Discretion + RESEARCH A1 / Open Q 2 |
| D7 | persistent_workers on the SSL loader (build_cvcspc_loader, Plan 01); map_location='cpu' on resume (re-applying the annealed phase_gap from the resumed epoch); backbone.pt write passes update_latest=False (Plan-01 cvcspc_pretrain); metrics_history/triplet_acc_history from the latest.txt-pointed checkpoint for viz; .ipynb with Cell A on the FIRST ship; one runnable unit at a time; restart runtime / importlib.reload after git pull; frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=the staged traj_nan.json. | CONTEXT D6/D7 + RESEARCH §Frame Extraction |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 2/3/4/6 + D7)

1. Cell A first: from backend.training.aqa.harness import _envinit is the FIRST executable line of Step 0 (F8 — CUBLAS_WORKSPACE_CONFIG before CUDA). PyAV: the unlabeled videos are decoded by extract_frames_for_ssl via cv2 (NOT read_video) — PyAV is not strictly needed for the image SSL path; stage_unlabeled_squat_videos copies/extracts the zip (no decode). Skip the torch-video PyAV install if not otherwise needed; keep the scipy/sklearn/numpy/cv2 dep-version probe.
2. persistent_workers via build_cvcspc_loader (Plan 01, num_workers>0 guard).
3. map_location='cpu' on load_latest_checkpoint at resume (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume (run_cvcspc_pretrain_epoch, Plan 01).
5. backbone.pt write passes update_latest=False (Plan-01 cvcspc_pretrain) so latest.txt epoch-resume pointer is never clobbered.
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch.
7. metrics_history + triplet_acc_history loaded from the latest.txt-pointed checkpoint for viz (NOT backbone.pt, frozen at the best epoch — [[reference_best_pt_metrics_history_is_stale]]).
8. config_hash via hash_config over the CVCSPCConfig recipe keys (recipe change invalidates resume).
9. No AMP (fp32) — paper fidelity + determinism.
10. frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=the staged traj_nan.json (the staged SSL dirs — NOT a labeled path).
11. The frame-extract is disconnect-safe (skip-existing: a {video_id}/ dir with >=1 frame is skipped — Pitfall 7); it runs once per session before the SSL loop.
12. The traj_nan.json exclusion is applied by the Plan-01 dataset (CITED dataloader.py:67-68); the degenerate-trajectory guard is the fallback if traj_nan.json is absent. The phase-gap anneal ((epoch+1)%4==0 -> gap-=1, floor 30°) is implemented in run_cvcspc_pretrain_epoch (Plan 01) and re-applied on resume.
13. Restart the Colab runtime / importlib.reload after git pull (a stale SSL loss wastes compute — [[reference_colab_module_reload_after_git_pull]]).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: the live trajectory/phase-matching correctness (a wrong live format wastes the SSL compute), the traj_nan exclusion (an un-excluded NaN clip produces garbage phases), the multi-session resume pointer, the disk for the extracted frames, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| Drive unlabeled Squat videos/trajectories/traj_nan -> frame-extract + SSL dataset | Reading the staged 4,970 Squat clips + bar_trajectories JSONs + traj_nan.json; clip IDs from the dataset release, no path traversal. Staged once per session; extract_frames_for_ssl decodes to {video_id}/ dirs. |
| The live traj<->frame mapping + phase-matching + traj_nan exclusion -> the SSL burn | A wrong live mapping/format or an un-excluded NaN trajectory silently produces meaningless triplets across the SSL run. The Plan-01 synthetic tests proved the LOGIC + the traj_nan exclusion; the gated probe (Task 1) confirms the LIVE format + the exclusion count at the cheapest point. |
| In-memory SSL state -> Drive checkpoint (multi-session) | epoch_NNN.pt + backbone.pt over Drive FUSE; the run depends on the resume pointer never being clobbered (the update_latest=False contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | The live trajectory format / traj<->frame mapping / phase-matching / traj_nan exclusion [confirmed-on-synthetic, but the live format is the risk] — a wrong live mapping or an un-excluded NaN clip silently produces meaningless triplets across the SSL burn | mitigate | Task 1 is a BLOCKING human-verify probe AFTER the frame-extract, BEFORE the SSL burn: the {video_id}/ frame layout, the traj-length-vs-frame-count mapping, the traj_nan exclusion count, the _traj2phase sanity, and the phase-matched anchor/positive/negative on a plot + frame grid. The Plan-01 synthetic tests proved the logic + the exclusion; this confirms the live format. HIGH-cost-if-wrong, caught at the cheapest point. |
| T-07-11 | Denial of service | The triplet-accuracy stays flat at ~50% (the SSL learns nothing) — compute wasted | mitigate | The triplet-accuracy monitor (AP<AN every cadence epochs, Plan-01) + the epoch-5 gate (Step 3 acceptance) catch it before the full run; if flat, the [ASSUMED] projection head is the likely cause -> the user ablates 512->512 via the config knobs (D-proj-ablation, surfaced as a decision). ResNet-18 ImageNet-init is stable (no collapse expected). MEDIUM — monitored, not silent. |
| T-07-12 | Integrity | The multi-session resume pointer (latest.txt) corrupted by the backbone.pt write | mitigate | backbone.pt write passes update_latest=False (Plan-01 cvcspc_pretrain); per-epoch atomic_save_checkpoint + prune; resume verified by a paste-back of "Resumed from epoch_NNN.pt" on the first post-disconnect epoch + the re-applied annealed phase_gap. MEDIUM — the contract is Phase-2/4-proven. |
| T-07-13 | Denial of service | Disk exhaustion: ~500k extracted frames (4,970 clips x ~100 frames) on Colab /content | accept | extract_frames_for_ssl is skip-existing + writes JPEGs (small); Step 0 logs the free disk after extraction. Colab /content is ~70-100 GB; ~500k small JPEGs fit. LOW — probed at extract time, resume-safe. |
| T-07-14 | Integrity | Committing dataset media / weights (the extracted frames, the backbone.pt) | accept | All media + weights gitignored / Drive-staged. This plan commits only the notebook .py/.ipynb; the frames live on /content (ephemeral), backbone.pt on Drive. Stage files by name only. LOW — gitignore contract established. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). cv2 (frame-extract) is in requirements; no PyAV needed for the image SSL path. No [ASSUMED]/[SUS] packages. |

T-07-10 (the live trajectory/phase-matching format + the traj_nan exclusion) is the highest-cost threat; the entire Step-0-probe-before-SSL sequencing exists to catch it. No web-app threats apply.

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Notebook Cell A + Step 0 stage(+traj_nan) + frame-extract + the trajectory/phase-matching probe (BLOCKING — human confirms the live format + the traj_nan exclusion before any SSL GPU)</name>
  <files>
    backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py,
    backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.ipynb,
    .planning/phases/07-image-based-errors-cvcspc/figures/.gitkeep,
    .planning/phases/07-image-based-errors-cvcspc/figures/cvcspc_triplet_probe.png
  </files>
  <read_first>
    backend/training/aqa/notebooks/07_ohp_md_ssl.py (the FULL Squat/OHP SSL notebook — Cell A + Step 0 stage pattern; the SSL dataset smoke + the sanity-grid + the probe idioms — copy + swap to the image CVCSPC path),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (Cell A bootstrap + the importlib.reload-after-pull note + the mount_drive/stage pattern),
    backend/training/aqa/harness/colab.py (stage_unlabeled_squat_videos + extract_frames_for_ssl from Plan 01 — the staging + frame-extract calls; confirm whether stage_unlabeled_squat_videos stages traj_nan.json, and if not, the notebook copies it from the release alongside the trajectories),
    backend/training/aqa/datasets/cvcspc_ssl.py (the Plan-01 ShallowSquatSSLDataset + _traj2phase + the traj_nan exclusion + the phase-matched __getitem__ — the probe plots the phase + the triplet selection + reports the exclusion count),
    Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/dataloader.py (the OFFICIAL traj_nan.json exclusion at lines 67-68 + the get_phase + the cross-rep triplet — ground the live-format expectations),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§SSL Dataloader — the frame-dir layout {video_id}/, the traj<->frame 1:1 expectation, the traj_nan exclusion, Open Q 1; §Frame Extraction the cv2 loop + the ~10-20 min cost; Pitfall 2 frame-dir layout, Pitfall 7 disconnect-safe),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2a frame-extract + the phase signal; the {video}_{rep}_{frame} string-ID gotcha)
  </read_first>
  <what-built>
    The Phase 7 CVCSPC SSL notebook scaffold (10_shallow_squat_cvcspc_ssl.py + .ipynb pair, Cell A baked in per [[feedback_deliver_colab_as_ipynb]] D6) with:
    - Cell A: clone-or-pull the fitnova repo (branch fresh-start) + sys.path.insert (verbatim from the OHP SSL notebook's Cell A; change only the title comment + the sanity-module list to the Phase-7 modules cvcspc_ssl.py / cvcspc_pretrain.py).
    - Step 0: from backend.training.aqa.harness import _envinit FIRST; the dep-version probe (numpy/scipy/sklearn/cv2 — NO PyAV needed for the image SSL path); L4 GPU assert (>=22 GB ample; ResNet-18 SSL is light, the cost is the frame-extract); Drive mount; stage_unlabeled_squat_videos(DRIVE_ROOT, ...) (the unlabeled Squat videos.zip + bar_trajectories + traj_nan.json — the Phase-4 SSL set, 4,970 clips; the user supplies the Drive root — note the unlabeled videos may be in a different release folder than the labeled images, per RESEARCH §Environment -3-004 vs -3-001; the user supplies the correct root). CONFIRM traj_nan.json is staged (if stage_unlabeled_squat_videos does not copy it, the notebook copies it from the release into the trajectories dir + sets TRAJ_NAN_PATH). THEN extract_frames_for_ssl("/content/squat_unlabeled_videos", "/content/squat_ssl_frames", skip_existing=True) -> the ~500k frames into {video_id}/ dirs (tqdm, ~10-20 min, skip-existing). Log the videos.zip size + the extracted-frame count + free /content disk (T-07-13).
    - THE PROBE: ls the staged squat_ssl_frames dir (count {video_id}/ dirs ~4970) + the squat_trajectories dir + confirm traj_nan.json present; for 2-5 sample clips report (RESEARCH §SSL Dataloader): (1) the {video_id}/ frame count vs the trajectory length (len(json)) for the SAME clip -> the traj<->frame mapping (1:1 expected); (2) _traj2phase(load traj) on a sample -> confirm it spans ~0..360°; (3) instantiate ShallowSquatSSLDataset(frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=TRAJ_NAN_PATH); print len(ds) (the clip count AFTER the traj_nan exclusion — report how many clips traj_nan dropped); call ds[0] a few times; for one draw, plot the two clips' y_center/phase curves with the anchor/positive/negative phases overlaid + decode the anchor/positive/negative frames into a grid (denormalized for viz). Save the probe figure to .planning/phases/07-image-based-errors-cvcspc/figures/cvcspc_triplet_probe.png BEFORE plt.show() with defensive mkdir.
  </what-built>
  <how-to-verify>
    1. Confirm Cell A runs: repo cloned/pulled at HEAD, no ModuleNotFoundError for backend; the cvcspc_ssl.py / cvcspc_pretrain.py modules present (proves the fresh-start pull worked).
    2. Confirm Step 0: the deps print versions; GPU is present; Drive mounted; stage_unlabeled_squat_videos reports cache-hit or fresh-stage with ~4970 clips + the trajectories + traj_nan.json; extract_frames_for_ssl reports the extracted-frame count + skipped count + free disk. If stage_unlabeled_squat_videos raises FileNotFoundError, the Drive root is wrong (the unlabeled videos may be in -3-004, not the -3-001 of the labeled images — the user supplies the correct root). Confirm TRAJ_NAN_PATH points at a real traj_nan.json (or note it is absent — the dataset then warns + uses the degenerate guard).
    3. Read the probe output: confirm the {video_id}/ frame layout, the traj-length-vs-frame-count mapping (1:1? — RESEARCH §SSL Dataloader), the traj_nan exclusion count (how many clips dropped), and _traj2phase spanning ~0..360°.
    4. THE FORMAT/PHASE-MATCHING DECISION: look at the y_center/phase plot + the decoded anchor/positive/negative frame grid for the sample draw. Confirm the anchor (v0) + positive (v1) are at a SIMILAR pose (same bar-trajectory phase, different rep) and the negative (v1) is at a DIFFERENT pose (>= phase_gap away). Reply confirming the phase-matching looks correct + the traj<->frame mapping (1:1 or the recorded mapping) + the traj_nan exclusion applied.
    5. If the format/mapping is NOT as expected (e.g. the frame count != the trajectory length, or the trajectory JSON is a per-frame BBox like OHP rather than a flat list, or traj_nan.json is missing), describe what it actually is so cvcspc_ssl._traj2phase / the staging is corrected before the SSL burn. (NOTE: the Squat unlabeled bar_trajectories are the Phase-4 SSL set — Phase 4 confirmed them as flat y-lists, 1:1 with frames; this probe re-confirms for the image-frame path + confirms traj_nan.json staged.)
  </how-to-verify>
  <action>
    This is a BLOCKING human-verify probe — the live-format gate before the SSL compute (RESEARCH §SSL Dataloader Open Q). The executor lands Cell A + Step 0 (stage + traj_nan staging + frame-extract) + the probe in the notebook .py AND ships the .ipynb pair (Cell A baked in — D6). The user runs Cell A + Step 0 + the probe on the fresh L4 Colab (prompt the user FIRST — [[feedback_heavy_training_new_notebook]]) and pastes back: the staged/extracted counts, the traj_nan exclusion count, the probe report (the traj<->frame mapping + the phase span), the cvcspc_triplet_probe.png figure (or screenshot), and the CONFIRMATION that the phase-matched triplet looks correct (anchor/positive same pose, negative different). The executor does NOT proceed to the SSL pretrain (Task 2) until this paste-back confirms the live format + mapping + the traj_nan exclusion + the phase-matching. One runnable unit; this is the [[feedback_interactive_execution]] "understand the data before building" gate. Atomic commit: feat(07): notebook Cell A + Step 0 stage(+traj_nan) + frame-extract + CVCSPC triplet probe (Task 1, D2a/D2-trajnan / RESEARCH §SSL Dataloader).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py').read()); print('parse ok')" && grep -q "_envinit" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "stage_unlabeled_squat_videos" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "extract_frames_for_ssl" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "ShallowSquatSSLDataset" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "traj_nan" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook .py parses; Cell A + Step 0 (stage + traj_nan staging + frame-extract) + the probe present; .ipynb pair shipped (Cell A baked in)
    - Step 0 calls stage_unlabeled_squat_videos + extract_frames_for_ssl ({video_id}/ frame dirs, skip-existing); confirms/stages traj_nan.json + sets TRAJ_NAN_PATH; logs the extracted-frame count + free disk
    - The probe reports the traj<->frame mapping (1:1 expected) + the traj_nan exclusion count + the _traj2phase span (~0..360°) + saves cvcspc_triplet_probe.png before plt.show()
    - User pastes back the counts + the traj_nan exclusion count + the probe report + the figure + the CONFIRMATION that the phase-matched triplet looks correct (anchor/positive same pose, negative different)
    - NO SSL pretraining yet; cvcspc_ssl.py NOT corrected unless the paste-back reveals a live-format deviation
  </acceptance_criteria>
  <dependencies>Plan 01 (cvcspc_ssl.ShallowSquatSSLDataset with traj_nan_path + cvcspc_pretrain + colab.extract_frames_for_ssl/stage_unlabeled_squat_videos exist)</dependencies>
  <done>User pastes back the extracted-frame counts + the traj_nan exclusion count + the probe report + cvcspc_triplet_probe.png + the confirmation the phase-matched triplet is correct on real frames; Task 2 (the SSL pretrain) is authorized with the confirmed live format/mapping + the exclusion applied. NO SSL pretraining yet.</done>
  <resume-signal>Reply confirming the phase-matched triplet looks correct (anchor/positive same pose, negative different) + the traj<->frame mapping (1:1, or the deviation) + the traj_nan exclusion applied. Or paste the probe output + the figure and ask for guidance.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Notebook Step 1 SSL dataset smoke + Step 2 VRAM probe + Step 3 epoch-0 timing gate (BLOCKING — user authorizes the SSL run)</name>
  <files>backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py</files>
  <read_first>
    backend/training/aqa/notebooks/07_ohp_md_ssl.py (Step 1 VRAM probe + Step 2 epoch-0 timing-gate patterns — the max_memory_allocated idiom + estimated_total_h + the checkpoint round-trip; the 3-branch SSL forward),
    backend/training/aqa/harness/cvcspc_pretrain.py (run_cvcspc_pretrain_epoch + CVCSPCConfig + build_cvcspc_model + cvcspc_triplet_loss + _triplet_accuracy — Step 2 builds the SSL forward, Step 3 calls run_cvcspc_pretrain_epoch with max_epochs=1),
    backend/training/aqa/datasets/cvcspc_ssl.py (ShallowSquatSSLDataset + build_cvcspc_loader — Step 1 smoke-tests a triplet batch; the traj_nan_path kwarg),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§SSL Recipe — batch 25 paper-exact, lr 1e-4, 100ep; the triplet-accuracy monitor; the convergence pattern; §Environment ResNet-18 SSL is light),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2 the recipe; Discretion the projection-head ablation knobs)
  </read_first>
  <decision>
    Should we proceed with the full CVCSPC SSL pretrain at batch_size=25 (paper-exact) with the CVCSPCConfig defaults, or downscope (smaller batch / fewer epochs) before committing to the run?
  </decision>
  <context>
    The SSL pretrain reuses the Plan-01 cvcspc_pretrain + recipe UNCHANGED. ResNet-18 SSL at batch 25/224² is light (RESEARCH §Environment); the cost was the one-time frame-extract (Task 1). Three cells:

    Step 1 (SSL dataset smoke — the live re-confirmation through the loader): ds = ShallowSquatSSLDataset(frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=TRAJ_NAN_PATH, ssl_contrastive_phase_gap=30.0); print len(ds) (the clip count AFTER the traj_nan exclusion); loader = build_cvcspc_loader(ds, CVCSPCConfig(), seed=42); batch = next(iter(loader)); assert each of batch anchor/positive/negative has shape (batch_size, 3, 224, 224) float32; print the three shapes + a denormalized anchor/positive/negative strip (the second confirmation of the Task-1 phase-matching through the full dataset).

    Step 2 (VRAM probe): backbone, projector = build_cvcspc_model(CVCSPCConfig()) to cuda; pull one SSL batch; run a 3-branch forward (phi_a/p/n = projector(backbone(branch))) + cvcspc_triplet_loss + loss.backward(); print torch.cuda.max_memory_allocated()/1e9 (peak backward VRAM at batch 25). Assert it fits L4 (< 22 GB). ALSO sanity-check _triplet_accuracy on the one batch returns a float in [0,1] AND the projector output is unit-norm [B,128].

    Step 3 (epoch-0 timing gate): run_cvcspc_pretrain_epoch(run_name="shallow_squat_cvcspc_v1_timing", drive_root=MYDRIVE, frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=TRAJ_NAN_PATH, seed=42, resume=False, max_epochs=1) — one fresh epoch under the production code path on the ~4970 clips; paste back epoch_0 wall-time, estimated_total_h = epoch_0_time * CVCSPCConfig.max_epochs / 3600, the epoch-0 ssl_loss (sanity: finite, decreasing within the epoch), GPU util. Verify the checkpoint round-trip: torch.load(epoch_000.pt, map_location='cpu'), assert the SSL payload keys + code_version "phase07-cvcspc-pretrain". The timing run lands in a separate _timing dir.

    Decision criteria: estimated_total_h reasonable (<= the user's session budget; expect minutes-to-low-hours — ResNet-18 SSL is light) and VRAM fits at batch 25 -> option-a (proceed batch 25). estimated_total_h too high OR VRAM tight -> option-b (batch 16 / fewer epochs — note the paper used batch 25).
  </context>
  <options>
    <option id="option-a">
      <name>Proceed at batch 25 (paper-exact), CVCSPCConfig defaults (100-epoch baseline to triplet-accuracy plateau)</name>
      <pros>Paper-faithful batch size + recipe (3-term loss, masking aug, phase-gap anneal); ResNet-18 SSL is light so it fits a session; identical to the reconstructed CVCSPC.</pros>
      <cons>If convergence needs the full 100 epochs + the per-epoch frame-loading is I/O-bound, the run spans a couple sessions (the resume contract carries it).</cons>
    </option>
    <option id="option-b">
      <name>Drop batch (16) / cap epochs if VRAM tight or epoch-0 slow</name>
      <pros>Lower VRAM/time; choose only if Step 2 is tight or Step 3 estimated_total_h is too high.</pros>
      <cons>A deviation from the paper's batch 25 — document it in the SUMMARY (Plan 05).</cons>
    </option>
  </options>
  <action>This is a BLOCKING decision checkpoint. The executor lands Step 1 (SSL dataset smoke) + Step 2 (VRAM probe) + Step 3 (epoch-0 timing gate) in the notebook .py. The user runs them on the L4 (the frame-extract from Task 1 is already done) and pastes back: the triplet batch shapes + the post-exclusion clip count + the sanity grid (Step 1); the peak backward VRAM at batch 25 + the unit-norm projector check (Step 2); epoch_0 wall-time + estimated_total_h + the epoch-0 ssl_loss + GPU util + the checkpoint round-trip (Step 3). The user replies option-a / option-b. No code change unless option-b (CVCSPCConfig(batch_size=16) or a lower max_epochs). Atomic commit: feat(07): notebook Step 1 SSL smoke + Step 2 VRAM + Step 3 timing gate (Task 2, D2 / RESEARCH §SSL Recipe).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py').read()); print('parse ok')" && grep -q "max_memory_allocated" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "estimated_total_h" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "shallow_squat_cvcspc_v1_timing" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "build_cvcspc_loader" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 1 + Step 2 + Step 3 present; Step 1 instantiates ShallowSquatSSLDataset(traj_nan_path=...) + build_cvcspc_loader + asserts the three triplet shapes (B,3,224,224) + prints the post-exclusion clip count; Step 2 the batch-25 peak backward VRAM fits + _triplet_accuracy returns [0,1] + the projector is unit-norm
    - Step 3 runs epoch-0 under run_cvcspc_pretrain_epoch (max_epochs=1) + verifies the checkpoint round-trip (code_version "phase07-cvcspc-pretrain")
    - User pastes back the triplet shapes + the post-exclusion count + the sanity grid + the VRAM + epoch_0 time + estimated_total_h + the round-trip, and replies option-a / option-b
    - NO full SSL run yet
  </acceptance_criteria>
  <dependencies>Task 1 (the gated probe — confirms the live format + the frames are extracted + traj_nan staged)</dependencies>
  <done>User replies option-a / option-b; Task 3 inherits the chosen batch/epochs; the triplet batch shapes + the post-exclusion count + VRAM fit + epoch-0 timing + the checkpoint round-trip all verified before the SSL burn is authorized</done>
  <resume-signal>Type "option-a" or "option-b". Or paste back the triplet shapes + the VRAM + epoch_0 time + estimated_total_h and ask for guidance.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 4 — full CVCSPC SSL pretrain (multi-session resume) + the triplet-accuracy convergence checkpoint</name>
  <files>backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py</files>
  <read_first>
    backend/training/aqa/harness/cvcspc_pretrain.py (run_cvcspc_pretrain_epoch — Step 4 calls it with resume=True, traj_nan_path=TRAJ_NAN_PATH; the triplet-accuracy monitor + the backbone.pt-best + the phase-gap anneal, all implemented in Plan 01),
    backend/training/aqa/notebooks/07_ohp_md_ssl.py (the OHP Step 3 full-SSL-pretrain pattern — the resume=True single-cell-is-the-whole-run idiom + the staged per-epoch paste-back + the disconnect/resume recovery + the convergence gate),
    backend/training/aqa/harness/colab.py (load_latest_checkpoint — the "Resumed from epoch_NNN.pt" log line the resume paste-back verifies),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§SSL Recipe — the convergence criterion: triplet-accuracy rises above 50% chance; the backbone = triplet-accuracy-best epoch; A1 the projection-head ablation if flat; the phase-gap anneal),
    .planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md (IMG-01 — SSL triplet-accuracy rises above chance; backbone.pt produced),
    .planning/phases/04-squat-motion-disentangling-ssl/04-02-SUMMARY.md (the v2 SSL-run reality — the monitor cadence + the convergence pattern; the OHP/Squat run expects a similar shape, though CVCSPC uses triplet-accuracy not linear-probe)
  </read_first>
  <action>
    Fill in Step 4: the full CVCSPC SSL pretrain. config = CVCSPCConfig() (or batch 16 per Task 2's decision); RUN_NAME = "shallow_squat_cvcspc_v1"; result = run_cvcspc_pretrain_epoch(run_name=RUN_NAME, drive_root=MYDRIVE, frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=TRAJ_NAN_PATH, seed=42, config=config, resume=True, max_epochs=config.max_epochs). resume=True so a mid-run Colab disconnect is recoverable (re-run this cell after restart -> resumes from latest.txt + re-applies the annealed phase_gap). The trainer checkpoints every epoch + writes backbone.pt on triplet-accuracy improvement (update_latest=False) + anneals the phase-gap ((epoch+1)%4==0 -> gap-=1, floor 30°), so this single cell IS the whole run. Print final epoch reached, backbone path, best triplet-accuracy, the final phase_gap.

    This is one runnable unit but ASYNC + possibly multi-session — acceptance is a STAGED paste-back per [[feedback_interactive_execution]]: (a) FIRST, the convergence gate at the first triplet-accuracy check (epoch ~5, the triplet_acc_cadence) — paste back the epoch-0..5 ssl_loss (decreasing) + the epoch-5 triplet-accuracy (rising above 50% chance = IMG-01 convergence signal). If the triplet-accuracy stays flat at ~50%, STOP + report: the [ASSUMED] 512->128->128 projection head is the likely cause — the user may ablate to 512->512 via the CVCSPCConfig projector knobs (D-proj-ablation, a surfaced decision), OR verify the phase-gap/masking are active; do NOT open-ended iterate. (b) on any mid-run disconnect: re-run Step 0 (re-stage is skip-existing/fast) + Step 4 -> the first post-resume epoch logs "Resumed from epoch_NNN.pt" + the re-applied annealed phase_gap. (c) FINAL: the run completes (triplet-accuracy plateau or max_epochs) — paste back the final epoch + the best triplet-accuracy + the backbone.pt path on Drive + the final phase_gap. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Plan 04 until the SSL run completes + the backbone.pt exists.

    Atomic commit: feat(07): notebook Step 4 full CVCSPC SSL pretrain (Task 3, D2-ssl / IMG-01).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py').read()); print('parse ok')" && grep -q "run_cvcspc_pretrain_epoch" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "shallow_squat_cvcspc_v1" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py && grep -q "resume=True" backend/training/aqa/notebooks/10_shallow_squat_cvcspc_ssl.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 4 present; run_cvcspc_pretrain_epoch(run_name="shallow_squat_cvcspc_v1", frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=TRAJ_NAN_PATH, resume=True)
    - Runnable unit (Colab, multi-session possible): epoch-0..5 paste-back shows ssl_loss decreasing + the epoch-5 triplet-accuracy rising above 50% chance (IMG-01); on disconnect, the resume paste-back logs "Resumed from epoch_NNN.pt" + the re-applied phase_gap; FINAL = final epoch + best triplet-accuracy + backbone.pt path + the final phase_gap
    - If triplet-accuracy flat at ~50%: the projection-head ablation (512->512 via the config knobs) is surfaced as a decision (D-proj-ablation), NOT silently iterated; the masking/phase-gap are verified active
    - backbone.pt on Drive at phase07/shallow_squat_cvcspc_v1/backbone.pt (the triplet-accuracy-best epoch); backbone.pt write passed update_latest=False (latest.txt not clobbered)
  </acceptance_criteria>
  <dependencies>Task 2 (VRAM/timing gate — authorizes the run + the batch size)</dependencies>
  <done>The faithful CVCSPC SSL pretrain ran on the 4,970 unlabeled Squat frame dirs + bar-trajectories (with the traj_nan clips excluded + the phase-gap annealing) and converged (triplet-accuracy rose above chance, IMG-01); backbone.pt (the triplet-accuracy-best epoch) is on Drive — the fork point Plan 04 fine-tunes the seeds from</done>
</task>

</tasks>

<verification>
## Mapping to Phase 7 ROADMAP Success Criterion + IMG-01

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (partial): the CVCSPC image pipeline trains a self-supervised model | Tasks 2 (gate), 3 (run) | Task 3 epoch-0..5 paste-back: ssl_loss decreasing + triplet-accuracy rising above chance (IMG-01); backbone.pt on Drive |
| The SSL backbone for the fine-tune | Task 3 | backbone.pt (triplet-accuracy-best epoch) at phase07/shallow_squat_cvcspc_v1/ — the Plan 04 fork point |
| The live trajectory/phase-matching format + traj_nan exclusion gate (before the SSL burn) | Task 1 (blocking) | Task 1 probe paste-back confirms the {video_id}/ layout + the traj<->frame mapping + the traj_nan exclusion count + the phase-matched triplet on real frames |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| Frame-extraction (Step 0, idempotent skip-existing) | Task 1 | integration (Colab) | 2 |
| The trajectory/phase-matching + traj_nan exclusion live format (manual probe) | Task 1 | manual (gated Colab) | 2 |
| IMG-01 SSL convergence (triplet-accuracy above chance) | Task 3 | integration (Colab paste-back) | 2 |

(IMG-02 SSL-init test F1 + the baseline->CVCSPC lift is Plan 04.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user on the L4 Colab
- Task 1 (BLOCKING probe): the frames extracted ({video_id}/ dirs, skip-existing) + traj_nan.json staged + the live trajectory/phase-matching format + the exclusion count confirmed on 2-5 real clips BEFORE any SSL GPU burn; cvcspc_triplet_probe.png pasted back
- The faithful CVCSPC SSL pretrain ran on the 4,970 unlabeled Squat frame dirs + bar-trajectories (with the traj_nan clips excluded) via the Plan-01 cvcspc_pretrain.run_cvcspc_pretrain_epoch (the 3-term loss, the masking aug, the phase-gap anneal) and converged: ssl_loss down, triplet-accuracy above 50% chance (IMG-01)
- backbone.pt (the triplet-accuracy-best epoch) on Drive at phase07/shallow_squat_cvcspc_v1/backbone.pt — the Plan 04 fork point; backbone.pt write passed update_latest=False
- Disconnect-safe across the multi-session run: the resume paste-back logs "Resumed from epoch_NNN.pt" + the re-applied annealed phase_gap; frames_root/trajectories_root/traj_nan_path point at the staged SSL dirs; the frame-extract is skip-existing
- If triplet-accuracy flat: the projection-head ablation (512->512 via the config knobs) surfaced as a decision (D-proj-ablation), NOT silently iterated; NO collapse-abort (ResNet-18 ImageNet-init is stable)
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D6); every D2/D2a decision carries an inline citation in the plan (NOT in source — project rule)
- One accurate threat_model: offline pipeline, no high-severity threats; T-07-10 (the live trajectory/phase-matching format + the traj_nan exclusion) is the gated probe; T-07-SC accept (zero new installs)
</success_criteria>

<output>
After Task 3 (backbone.pt on Drive), this plan is complete. Plan 04 (Wave 3) loads backbone.pt, runs the multi-seed fine-tune + the ensemble + val-tuned threshold + test eval + results.pkl + the baseline->CVCSPC->paper(0.869) comparison. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 04 once Task 3's backbone.pt is confirmed on Drive (AND Plan 02's baseline is done — Plan 04 depends on both). The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 7 Plan 03 written 2026-05-30. Wave 2: the faithful CVCSPC SSL pretrain on the 4,970 unlabeled Squat frame dirs -> backbone.pt (mirrors Phase 4 Plan 02 / Phase 6 Plan 03's SSL run). 3 tasks (a blocking frame-extract+trajectory probe + a blocking VRAM/timing gate + the SSL run). NO new production logic — this plan WIRES the Plan-01 cvcspc_pretrain.run_cvcspc_pretrain_epoch on the Plan-01 ShallowSquatSSLDataset. The decisive RESEARCH verdict (CVCSPC feasible standalone on the BackSquat unlabeled set — no BarbellRow) is the basis; the Step-0 frame-extraction (D2a) is the one CVCSPC-specific data step.

REVISION 2026-05-30 (checker pass against the actual code):
- BLOCKER 1 consistency (the trainer seam): cvcspc_pretrain.run_cvcspc_pretrain_epoch is the Plan-01 NEW self-contained CVCSPC SSL trainer (it MIRRORS md_pretrain's contract but does NOT import its R(2+1)D run_md_pretrain_epoch). The interfaces/decisions/objective now state this explicitly; the SSL trainer routes checkpoints to phase07 + keeps update_latest=False for backbone.pt.
- BLOCKER 2 (traj_nan exclusion): the official dataloader.py:67-68 loads traj_nan.json + excludes those clips before phase-matching. FIX: Step 0 now STAGES traj_nan.json alongside the trajectories (confirming stage_unlabeled_squat_videos copies it, else the notebook copies it from the release) + sets TRAJ_NAN_PATH; the ShallowSquatSSLDataset is instantiated with traj_nan_path everywhere (Step 1 + Step 3 + run_cvcspc_pretrain_epoch); the Task-1 probe + Step 1 report the clip count AFTER the exclusion (how many clips traj_nan dropped); the must-haves + key_links + threat_model + acceptance criteria now require the exclusion + its verification. D2-trajnan added.
- SHOULD-FIX #4 (phase-gap anneal): the anneal ((epoch+1)%4==0 -> gap-=1, floor 30°, CITED train_test.py:172-179) is IMPLEMENTED in the Plan-01 run_cvcspc_pretrain_epoch; this plan documents it as implemented (D2-ssl, the determinism checklist, the resume re-application, the Step-4 print of the final phase_gap) — the deviation is NOT taken; the anneal runs.

Wave assignment: depends_on ["07-01"] ONLY (the Wave-0 scaffold), NOT Plan 02 — the SSL pretrain and the supervised baseline are independent (different checkpoint dirs phase07/shallow_squat_cvcspc_v1 vs phase07/shallow_squat_baseline_seed{N}; different notebook files — no files_modified overlap), so this is wave 2 and can run in PARALLEL with Plan 02 (wave 1). Plan 04 (the fine-tune) depends on BOTH (the backbone from 03 + the baseline control from 02).

Two blocking gates (mirroring Phase 4/6): Task 1 = the frame-extract + the traj_nan staging + the live trajectory/phase-matching human-verify probe (the Plan-01 synthetic tests proved the LOGIC + the exclusion; this confirms the LIVE format — the {video_id}/ layout, the traj<->frame 1:1 mapping, the traj_nan exclusion count, the phase-matched triplet on real frames); Task 2 = the VRAM/timing decision gate. The triplet-accuracy monitor (AP<AN, Plan-01) is the convergence signal — NO collapse-abort (ResNet-18 ImageNet-init is stable, unlike R(2+1)D-18 from Kinetics). The one in-flight escape hatch is the projection-head ablation (512->512 via the config knobs) if triplet-accuracy is flat — surfaced as a decision, not silent (D-proj-ablation).

Project rule honored: NO AI-narration/citation comments in source — the notebook documents the recipe factually; the citations live in this plan + the commit messages.

Security: accurate, not boilerplate. Offline ML pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-07-10 (the live trajectory/phase-matching + the traj_nan exclusion — the gated probe, highest cost-if-wrong), T-07-11 (flat triplet-accuracy — monitored + the projection ablation escape), T-07-12 (resume pointer — Phase-2/4-proven), T-07-13/14 accept (disk + no-media-committed), T-07-SC accept (zero installs — cv2 in requirements). No web-app threats invented.
</revision_log>
</output>
