---
phase: 07-image-based-errors-cvcspc
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/training/aqa/datasets/shallow_squat.py
  - backend/training/aqa/datasets/cvcspc_ssl.py
  - backend/training/aqa/harness/image_supervised_train.py
  - backend/training/aqa/harness/cvcspc_pretrain.py
  - backend/training/aqa/harness/colab.py
  - backend/training/aqa/datasets/test_shallow_squat.py
  - backend/training/aqa/datasets/test_cvcspc_ssl.py
autonomous: true
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "ShallowSquatDataset(split='train', images_root=..., labels_path=..., splits_root=...) reads labels_shallow_depth.json (a FLAT {id:0/1} dict — NOT interval lists; do NOT route through splits._label_value) + splits/{split}_ids.json, and __getitem__ returns (tensor[3,224,224] float32 ImageNet-normalized, scalar float32 label in {0.0,1.0}) — proven on a synthetic tmp_path crop set before any Colab work (D3)"
    - "_compute_pos_weight over the train ids returns a [1] tensor (SINGLE binary head — Shallow-Squat is ONE binary error 'shallow depth', NOT a 2-error pair like KIE/KFE; the head is Linear(512,1), the loss BCEWithLogitsLoss on a single logit, the eval a single score column — NOT [2]) via (N_train - n_pos)/max(n_pos,1) computed from the TRAIN split regardless of which split the instance represents — the near-balanced ~1.28 weighting proven on synthetic 2/4-positive records (D5)"
    - "cvcspc_triplet_loss(phi_anc, phi_pos, phi_neg) on L2-normalized synthetic feature triplets equals the CITED train_test.py:64-68 three-term closed form -log(e^-d_ap / (e^-d_ap + e^-d_an + e^-d_pn)) within 1e-6 AND is DIRECTIONAL — loss(d_ap small, d_an large, d_pn large) < loss(all-equal): the loss must reward an anchor close to its positive and far from its negative, so a wrong sign / missing pos-neg term is caught not just by the closed-form value but by the directionality assertion (the highest-leverage pre-GPU check; the wrong loss would waste the frame-extract + SSL burn — RESEARCH §CVCSPC Method)"
    - "CVCSPCProjectionHead(512,128,128) applies Linear->ReLU->Linear then F.normalize(dim=-1,p=2) and outputs unit-norm [B,128] (NO BatchNorm1d — small batch 25, CITED RESEARCH §Architecture); the hidden/out dims are config knobs (projector_hidden/projector_out_dim on CVCSPCConfig) so the [ASSUMED] 512->128->128 is ablatable to 512->512 without a code edit; build_cvcspc_model loads ImageNet ResNet-18 weights (strict=False), sets backbone.fc=Identity, returns (backbone, projector)"
    - "ShallowSquatSSLDataset._traj2phase normalizes a per-clip bar-trajectory (y-position) to [0,1] then x360 degrees (CITED dataloader.py:20-27 get_phase), guards degenerate (empty/all-NaN/constant) trajectories with a ValueError, and the dataset EXCLUDES the official traj_nan.json clips at construction (CITED dataloader.py:67-68); the phase-matched triplet construction picks anchor@v0 + positive@v1 at a SHARED random phase + negative@v1 at a phase >= phase_gap away — verified on synthetic trajectories (anchor/positive phases within tolerance, negative phase-gap >= the gap, the empty/NaN-trajectory guard raises, all frame indices in range); IDs are {video}_{rep}_{frame} STRING keys (no int-cast)"
    - "The CVCSPC masking augmentation (top random 40-50% of rows blacked out, prob ~0.5 — the ONLY active aug in the shipped dataloader.py:99-102 / image_augmentations.py) is a Wave-0 unit-tested transform: _mask zeroes the top mask_amt fraction of rows with prob mask_prob, and a deterministic-seed unit test asserts (a) with prob=1.0 the top k rows are exactly zero and the bottom rows are untouched, (b) with prob=0.0 the image is unchanged"
    - "image_supervised_train.run_image_epoch is a NEW, SELF-CONTAINED 2D-image trainer (build_resnet18 ResNet-18 IMAGENET1K_V1 + Linear(512,1); a 4-D (B,3,H,W) _val_pass; the checkpoint/resume/early-stop/threshold-sweep contracts) that reuses ONLY eval/metrics.py + the colab.py checkpoint/RNG primitives — it does NOT import or call supervised_train.run_supervised_epoch / _build_dataloaders / _val_pass / build_model (those hardcode r2plus1d_18 + a 5-input-frame stem and assume 5-D (B,3,T,H,W) video tensors — there is NO model_cls seam). run_image_epoch exposes dataset_cls=ShallowSquatDataset + checkpoint_phase='phase07'; run_cvcspc_pretrain_epoch (a NEW module that MIRRORS the md_pretrain contract but does NOT import its R(2+1)D-specific epoch loop) exposes ssl_dataset_cls=ShallowSquatSSLDataset + checkpoint_phase='phase07'"
    - "python -m pytest backend/training/aqa/ -m 'not slow' -q exits 0 with the new Shallow-Squat + CVCSPC unit tests green AND the existing Phase-3/4/6 Squat+OHP suite still green — the Wave-0 GPU-burn gate is locked behind a green pure-function suite + a no-regression proof"
  artifacts:
    - path: "backend/training/aqa/datasets/shallow_squat.py"
      provides: "ShallowSquatDataset (single-image JPEG loader, ImageNet norm, official Shallow-Squat splits, flat {id:0/1} labels) + _compute_pos_weight ([1] SINGLE-binary) + build_loaders convenience factory + seed_worker. Mirrors squat.py structure MINUS video decode/Kinetics norm. logger aqa.phase07."
      contains: "class ShallowSquatDataset"
    - path: "backend/training/aqa/datasets/cvcspc_ssl.py"
      provides: "ShallowSquatSSLDataset (phase-matched cross-rep triplet over the unlabeled Squat frame dirs + bar-trajectory JSONs, with the traj_nan.json exclusion) + _traj2phase (degenerate-guarded) + the genuinely-new phase-matching __getitem__ + masking-only aug + build_cvcspc_loader + seed_worker. Mirrors squat_ssl.py contract; the phase signal + the cross-rep contrast + the NaN-exclusion are the new logic. logger aqa.phase07."
      contains: "class ShallowSquatSSLDataset"
    - path: "backend/training/aqa/harness/image_supervised_train.py"
      provides: "NEW self-contained 2D-image trainer: ImageConfig + build_resnet18 (ResNet-18 IMAGENET1K_V1 + Linear(512,1)) + _build_dataloaders(dataset_cls=ShallowSquatDataset) + _val_pass (binary [B,1], 4-D input) + run_image_epoch (checkpoint/resume/early-stop/threshold-sweep seam + checkpoint_phase='phase07'). Reuses ONLY eval/metrics.py + colab.py primitives. Does NOT import supervised_train (R(2+1)D / 5-D). logger aqa.phase07."
      contains: "def run_image_epoch"
    - path: "backend/training/aqa/harness/cvcspc_pretrain.py"
      provides: "NEW self-contained CVCSPC SSL trainer: CVCSPCConfig + CVCSPCProjectionHead (no-BN, configurable dims) + cvcspc_triplet_loss (3-term, CITED) + build_cvcspc_model + build_cvcspc_checkpoint_payload (triplet_acc_history) + _triplet_accuracy monitor + run_cvcspc_pretrain_epoch (backbone.pt on triplet-acc-best, update_latest=False; phase-gap anneal; checkpoint_phase='phase07'). MIRRORS the md_pretrain contract; does NOT import md_pretrain's R(2+1)D epoch loop. Reuses ONLY colab.py primitives. logger aqa.phase07."
      contains: "def cvcspc_triplet_loss"
    - path: "backend/training/aqa/harness/colab.py"
      provides: "ADD (additive, no edits to existing fns) — stage_shallow_squat_images (images.zip -> crops_unaligned/{id}.jpg via zipfile.extractall, NOT the mp4-only extractor) + extract_frames_for_ssl (decode unlabeled Squat mp4s -> {frames_root}/{video_id}/frame_{:06d}.jpg, skip-existing, tqdm). Reuses _copy_with_resume_and_progress + stage_unlabeled_squat_videos."
      contains: "def stage_shallow_squat_images"
    - path: "backend/training/aqa/datasets/test_shallow_squat.py"
      provides: "Unit tests IMG-01 — split sizes (2542/529/540, local-archive skipif), pos_weight [1]-shape + formula, __getitem__ shape (3,224,224)+scalar label, single-logit label shape, ImageNet norm range, build_loaders dict, the image_supervised_train dataset_cls signature seam + the colab additions. Mirrors test_ohp.py."
      contains: "def test_getitem_shape"
    - path: "backend/training/aqa/datasets/test_cvcspc_ssl.py"
      provides: "Unit tests IMG-01 — cvcspc_triplet_loss closed-form + DIRECTIONALITY, CVCSPCProjectionHead unit-norm + no-BN + configurable dims, _traj2phase normalization + degenerate-guard, the masking aug, the traj_nan exclusion, the phase-matched triplet construction on synthetic trajectories, the ssl_dataset_cls signature seam. Mirrors test_ohp_ssl.py."
      contains: "def test_cvcspc_triplet_loss"
  key_links:
    - from: "backend/training/aqa/datasets/shallow_squat.py"
      to: "labels_shallow_depth.json (flat {id:0/1}) + splits/{split}_ids.json — read DIRECTLY (NOT via splits.index/_label_value, which int-casts interval lists)"
      via: "json.loads in __init__"
      pattern: "labels_shallow_depth"
    - from: "backend/training/aqa/harness/image_supervised_train.py"
      to: "backend/training/aqa/datasets/shallow_squat.ShallowSquatDataset (dataset_cls seam) + eval/metrics.{f1_per_error,pr_auc_per_error,threshold_sweep} (REUSED UNCHANGED) + harness.colab.{atomic_save_checkpoint,load_latest_checkpoint,...} (REUSED UNCHANGED) — NOT supervised_train"
      via: "import + run_image_epoch wires them"
      pattern: "from backend.training.aqa.eval.metrics import"
    - from: "backend/training/aqa/harness/cvcspc_pretrain.py"
      to: "backend/training/aqa/datasets/cvcspc_ssl.{ShallowSquatSSLDataset,build_cvcspc_loader} + harness.colab atomic-checkpoint primitives — NOT md_pretrain"
      via: "import + run_cvcspc_pretrain_epoch wires them"
      pattern: "from backend.training.aqa.datasets.cvcspc_ssl import"
    - from: "backend/training/aqa/datasets/cvcspc_ssl.py"
      to: "traj_nan.json (the official NaN-trajectory exclusion list, CITED dataloader.py:67-68) — clips excluded at construction"
      via: "json.loads in __init__; exclude from self._clip_ids"
      pattern: "traj_nan"
    - from: "backend/training/aqa/harness/colab.py"
      to: "stage_unlabeled_squat_videos (REUSED — stages the unlabeled Squat videos.zip + bar_trajectories) + _copy_with_resume_and_progress"
      via: "extract_frames_for_ssl consumes the staged mp4s; stage_shallow_squat_images mirrors the trajectory-zip extractall branch"
      pattern: "def extract_frames_for_ssl"
---

<objective>
Land all genuinely-new Phase 7 code as scaffolds with their pure-function cores FULLY IMPLEMENTED and unit-tested, plus the two additive colab staging/frame-extract functions, and verify everything LOCALLY ON CPU before any Colab GPU burn. This is the Phase-4/6 Wave-0 pattern (04-01-PLAN, 06-01-PLAN). The new method this phase introduces — CVCSPC pose-contrastive SSL — has three unit-testable cores that MUST be proven on synthetic data before the frame-extraction (~10-20 min) + SSL pretrain compute: (1) the **3-term contrastive loss** (CITED train_test.py:64-68, a 3-term denominator that DEVIATES from the paper's 2-term Eq.1 — implement the code version), verified by both a closed-form value AND a directionality assertion; (2) the **phase-matched cross-rep triplet** construction (anchor + positive at a shared bar-trajectory phase from two DIFFERENT reps; negative at a phase >= gap away), WITH the official traj_nan.json NaN-trajectory exclusion (CITED dataloader.py:67-68) so phase computation never divides by zero; and (3) the **masking augmentation** (the only active aug in the shipped code). A wrong loss, a wrong phase-matching, an un-excluded NaN trajectory, or a wrong mask would silently waste the SSL run. This plan locks all three behind a green pytest suite.

CRITICAL ARCHITECTURE NOTE (the trainer seam does NOT exist for a 2D image model): the existing harness/supervised_train.py is a VIDEO trainer — build_model() (line ~114) hardcodes torchvision r2plus1d_18 + a 5-input-frame stem and returns a model that consumes (B,3,T,H,W) 5-D tensors; _build_dataloaders / _val_pass / run_supervised_epoch all assume that 5-D shape. There is a dataset_cls + checkpoint_phase seam but NO model_cls seam. The ResNet-18 image path is (B,3,H,W) 4-D. Therefore the Phase-7 image trainer is a NEW SELF-CONTAINED module (image_supervised_train.py) that implements run_image_epoch end-to-end — it reuses ONLY eval/metrics.py (which works on score columns) + the colab.py checkpoint/RNG primitives, and does NOT import supervised_train's epoch loop. Likewise cvcspc_pretrain.py is a NEW module that MIRRORS the md_pretrain contract (config_repr/hash, run_dir, AdamW+cosine, per-epoch monitor, backbone.pt on best, update_latest=False, prune, resume) but does NOT import md_pretrain's R(2+1)D-specific run_md_pretrain_epoch.

Purpose: IMG-01 (the CVCSPC ResNet-18 image pipeline — baseline + self-supervised) + IMG-02 (the Shallow-Squat dataset the baseline + fine-tune consume). Everything downstream (Plans 02-04) reuses these modules + eval/metrics.py (UNCHANGED) + the colab harness primitives. The net-new code is: two new dataset modules (shallow_squat.py is a single-image analog of squat.py minus video decode; cvcspc_ssl.py is the phase-contrastive triplet dataset with the new phase-matching logic + the NaN-exclusion), two new self-contained trainers (image_supervised_train.py with a ResNet-18/binary-head 4-D loop; cvcspc_pretrain.py with the 3-term loss + triplet-accuracy monitor), two new test files, and the two additive colab functions. This plan implements + tests those WITHOUT GPU, before the data is even staged.

Output: 4 new + 1 edited .py files under backend/training/aqa/{datasets,harness}/ — 2 new dataset modules, 2 new self-contained trainers, 2 new test files, colab.py additive. The Phase-3/4/6 Squat+OHP suite stays green (the new modules are independent; colab.py edits are additive).

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]]: each task hands the user exactly one runnable unit (a pytest invocation or a python -c smoke) and stops until output is pasted back. NEVER pre-write task N+1 before task N's paste-back is in (reading ahead is fine, writing ahead is breach). These tasks run LOCALLY (no GPU, no Colab, no Drive) — CPU-only pure-function tests, so paste-back is fast. The split-size test reads the local Shallow-Squat archive (offline inspection only, per the P2/P6 precedent — training reads from Drive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md
@.planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md
@.planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md
@.planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md
@backend/training/aqa/datasets/squat.py
@backend/training/aqa/datasets/squat_ssl.py
@backend/training/aqa/datasets/splits.py
@backend/training/aqa/harness/supervised_train.py
@backend/training/aqa/harness/md_pretrain.py
@backend/training/aqa/harness/colab.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/datasets/test_ohp.py
@backend/training/aqa/datasets/test_ohp_ssl.py
@Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/dataloader.py
@Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/train_test.py
@CLAUDE.md

<interfaces>
Contracts this plan creates that Plans 02/03/04 consume. The executor implements these EXACT signatures — no codebase scavenger hunt. Full code excerpts live in 07-PATTERNS.md (cited per task in read_first); do NOT re-derive them. All new modules use `logger = logging.getLogger("aqa.phase07")` and start with `from __future__ import annotations` (CLAUDE.md backend convention). PROJECT RULE — NO AI-narration comments in source: no "RESEARCH §", no "mirrors X", no D-number citations IN CODE. Comment only a non-obvious WHY (the 3-term-vs-2-term deviation is one such WHY). The citations live in this plan + the commit messages, NOT the .py files.

IMPORTANT — the trainers are NEW SELF-CONTAINED modules, NOT reuses of the video trainers. supervised_train.build_model() hardcodes r2plus1d_18 (a 5-D (B,3,T,H,W) video model) with no model_cls seam, and _build_dataloaders/_val_pass assume 5-D tensors; md_pretrain.run_md_pretrain_epoch is R(2+1)D-specific. The Phase-7 image trainer + SSL trainer therefore implement their loops END-TO-END, reusing ONLY eval/metrics.py + the colab.py checkpoint/RNG primitives. They MAY copy small helper bodies (seed_worker, _set_global_seed, the config_repr/hash idiom, the resume branch) verbatim from supervised_train/md_pretrain — the Phase-4 D9 copy-not-import precedent — but they do NOT import the video epoch loops.

datasets/shallow_squat.py (mirror squat.py MINUS video decode):
- IMAGENET_MEAN=[0.485,0.456,0.406], IMAGENET_STD=[0.229,0.224,0.225] (module constants; NEVER import transforms.py's Kinetics norm).
- _compute_pos_weight(train_records: list[tuple[str,int]]) -> torch.Tensor — (N - pos)/max(pos,1); returns shape [1] (SINGLE binary head, NOT [2] — Shallow-Squat is ONE binary error).
- class ShallowSquatDataset(Dataset).__init__(split: Literal["train","val","test"], *, images_root, labels_path, splits_root, train_aug=True, seed=42) — loads labels_shallow_depth.json (flat {id:0/1}) + splits/{split}_ids.json + splits/train_ids.json; self.records=[(id,labels[id]) for id in split_ids if id in labels]; pos_weight ALWAYS from train_ids; train transform = RandomResizedCrop(224,scale=(0.8,1.0))+RandomHorizontalFlip(0.5)+ColorJitter(0.2,0.2)+ToTensor()+Normalize(IMAGENET); val/test transform = Resize(256)+CenterCrop(224)+ToTensor()+Normalize(IMAGENET). __getitem__ -> (Image.open(images_root/f"{id}.jpg").convert("RGB") |> transform [3,224,224] float32, torch.tensor(label, float32) SCALAR — a single label, NOT a 2-vector).
- build_loaders(*, batch_size=32, num_workers=4, **dataset_kwargs) -> {"train","val","test": DataLoader} — persistent_workers=(num_workers>0); worker_init_fn=seed_worker; train shuffle=True drop_last=False; val/test shuffle=False. (Phase 7 does NOT clamp num_workers=0 — use the supervised_train pattern.)
- seed_worker(worker_id) — copy verbatim from supervised_train.seed_worker (the torch.initial_seed()%2**32 + cv2.setNumThreads(0) body).

datasets/cvcspc_ssl.py (mirror squat_ssl.py; the phase signal + cross-rep contrast + NaN-exclusion are NEW):
- _traj2phase(traj: np.ndarray) -> np.ndarray — take the y-position column (CITED dataloader.py:20-27 get_phase uses traj[:,1]; if the staged trajectory is a flat 1-D y-list, use it directly), NaN-interpolate (reuse squat_ssl's np.isnan/np.interp pattern), normalize to [0,1] via (t-min)/max(max-min,1e-8), x360.0 -> degrees. GUARD: if the trajectory is empty, all-NaN, or constant (max-min < 1e-8 after interp / fewer than 2 valid points), raise ValueError("degenerate trajectory") so the dataset excludes the clip rather than dividing by zero / producing a garbage flat phase.
- class ShallowSquatSSLDataset(Dataset).__init__(*, frames_root, trajectories_root, traj_nan_path=None, ssl_contrastive_phase_gap=30.0, mask_prob=0.5, mask_amt_lo=0.4, mask_amt_hi=0.5, seed=42) — frame_ids = {dir.name for dir in frames_root.iterdir() if dir.is_dir()}; self._traj_paths={p.stem:p for p in Path(trajectories_root).rglob("*.json")}; LOAD THE NaN-TRAJECTORY EXCLUSION (CITED dataloader.py:67-68): if traj_nan_path is not None and Path(traj_nan_path).exists(): nan_stems = {Path(x).stem for x in json.loads(Path(traj_nan_path).read_text())} (traj_nan.json is a list of trajectory FILEnames whose bar-trajectory is NaN — strip the .json to match the {video}_{rep}_{frame} stems); else nan_stems=set() with a logger.warning that the exclusion file was not found (the unlabeled Squat set's traj_nan.json SHOULD be staged alongside the trajectories). self._clip_ids=sorted((frame_ids & set(self._traj_paths)) - nan_stems); ALSO defensively skip any clip whose _traj2phase raises (the GUARD above) during a one-time construction scan so phase computation never hits a divide-by-zero at __getitem__. store phase_gap + mask params + IMAGENET transform (Resize(256)+CenterCrop(224)+ToTensor()+Normalize). logger.info the clip count AFTER exclusion. NOTE the gotcha: clip ids are {video}_{rep}_{frame} STRING stems — NEVER int-cast (do not reuse splits._label_value).
- __getitem__(idx) -> dict {"anchor","positive","negative": tensor[3,224,224]} — v0=self._clip_ids[idx]; v1=a DIFFERENT random clip; load both phase arrays via _traj2phase; pick a shared random phase p in the overlap of the two phase ranges; anchor=frame@v0 nearest p; positive=frame@v1 nearest p; negative=frame@v1 at a phase >= self.phase_gap from p (sample then assert |phase-p|>=gap, fallback to argmax distance); load each JPEG from {frames_root}/{video_id}/{frame_file}, apply transform, then masking aug independently per branch.
- _mask(img: torch.Tensor) -> torch.Tensor — with prob mask_prob, zero the top random.uniform(mask_amt_lo,mask_amt_hi) fraction of rows (img[:, :k, :]=0). The masking-only aug; CITED dataloader.py:99-102 (only augmentations_dict['masking'] active) / image_augmentations.py. All OTHER augs absent.
- build_cvcspc_loader(dataset, config, *, seed=42) -> DataLoader — copy squat_ssl.build_ssl_loader verbatim, rename; persistent_workers=(num_workers>0); shuffle=True; drop_last=True.
- seed_worker — copy verbatim.

harness/image_supervised_train.py (NEW self-contained 2D-image trainer; mirror supervised_train's contracts in a 4-D form, but do NOT import it):
- @dataclass ImageConfig(learning_rate=1e-4, weight_decay=0.0, batch_size=32, num_workers=4, max_epochs=50, early_stop_patience=8, scheduler_name="cosine", scheduler_t_max=None, model_arch="resnet18_imagenet_v1", loss_name="BCEWithLogitsLoss(pos_weight=dataset.pos_weight)") + __post_init__ sets scheduler_t_max=max_epochs.
- build_resnet18() -> nn.Module — resnet18(weights=ResNet18_Weights.IMAGENET1K_V1); assert fc.in_features==512; model.fc=nn.Linear(512,1). (named build_resnet18 to make it unmistakably a 2D-image builder, NOT the video build_model.)
- _build_dataloaders(seed, config, images_root, labels_path, splits_root, *, dataset_cls=ShallowSquatDataset) -> {"train","val","test": DataLoader} — common_kwargs={images_root,labels_path,splits_root,seed}; dataset_cls(split=..., train_aug=...,**common_kwargs); persistent_workers=(num_workers>0); seed_worker; seeded generator. 4-D (B,3,H,W) tensors — NO video kwargs (num_frames/crop_size/train_jitter_frames). seed_worker + _set_global_seed copied verbatim from supervised_train (copy, not import).
- _val_pass(model, loader, criterion, device) -> (loss_mean, scores[N], labels[N]) — binary 4-D: model(clip[B,3,H,W]) -> logits[B,1]; scores=torch.sigmoid(torch.cat(logits)).squeeze(-1).numpy() (1-D [N]); labels 1-D int [N]. tqdm-wrapped.
- run_image_epoch(*, run_name, drive_root="/content/drive/MyDrive", images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=42, config=None, resume=True, max_epochs=None, dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase="phase07") -> dict — implement END-TO-END (do NOT call supervised_train.run_supervised_epoch): config_hash via hash_config over the ImageConfig keys; run_dir={drive_root}/FitNova/checkpoints/{checkpoint_phase}/{run_name}; build model via model_builder() (default build_resnet18 — the seam Plan 04 uses to inject the CVCSPC-backbone builder); Adam(lr,wd); CosineAnnealingLR(T_max); per-epoch train loop on 4-D batches; _val_pass; per-epoch val F1 at 0.5 proxy via f1_per_error(val_labels, (val_scores>=0.5).astype(int)) (SINGLE head — no macro average); BCEWithLogitsLoss(pos_weight); atomic epoch_NNN.pt every epoch (update_latest=True) + best.pt on val-F1 improvement (update_latest=False per the Phase-4/6 carry-fix); prune_checkpoints(keep_last=3,keep_best=True); early-stop patience; map_location='cpu' resume; payload code_version="phase07-image-baseline", best_thresholds=None. Returns {epoch, metrics_history, best_f1_val, best_thresholds:None, checkpoint_path, best_checkpoint_path, config_hash}. NOTE the model_builder seam is added HERE in Wave 0 (default build_resnet18) so Plan 04's CVCSPC fine-tune passes model_builder=lambda: build_cvcspc_finetune_model(...) with no further edit to this module.

harness/cvcspc_pretrain.py (NEW self-contained CVCSPC SSL trainer; mirror md_pretrain's contract but do NOT import its epoch loop):
- _CVCSPC_CHECKPOINT_KEYS = (epoch, backbone_state_dict, projector_state_dict, optimizer_state_dict, scheduler_state_dict, rng_state, metrics_history, triplet_acc_history, config_hash, config_repr, code_version).
- @dataclass CVCSPCConfig(learning_rate=1e-4, weight_decay=0.0, batch_size=25, num_workers=4, max_epochs=100, projector_hidden=128, projector_out_dim=128, phase_gap_start=30.0, phase_gap_decay_every=4, mask_prob=0.5, mask_amt_lo=0.4, mask_amt_hi=0.5, triplet_acc_cadence=5, scheduler_name="cosine", model_arch="resnet18_cvcspc_ssl_v1") + __post_init__ sets scheduler_t_max=max_epochs. (projector_hidden/projector_out_dim are config knobs so the [ASSUMED] 512->128->128 head is ablatable to 512->512 without a code edit — the NICE-TO-HAVE.)
- class CVCSPCProjectionHead(nn.Module).__init__(in_dim=512, hidden=128, out_dim=128) — nn.Sequential(Linear(in,hidden), ReLU(inplace=True), Linear(hidden,out)); forward returns F.normalize(net(x), dim=-1, p=2). NO BatchNorm1d (small batch 25). dims taken from config so they are ablatable.
- cvcspc_triplet_loss(phi_anc, phi_pos, phi_neg, *, eps=1e-8) -> torch.Tensor — d_ap=((a-p)**2).sum(-1); d_an=((a-n)**2).sum(-1); d_pn=((p-n)**2).sum(-1); num=exp(-d_ap); den=num+exp(-d_an)+exp(-d_pn); return (-log(num/(den+eps))).mean(). (3-term, squared L2 — the code version; the WHY-comment notes the paper Eq.1 is 2-term.)
- build_cvcspc_model(config) -> (backbone, projector) — resnet18(weights=None); load resnet18(IMAGENET1K_V1).state_dict() strict=False; assert fc.in_features==512; backbone.fc=nn.Identity(); projector=CVCSPCProjectionHead(512, config.projector_hidden, config.projector_out_dim).
- build_cvcspc_checkpoint_payload(*, epoch, backbone, projector, optimizer, scheduler, metrics_history, triplet_acc_history, config_hash, config_repr) -> dict — the _CVCSPC_CHECKPOINT_KEYS set; code_version="phase07-cvcspc-pretrain".
- _triplet_accuracy(backbone, projector, loader, device) -> float — frozen, no-grad fraction of triplets where ||phi_a-phi_p||^2 < ||phi_a-phi_n||^2 (CITED train_test.py:130-134) — the convergence monitor (replaces md_pretrain's _linear_probe; no labeled loader needed at SSL time).
- run_cvcspc_pretrain_epoch(*, run_name, drive_root="/content/drive/MyDrive", frames_root="/content/squat_ssl_frames", trajectories_root="/content/squat_trajectories", traj_nan_path=None, seed=42, config=None, resume=True, max_epochs=None, ssl_dataset_cls=ShallowSquatSSLDataset, checkpoint_phase="phase07") -> dict — implement END-TO-END (do NOT call run_md_pretrain_epoch): AdamW(lr,wd); cosine; per-epoch ssl_loss + the triplet-accuracy monitor every config.triplet_acc_cadence epochs on a held-out subset of the SAME loader; backbone.pt on triplet-acc improvement (update_latest=False); per-epoch atomic checkpoint + prune; map_location='cpu' resume; the phase-gap anneal at epoch end (CITED train_test.py:172-179: if (epoch+1)%config.phase_gap_decay_every==0 and config.phase_gap_start>30: config.phase_gap_start=max(30.0, config.phase_gap_start-1.0); loader.dataset.phase_gap=config.phase_gap_start). NO collapse-abort (ResNet-18 ImageNet-init is stable). Returns {epoch, metrics_history, triplet_acc_history, backbone_path, checkpoint_path, config_hash}.

harness/colab.py (ADD — additive, no edits to existing fns):
- _SHALLOW_SQUAT_IMAGES_EXPECT_COUNT = 3738.
- stage_shallow_squat_images(drive_root_3001, *, local_root="/content/squat_shallow_images", expect_count=_SHALLOW_SQUAT_IMAGES_EXPECT_COUNT) -> str — src={drive_root_3001}/Fitness-AQA_dataset_release/Squat/Labeled_Dataset/Shallow_Squat_Error_Dataset/images.zip; byte-resume copy then zipfile.extractall (JPEG members — NOT _extract_with_resume_and_progress which is mp4-only); verify count of *.jpg recursively == expect_count. Also copy labels_shallow_depth.json + the splits/ dir alongside (the EDA/training notebooks read them from local_root). Mirror the trajectory-zip branch of stage_unlabeled_squat_videos.
- extract_frames_for_ssl(videos_root, frames_root, *, skip_existing=True) -> int — for each mp4 in Path(videos_root).glob("*.mp4"): video_id=stem; if skip_existing and (frames_root/video_id).exists() and any(...iterdir()): skip; else cv2.VideoCapture read loop writing {frames_root}/{video_id}/frame_{i:06d}.jpg via cv2.imwrite; tqdm progress (cell >2 min); return total frames. Disconnect-safe via skip_existing.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

Locked from CONTEXT.md (D2/D3/D5/D6/D7) + RESEARCH §CVCSPC Method/§Architecture/§Supervised + PATTERNS. The decisive RESEARCH deltas vs the Squat/OHP video pipeline: (1) single-image input (NO video decode, NO temporal axis, NO Kinetics norm — ImageNet norm + 224²); (2) the CVCSPC pretext is phase-matched CROSS-REP (two different reps at the same bar-trajectory phase), NOT half-cycle; (3) the loss is the CITED 3-term code version, deviating from the paper's 2-term Eq.1; (4) the trainers are NEW self-contained modules because the video trainers hardcode r2plus1d_18 / 5-D tensors with no model_cls seam.

| # | Decision | Source |
|---|----------|--------|
| D3 | ShallowSquatDataset = single-image JPEG -> 224² -> ImageNet mean/std. NO frame sampling, NO decode_clip, NO Kinetics norm. Labels are a FLAT {id:0/1} dict (labels_shallow_depth.json) read directly — NOT routed through splits.index/_label_value (which int-casts interval-list labels). IDs are {video}_{rep}_{frame} string keys. | CONTEXT D3 + RESEARCH §Pattern 1 / Pitfall 1 |
| D5 | SINGLE binary head — Shallow-Squat is ONE binary error ("shallow depth"), unlike Squat KIE/KFE or OHP Elbows/Knees (which are 2 errors). The head is nn.Linear(512,1), the loss BCEWithLogitsLoss on a single logit, pos_weight shape [1] (NOT [2]), and the eval a single score column (eval/metrics.py operates on a single score column). Shallow-Squat ~44% positive (1584/3611) -> near-balanced, train pos_weight ~1.28; the weighting is a minor correction (far less load-bearing than Squat KIE's 14%). The dataset label shape, the model head, the loss, and the eval are all the single-logit contract. | CONTEXT D5 + RESEARCH §Supervised Recipe + checker SHOULD-FIX #6 |
| D2-loss | cvcspc_triplet_loss = the 3-term code version (CITED train_test.py:64-68): -log(e^-d_ap / (e^-d_ap + e^-d_an + e^-d_pn)), squared L2 on L2-normalized features. The paper Eq.1 shows a 2-term denominator — implement the CODE version; the deviation is documented in FINDINGS (Plan 05), and is the ONE non-obvious WHY-comment allowed in cvcspc_pretrain.py. The loss is verified by BOTH a closed-form value AND a directionality assertion (loss must decrease as the anchor moves closer to its positive / farther from its negative). | CONTEXT D2 + RESEARCH §Loss / §Open Q + checker SHOULD-FIX #3 |
| D2-pretext | The CVCSPC triplet contrasts phase-matched frames across two DIFFERENT Squat reps (anchor@v0 + positive@v1 at a shared bar-trajectory phase; negative@v1 at a phase >= phase_gap away). Phase = normalize(bar-traj y) x360° (CITED dataloader.py:20-27). The official traj_nan.json NaN-trajectory clips are EXCLUDED at construction (CITED dataloader.py:67-68); _traj2phase guards degenerate trajectories. Masking-only aug (top 40-50% blackout, p~0.5; all other augs commented out in the shipped code — CITED dataloader.py:99-102). Phase-gap anneals 30°-> over epochs (CITED train_test.py:172-179). | CONTEXT D2/D2a + RESEARCH §Triplet Construction / §Augmentations + checker BLOCKER 2 |
| D2-proj | CVCSPCProjectionHead = 512->128->128 (Linear-ReLU-Linear), NO BatchNorm1d (small batch 25; CVCSPC paper does not use BN in the head), F.normalize at the end. [ASSUMED dims — linear_layers.py absent]; the dims are CVCSPCConfig knobs (projector_hidden/projector_out_dim) so they are ablatable to 512->512 without a code edit if triplet-accuracy is poor (the NICE-TO-HAVE config knob). | CONTEXT Discretion + RESEARCH §Architecture / A1 + checker NICE-TO-HAVE |
| D6-reuse | eval/metrics.py REUSED UNCHANGED (binary single-head f1/pr_auc/threshold_sweep — single score column). The colab harness primitives (atomic_save_checkpoint/load_latest_checkpoint/prune/hash_config/capture_rng_state) REUSED UNCHANGED. The TRAINERS are NEW self-contained modules: image_supervised_train.py implements run_image_epoch end-to-end (a build_resnet18 4-D loop with the dataset_cls/model_builder/checkpoint_phase seams + checkpoint/resume/early-stop/threshold-sweep) and does NOT import supervised_train (r2plus1d_18 / 5-D / no model_cls seam); cvcspc_pretrain.py implements run_cvcspc_pretrain_epoch end-to-end (mirroring md_pretrain's contract with triplet-accuracy replacing the linear-probe) and does NOT import md_pretrain. Small helpers (seed_worker, _set_global_seed, the config_repr/hash + resume idiom) may be COPIED verbatim (the Phase-4 D9 copy-not-import precedent). | CONTEXT D6 + RESEARCH §Architecture Patterns + checker BLOCKER 1 |
| D7-landmines | persistent_workers=(num_workers>0) on every loader; from __future__ import annotations first line; logger aqa.phase07; best.pt/backbone.pt writes pass update_latest=False; map_location='cpu' on resume; PIL Image.open for JPEG (tv 0.26 removed read_video — read_image still exists but PIL matches the official code); images.zip uses zipfile.extractall NOT the mp4-only extractor; frame dirs are {video_id}/frame.jpg subdirs (NOT flat); the SSL dataset excludes traj_nan.json clips. | CONTEXT D6/D7 + RESEARCH Pitfalls 2/3/7 |

</decisions>

<determinism_checklist>
## Determinism / hygiene preconditions (carry Phase 2/3/4/6 + D7 landmines)

1. logger = logging.getLogger("aqa.phase07") in every new module (NOT phase04/phase06).
2. from __future__ import annotations as the first import in every new file (CLAUDE.md; every existing backend/training/aqa/ file has it).
3. persistent_workers = config.num_workers > 0 guard in build_loaders + build_cvcspc_loader + image_supervised_train._build_dataloaders. Always pair with worker_init_fn=seed_worker.
4. seed_worker + _set_global_seed COPIED (not imported) per the Phase-4 D9 precedent (md_pretrain copies from supervised_train). The trainers do NOT import the video epoch loops.
5. ImageNet norm constants are MODULE-LEVEL in shallow_squat.py + cvcspc_ssl.py — NEVER import from transforms.py (Kinetics norm). Pitfall 1: wrong norm -> F1 stuck ~0.44 (random on near-balanced data).
6. Labels: labels_shallow_depth.json is a FLAT {id:0/1} dict (D3) — json.loads then labels[id], NO int-cast, NO splits._label_value (that function int-casts interval lists, the wrong contract for this flat dict). IDs are {video}_{rep}_{frame} strings. The model head is Linear(512,1), pos_weight [1], the label a scalar (D5 single-head).
7. CVCSPC frame dirs: {frames_root}/{video_id}/frame_{:06d}.jpg subdirs (Pitfall 2) — the dataset does os.listdir per video_id dir. extract_frames_for_ssl writes that layout; skip_existing checks the dir exists + non-empty (Pitfall 7).
8. images.zip extract: zipfile.extractall (JPEG members) — NOT _extract_with_resume_and_progress (mp4-only filter, would silently skip all JPEGs — Pitfall 3).
9. CVCSPC SSL data: the traj_nan.json NaN-trajectory clips are EXCLUDED at construction (CITED dataloader.py:67-68); _traj2phase raises ValueError on a degenerate (empty/all-NaN/constant) trajectory so phase computation never divides by zero (BLOCKER 2).
10. test files: torch = pytest.importorskip("torch") at module top; the split-size test is skipif on a missing local Shallow-Squat archive (reads real files). The loss/triplet/proj/mask/traj-nan tests are pure CPU, sub-second, no skipif.
11. NO collapse-abort in cvcspc_pretrain (ResNet-18 ImageNet-init is far more stable than R(2+1)D-18 from Kinetics on a small unlabeled set — the Phase-4 collapse guard is not carried; the triplet-accuracy monitor is the convergence signal instead).
12. NO AI-narration comments in source (project rule): no "RESEARCH §", no "mirrors squat_ssl", no D-numbers IN CODE. The 3-term-vs-2-term loss deviation is the one allowed non-obvious WHY-comment. All other citations live in this plan + the commit messages.

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training + evaluation pipeline (D1 descopes serving/API entirely): NO network surface, NO authentication, NO untrusted user input at any point, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. There are NO high-severity threats — injection/XSS/authz do not apply to a CPU-only dataset-module test suite. The genuine constraints are: the CVCSPC loss + phase-matching correctness (a wrong implementation wastes the SSL compute), the NaN-trajectory exclusion (an un-excluded NaN clip produces garbage phases), not committing dataset media/weights, and dataset-license compliance. Per the planning brief: state this honestly rather than inventing web-app threats.

| Boundary | Description |
|----------|-------------|
| Synthetic feature triplets -> cvcspc_triplet_loss | The 3-term loss is the ONE new contrastive core. A wrong sign, a missing pos-neg term, or non-squared distances silently produces a meaningless gradient across the SSL run. Caught by the closed-form + directionality unit test (this plan). |
| Synthetic bar-trajectories + traj_nan.json -> the phase-matched triplet | A wrong _traj2phase normalization, an un-excluded NaN trajectory (divide-by-zero / garbage phase), or anchor/positive NOT phase-matched, or negative NOT phase-gap away, produces meaningless contrastive pairs. Caught by the synthetic-trajectory unit test + the NaN-guard test (this plan); the live frame-extract + format is re-confirmed in Plan 03's Step 0 probe. |
| Local Shallow-Squat archive (research-use-only) -> code | Reading the local archive for the split-size test (offline inspection only). License: non-commercial, do not redistribute. No dataset media committed (gitignored / Drive-staged). |
| New modules -> Phase 3/4/6 behavior | The new modules are independent; colab.py edits are additive. A wrong edit could break the existing Squat+OHP suite. Caught by the no-regression pytest run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | cvcspc_triplet_loss (the one new contrastive core) — wrong term/sign/squaring produces a meaningless gradient across the SSL burn | mitigate | test_cvcspc_triplet_loss asserts the value equals the CITED train_test.py:64-68 three-term closed form within 1e-6 on synthetic L2-normalized triplets AND asserts DIRECTIONALITY (loss(d_ap small, d_an large, d_pn large) < loss(all-equal)). HIGH-cost-if-wrong, caught at the cheapest point (a CPU unit test). The 3-term-vs-2-term paper deviation is the one documented WHY-comment. |
| T-07-02 | Tampering | ShallowSquatSSLDataset phase-matching / NaN-exclusion — wrong _traj2phase, an un-excluded NaN trajectory (divide-by-zero / garbage phase), or anchor/positive not phase-matched or negative not gap-away produces meaningless triplets | mitigate | test asserts _traj2phase normalization on a synthetic ramp (min->0°, max->360°), the degenerate-guard raises on empty/constant/all-NaN, the traj_nan.json clips are excluded from self._clip_ids, the triplet construction picks anchor/positive within a small phase tolerance + negative phase-gap >= the gap + all frame indices in range. The live frame layout/format is re-confirmed in Plan 03's Step 0. MEDIUM, caught on synthetic data. |
| T-07-03 | Integrity | Wrong norm (Kinetics instead of ImageNet) on ShallowSquatDataset — F1 stuck at ~0.44 | mitigate | ImageNet norm constants are module-level in shallow_squat.py; the test asserts pixel values after the val transform fall in [-3,3] (ImageNet range), NOT the Kinetics range. NEVER import transforms.py (Pitfall 1). LOW — a constant, range-tested. |
| T-07-04 | Integrity | colab.py additive edits or the new modules silently break the Phase 3/4/6 Squat+OHP suite | mitigate | The new modules are independent; stage_shallow_squat_images + extract_frames_for_ssl are additive (no edits to existing fns). Task 5's verify runs the FULL existing suite (-m "not slow") and asserts it stays green. LOW — additive, regression-tested. |
| T-07-05 | Integrity | Committing dataset media / weights (the Shallow-Squat crops, the unlabeled Squat videos) — license + repo-bloat | accept | All dataset media + weights are gitignored / Drive-staged (Phase 1-6 contract). This plan commits only .py source + tests; the split-size test reads (does not copy) the local archive. Stage files by name only. LOW — the gitignore contract is established; no media touched. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs this phase (RESEARCH Package Legitimacy Audit: "No new packages are installed in this phase"; torch/torchvision/PIL/numpy/sklearn/cv2 all Colab-provided or in backend/requirements.txt). No [ASSUMED]/[SUS]/[SLOP] packages exist; the legitimacy checkpoint is not applicable. |

T-07-01 (the 3-term loss) is the one to catch — its CPU unit test (closed-form + directionality) is the whole point of Wave 0 (verify the new contrastive core before the SSL compute). All threats MEDIUM at worst; none block execution. No web-app threats apply (offline pipeline).

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement datasets/shallow_squat.py (ShallowSquatDataset + [1] pos_weight + build_loaders) + the IMG-01 dataset tests</name>
  <files>
    backend/training/aqa/datasets/shallow_squat.py,
    backend/training/aqa/datasets/test_shallow_squat.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat.py (the FULL file, 208 lines — the template: module docstring, _compute_pos_weight lines 41-58, SquatKIEKFEDataset lines 61-161, build_loaders lines 164-208; shallow_squat DROPS the video decode + Kinetics norm + the splits.index call AND collapses the 2-head label to a single scalar),
    backend/training/aqa/datasets/test_ohp.py (the FULL file — the test patterns: the local-archive skipif guard lines 22-25, the pos_weight pure test lines 62-77, the synthetic-data shape test lines 99-145, the dataset_cls signature seam test lines 152-178),
    backend/training/aqa/harness/supervised_train.py (seed_worker lines 144-161 — COPY verbatim into shallow_squat.py per the D9 copy-not-import precedent),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the "datasets/shallow_squat.py" section — the exact imports block, _compute_pos_weight [1]-shape, the ShallowSquatDataset skeleton, the build_loaders factory, the test_shallow_squat.py test examples 1-6),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Code Examples — the ImageNet transform + the ShallowSquatDataset skeleton; §Pitfall 1 wrong-norm; the data layout: crops_unaligned/{id}.jpg, labels_shallow_depth.json flat {id:0/1}, splits/{train,val,test}_ids.json sizes 2542/529/540),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D3 image pipeline + D5 SINGLE-head class balance + the ID format {video}_{rep}_{frame})
  </read_first>
  <action>
    Create shallow_squat.py as a single-image analog of squat.py (PATTERNS "datasets/shallow_squat.py" section). Changes vs squat.py: (1) module docstring documents the Shallow-Squat numbers (3611 crops, splits 2542/529/540, 43.9% pos, flat {id:0/1} labels, ImageNet norm 224², SINGLE binary error) — NO AI-narration/citation comments per the project rule, just the factual docstring; (2) logger = logging.getLogger("aqa.phase07"); (3) imports: from __future__ import annotations; json; logging; pathlib.Path; typing.Literal; torch; torchvision.transforms as T; PIL.Image; torch.utils.data DataLoader/Dataset — do NOT import transforms.py (Kinetics norm); (4) module constants IMAGENET_MEAN=[0.485,0.456,0.406], IMAGENET_STD=[0.229,0.224,0.225]; (5) _compute_pos_weight(train_records: list[tuple[str,int]]) -> torch.Tensor returns shape [1] = [(N-pos)/max(pos,1)] (SINGLE binary head — the WHY-comment: single-label binary, so [w] not [w_kie,w_kfe]); (6) class ShallowSquatDataset(Dataset).__init__(split, *, images_root, labels_path, splits_root, train_aug=True, seed=42) — labels=json.loads(Path(labels_path).read_text()); ids=json.loads(Path(splits_root,f"{split}_ids.json").read_text()); train_ids=json.loads(Path(splits_root,"train_ids.json").read_text()); self.records=[(i,labels[i]) for i in ids if i in labels]; pos_weight=_compute_pos_weight([(i,labels[i]) for i in train_ids if i in labels]) ALWAYS from train; self.transform = the train Compose (RandomResizedCrop(224,scale=(0.8,1.0)),RandomHorizontalFlip(0.5),ColorJitter(0.2,0.2),ToTensor(),Normalize(IMAGENET)) if train_aug else the val Compose (Resize(256),CenterCrop(224),ToTensor(),Normalize(IMAGENET)); (7) __getitem__ -> (self.transform(Image.open(self.images_root/f"{id}.jpg").convert("RGB")), torch.tensor(label, dtype=torch.float32)) — a SCALAR label (single binary error), NOT a 2-vector; (8) build_loaders(*, batch_size=32, num_workers=4, **dataset_kwargs) per PATTERNS (persistent_workers=(num_workers>0), worker_init_fn=seed_worker, train shuffle=True, val/test shuffle=False, drop_last=False); (9) seed_worker copied verbatim from supervised_train (torch.initial_seed()%2**32 + np/random + cv2.setNumThreads(0) in a try/except).

    Create test_shallow_squat.py (mirror test_ohp.py) with: module top from __future__ import annotations; json; pathlib.Path; pytest; torch=pytest.importorskip("torch"); a _LOCAL_SQUAT_3001 path (Fitness-AQA/Fitness-AQA_dataset_release-20260518T073812Z-3-001) + _LOCAL_ARCHIVE_EXISTS guard. Tests: (a) test_pos_weight — _compute_pos_weight([("a",1),("b",0),("c",1),("d",0)]) returns shape (1,) approx 1.0 (assert tuple(out.shape) == (1,) — the single-head shape, NOT (2,)); (b) test_getitem_shape(tmp_path) — write 2 synthetic JPEGs (PIL.Image.fromarray random uint8 256x256x3) to a crops dir + a flat labels json + splits/{train,val,test}_ids.json listing the ids; ShallowSquatDataset(split="train", images_root=crops, labels_path=..., splits_root=..., train_aug=False); assert ds[0] is (tensor (3,224,224) float32, a SCALAR float32 label — assert label.ndim==0 and float(label) in {0.0,1.0}); (c) test_imagenet_norm — same synthetic dataset (or an all-white image), assert the val-transform tensor min>-3.0 and max<3.0 (ImageNet range, NOT Kinetics); (d) test_build_loaders(tmp_path) — build_loaders(images_root=...,labels_path=...,splits_root=...,batch_size=2,num_workers=0) returns a dict with exactly {"train","val","test"} keys, each a DataLoader; AND pull one batch, assert the label batch shape is (B,) (single-head — NOT (B,2)); (e) test_split_sizes — @pytest.mark.skipif(not _LOCAL_ARCHIVE_EXISTS) — construct against the local archive's Shallow_Squat_Error_Dataset, assert len==2542/529/540 for train/val/test. Keep all tests fast (no GPU; PIL transforms are sub-second).

    Single runnable unit — user pastes back: (1) python -c AST-parse of shallow_squat.py prints "parse ok"; (2) python -m pytest backend/training/aqa/datasets/test_shallow_squat.py -k "not split_sizes" -x -v (the synthetic + pure tests pass) AND optionally test_split_sizes against the local archive (passes 2542/529/540 or skips cleanly).

    Atomic commit: feat(07): shallow_squat.py ShallowSquatDataset + [1] pos_weight + IMG-01 dataset tests (Task 1, D3/D5).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/datasets/shallow_squat.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_shallow_squat.py -k "not split_sizes" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - shallow_squat.py parses; defines ShallowSquatDataset + _compute_pos_weight + build_loaders + seed_worker; logger aqa.phase07; from __future__ import annotations first line; ImageNet norm constants module-level; NO transforms.py import
    - _compute_pos_weight returns shape [1] (test asserts tuple(shape)==(1,), NOT (2,)); the synthetic 2/4-positive case gives ~1.0 — the SINGLE-head contract (D5)
    - __getitem__ returns (tensor[3,224,224] float32, SCALAR float32 label — label.ndim==0); the label batch from build_loaders is shape (B,), NOT (B,2); labels read from the FLAT {id:0/1} dict directly (no int-cast, no splits._label_value)
    - test_imagenet_norm confirms the val transform produces [-3,3] pixels (ImageNet, not Kinetics); NO AI-narration/citation comments in the source
    - Runnable unit: user pastes back the AST-parse + the dataset tests passing (split_sizes passes against the local archive or skips cleanly)
  </acceptance_criteria>
  <dependencies>None — Wave 0 entry task</dependencies>
  <done>ShallowSquatDataset = the single-image analog of the Squat dataset (ImageNet norm, flat-dict labels, [1] SINGLE-head pos_weight, scalar label); the labeled Shallow-Squat pipeline the baseline + fine-tune consume exists + is shape/norm-proven on synthetic data; the official-split reconciliation passes against the local archive</done>
</task>

<task type="auto">
  <name>Task 2: Implement harness/cvcspc_pretrain.py pure-function cores (3-term loss + projection head + build_cvcspc_model) + the IMG-01 loss/proj tests</name>
  <files>
    backend/training/aqa/harness/cvcspc_pretrain.py,
    backend/training/aqa/datasets/test_cvcspc_ssl.py
  </files>
  <read_first>
    backend/training/aqa/harness/md_pretrain.py (the FULL file, 525 lines — the template: imports 22-47, _SSL_CHECKPOINT_KEYS 53-65, MDConfig 68-95, ProjectionHead 98-118, md_triplet_loss 121-162, build_md_model 165-189, build_ssl_checkpoint_payload 192-223, seed_worker/_set_global_seed 226-244, _linear_probe 288-312 which _triplet_accuracy REPLACES, run_md_pretrain_epoch 315-525 — MIRROR the contract, do NOT import the run loop),
    Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/train_test.py (the official loss + the triplet-accuracy monitor + the phase-gap anneal — ground the 3-term loss closed form, _triplet_accuracy (AP<AN), and the anneal at train_test.py:172-179),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the "harness/cvcspc_pretrain.py" section — the exact CVCSPCConfig, CVCSPCProjectionHead (NO BatchNorm, configurable dims), cvcspc_triplet_loss (3-term), build_cvcspc_model, build_cvcspc_checkpoint_payload, _triplet_accuracy bodies to implement),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Loss Function — the 3-term code version vs the 2-term paper Eq.1, the deviation to document; §Architecture — the [ASSUMED] 512->128->128 head, no BN; §Code Examples — the cvcspc_triplet_loss + the ResNet-18 backbone load),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2 the 3-term loss + the masking-only pretext; Discretion the projection-head dims)
  </read_first>
  <action>
    Create cvcspc_pretrain.py as a NEW SELF-CONTAINED CVCSPC SSL trainer that MIRRORS the md_pretrain.py contract with the 3-term-loss + triplet-accuracy deltas (PATTERNS "harness/cvcspc_pretrain.py" section) — it does NOT import md_pretrain's R(2+1)D run_md_pretrain_epoch. THIS TASK implements the PURE-FUNCTION CORES + their tests; the run_cvcspc_pretrain_epoch training-loop body is landed in Task 3 (split so the highest-leverage check — the loss — is proven first, the Phase-4 Wave-0 precedent). Per PATTERNS:

    1. Imports = the md_pretrain.py import set (from __future__ import annotations; dataclasses; logging; os; random; time; typing.Any; numpy; torch; torch.nn as nn; torch.nn.functional as F; DataLoader) + from backend.training.aqa.datasets.cvcspc_ssl import ShallowSquatSSLDataset, build_cvcspc_loader + from backend.training.aqa.harness.colab import the atomic-checkpoint primitives. Do NOT import md_pretrain. logger = logging.getLogger("aqa.phase07").
    2. _CVCSPC_CHECKPOINT_KEYS tuple (epoch, backbone_state_dict, projector_state_dict, optimizer_state_dict, scheduler_state_dict, rng_state, metrics_history, triplet_acc_history, config_hash, config_repr, code_version) — replaces md_pretrain's linear_probe_history with triplet_acc_history.
    3. @dataclass CVCSPCConfig per the interfaces block (lr=1e-4, wd=0.0, batch_size=25, num_workers=4, max_epochs=100, projector_hidden=128, projector_out_dim=128, phase_gap_start=30.0, phase_gap_decay_every=4, mask_prob=0.5, mask_amt_lo=0.4, mask_amt_hi=0.5, triplet_acc_cadence=5, scheduler_name="cosine", model_arch="resnet18_cvcspc_ssl_v1") + __post_init__ scheduler_t_max=max_epochs. projector_hidden/projector_out_dim are knobs so the head dims are ablatable to 512->512 without a code edit.
    4. class CVCSPCProjectionHead(in_dim=512, hidden=128, out_dim=128) — nn.Sequential(Linear,ReLU(inplace=True),Linear); forward returns F.normalize(net(x),dim=-1,p=2). NO BatchNorm1d — the WHY-comment: small batch 25, BN unstable; the CVCSPC head has no BN. The hidden/out dims come from build_cvcspc_model's config so they are configurable.
    5. cvcspc_triplet_loss(phi_anc, phi_pos, phi_neg, *, eps=1e-8) — d_ap/d_an/d_pn = squared-L2 sums; num=exp(-d_ap); den=num+exp(-d_an)+exp(-d_pn); return (-log(num/(den+eps))).mean(). The ONE allowed non-obvious WHY-comment: "Three-term denominator (the shipped code, train_test.py); the paper Eq.1 shows two terms — deviation recorded in FINDINGS." (this WHY is load-bearing for [[feedback_ai_correctness]] — keep it.)
    6. build_cvcspc_model(config) -> (backbone, projector) — lazy torchvision import; resnet18(weights=None); load resnet18(IMAGENET1K_V1).state_dict() with strict=False; assert backbone.fc.in_features==512; backbone.fc=nn.Identity(); projector=CVCSPCProjectionHead(512,config.projector_hidden,config.projector_out_dim); return both.
    7. build_cvcspc_checkpoint_payload(...) — the _CVCSPC_CHECKPOINT_KEYS dict; code_version="phase07-cvcspc-pretrain". seed_worker + _set_global_seed copied verbatim from md_pretrain (copy, not import).
    8. _triplet_accuracy(backbone, projector, loader, device) -> float — frozen no-grad fraction where ((phi_a-phi_p)**2).sum(-1) < ((phi_a-phi_n)**2).sum(-1), over batches of {anchor,positive,negative}.
    9. Add a placeholder run_cvcspc_pretrain_epoch stub with the FULL signature from the interfaces block (so Task 3 fills the body + the dataset_cls seam test in Task 5 can inspect the signature) — the stub may `raise NotImplementedError("body landed in Task 3")` OR be left as the signature + a docstring; the AST-parse must pass. (Per [[feedback_one_cell_at_a_time_strict]], do NOT implement the loop body here — that is Task 3.)

    In test_cvcspc_ssl.py (mirror test_ohp_ssl.py), add the LOSS + PROJ tests (this task's deliverable): module top from __future__ import annotations; json; pathlib.Path; numpy; pytest; torch=pytest.importorskip("torch"). (a) test_cvcspc_triplet_loss — build 3 synthetic [4,8] tensors, L2-normalize (F.normalize) them; compute the expected 3-term value by hand (numpy: -log(e^-d_ap/(e^-d_ap+e^-d_an+e^-d_pn)) per row, mean); assert cvcspc_triplet_loss(...) approx the hand value within 1e-6; (b) test_cvcspc_loss_directional — construct two cases on L2-normalized vectors: GOOD = anchor≈positive (d_ap small) AND negative far from anchor (d_an large, d_pn large); BAD = all three (near-)equal (d_ap≈d_an≈d_pn≈0); assert cvcspc_triplet_loss(GOOD) < cvcspc_triplet_loss(BAD). This proves the loss is DIRECTIONAL (rewards anchor-close-to-positive / far-from-negative), catching a sign flip or a missing pos-neg term that a single hand-value check could miss; (c) test_projection_head — CVCSPCProjectionHead(512,128,128)(torch.randn(4,512)) returns shape (4,128) + each row unit-norm (norm approx 1.0 within 1e-5); assert NO BatchNorm1d module in the head (not any(isinstance(m,nn.BatchNorm1d) for m in head.modules())); AND test the dims are configurable: CVCSPCProjectionHead(512,512,512)(torch.randn(2,512)) returns (2,512) (the 512->512 ablation knob works).

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/datasets/test_cvcspc_ssl.py -k "loss or projection" -x -v (the loss closed-form + directionality + the proj-head/dims tests pass; pure CPU, sub-second). Also python -c AST-parse of cvcspc_pretrain.py prints "parse ok".

    Atomic commit: feat(07): cvcspc_pretrain.py 3-term loss + projection head + build_cvcspc_model + IMG-01 loss tests (Task 2, D2-loss/D2-proj).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/harness/cvcspc_pretrain.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_cvcspc_ssl.py -k "loss or projection" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - cvcspc_pretrain.py parses; defines CVCSPCConfig + CVCSPCProjectionHead + cvcspc_triplet_loss + build_cvcspc_model + build_cvcspc_checkpoint_payload + _triplet_accuracy + the run_cvcspc_pretrain_epoch signature (body in Task 3); logger aqa.phase07; does NOT import md_pretrain
    - test_cvcspc_triplet_loss: the 3-term value matches the hand-computed closed form within 1e-6; test_cvcspc_loss_directional: loss(d_ap small, d_an/d_pn large) < loss(all-equal) — the loss is DIRECTIONAL (SHOULD-FIX #3)
    - CVCSPCProjectionHead outputs unit-norm [B,128] with NO BatchNorm1d module + the dims are configurable (512->512 ablation works); build_cvcspc_model loads ImageNet weights (strict=False), fc=Identity
    - The 3-term-vs-2-term deviation is the ONE WHY-comment present; NO other AI-narration/citation comments in the source
    - Runnable unit: user pastes back the loss + projection tests passing + the AST-parse
  </acceptance_criteria>
  <dependencies>None structurally (cvcspc_ssl.py is imported at module top but the loss/proj tests do not instantiate the dataset; to keep the top-level import resolvable, this task creates a minimal cvcspc_ssl.py stub with just the class skeleton + build_cvcspc_loader signature; Task 3 fills the bodies).</dependencies>
  <done>The CVCSPC 3-term loss (the highest-leverage pre-GPU check, proven by closed-form AND directionality) + the configurable no-BN projection head are implemented + proven on synthetic data against the CITED closed form; the SSL model builder + checkpoint payload exist; the run-loop body is staged for Task 3; the trainer is self-contained (no md_pretrain import)</done>
</task>

<task type="auto">
  <name>Task 3: Implement datasets/cvcspc_ssl.py (phase-matched triplet + _traj2phase + masking aug + traj_nan exclusion) + cvcspc_pretrain.run loop + the IMG-01 triplet tests</name>
  <files>
    backend/training/aqa/datasets/cvcspc_ssl.py,
    backend/training/aqa/harness/cvcspc_pretrain.py,
    backend/training/aqa/datasets/test_cvcspc_ssl.py
  </files>
  <read_first>
    backend/training/aqa/datasets/squat_ssl.py (the FULL file, 273 lines — the template: imports 24-44, _load_trajectory NaN-interp lines 149-167 (REUSE the np.isnan/np.interp pattern for _traj2phase), _augment coin-flip lines 169-208 (the per-branch independent-aug pattern; CVCSPC keeps ONLY masking), __getitem__ lines 210-242 (REPLACE the half-cycle logic with cross-rep phase-matching), seed_worker 245-249, build_ssl_loader 252-272 (COPY verbatim -> build_cvcspc_loader)),
    Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/dataloader.py (the OFFICIAL CVCSPC dataloader — ground get_phase lines 20-27 (y-normalize x360), the traj_nan.json exclusion lines 39-40/67-68 (exclude_list), the cross-rep positive + phase-gap negative lines 62-79, the masking-only _augment lines 99-102, _load_frame lines 92-97),
    backend/training/aqa/harness/md_pretrain.py (run_md_pretrain_epoch 315-525 — the loop CONTRACT to MIRROR (NOT import): config_repr/hash, run_dir, AdamW+cosine, per-epoch ssl_loss, the monitor-every-N-epochs cadence, backbone.pt-best update_latest=False, prune, map_location='cpu' resume, the return dict — Task 3 mirrors it with _triplet_accuracy replacing _linear_probe + the phase-gap anneal),
    Fitness-AQA-Code/Code_Release/pose_contrastive_learning/self_supervised_learning/train_test.py (the phase-gap anneal at train_test.py:172-179 — the exact (epoch+1)%4==0 -> gap-=1 decrement to implement),
    backend/training/aqa/harness/cvcspc_pretrain.py (the Task-2 cores — cvcspc_triplet_loss, build_cvcspc_model, _triplet_accuracy, build_cvcspc_checkpoint_payload, CVCSPCConfig — the loop wires them),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the "datasets/cvcspc_ssl.py" section — the imports, the FLAT-glob-vs-rglob note (trajectories rglob; frame dirs iterdir), the _traj2phase + the cross-rep __getitem__ + the masking _augment + build_cvcspc_loader; the "run_cvcspc_pretrain_epoch" substitutions list),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Triplet Construction — anchor@v0 + positive@v1 at a shared phase + negative@v1 >= gap away; §What Phase Means + traj2phase x360; §Augmentations masking-only; §SSL Dataloader the traj_nan exclusion note; Pitfall 2 frame-dir layout, Pitfall 7 disconnect-safe extract),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2/D2a the pretext + frame-extraction; the {video}_{rep}_{frame} string-ID gotcha)
  </read_first>
  <action>
    Three parts: (A) implement cvcspc_ssl.py (the phase-matched triplet dataset — the genuinely-new logic, with the traj_nan exclusion + the masking aug), (B) fill the run_cvcspc_pretrain_epoch loop body in cvcspc_pretrain.py (with the phase-gap anneal), then (C) the triplet/traj-nan/mask unit tests.

    (A) cvcspc_ssl.py per PATTERNS "datasets/cvcspc_ssl.py" + the OFFICIAL dataloader.py: imports = squat_ssl.py minus scipy.ndimage/decode_clip/ssl_augs, PLUS PIL.Image + torchvision.transforms as T; logger aqa.phase07; module docstring documents the phase-matched cross-rep pretext + the NaN-exclusion (factual, NO citation comments). IMAGENET_MEAN/STD module constants.
      (1) _traj2phase(traj) -> np.ndarray: np.asarray(float); take the y-position (if the staged trajectory is 2-D [N,2] take col 1 per dataloader.py get_phase traj[:,1]; if it is a flat 1-D y-list use it directly); NaN-interpolate via the squat_ssl np.isnan/np.interp pattern; GUARD — if empty, fewer than 2 valid (non-NaN) points, or constant (max-min < 1e-8 after interp), raise ValueError("degenerate trajectory"); normalize (t-min)/max(max-min,1e-8); x360.0.
      (2) class ShallowSquatSSLDataset.__init__(*, frames_root, trajectories_root, traj_nan_path=None, ssl_contrastive_phase_gap=30.0, mask_prob=0.5, mask_amt_lo=0.4, mask_amt_hi=0.5, seed=42): frame_ids = {p.name for p in Path(frames_root).iterdir() if p.is_dir()}; self._traj_paths={p.stem:p for p in Path(trajectories_root).rglob("*.json")}; LOAD THE NaN-EXCLUSION (CITED dataloader.py:67-68): nan_stems = {Path(x).stem for x in json.loads(Path(traj_nan_path).read_text())} if traj_nan_path and Path(traj_nan_path).exists() else set() (with a logger.warning if the file is absent — traj_nan.json SHOULD be staged with the trajectories); candidates = (frame_ids & set(self._traj_paths)) - nan_stems; ALSO scan candidates once at construction and DROP any whose _traj2phase raises (the degenerate guard) so phase computation never divides by zero at __getitem__; self._clip_ids=sorted(the survivors); store frames_root/phase_gap/mask params; self.transform = Resize(256)+CenterCrop(224)+ToTensor()+Normalize(IMAGENET) (deterministic — augmentation is the masking, applied after). logger.info the clip count after exclusion. Handle len(self._clip_ids)<2 defensively (the synthetic test uses >=2).
      (3) _frame_files(clip_id) -> sorted list of frame filenames under {frames_root}/{clip_id}/ (os.listdir/sorted).
      (4) _load_frame(clip_id, frame_idx) -> the transformed tensor for the frame_idx-th file (clamp frame_idx to len-1, per dataloader.py:95).
      (5) _mask(img: torch.Tensor) -> torch.Tensor: with prob self.mask_prob, k=int(H*random.uniform(mask_amt_lo,mask_amt_hi)); img[:, :k, :]=0; return img. The masking-only aug (CITED dataloader.py:99-102 — only augmentations_dict['masking'] active).
      (6) __getitem__(idx): v0=self._clip_ids[idx]; v1=self._clip_ids[a different random idx]; phase0=_traj2phase(load traj v0), phase1=_traj2phase(load traj v1); p_anchor=np.random.uniform(max(phase0.min(),phase1.min()), min(phase0.max(),phase1.max())) (the overlap; fall back to phase0's range if no overlap); anchor_idx=argmin(|phase0 - p_anchor|) on v0; positive_idx=argmin(|phase1 - p_anchor|) on v1; negative candidates = indices on v1 where |phase1 - p_anchor| >= self.phase_gap; negative_idx = a random such candidate (fallback: argmax(|phase1 - p_anchor|) if none — per dataloader.py:75-78); map each (clip, phase-array index) -> frame FILE via _frame_files (the trajectory is 1:1 with frames — clamp to the frame-file count); load+transform each; apply _mask independently per branch; return {"anchor","positive","negative"}.
      (7) build_cvcspc_loader + seed_worker copied verbatim (rename).

    (B) cvcspc_pretrain.run_cvcspc_pretrain_epoch — fill the body MIRRORING run_md_pretrain_epoch's CONTRACT (do NOT import it) with these substitutions (PATTERNS list): config_repr over the CVCSPCConfig keys (incl. projector dims, phase_gap_start, phase_gap_decay_every, mask params) -> hash_config; run_dir={drive_root}/FitNova/checkpoints/{checkpoint_phase}/{run_name}; loader=build_cvcspc_loader(ssl_dataset_cls(frames_root=...,trajectories_root=...,traj_nan_path=traj_nan_path,ssl_contrastive_phase_gap=config.phase_gap_start,mask_prob/amt=...,seed=seed), config, seed=seed); backbone,projector=build_cvcspc_model(config) to device; AdamW(list(backbone.params)+list(projector.params), lr, wd); CosineAnnealingLR(T_max); per-epoch: 3-branch forward (phi_a/p/n=projector(backbone(branch))) + cvcspc_triplet_loss + backward; ssl_loss into metrics_history; every config.triplet_acc_cadence epochs call _triplet_accuracy on a held-out slice of the loader (or a fresh pass) -> triplet_acc_history; build_cvcspc_checkpoint_payload -> atomic epoch_NNN.pt (update_latest=True) + backbone.pt on triplet-acc improvement (update_latest=False); prune_checkpoints(keep_last=3,keep_best=True); THE PHASE-GAP ANNEAL at epoch end (CITED train_test.py:172-179): if (epoch+1) % config.phase_gap_decay_every == 0 and config.phase_gap_start > 30.0: config.phase_gap_start = max(30.0, config.phase_gap_start - 1.0); loader.dataset.phase_gap = config.phase_gap_start (mutate the live dataset so the next epoch's triplets use the tighter gap); map_location='cpu' resume restoring backbone+projector+optimizer+scheduler+rng+histories AND re-applying the annealed phase_gap from the resumed epoch (recompute config.phase_gap_start from start_epoch so a mid-anneal resume is correct, then set loader.dataset.phase_gap). NO collapse-abort. Return {epoch, metrics_history, triplet_acc_history, backbone_path, checkpoint_path, config_hash}.

    (C) In test_cvcspc_ssl.py add test_traj2phase + test_traj2phase_degenerate + test_mask + test_traj_nan_exclusion + test_phase_matched_triplet:
      (a) test_traj2phase — _traj2phase(np.array([0,1,2,3,4],float)) returns [0, 90, 180, 270, 360] (min->0, max->360) within 1e-6; with a NaN injected mid-array, asserts no residual NaN.
      (b) test_traj2phase_degenerate — _traj2phase on an all-NaN array AND on a constant array ([5,5,5,5]) each raises ValueError (the divide-by-zero / garbage-phase guard, BLOCKER 2).
      (c) test_mask — build a torch.ones(3,224,224); _mask with mask_prob=1.0, mask_amt_lo=mask_amt_hi=0.5 (fixed) -> assert the top 112 rows are exactly 0 and the bottom rows are still 1; _mask with mask_prob=0.0 -> the image is unchanged (the NICE-TO-HAVE masking unit test).
      (d) test_traj_nan_exclusion(tmp_path) — build 3 synthetic clips (frame dirs + trajectory JSONs), write traj_nan.json listing clip c2's trajectory filename; ShallowSquatSSLDataset(frames_root=...,trajectories_root=...,traj_nan_path=...); assert "c2" NOT in ds._clip_ids and len(ds._clip_ids)==2 (the official-exclusion contract, BLOCKER 2).
      (e) test_phase_matched_triplet(tmp_path) — build 2 synthetic clips: frames_root/{c0,c1}/ each with N frame JPEGs (tiny PIL random), trajectories_root/{c0,c1}.json each a monotone ramp (so phase = linear 0..360); ds=ShallowSquatSSLDataset(frames_root=...,trajectories_root=...,ssl_contrastive_phase_gap=30.0); set a fixed seed; call ds[0] a few times; assert it returns {"anchor","positive","negative"} each (3,224,224) float32; AND test the internal selection (expose the chosen phases/indices via a small helper or recompute): the anchor phase and positive phase are within a small tolerance of the shared phase, and the negative phase differs by >= the phase_gap. Keep all pure CPU (tiny images), sub-second.

    Single runnable unit — user pastes back python -m pytest backend/training/aqa/datasets/test_cvcspc_ssl.py -x -v (ALL cvcspc tests green — the Task-2 loss/proj + the Task-3 traj/degenerate/mask/traj-nan/triplet) AND python -c AST-parse of both cvcspc_ssl.py + cvcspc_pretrain.py prints "parse ok".

    Atomic commit: feat(07): cvcspc_ssl.py phase-matched triplet + traj_nan exclusion + masking aug + cvcspc_pretrain run loop (phase-gap anneal) + IMG-01 tests (Task 3, D2-pretext/D2a).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/training/aqa/datasets/cvcspc_ssl.py','backend/training/aqa/harness/cvcspc_pretrain.py']]; print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_cvcspc_ssl.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - cvcspc_ssl.py parses; defines ShallowSquatSSLDataset + _traj2phase + _mask + build_cvcspc_loader + seed_worker; logger aqa.phase07; trajectories via rglob, frame dirs via iterdir; IDs are string stems (no int-cast)
    - _traj2phase maps min->0° max->360° within 1e-6 + interpolates NaN AND raises ValueError on a degenerate (empty/constant/all-NaN) trajectory (BLOCKER 2 guard)
    - The traj_nan.json clips are EXCLUDED from self._clip_ids at construction (test_traj_nan_exclusion: the listed clip is absent, BLOCKER 2); the masking aug zeroes the top mask_amt fraction with prob mask_prob (test_mask: prob=1 zeroes top k, prob=0 unchanged)
    - __getitem__ returns {anchor,positive,negative} (3,224,224) float32; anchor@v0 + positive@v1 at a shared phase, negative@v1 phase-gap away — proven on synthetic ramps
    - run_cvcspc_pretrain_epoch body landed: AdamW+cosine, the triplet-accuracy monitor every cadence epochs, backbone.pt on triplet-acc-best (update_latest=False), THE PHASE-GAP ANNEAL ((epoch+1)%decay_every==0 -> gap-=1, floor 30°, mutating loader.dataset.phase_gap; resume re-applies the annealed gap — SHOULD-FIX #4), map_location='cpu' resume; NO collapse-abort; does NOT import md_pretrain
    - All test_cvcspc_ssl.py tests pass; NO AI-narration/citation comments in either source (the 3-term WHY-comment lives in cvcspc_pretrain only)
    - Runnable unit: user pastes back the full test_cvcspc_ssl.py suite green + the AST-parse
  </acceptance_criteria>
  <dependencies>Task 2 (cvcspc_pretrain cores — the loop wires cvcspc_triplet_loss/build_cvcspc_model/_triplet_accuracy; if Task 2 left a minimal cvcspc_ssl.py stub, this task fills it)</dependencies>
  <done>The genuinely-new CVCSPC logic (phase-matched cross-rep triplet + _traj2phase with the degenerate-guard + the traj_nan.json exclusion + masking aug) is implemented + proven on synthetic data; the SSL pretrain run-loop is complete (with the phase-gap anneal) + wired to the Task-2 cores; the SSL pipeline Plan 03 consumes exists, gated behind a green CPU suite before the frame-extract + GPU burn</done>
</task>

<task type="auto">
  <name>Task 4: Implement harness/image_supervised_train.py (NEW self-contained ResNet-18 trainer: ImageConfig + build_resnet18 + 4-D _val_pass + run_image_epoch) + the dataset_cls/model_builder seam test</name>
  <files>
    backend/training/aqa/harness/image_supervised_train.py,
    backend/training/aqa/datasets/test_shallow_squat.py
  </files>
  <read_first>
    backend/training/aqa/harness/supervised_train.py (the FULL file, 610 lines — the CONTRACT to MIRROR (NOT import): SupervisedConfig 77-105, build_model 114-136 (r2plus1d_18 / 5-D — image_supervised_train's build_resnet18 is the 4-D analog), seed_worker/_set_global_seed 144-184 (COPY verbatim), _build_dataloaders 192-278 (the dataset_cls seam + the persistent_workers guard — but DROP the 5-D video kwargs num_frames/crop_size/train_jitter_frames), _val_pass 281-330 (the [B,2] 5-D version — image_supervised_train makes [B,1] 4-D), run_supervised_epoch 338-610 (the full checkpoint/resume/early-stop loop to REIMPLEMENT for 4-D, NOT call)),
    backend/training/aqa/datasets/shallow_squat.py (the Task-1 ShallowSquatDataset — the dataset_cls default + the images_root/labels_path/splits_root kwargs it passes; the [1] pos_weight + scalar label),
    backend/training/aqa/eval/metrics.py (f1_per_error + pr_auc_per_error + threshold_sweep — REUSED UNCHANGED; SINGLE-head, operate on 1-D arrays for the single Shallow-Squat error),
    backend/training/aqa/harness/colab.py (atomic_save_checkpoint/load_latest_checkpoint/prune_checkpoints/hash_config/capture_rng_state/restore_rng_state — REUSED UNCHANGED),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the "harness/image_train.py" section — the exact ImageConfig, the ResNet-18 builder, _build_dataloaders (drop the video kwargs), the binary _val_pass squeeze, the run_image_epoch signature + the binary val-F1 + the payload code_version + the dataset_cls seam — note the module is named image_supervised_train.py),
    backend/training/aqa/datasets/test_ohp.py (test_dataset_cls_injection lines 152-178 — the signature-seam test to mirror)
  </read_first>
  <action>
    Create image_supervised_train.py as a NEW SELF-CONTAINED 2D-image trainer that MIRRORS supervised_train.py's contracts in a 4-D (B,3,H,W) ResNet-18 form (PATTERNS "harness/image_train.py" section) — it does NOT import supervised_train (which hardcodes r2plus1d_18 + a 5-input-frame stem + 5-D _build_dataloaders/_val_pass and has no model_cls seam). Per PATTERNS + the interfaces block:

    1. Imports = from __future__ import annotations; dataclasses; logging; os; random; time; typing.Any; numpy; torch; torch.nn as nn; DataLoader + from backend.training.aqa.datasets.shallow_squat import ShallowSquatDataset + from backend.training.aqa.eval.metrics import f1_per_error, pr_auc_per_error + from backend.training.aqa.harness.colab import the checkpoint/RNG primitives. Do NOT import supervised_train. logger = logging.getLogger("aqa.phase07").
    2. @dataclass ImageConfig per the interfaces block (lr=1e-4, wd=0.0, batch_size=32, num_workers=4, max_epochs=50, early_stop_patience=8, scheduler_name="cosine", scheduler_t_max=None, model_arch="resnet18_imagenet_v1", loss_name=...) + __post_init__ scheduler_t_max=max_epochs.
    3. build_resnet18() -> nn.Module — lazy torchvision import (from torchvision.models import ResNet18_Weights, resnet18); resnet18(weights=ResNet18_Weights.IMAGENET1K_V1); assert fc.in_features==512 (the WHY-comment: catches torchvision API drift); model.fc=nn.Linear(512,1). (Named build_resnet18 — unmistakably a 2D-image builder, NOT the video build_model.)
    4. seed_worker + _set_global_seed copied verbatim from supervised_train (copy, not import).
    5. _build_dataloaders(seed, config, images_root, labels_path, splits_root, *, dataset_cls=ShallowSquatDataset) — common_kwargs={images_root,labels_path,splits_root,seed} (DROP num_frames/crop_size/train_jitter_frames — video-only); dataset_cls(split=...,train_aug=...,**common_kwargs); persistent_workers=(num_workers>0); worker_init_fn=seed_worker; seeded generator; train shuffle=True, val/test shuffle=False, drop_last=False.
    6. _val_pass(model, loader, criterion, device) -> (loss_mean, scores[N], labels[N]) — 4-D binary: for clip[B,3,H,W],label in loader: logits=model(clip) -> [B,1]; accumulate; scores=torch.sigmoid(torch.cat(logits_chunks)).squeeze(-1).numpy() (1-D [N]); labels 1-D int [N]. tqdm-wrapped. (The criterion target may need label.unsqueeze(-1) to match [B,1] logits — match BCEWithLogitsLoss's shape contract.)
    7. run_image_epoch(*, run_name, drive_root="/content/drive/MyDrive", images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=42, config=None, resume=True, max_epochs=None, dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase="phase07") — REIMPLEMENT the run_supervised_epoch loop for 4-D (do NOT call it): config_repr over the ImageConfig keys -> hash_config; run_dir; model=model_builder() (default build_resnet18 — the seam Plan 04 injects the CVCSPC-backbone builder through); pos_weight=train_loader.dataset.pos_weight; criterion=BCEWithLogitsLoss(pos_weight); Adam(lr,wd); CosineAnnealingLR; per-epoch 4-D train loop; _val_pass; val_f1=f1_per_error(val_labels,(val_scores>=0.5).astype(int)) (SINGLE head — no macro average); val_pr_auc=pr_auc_per_error(val_labels,val_scores); metrics_history entry (val_f1, val_pr_auc, losses, wall-time); payload with code_version="phase07-image-baseline", best_thresholds=None; atomic epoch_NNN.pt (update_latest=True) + best.pt on val_f1 improvement (update_latest=False per the Phase-4/6 carry-fix); prune_checkpoints(keep_last=3,keep_best=True); early-stop on val_f1 plateau; map_location='cpu' resume. Return {epoch, metrics_history, best_f1_val, best_thresholds:None, checkpoint_path, best_checkpoint_path, config_hash}.

    In test_shallow_squat.py add test_image_trainer_seams (mirror test_ohp.py lines 152-178): import _build_dataloaders + run_image_epoch + build_resnet18 + ShallowSquatDataset + inspect.signature; assert _build_dataloaders + run_image_epoch each have a keyword-only dataset_cls defaulting to ShallowSquatDataset; assert run_image_epoch has a keyword-only model_builder defaulting to build_resnet18 (the Plan-04 seam) AND checkpoint_phase defaulting to "phase07". Fast, no GPU/data. (Do NOT import the model — just inspect signatures so the test is CPU-instant.)

    Single runnable unit — user pastes back python -c AST-parse of image_supervised_train.py prints "parse ok" AND python -m pytest backend/training/aqa/datasets/test_shallow_squat.py::test_image_trainer_seams -x -v (the seams proven at signature level; the real ResNet-18 training is Plan 02's Colab run).

    Atomic commit: feat(07): image_supervised_train.py ResNet-18 4-D trainer + binary val-pass + run_image_epoch + dataset_cls/model_builder seam test (Task 4, D3/D6).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/harness/image_supervised_train.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/datasets/test_shallow_squat.py::test_image_trainer_seams -x -v</automated>
  </verify>
  <acceptance_criteria>
    - image_supervised_train.py parses; defines ImageConfig + build_resnet18 + _build_dataloaders + _val_pass + run_image_epoch + seed_worker/_set_global_seed; logger aqa.phase07; does NOT import supervised_train (no r2plus1d / 5-D)
    - build_resnet18 is resnet18(IMAGENET1K_V1) + Linear(512,1) (SINGLE binary head, NOT 2); _val_pass takes 4-D (B,3,H,W) batches and squeezes to 1-D scores; val F1 is single-head (no macro average)
    - _build_dataloaders + run_image_epoch expose a keyword-only dataset_cls=ShallowSquatDataset + checkpoint_phase="phase07"; run_image_epoch ALSO exposes model_builder=build_resnet18 (the Plan-04 CVCSPC-backbone injection seam); eval/metrics.py + colab primitives imported UNCHANGED
    - best.pt write passes update_latest=False (the Phase-4/6 carry-fix); payload code_version="phase07-image-baseline"
    - test_image_trainer_seams passes (dataset_cls, model_builder, checkpoint_phase defaults asserted); NO AI-narration/citation comments in the source
    - Runnable unit: user pastes back the AST-parse + the seam test passing
  </acceptance_criteria>
  <dependencies>Task 1 (ShallowSquatDataset — the dataset_cls default + the images_root/labels_path/splits_root kwargs + the [1] pos_weight)</dependencies>
  <done>image_supervised_train.py = a NEW self-contained 2D-image trainer (ResNet-18 IMAGENET1K_V1 + Linear(512,1), 4-D _val_pass, ImageNet pipeline) with the dataset_cls/model_builder/checkpoint_phase seams + the checkpoint/resume/early-stop/threshold-sweep contracts, reusing ONLY eval/metrics.py + colab primitives (NOT supervised_train's r2plus1d/5-D loop); the baseline trainer Plan 02 drives exists; the SSL fine-tune (Plan 04) reuses it via the model_builder seam</done>
</task>

<task type="auto">
  <name>Task 5: Add colab.py staging + frame-extraction (additive) + the no-regression gate</name>
  <files>
    backend/training/aqa/harness/colab.py,
    backend/training/aqa/datasets/test_shallow_squat.py
  </files>
  <read_first>
    backend/training/aqa/harness/colab.py (stage_squat_videos lines 317-443 + stage_unlabeled_squat_videos lines 449-538 (the trajectory-zip extractall branch lines 518-522 is the JPEG-zip template) + _copy_with_resume_and_progress line 93 + _extract_with_resume_and_progress lines 168-244 (the skip-existing _do_one pattern at 218-226 — extract_frames_for_ssl mirrors it) + the _SQUAT_*_EXPECT_COUNT constants),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the "harness/colab.py — ADDITIONS ONLY" section — the exact stage_shallow_squat_images (zipfile.extractall, NOT mp4-only) + extract_frames_for_ssl (cv2 read loop, {video_id}/ subdir, skip-existing, tqdm) bodies),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Frame Extraction — the cv2.VideoCapture loop + the ~10-20 min cost + disconnect-safe skip-existing; §Pattern 4 stage_shallow_squat_images; Pitfall 2 frame-dir layout, Pitfall 3 images.zip extractall, Pitfall 7 disconnect-safe),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2a the Step-0 frame-extraction; D6 carry-forward; the -3-001 (images + trajectories) vs -3-004 (unlabeled videos) Drive-folder note in RESEARCH §Environment)
  </read_first>
  <action>
    Two additive colab.py functions + the no-regression gate. Per PATTERNS "harness/colab.py — ADDITIONS ONLY" (do NOT touch any existing function):

    1. ADD _SHALLOW_SQUAT_IMAGES_EXPECT_COUNT = 3738 + stage_shallow_squat_images(drive_root_3001, *, local_root="/content/squat_shallow_images", expect_count=_SHALLOW_SQUAT_IMAGES_EXPECT_COUNT) -> str: cache-hit if local_root/crops_unaligned has expect_count *.jpg; else src=Path(drive_root_3001)/"Fitness-AQA_dataset_release/Squat/Labeled_Dataset/Shallow_Squat_Error_Dataset/images.zip"; byte-resume copy to /content/squat_shallow_images.zip; zipfile.extractall to local_root (JPEG members — NOT _extract_with_resume_and_progress, which filters .mp4 and would skip every JPEG — the WHY-comment); verify count of *.jpg recursively under local_root == expect_count (RuntimeError if not). ALSO copy labels_shallow_depth.json + the splits/ dir (train/val/test_ids.json) from the same Shallow_Squat_Error_Dataset folder into local_root (the training/EDA notebooks read images_root=local_root/crops_unaligned, labels_path=local_root/labels_shallow_depth.json, splits_root=local_root/splits). Reclaim the local zip on success. Mirror the trajectory-zip branch of stage_unlabeled_squat_videos.

    2. ADD extract_frames_for_ssl(videos_root, frames_root, *, skip_existing=True) -> int: import cv2 + tqdm (try/except); mp4s=sorted(Path(videos_root).glob("*.mp4")); for each: video_id=stem; out_dir=Path(frames_root)/video_id; if skip_existing and out_dir.exists() and any(out_dir.iterdir()): skip (count toward skipped); else out_dir.mkdir(parents=True,exist_ok=True); cap=cv2.VideoCapture(str(mp4)); read loop writing cv2.imwrite(str(out_dir/f"frame_{i:06d}.jpg"), frame) per frame; cap.release(); tqdm over the mp4 list with postfix extracted/skipped; return total frames written. The {video_id}/frame_{:06d}.jpg subdir layout is required by ShallowSquatSSLDataset (Pitfall 2); skip_existing makes it disconnect-safe (Pitfall 7); the cell takes >2 min so tqdm is mandatory.

    3. In test_shallow_squat.py add test_colab_additions_present (no I/O, no cv2 run): import backend.training.aqa.harness.colab as colab; assert callable(colab.stage_shallow_squat_images) and callable(colab.extract_frames_for_ssl); inspect.signature(stage_shallow_squat_images) has drive_root_3001 + a keyword-only local_root + expect_count; inspect.signature(extract_frames_for_ssl) has videos_root + frames_root + a keyword-only skip_existing. (A signature-level presence test — the real staging/extraction is exercised in Plan 02/03's Colab runs.) Fast.

    4. The no-regression GATE — this is the Wave-0 exit. Run the FULL existing suite + the new Phase-7 tests together: python -m pytest backend/training/aqa/ -m "not slow" -q must be GREEN (the new shallow_squat + cvcspc tests + the existing Phase-3/4/6 Squat+OHP suite — the new modules are independent + colab.py edits additive, so nothing regresses).

    Single runnable unit — user pastes back: python -c AST-parse of colab.py prints "parse ok" AND python -m pytest backend/training/aqa/ -m "not slow" -q (ALL green — new Phase-7 + existing Squat+OHP).

    Atomic commit: feat(07): colab.py stage_shallow_squat_images + extract_frames_for_ssl + Wave-0 no-regression gate (Task 5, D2a/D6).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/harness/colab.py').read()); print('parse ok')" && python -m pytest backend/training/aqa/ -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - colab.py parses; stage_shallow_squat_images + extract_frames_for_ssl added; the existing functions UNCHANGED (git diff purely additive)
    - stage_shallow_squat_images uses zipfile.extractall (JPEG — NOT the mp4-only _extract_with_resume_and_progress); copies labels + splits alongside; verifies 3738 *.jpg
    - extract_frames_for_ssl writes {frames_root}/{video_id}/frame_{:06d}.jpg (subdir layout — Pitfall 2), skip-existing (Pitfall 7), tqdm
    - test_colab_additions_present passes (signatures present); NO AI-narration/citation comments in the added code (the zipfile-vs-mp4-extractor WHY is allowed)
    - The Wave-0 GATE: python -m pytest backend/training/aqa/ -m "not slow" -q exits 0 — the new Phase-7 unit tests + the existing Phase-3/4/6 Squat+OHP suite ALL green (no regression)
    - Runnable unit: user pastes back the AST-parse + the full -m "not slow" suite green
  </acceptance_criteria>
  <dependencies>Task 1 (shallow_squat exists so the suite collects), Task 3 (cvcspc_ssl exists), Task 4 (image_supervised_train exists) — the full suite needs all new modules importable</dependencies>
  <done>The two additive colab functions (images-zip staging via extractall + the disconnect-safe SSL frame-extraction) exist; the existing Squat+OHP suite stays green; Wave 0 is complete and the GPU-burn gate is locked behind a green pure-function suite + a no-regression proof — Plans 02-04 can stage data + train</done>
</task>

</tasks>

<verification>
## Mapping to IMG-01/IMG-02 + the Wave-0 gate

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| IMG-01: ShallowSquatDataset shape + [1] pos_weight + splits + ImageNet norm | Task 1 | test_getitem_shape + test_pos_weight (shape (1,)) + test_imagenet_norm + test_split_sizes (local archive) pass |
| IMG-01: CVCSPC 3-term loss + projection head | Task 2 | test_cvcspc_triplet_loss (closed form 1e-6) + test_cvcspc_loss_directional + test_projection_head (no-BN, configurable dims) pass |
| IMG-01: CVCSPC phase-matched triplet + _traj2phase + traj_nan exclusion + mask + SSL run-loop | Task 3 | test_traj2phase + test_traj2phase_degenerate + test_mask + test_traj_nan_exclusion + test_phase_matched_triplet pass; run_cvcspc_pretrain_epoch body landed with the phase-gap anneal |
| IMG-02: the ResNet-18 image trainer (baseline + SSL fine-tune driver) | Task 4 | image_supervised_train.py parses (no supervised_train import); build_resnet18 resnet18+Linear(512,1); dataset_cls/model_builder seam test passes |
| The colab staging + frame-extraction (the SSL Step-0 data path) | Task 5 | stage_shallow_squat_images + extract_frames_for_ssl present; signatures asserted |
| Wave-0 GPU-burn gate | Task 5 | `python -m pytest backend/training/aqa/ -m "not slow" -q` exits 0 (new Phase-7 + existing Squat+OHP) |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| Unit: shallow_squat dataset (shape/label/split/norm/[1]-head) | Task 1 | unit (synthetic + local-archive skipif) | 0 |
| Unit: CVCSPC 3-term loss (closed-form + directionality) | Task 2 | unit (synthetic) | 0 |
| Unit: CVCSPC projection head (unit-norm, no-BN, configurable dims) | Task 2 | unit (synthetic) | 0 |
| Unit: phase-matching triplet + _traj2phase + degenerate-guard + traj_nan exclusion + mask | Task 3 | unit (synthetic trajectories) | 0 |
| The dataset_cls/model_builder/checkpoint_phase seam (image_supervised_train) | Task 4 | unit (signature) | 0 |
| No-regression (Phase-3/4/6 suite still green) | Task 5 | integration (full suite) | 0 |

(IMG-02 baseline test F1 is Plan 02; IMG-01 SSL triplet-accuracy convergence is Plan 03; IMG-02 SSL-init test F1 + the lift is Plan 04.)
</verification>

<success_criteria>
- All 5 tasks complete with paste-back acceptance from the user (LOCAL CPU — fast paste-back)
- shallow_squat.py = the single-image analog of squat.py (ImageNet norm, flat-dict labels read directly, [1] SINGLE-head pos_weight, scalar label); the official-split reconciliation passes 2542/529/540
- cvcspc_pretrain.py = a NEW self-contained CVCSPC SSL trainer mirroring md_pretrain's contract (NOT importing its R(2+1)D loop): the 3-term loss proven against the CITED closed form within 1e-6 + directionality, the no-BN configurable projection head, build_cvcspc_model, the triplet-accuracy monitor, the run-loop with the phase-gap anneal
- cvcspc_ssl.py = the phase-matched cross-rep triplet dataset (the genuinely-new logic): _traj2phase (min->0° max->360°, degenerate-guarded), the traj_nan.json exclusion, anchor/positive at a shared phase from two reps, negative phase-gap away, masking-only aug — proven on synthetic trajectories
- image_supervised_train.py = a NEW self-contained 2D-image trainer (ResNet-18 IMAGENET1K_V1 + Linear(512,1), 4-D _val_pass) with the dataset_cls/model_builder/checkpoint_phase seams, reusing ONLY eval/metrics.py + colab primitives (NOT supervised_train's r2plus1d/5-D loop)
- colab.py ADD stage_shallow_squat_images (extractall, NOT mp4-only) + extract_frames_for_ssl ({video_id}/ subdirs, skip-existing, tqdm) — additive; the existing functions untouched
- The new Phase-7 unit tests green (dataset shape/norm/splits/[1]-head, the 3-term loss closed-form + directionality, the no-BN configurable projection head, _traj2phase + degenerate-guard, the traj_nan exclusion, the mask, the phase-matched triplet, the seams) + NO Squat/OHP regression under -m "not slow"
- Every D2/D3/D5/D6 decision is honored; NO AI-narration/citation comments in source (the ONE allowed WHY-comment = the 3-term-vs-2-term loss deviation, load-bearing for [[feedback_ai_correctness]]); citations live in this plan + the commit messages
- One accurate threat_model: offline pipeline, no high-severity threats; T-07-01 (the new 3-term loss) caught by a CPU unit test (closed-form + directionality); T-07-SC accept (zero new installs)
- NO GPU, NO Colab, NO Drive touched — this is the pure-function Wave-0 gate before the frame-extract + SSL burn (Plan 03) + the baseline run (Plan 02)
</success_criteria>

<output>
After Task 5 (green Wave-0 suite + no Squat/OHP regression), this plan is complete. Plan 02 (Wave 1) trains the ImageNet ResNet-18 supervised baseline (the SSL-lift control, the guaranteed IMG-02 result) on Colab. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 02 once Task 5's no-regression suite is green. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 7 Plan 01 written 2026-05-30. Wave 0: pure-function scaffolds + unit tests, CPU-verified before any GPU burn (the Phase-4/6 Wave-0 precedent, 04-01-PLAN / 06-01-PLAN). 5 tasks. The net-new code is two dataset modules (shallow_squat.py = single-image analog of squat.py minus video decode/Kinetics norm; cvcspc_ssl.py = the phase-matched cross-rep triplet + the traj_nan exclusion, the genuinely-new logic), two NEW SELF-CONTAINED trainers (image_supervised_train.py = a ResNet-18/binary 4-D loop; cvcspc_pretrain.py = the 3-term loss + triplet-accuracy mirror of md_pretrain), two test files, and two additive colab functions. eval/metrics.py + the colab harness primitives reused UNCHANGED.

REVISION 2026-05-30 (checker pass against the actual code):
- BLOCKER 1 (the trainer seam does NOT exist for a 2D image model): supervised_train.build_model() hardcodes r2plus1d_18 + a 5-input-frame stem and returns a VIDEO model; _build_dataloaders/_val_pass/run_supervised_epoch assume 5-D (B,3,T,H,W); there is a dataset_cls+checkpoint_phase seam but NO model_cls seam. FIX: committed fully to a NEW self-contained image_supervised_train.py (renamed from image_train.py to make the separation unmistakable) implementing run_image_epoch END-TO-END (build_resnet18 + a 4-D _val_pass + the checkpoint/resume/early-stop/threshold-sweep contracts), reusing ONLY eval/metrics.py + the colab primitives; removed all "reuse run_supervised_epoch" language; the interfaces/decisions/success-criteria now state the trainers do NOT import the video epoch loops (small helpers may be COPIED per the Phase-4 D9 precedent). cvcspc_pretrain.py likewise MIRRORS md_pretrain's contract but does NOT import run_md_pretrain_epoch; it routes checkpoints to phase07 + keeps update_latest=False for backbone.pt/best.pt. ADDED the model_builder seam to run_image_epoch in Wave 0 (default build_resnet18) so Plan 04's CVCSPC fine-tune injects its backbone-loading builder with no further edit.
- BLOCKER 2 (traj_nan exclusion): the official dataloader.py:67-68 loads traj_nan.json and excludes those clips before phase-matching. FIX: ShallowSquatSSLDataset now loads traj_nan_path + excludes the listed clip stems at construction; _traj2phase raises ValueError on a degenerate (empty/all-NaN/constant) trajectory; run_cvcspc_pretrain_epoch threads traj_nan_path through. Added test_traj_nan_exclusion + test_traj2phase_degenerate (the empty/NaN-trajectory guard) to the Wave-0 suite (Task 3).
- SHOULD-FIX #6 (single-error head): Shallow-Squat is ONE binary error, not 2. FIX: the head is Linear(512,1), the loss BCEWithLogitsLoss on a single logit, pos_weight [1], the label a scalar, the eval a single score column — wired consistently across 07-01 (dataset label shape + [1] pos_weight + build_resnet18 + 4-D _val_pass). D5 + the must-haves + the acceptance criteria now assert the shapes (pos_weight (1,), label.ndim==0, batch label (B,) not (B,2)).
- SHOULD-FIX #3 (3-term loss directionality): added test_cvcspc_loss_directional asserting loss(d_ap small, d_an/d_pn large) < loss(all-equal) — not just the hand-computed value (Task 2).
- SHOULD-FIX #4 (phase-gap annealing): train_test.py:172-179 anneals the gap 30°-> ((epoch+1)%4==0 -> gap-=1). FIX: IMPLEMENTED the anneal in run_cvcspc_pretrain_epoch (decrement at epoch end, floor 30°, mutate loader.dataset.phase_gap; resume re-applies the annealed gap) — Task 3.
- NICE-TO-HAVE: projection-head dims are now CVCSPCConfig knobs (projector_hidden/projector_out_dim, ablatable to 512->512); the CVCSPC masking aug (top 40-50% blackout) is a Wave-0 unit-tested transform (test_mask).

Task ordering rationale: Task 2 (the 3-term loss) is split out and proven FIRST because it is the highest-leverage pre-GPU check (a wrong loss wastes the frame-extract + SSL burn — the Phase-4 Wave-0 precedent). Task 3 then lands the phase-matching dataset + the run-loop. Task 2's top-level import of cvcspc_ssl is handled by Task 2 creating a minimal cvcspc_ssl stub (class + build_cvcspc_loader signatures) that Task 3 fills.

Key deltas vs the Squat/OHP video pipeline, baked into the plan: (1) single-image input (NO decode_clip/temporal axis/Kinetics norm — ImageNet 224²); (2) labels are a FLAT {id:0/1} dict read directly (NOT splits._label_value); (3) the CVCSPC pretext is phase-matched CROSS-REP (two reps at a shared bar-trajectory phase), NOT half-cycle, WITH the traj_nan exclusion; (4) the loss is the CITED 3-term code version (closed-form + directionality tested); (5) SINGLE binary head — Linear(512,1), pos_weight [1], scalar label; (6) NEW self-contained trainers (the video trainers have no model_cls seam + assume 5-D); (7) NO collapse-abort (ResNet-18 ImageNet-init is stable; triplet-accuracy is the monitor).

Project rule honored: NO AI-narration/citation comments in source. The only allowed non-obvious WHY-comment is the 3-term-vs-2-term loss deviation (load-bearing for academic correctness). All citations live in this plan + the commit messages.

Security: accurate, not boilerplate. Offline ML pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. Genuine items only: T-07-01 (the 3-term loss — closed-form + directionality CPU test), T-07-02 (the phase-matching + the NaN-exclusion — synthetic-trajectory + traj_nan tests), T-07-03 (ImageNet-not-Kinetics norm — range test), T-07-04 (no Squat/OHP regression — full suite), T-07-05 (no dataset media/weights committed — gitignored), T-07-SC accept (zero new installs). No web-app threats invented.
</revision_log>
</output>
