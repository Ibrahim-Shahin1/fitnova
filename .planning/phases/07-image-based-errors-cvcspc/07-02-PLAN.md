---
phase: 07-image-based-errors-cvcspc
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - backend/training/aqa/notebooks/09_shallow_squat_baseline.py
  - backend/training/aqa/notebooks/09_shallow_squat_baseline.ipynb
autonomous: false
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "An ImageNet-pretrained ResNet-18 (IMAGENET1K_V1 init, nn.Linear(512,1) SINGLE binary head, end-to-end fine-tune, BCEWithLogitsLoss with the train pos_weight ~1.28 on a single logit, Adam lr 1e-4, 50ep/8-patience cosine, batch 32) trains to convergence on the official 2542-crop Shallow-Squat train split via the Plan-01 image_supervised_train.run_image_epoch (dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase='phase07') — val F1 strictly improves over the first epochs (IMG-02 baseline)"
    - "Multiple seeds (42, 1337, optionally 7) train sequentially, each producing best.pt at phase07/shallow_squat_baseline_seed{N}/best.pt; the seeds are aggregated by mean-of-sigmoids and a single decision threshold is tuned on the ensemble val scores via eval/metrics.threshold_sweep (NOT per-seed thresholds then vote, D4) — the per-seed val F1 recorded as variance evidence"
    - "Final test F1 is reported on the official 540-crop Shallow-Squat test split at the val-tuned threshold, computed in code (NOT hand-typed) on the single score column, and recorded as the SSL-lift CONTROL — the paper has NO supervised-ImageNet baseline row for Shallow-Squat (RESEARCH §Supervised), so this measured baseline is the only control the Plan 04 CVCSPC lift is computed against; SimSiam 0.829 / CVCSPC 0.869 are the paper image rows noted for context"
    - "The baseline is disconnect-safe: it auto-resumes from My Drive/FitNova/checkpoints/phase07/shallow_squat_baseline_seed{N}/latest.txt with map_location='cpu', checkpoints every epoch via the Phase-2 atomic-write contract, and a mid-run Colab disconnect resumes cleanly without restarting; best.pt write passes update_latest=False"
    - "The notebook stages the Shallow-Squat images.zip via stage_shallow_squat_images (3738 JPEGs + labels_shallow_depth.json + splits/ to /content/squat_shallow_images), runs the official-split reconciliation (2542/529/540) live, and runs a model+VRAM probe (assert the head is nn.Linear(512,1) and a forward on zeros[2,3,224,224] gives [2,1]) + an epoch-0 timing gate (BLOCKING decision) before the multi-seed run is authorized"
  artifacts:
    - path: "backend/training/aqa/notebooks/09_shallow_squat_baseline.py"
      provides: "Phase 7 baseline jupytext notebook — Cell A bootstrap + Step 0 env/stage(images.zip) + Step 1 split reconciliation + dataset smoke + Step 2 model+VRAM probe (single-logit head assert) + Step 3 epoch-0 timing gate (BLOCKING) + Step 4 multi-seed training + Step 5 ensemble + val threshold + Step 6 test eval. The SSL pretrain notebook (10) is Plan 03; the fine-tune notebook (11) is Plan 04."
      contains: "run_image_epoch"
    - path: "backend/training/aqa/notebooks/09_shallow_squat_baseline.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D6 landmine)"
      contains: "shallow_squat"
  key_links:
    - from: "backend/training/aqa/notebooks/09_shallow_squat_baseline.py"
      to: "backend/training/aqa/harness/image_supervised_train.run_image_epoch (dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase='phase07') + harness.colab.stage_shallow_squat_images"
      via: "notebook cell imports"
      pattern: "dataset_cls=ShallowSquatDataset"
    - from: "backend/training/aqa/notebooks/09_shallow_squat_baseline.py"
      to: "backend/training/aqa/eval/metrics.py (threshold_sweep, f1_per_error, pr_auc_per_error — REUSED UNCHANGED, single score column)"
      via: "notebook cell imports for Step 5/6"
      pattern: "from backend.training.aqa.eval.metrics import"
    - from: "backend/training/aqa/notebooks/09_shallow_squat_baseline.py"
      to: "backend/training/aqa/datasets/shallow_squat.ShallowSquatDataset (the Plan-01 dataset)"
      via: "notebook cell imports + dataset_cls injection"
      pattern: "ShallowSquatDataset"
---

<objective>
Train the ImageNet-pretrained ResNet-18 supervised baseline for Shallow-Squat depth-error detection and report F1 on the official 540-crop test split — the guaranteed IMG-02 result + the SSL-lift CONTROL. This is Wave 1: stage the Shallow-Squat crops, reconcile the official splits live, probe VRAM/timing, then run the multi-seed baseline via the Plan-01 image_supervised_train.run_image_epoch (the NEW self-contained 2D-image trainer — NOT the r2plus1d_18 video trainer) + the UNCHANGED eval/metrics.py. The baseline ships FIRST per CONTEXT D2 — it is the guaranteed result if CVCSPC SSL underperforms, and it is the control the Plan 04 CVCSPC lift is measured against.

Purpose: IMG-02 (Shallow-Squat detector trained; F1 on the official split) + IMG-01 (the ResNet-18 image pipeline, supervised half) + ROADMAP Phase 7 success criterion 1 ("a CVCSPC ResNet-18 image pipeline trains baseline ... models") + criterion 2 ("Shallow-Squat ... reports F1 on the official splits"). There is NO paper supervised-ImageNet baseline row for Shallow-Squat (RESEARCH §Supervised — Table 3 has SimSiam 0.829 + CVCSPC 0.869, both image-SSL, no plain-supervised row), so this measured baseline is the new control. Set defense expectations: the baseline is the control, and the narrative is baseline -> CVCSPC -> paper CVCSPC 0.869.

Output: the Phase 7 baseline notebook (Cell A + Step 0 staging + split reconciliation + VRAM/timing gates + multi-seed training + ensemble + threshold sweep + test eval) delivered as paired .py + .ipynb; the shallow_squat_baseline_seed{N} best.pt + per-epoch checkpoints on Drive at phase07/; the baseline F1 on the official Shallow-Squat test split.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on a FRESH Colab notebook ([[feedback_heavy_training_new_notebook]] — prompt the user before the first training cell; ResNet-18 is LIGHT so an L4 is ample, a T4 is acceptable). Each task is ONE runnable unit; the user runs it and pastes output back. NEVER pre-write task N+1 before task N's paste-back. After any git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]). The baseline training cell may span sessions — its checkpoint/resume must be bulletproof ([[feedback_notebook_disconnect_safe]]); tqdm/in-loop progress on any cell >2 min ([[feedback_progress_bars_required]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md
@.planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md
@.planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md
@.planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md
@.planning/phases/07-image-based-errors-cvcspc/07-01-PLAN.md
@.planning/phases/06-overhead-press/06-02-PLAN.md
@.planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md
@backend/training/aqa/datasets/shallow_squat.py
@backend/training/aqa/harness/image_supervised_train.py
@backend/training/aqa/harness/colab.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/notebooks/06_ohp_supervised_baseline.py
@CLAUDE.md

<interfaces>
This plan REUSES the Plan-01 image_supervised_train.run_image_epoch UNCHANGED + eval/metrics.py UNCHANGED + the colab staging from Plan 01. It writes ONLY notebook cells. Plan 04 reuses the same trainer for the CVCSPC-init fine-tune (via the model_builder seam). NO new production logic.

Reused UNCHANGED (Plan 01):
- image_supervised_train.run_image_epoch(*, run_name, drive_root="/content/drive/MyDrive", images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=42, config=None, resume=True, max_epochs=None, dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase="phase07") -> {epoch, metrics_history, best_f1_val, best_thresholds:None, checkpoint_path, best_checkpoint_path, config_hash}. ImageConfig defaults are the recipe (Adam lr=1e-4 wd=0, batch 32, 50ep, 8-patience cosine). This is the NEW self-contained 2D-image trainer (build_resnet18 + a 4-D (B,3,H,W) loop) — NOT supervised_train (r2plus1d_18 / 5-D).
- image_supervised_train.build_resnet18() -> resnet18(IMAGENET1K_V1) + nn.Linear(512,1) — the SINGLE-logit head; used to reload best.pt for the val/test passes.
- image_supervised_train.ImageConfig — the recipe dataclass; pass ImageConfig() or ImageConfig(batch_size=16) per Task 1's decision.
- harness.colab.stage_shallow_squat_images(drive_root_3001, *, local_root="/content/squat_shallow_images", expect_count=3738) -> str.
- eval/metrics.threshold_sweep / f1_per_error / pr_auc_per_error / confusion_matrix_per_error (SINGLE binary head — operate on 1-D arrays / a single score column).

Produced artifact contract (Plan 04 input — the baseline control row):
- Drive: My Drive/FitNova/checkpoints/phase07/shallow_squat_baseline_seed{42,1337,...}/best.pt — the baseline checkpoints; the ensemble test F1 is the SSL-lift control (recorded in the notebook + folded into Plan 04's results.pkl).

Comparison numbers (RESEARCH §Supervised, CITED — use EXACTLY these; NO plain-supervised paper row exists):
- Paper CVCSPC (the Plan-04 fine-tune target): 0.8694.
- Paper SimSiam (image SSL): 0.8286.
- Paper OpenPose-TDM (2D pose): 0.8340.
- NO paper supervised-ImageNet row for Shallow-Squat — this measured baseline is the only supervised control; the SSL lift = (Plan-04 CVCSPC ensemble) - (this baseline).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D2-baseline | Supervised baseline = ResNet-18 IMAGENET1K_V1 init, end-to-end fine-tune, nn.Linear(512,1) SINGLE binary head, BCEWithLogitsLoss(pos_weight ~1.28) on a single logit, Adam lr=1e-4 wd=0, CosineAnnealingLR, 50ep/8-patience, batch 32, 224² ImageNet-norm. Via image_supervised_train.run_image_epoch (the Plan-01 NEW 2D-image trainer, UNCHANGED) with dataset_cls=ShallowSquatDataset, model_builder=build_resnet18, checkpoint_phase="phase07". Baseline ships FIRST (the guaranteed IMG-02 result if CVCSPC underperforms). | CONTEXT D2 + RESEARCH §Supervised + checker BLOCKER 1 / SHOULD-FIX #6 |
| D2-control | There is NO paper supervised-ImageNet baseline row for Shallow-Squat (RESEARCH §Supervised — Table 3 has SimSiam 0.829 + CVCSPC 0.869 image-SSL rows, no plain-supervised row). The measured baseline is the ONLY SSL-lift control; the headline is our baseline -> our CVCSPC -> paper CVCSPC 0.869. Set defense expectations: the baseline is the control, the SimSiam 0.829 image-SSL row is the secondary context. | RESEARCH §Supervised / §State of the Art |
| D4-rigor | Multi-seed ensemble (seeds 42, 1337 default; 7 optional — 2-seed acceptable per CONTEXT D4) + mean-of-sigmoids + a single decision threshold tuned on the ENSEMBLE val scores via threshold_sweep (NOT per-seed thresholds then vote). Per-seed val F1 recorded (variance). ResNet-18 trains in minutes — multi-seed is cheap. | CONTEXT D4 |
| D5-loss | SINGLE binary head (Shallow-Squat is ONE error). BCEWithLogitsLoss with the train-derived pos_weight (~1.28 — near-balanced 1584/3611=43.9% pos) on a single logit. The weighting is a minor correction (far less load-bearing than Squat KIE's 14%). PR-AUC threshold-free; the val-tuned single threshold applied to test. eval/metrics.py reused UNCHANGED on the single score column. | CONTEXT D5 + RESEARCH §Supervised + checker SHOULD-FIX #6 |
| D7 | persistent_workers on the loaders (already in image_supervised_train._build_dataloaders); map_location='cpu' on resume; metrics_history from the latest.txt-pointed checkpoint for viz (NOT best.pt — [[reference_best_pt_metrics_history_is_stale]]); best.pt write passes update_latest=False; .ipynb with Cell A on the FIRST ship; one runnable unit at a time; restart runtime / importlib.reload after git pull; the images.zip is staged via stage_shallow_squat_images (extractall, not the mp4-only extractor). | CONTEXT D6/D7 + RESEARCH Pitfall 3 |
| D8-VRAM | VRAM measured in-notebook (Step 2) before committing — ResNet-18 at batch 32/224² is LIGHT (well under any modern Colab GPU); the probe confirms it + asserts the single-logit head ([2,3,224,224]->[2,1]) + the epoch-0 timing gate confirms the ~minutes/epoch estimate before the multi-seed run. T4 acceptable (ResNet-18 is light); L4 ample. | RESEARCH §Environment / §Supervised |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 2/3/4/6 + D7)

1. Cell A first: from backend.training.aqa.harness import _envinit is the FIRST executable line of Step 0 (F8 — sets CUBLAS_WORKSPACE_CONFIG before any CUDA context). PyAV is NOT needed — images use PIL, not video decode; skip the PyAV install cell. Keep the scipy/sklearn/numpy dep-version probe.
2. persistent_workers via image_supervised_train._build_dataloaders (Plan 01, num_workers>0 guard).
3. map_location='cpu' on load_latest_checkpoint at resume (Phase 3 fix a0841b4).
4. 4-RNG capture/restore in every epoch payload + at resume (run_image_epoch, Plan 01).
5. images_root/labels_path/splits_root point at /content/squat_shallow_images/... explicitly (the run_image_epoch defaults already do; pass them explicitly for clarity).
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch.
7. metrics_history loaded from the latest.txt-pointed checkpoint for the training-curve viz (NOT best.pt).
8. config_hash via hash_config over the ImageConfig recipe keys (recipe change invalidates resume).
9. No AMP (fp32) — the Phase-3 contract carried; ResNet-18 is light enough that fp32 is fine.
10. Restart the Colab runtime / importlib.reload after git pull ([[reference_colab_module_reload_after_git_pull]]).
11. PIL Image.open for JPEG (the dataset's load path); tv 0.26 read_video removal is irrelevant (no video decode).
12. SINGLE-logit head: the model is Linear(512,1); the val/test passes gather a 1-D score column; the threshold sweep + F1 operate on 1-D arrays (no macro average over heads).

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: the headline F1 computed in code (not fabricated), the multi-session resume pointer, the correct images.zip extraction (extractall, not the mp4-only extractor), the single-logit shape, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate — injection/XSS/authz do not apply to a Colab training notebook.

| Boundary | Description |
|----------|-------------|
| Drive Shallow-Squat images.zip -> the dataset | stage_shallow_squat_images reads images.zip + labels + splits; clip IDs from the release, no path traversal. Staged once per session; extractall (Pitfall 3 — the mp4-only extractor would skip every JPEG). |
| In-memory baseline state -> Drive checkpoint (multi-session) | epoch_NNN.pt + best.pt over Drive FUSE; the resume pointer (latest.txt) must survive a disconnect. The Phase-2 atomic-write contract mitigates; best.pt write passes update_latest=False. |
| Computed test F1 -> the SSL-lift control | The baseline F1 is the control the Plan-04 lift is measured against; it must be computed in code (threshold_sweep on val, applied to test) on the single score column, NOT hand-typed. macro is N/A (single head); the single-head F1 is the headline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | The headline baseline F1 fabricated or mis-thresholded (the Phase-3 precedent the user caught) | mitigate | The test F1 is computed in code: threshold_sweep on the ensemble val scores, applied to the test scores, f1_per_error in code (NOT hand-typed). The notebook prints the val-tuned threshold + the test F1; no literal F1 in the source. MEDIUM — computed in code. |
| T-07-07 | Tampering | images.zip extracted with the mp4-only extractor (silently skips all JPEGs -> empty dataset -> a meaningless run) | mitigate | stage_shallow_squat_images uses zipfile.extractall (Plan 01, Pitfall 3) + verifies 3738 *.jpg; Step 0 logs the count; Step 1 reconciles the splits (2542/529/540) live before training. LOW — Plan-01-tested + count-verified. |
| T-07-08 | Integrity | Multi-session resume pointer (latest.txt) corrupted, or the wrong images_root silently used | mitigate | run_image_epoch uses the Phase-2 atomic-write contract (latest.txt last) + map_location='cpu' resume; images_root passed explicitly; the split reconciliation (Step 1) confirms the right data. Resume verified by a paste-back of "Resumed from epoch_NNN.pt". LOW — the contract is Phase-2-proven. |
| T-07-09 | Integrity | Committing dataset media / weights (the Shallow-Squat crops, the baseline best.pt) | accept | All media + weights gitignored / Drive-staged (Phase 1-6 contract). This plan commits only the notebook .py/.ipynb; best.pt lives on Drive. Stage files by name only. LOW — gitignore contract established. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe is the guard; no PyAV needed (images use PIL). No [ASSUMED]/[SUS] packages. |

T-07-06 (headline arithmetic) is the highest-priority threat given the Phase-3 precedent; mitigated by computing the F1 in code via threshold_sweep + f1_per_error, no hand-typed literals. No web-app threats apply (offline pipeline).

</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Notebook Cell A + Step 0 stage + Step 1 split reconciliation + dataset smoke + Step 2 model+VRAM probe (single-logit head assert) + Step 3 epoch-0 timing gate (BLOCKING — user authorizes the run)</name>
  <files>
    backend/training/aqa/notebooks/09_shallow_squat_baseline.py,
    backend/training/aqa/notebooks/09_shallow_squat_baseline.ipynb
  </files>
  <read_first>
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (Cell A bootstrap lines 34-72 + Step 0 env pattern lines 88-120 — COPY Cell A verbatim, change the title comment + the sanity-module list to the Phase-7 modules + swap to the image staging; DROP the PyAV install block lines 97-105 — images use PIL, not video decode; keep the dep-version probe + the GPU assert + mount_drive),
    backend/training/aqa/harness/image_supervised_train.py (build_resnet18 + run_image_epoch + ImageConfig — Step 2 builds the model, Step 3 calls run_image_epoch with max_epochs=1, dataset_cls=ShallowSquatDataset),
    backend/training/aqa/datasets/shallow_squat.py (ShallowSquatDataset + build_loaders — Step 1 reconciles the splits + smoke-tests a batch; note the label batch is (B,) single-head),
    backend/training/aqa/harness/colab.py (stage_shallow_squat_images from Plan 01 — the staging call; mount_drive),
    .planning/phases/03-squat-supervised-baseline/03-01-SUMMARY.md (the Step 2 VRAM probe + Step 3 epoch-0 timing-gate patterns — the max_memory_allocated idiom + estimated_total_h + the checkpoint round-trip verify),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Supervised Recipe — batch 32, 50ep, the expected 0.80-0.87 ballpark; §Environment — ResNet-18 is light, T4 acceptable; §Pitfall 3 images.zip extractall),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2 baseline-first + D5 single-head pos_weight + the -3-001 Drive folder for the images.zip)
  </read_first>
  <decision>
    Should we proceed with the full multi-seed Shallow-Squat baseline at batch_size=32 with the locked ImageConfig (50ep/8-patience), or downscope (smaller batch / fewer epochs / fewer seeds) before committing?
  </decision>
  <context>
    The baseline reuses the Plan-01 image_supervised_train + recipe. ResNet-18 at batch 32/224² is LIGHT (RESEARCH §Environment). Cell A + Step 0 stage the Shallow-Squat images.zip; Step 1 reconciles the splits + smoke-tests a batch; Steps 2-3 are the gates:

    Step 0: from backend.training.aqa.harness import _envinit FIRST; the dep-version probe (numpy/scipy/sklearn — NO PyAV); GPU assert (ResNet-18 fits any modern Colab GPU; assert CUDA available); mount_drive; stage_shallow_squat_images(DRIVE_ROOT_3001, local_root="/content/squat_shallow_images") (3738 JPEGs + labels + splits — the user supplies the -3-001 Drive path). Log the images.zip size + the *.jpg count + free disk.

    Step 1 (split reconciliation + dataset smoke): build a ShallowSquatDataset per split (or build_loaders); assert len == 2542/529/540 (the official-split reconciliation, live); print the train pos_weight (~1.28, shape (1,)); pull one train batch, assert shape (B,3,224,224) float32 + the label batch (B,) float32 in {0,1} (SINGLE head — NOT (B,2)); print a denormalized sample crop (ImageNet-denorm for viz) so the user confirms the crops load correctly.

    Step 2 (model + VRAM probe): build_resnet18() to cuda; ASSERT the head is nn.Linear(512,1) (single binary error) and a forward on torch.zeros(2,3,224,224) gives shape [2,1]; pull one batch from a dataset_cls=ShallowSquatDataset loader; forward + backward (BCEWithLogitsLoss(pos_weight) on logits[B,1] vs label[B,1] — unsqueeze the label to match + loss.backward()); print torch.cuda.max_memory_allocated()/1e9 (peak backward VRAM at batch 32). Assert it fits (< the GPU's VRAM).

    Step 3 (epoch-0 timing gate): run_image_epoch(run_name="shallow_squat_baseline_seed42_timing", drive_root=MYDRIVE, images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=42, config=ImageConfig(), resume=False, max_epochs=1, dataset_cls=ShallowSquatDataset) — one fresh epoch under the production code path on the 2542-crop train split; paste back epoch_0 wall-time, estimated_total_h = epoch_0_time * config.max_epochs / 3600, the epoch-0 train loss + val F1 (sanity: loss finite, val F1 not degenerate), GPU util. Verify the checkpoint round-trip: torch.load(epoch_000.pt, map_location='cpu'), assert the payload keys + code_version "phase07-image-baseline". The timing run lands in a separate _timing run dir.

    Decision criteria: estimated_total_h <= 2 (ResNet-18 is light, expect minutes) and VRAM fits -> option-a (proceed batch 32, 2-3 seeds). estimated_total_h unexpectedly high OR VRAM tight -> option-b (smaller batch / fewer epochs / 2 seeds).
  </context>
  <options>
    <option id="option-a">
      <name>Proceed at batch 32, locked ImageConfig, 2-3 seeds (42, 1337, optionally 7)</name>
      <pros>ResNet-18 on 3.7k crops is minutes/epoch; multi-seed is cheap; consistent with the Squat (3-seed) / OHP (2-seed) ensemble protocol; VRAM trivially fits.</pros>
      <cons>None expected — ResNet-18 at batch 32/224² is light. If the GPU is a small T4 with other load, the probe catches it.</cons>
    </option>
    <option id="option-b">
      <name>Drop batch (16) / fewer epochs / 2 seeds if VRAM tight or epoch-0 slow</name>
      <pros>Lower VRAM/time; choose only if Step 2 is tight or Step 3 estimated_total_h is unexpectedly high.</pros>
      <cons>A minor recipe deviation — document it in the SUMMARY (Plan 05).</cons>
    </option>
  </options>
  <action>This is a BLOCKING decision checkpoint. The executor lands Cell A + Step 0 (image staging) + Step 1 (split reconciliation + dataset smoke) + Step 2 (model+VRAM probe + the single-logit head assert) + Step 3 (epoch-0 timing gate) in the notebook .py AND ships the .ipynb pair (Cell A baked in per D6 / [[feedback_deliver_colab_as_ipynb]]). The user runs them on a fresh Colab (prompt the user FIRST per [[feedback_heavy_training_new_notebook]]) and pastes back: the staged *.jpg count (3738) + the split reconciliation (2542/529/540) + the pos_weight (~1.28, shape (1,)) + a sample crop (Step 0/1); the head-shape ([2,3,224,224]->[2,1]) + peak backward VRAM (Step 2); epoch_0 wall-time + estimated_total_h + epoch-0 train loss + val F1 + the checkpoint round-trip (Step 3). The user replies option-a / option-b per the criteria. No code change unless option-b (ImageConfig(batch_size=16) or a lower max_epochs / 2 seeds). Atomic commit: feat(07): notebook Cell A + Step 0 stage + Step 1 reconciliation + Step 2 VRAM + Step 3 timing gate (Task 1, D2/D8).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/09_shallow_squat_baseline.py').read()); print('parse ok')" && grep -q "_envinit" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "stage_shallow_squat_images" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "max_memory_allocated" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "estimated_total_h" backend/training/aqa/notebooks/09_shallow_squat_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook .py parses; Cell A + Step 0 + Step 1 + Step 2 + Step 3 present; .ipynb pair shipped (Cell A baked in)
    - Step 0 calls stage_shallow_squat_images (NOT the mp4 extractor); logs the 3738 *.jpg count + free disk; NO PyAV install (images use PIL)
    - Step 1 reconciles the official splits live (2542/529/540) + prints pos_weight ~1.28 (shape (1,)) + the label batch shape (B,) (single head) + a sample crop; Step 2 asserts the nn.Linear(512,1) head ([2,3,224,224]->[2,1]) + the batch-32 peak VRAM fits
    - Step 3 runs epoch-0 under run_image_epoch (dataset_cls=ShallowSquatDataset, checkpoint_phase="phase07", max_epochs=1) + verifies the checkpoint round-trip (code_version "phase07-image-baseline")
    - User pastes back the counts + reconciliation + the single-logit head shape + VRAM + epoch_0 timing + estimated_total_h, and replies option-a / option-b; NO multi-seed run yet
  </acceptance_criteria>
  <dependencies>Plan 01 (image_supervised_train.run_image_epoch + build_resnet18 + ShallowSquatDataset + stage_shallow_squat_images exist)</dependencies>
  <done>User replies option-a / option-b; Task 2 inherits the chosen batch/epochs/seed-count; the data stages + reconciles, VRAM fits, epoch-0 timing + the single-logit head shape + the checkpoint round-trip all confirmed before the multi-seed baseline run is authorized</done>
  <resume-signal>Type "option-a" or "option-b". Or paste back the staged counts + the split reconciliation + the VRAM + epoch_0 time + estimated_total_h and ask for guidance.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Notebook Step 4 — full multi-seed Shallow-Squat baseline training (sequential seeds, multi-session resume) + early-epoch convergence check</name>
  <files>backend/training/aqa/notebooks/09_shallow_squat_baseline.py</files>
  <read_first>
    backend/training/aqa/harness/image_supervised_train.py (run_image_epoch — Step 4 calls it per seed with resume=True, dataset_cls=ShallowSquatDataset, checkpoint_phase="phase07"),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (Step 4/5 the OHP full-training-run pattern — the resume=True single-cell-is-the-whole-run idiom + per-epoch paste-back + disconnect/resume recovery),
    backend/training/aqa/harness/colab.py (load_latest_checkpoint — the "Resumed from epoch_NNN.pt" log line the resume paste-back verifies),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Supervised Recipe — the recipe + the 0.80-0.87 ballpark + the multi-seed protocol; A8),
    .planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md (IMG-02 — baseline val F1 improves; multi-seed ensemble protocol),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D4 — 3-seed default, 2-seed acceptable; mean-of-sigmoids)
  </read_first>
  <action>
    Fill in Step 4: the full multi-seed baseline run. Structure as SEQUENTIAL per-seed cells (one runnable unit each, D4 — seeds 42, 1337, optionally 7 per Task 1's decision). For each seed N: config = ImageConfig() (or batch 16 / fewer epochs per Task 1); result{N} = run_image_epoch(run_name=f"shallow_squat_baseline_seed{N}", drive_root=MYDRIVE, images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=N, config=config, resume=True, max_epochs=config.max_epochs, dataset_cls=ShallowSquatDataset). resume=True so a mid-run Colab disconnect is recoverable (re-run the seed's cell after restart -> resumes from latest.txt). The trainer checkpoints every epoch + writes best.pt on val-F1 improvement (update_latest=False), so each seed cell IS that seed's whole run. After all seeds: print the per-seed val F1 table (best_f1_val + best epoch per seed) — the D4 variance evidence.

    This is one runnable unit per seed (ResNet-18 is fast — minutes/epoch — so each seed is short, but a disconnect is still possible) — acceptance is a STAGED paste-back per [[feedback_interactive_execution]]: (a) FIRST: the seed-42 epoch-0..5 paste-back — val F1 strictly improving over the first epochs (IMG-02 convergence signal: loss decreasing, val F1 rising on the near-balanced data); (b) on any mid-run disconnect: re-run Step 0 + the seed cell -> the first post-resume epoch logs "Resumed from epoch_NNN.pt"; (c) FINAL: each seed completes (early-stop or max_epochs) — paste back the per-seed best val F1 + best epoch + the best.pt paths on Drive. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Step 5 until all seeds complete + the per-seed table is pasted back.

    Atomic commit (per seed completion): feat(07): notebook Step 4 baseline seed{N} (Task 2, D2-baseline/D4 / IMG-02).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/09_shallow_squat_baseline.py').read()); print('parse ok')" && grep -q "run_image_epoch" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "shallow_squat_baseline_seed" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "resume=True" backend/training/aqa/notebooks/09_shallow_squat_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 4 present; sequential per-seed cells (42, 1337, optionally 7); run_image_epoch(run_name="shallow_squat_baseline_seed{N}", dataset_cls=ShallowSquatDataset, checkpoint_phase="phase07", resume=True) with images_root/labels_path/splits_root explicit
    - Runnable unit (Colab, minutes/seed, possibly multi-session): seed-42 epoch-0..5 paste-back shows val F1 strictly improving (IMG-02); on disconnect, the resume paste-back logs "Resumed from epoch_NNN.pt"; FINAL per-seed best val F1 + best epoch + best.pt paths
    - best.pt on Drive at phase07/shallow_squat_baseline_seed{N}/best.pt per seed; per-epoch atomic checkpoint + prune; best.pt write passed update_latest=False
    - The per-seed val F1 table (D4 variance) pasted back at the end
  </acceptance_criteria>
  <dependencies>Task 1 (VRAM/timing gate — authorizes the run + the batch/epochs/seed-count)</dependencies>
  <done>The ImageNet ResNet-18 supervised baseline trains to convergence on the official 2542-crop train split across multiple seeds via the Plan-01 image_supervised_train trainer; val F1 improves (IMG-02); the shallow_squat_baseline_seed{N} best.pt are on Drive — the SSL-lift control checkpoints</done>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 5 + Step 6 — mean-of-sigmoids ensemble + val threshold sweep + test eval (the SSL-lift control F1)</name>
  <files>backend/training/aqa/notebooks/09_shallow_squat_baseline.py</files>
  <read_first>
    backend/training/aqa/eval/metrics.py (threshold_sweep + f1_per_error + pr_auc_per_error + confusion_matrix_per_error — REUSED UNCHANGED; SINGLE binary head, operate on a single score column),
    backend/training/aqa/harness/image_supervised_train.py (build_resnet18 — to reload each seed's best.pt for the val/test passes; _val_pass for the gather idiom — gather sigmoid scores on CPU as a 1-D column),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (Step 6/7 threshold-sweep + test-eval patterns — gather val scores, threshold_sweep, apply to test, the summary table; adapt from 2-head to 1-head),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Supervised — NO paper supervised-ImageNet baseline for Shallow-Squat; the headline baseline (control) -> CVCSPC -> paper CVCSPC 0.869; SimSiam 0.829; §Evaluation the threshold protocol),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D4 — mean-of-sigmoids + single ensemble-val threshold; D5 — single-head threshold sweep on val, applied to test; the baseline is the SSL-lift control; D7 metric honesty)
  </read_first>
  <action>
    Append Step 5 (ensemble + val threshold) + Step 6 (test eval). Step 5: for each seed N in the trained set: ckpt = torch.load(phase07/shallow_squat_baseline_seed{N}/best.pt, map_location='cuda'); model = build_resnet18(); model.load_state_dict(ckpt["model_state_dict"]); model.eval(); run a val pass (gather per-crop sigmoid scores on CPU, no per-batch F1) on the 529-crop val split + a test pass on the 540-crop test split via a dataset_cls=ShallowSquatDataset loader -> per_seed_val_scores[N] (529,), per_seed_test_scores[N] (540,). Capture test ids once if available. Ensemble = mean-of-sigmoids across seeds: ensemble_val = np.mean([per_seed_val_scores[N] for N], axis=0) (529,) in [0,1]; ensemble_test similarly (540,). Tune the single decision threshold on the ensemble VAL scores: t_star, f1_val = threshold_sweep(val_labels, ensemble_val) (SINGLE binary head, D4 — one threshold on the ensemble, NOT per-seed-then-vote). Print: per-seed val F1 (each at 0.5, D4 variance), the ensemble val F1 + the threshold.

    Step 6 (test eval): apply the val-tuned threshold to the ensemble TEST scores: test_pred = (ensemble_test >= t_star).astype(int); test_f1 = f1_per_error(test_labels, test_pred) (computed in code, NOT hand-typed — T-07-06); test_pr_auc = pr_auc_per_error(test_labels, ensemble_test); test_cm = confusion_matrix_per_error(test_labels, test_pred). ALSO compute the single-best-seed test F1 (the seed with the best val F1, at t_star) for a single-model number. Print the summary table: our baseline (control) test F1 + PR-AUC + the threshold, framed as the SSL-LIFT CONTROL (RESEARCH §Supervised — there is NO paper supervised-ImageNet baseline for Shallow-Squat, so this is the only control; the paper CVCSPC 0.869 + SimSiam 0.829 are noted as the Plan-04 fine-tune target + the image-SSL context, NOT a baseline comparison). Store the baseline numbers in the notebook namespace + note them for the Plan 04 results.pkl (the baseline row of the comparison). Do NOT write results.pkl here — that is Plan 04's source of truth (the baseline numbers get folded in there).

    Single runnable unit (Colab) — user pastes back: the per-seed val F1 (variance) + the ensemble val F1 + the val-tuned threshold (Step 5), and the test summary table (test F1 + PR-AUC + the confusion matrix, ensemble + single-model) framed as the SSL-lift control (Step 6).

    Atomic commit: feat(07): notebook Step 5 ensemble + threshold sweep + Step 6 test eval — baseline control F1 (Task 3, D4/D5 / IMG-02).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/09_shallow_squat_baseline.py').read()); print('parse ok')" && grep -q "threshold_sweep" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "f1_per_error" backend/training/aqa/notebooks/09_shallow_squat_baseline.py && grep -q "pr_auc_per_error" backend/training/aqa/notebooks/09_shallow_squat_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 + Step 6 present; loads the per-seed best.pt via build_resnet18; ensembles via mean-of-sigmoids (in [0,1], NOT logits); a SINGLE decision threshold on the ensemble VAL scores via threshold_sweep (NOT per-seed-then-vote); eval/metrics.py reused UNCHANGED on the single score column
    - test F1 computed in code via f1_per_error at the val-tuned threshold (NOT hand-typed — T-07-06); single-model number also reported
    - The baseline F1 is framed as the SSL-LIFT CONTROL (no paper supervised-ImageNet row for Shallow-Squat — RESEARCH §Supervised); paper CVCSPC 0.869 + SimSiam 0.829 noted as the Plan-04 target + context, not a baseline comparison
    - Runnable unit (Colab): user pastes back the per-seed val F1 + the ensemble val F1 + the threshold + the test summary table (F1 + PR-AUC + confusion)
  </acceptance_criteria>
  <dependencies>Task 2 (the per-seed best.pt on Drive)</dependencies>
  <done>The Shallow-Squat supervised baseline F1 is reported on the official 540-crop test split at the val-tuned threshold (ensemble + single-model) — the SSL-lift control the Plan 04 CVCSPC fine-tune is measured against; the baseline numbers are recorded for the Plan 04 comparison + results.pkl</done>
</task>

</tasks>

<verification>
## Mapping to Phase 7 ROADMAP Success Criteria + IMG-01/IMG-02

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (partial): the ResNet-18 image pipeline trains a baseline model | Tasks 1 (gate), 2 (run) | Task 2 seed-0..5 paste-back: val F1 improving on the 2542-crop train split (IMG-02); best.pt on Drive |
| SC2 (partial): Shallow-Squat reports F1 on the official split | Task 3 | Task 3 test summary table: baseline test F1 at the val-tuned threshold (the SSL-lift control) |
| The official-split reconciliation (2542/529/540) | Task 1 | Step 1 reconciliation paste-back live in Colab |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| IMG-02 baseline val F1 improves over the first epochs | Task 2 | integration (Colab paste-back) | 1 |
| IMG-02 baseline test F1 single-head (SSL-lift control) | Task 3 | integration (Colab paste-back) | 1 |
| Multi-seed ensemble mean-of-sigmoids protocol | Task 2 + Task 3 | integration (Colab) | 1 |

(IMG-01 SSL triplet-accuracy convergence is Plan 03; IMG-02 SSL-init test F1 + the lift is Plan 04.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user on Colab
- Task 1 (BLOCKING decision): the Shallow-Squat crops stage (3738 JPEGs) + reconcile (2542/529/540) live; the single-logit head asserted ([2,3,224,224]->[2,1]); VRAM fits at batch 32; epoch-0 timing + the checkpoint round-trip confirmed BEFORE the multi-seed run; user replies option-a / option-b
- The ImageNet ResNet-18 supervised baseline trains to convergence (val F1 improving, IMG-02) across multiple seeds via the Plan-01 image_supervised_train.run_image_epoch (dataset_cls=ShallowSquatDataset); shallow_squat_baseline_seed{N}/best.pt on Drive
- Baseline F1 reported on the official 540-crop Shallow-Squat test split at the val-tuned threshold (ensemble + single-model) on the single score column — the SSL-lift CONTROL (no paper supervised-ImageNet row for Shallow-Squat — RESEARCH §Supervised); eval/metrics.py reused unchanged
- Disconnect-safe: the baseline run resumes cleanly (the resume paste-back logs "Resumed from epoch_NNN.pt"); images_root/labels_path/splits_root explicit; best.pt write passes update_latest=False
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D6); every D2/D4/D5 decision carries an inline citation in the plan (NOT in the source — project rule); the test F1 computed in code (NOT hand-typed)
- One accurate threat_model: offline pipeline, no high-severity threats; T-07-06 (the headline F1 — computed in code) is the priority; T-07-SC accept (zero new installs)
</success_criteria>

<output>
After Task 3 (baseline test F1 recorded), this plan is complete. Plan 03 (Wave 2) runs the faithful CVCSPC SSL pretrain on the unlabeled Squat set (Step-0 frame extraction + the phase-contrastive triplet pretrain) -> backbone.pt. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 03 once Task 3's baseline numbers are confirmed. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 7 Plan 02 written 2026-05-30. Wave 1: the ImageNet ResNet-18 supervised baseline (the SSL-lift control, the guaranteed IMG-02 result). 3 tasks (a blocking VRAM/timing gate + the multi-seed run + the ensemble/threshold/test eval). NO new production logic — this plan WIRES the UNCHANGED image_supervised_train.run_image_epoch (Plan 01) + eval/metrics.py + stage_shallow_squat_images on the Shallow-Squat crops. The only new artifact is the notebook (paired .py + .ipynb, Cell A baked in).

REVISION 2026-05-30 (checker pass against the actual code):
- BLOCKER 1 consistency (the trainer seam): the Plan-01 trainer module is named image_supervised_train.py (NEW self-contained 2D-image trainer, NOT image_train.py / NOT supervised_train), and its model builder is build_resnet18 (NOT build_baseline_model). Renamed every reference here (interfaces, decisions, key_links, the file context @-ref, all task read_first/action/verify) from image_train.run_image_epoch -> image_supervised_train.run_image_epoch and build_baseline_model -> build_resnet18, and noted run_image_epoch exposes model_builder=build_resnet18 (the Plan-04 seam).
- SHOULD-FIX #6 (single-error head): made the single-logit contract explicit throughout — the head is nn.Linear(512,1), the loss BCEWithLogitsLoss on a single logit, pos_weight shape (1,), the label batch (B,) (NOT (B,2)), the eval a single score column. Step 1 now asserts the (B,) label batch; Step 2 now ASSERTS the head is Linear(512,1) and a forward on zeros[2,3,224,224] gives [2,1] (added to the must-haves + the acceptance criteria + the decision Step-2 body + D5).

Key framing: there is NO paper supervised-ImageNet baseline row for Shallow-Squat (RESEARCH §Supervised — Table 3 has SimSiam 0.829 + CVCSPC 0.869 image-SSL rows, no plain-supervised row). The measured baseline is the ONLY SSL-lift control; the paper CVCSPC 0.869 is the Plan-04 fine-tune target, not a baseline comparison.

Deltas vs the OHP baseline plan (06-02): single-image (no PyAV install, no trajectory probe — that was the OHP video delta; here the Wave-0 unit tests already proved the image dataset, so Wave 1 is just stage->reconcile->VRAM/timing->train->eval); ResNet-18 is light (T4 acceptable, expect minutes/epoch); single binary head (threshold_sweep + f1_per_error on a single score column, no macro). The blocking gate is a checkpoint:decision (VRAM/timing) — there is no separate trajectory-format human-verify probe because there is no trajectory in the supervised path (the SSL trajectory probe is Plan 03's Step 0).

Project rule honored: NO AI-narration/citation comments in source — the notebook documents the recipe factually; the D-number citations live in this plan + the commit messages. The test F1 is computed in code (threshold_sweep + f1_per_error), no hand-typed literals (the Phase-3 precedent the user caught).

Security: accurate, not boilerplate. Offline ML pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-07-06 (headline F1 — computed in code, the priority given the Phase-3 precedent), T-07-07 (images.zip extractall — Plan-01-tested + count-verified), T-07-08 (resume pointer — Phase-2-proven), T-07-09 accept (no media/weights committed), T-07-SC accept (zero installs). No web-app threats invented.
</revision_log>
</output>
