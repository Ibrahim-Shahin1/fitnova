---
phase: 07-image-based-errors-cvcspc
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py
  - backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.ipynb
  - .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl
autonomous: false
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "Multiple fine-tune seeds (42, 1337, optionally 7) run sequentially on one GPU from the SHARED Plan-03 backbone.pt via the image_train trainer with a backbone-loading model builder — each loads the CVCSPC backbone (NOT ImageNet-from-scratch) with a fresh nn.Linear(512,1) head, Adam lr=1e-4, BCEWithLogitsLoss(pos_weight ~1.28), 50ep/8-patience cosine — producing best.pt at phase07/shallow_squat_cvcspc_finetune_seed{N}/best.pt; per-seed val F1 recorded"
    - "The seeds are aggregated by mean-of-sigmoids and a SINGLE decision threshold is tuned on the ENSEMBLE val scores via eval/metrics.threshold_sweep (NOT per-seed thresholds then vote, D4); the per-seed val F1 recorded as variance evidence"
    - "Final test F1 is reported on the official 540-crop Shallow-Squat test split at the val-tuned threshold, with the comparison placing our baseline (Plan-02 control) -> our CVCSPC ensemble -> paper CVCSPC 0.8694 (CITED RESEARCH §Supervised) on identical metrics; the SSL lift = (our CVCSPC ensemble) - (our baseline), every number computed in code (NOT hand-typed); the val->test gap reported (no overfitting check)"
    - "results.pkl is written (BEFORE any figure) to .planning/phases/07-image-based-errors-cvcspc/figures/ holding: the SSL triplet-accuracy curve (from shallow_squat_cvcspc_v1's latest.txt-pointed checkpoint), per-seed baseline + CVCSPC-finetune curves, ensemble val+test scores + labels + (ids if available), the val-tuned threshold, test F1 (CVCSPC ensemble + single-model + per-seed), the baseline control (Plan 02), the ssl_lift, the val_test_gap, and paper_targets {cvcspc:0.8694, simsiam:0.8286, openpose_tdm:0.8340} — the source of truth for the Plan 05 deliverable pack"
    - "Each fine-tune is disconnect-safe (per-epoch atomic checkpoint, map_location='cpu' resume, best.pt write does not clobber latest.txt); the model builder loads the CVCSPC backbone_state_dict via load_state_dict(strict=False), asserts the backbone path is phase07/shallow_squat_cvcspc_v1/backbone.pt (NOT a from-scratch/ImageNet path), and the head is reset to a fresh nn.Linear(512,1) (the SSL projector discarded)"
  artifacts:
    - path: "backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py"
      provides: "Phase 7 fine-tune + ensemble + eval jupytext notebook — Cell A + Step 0 stage(labeled crops) + Step 1 backbone-path assert + a small build_cvcspc_finetune_model helper cell (loads backbone_state_dict, fresh Linear(512,1) head) + Step 2 multi-seed fine-tune + Step 3 ensemble + val threshold + Step 4 test eval + the baseline->CVCSPC->paper comparison + results.pkl. Figures are Plan 05."
      contains: "shallow_squat_cvcspc_finetune_seed"
    - path: "backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.ipynb"
      provides: "Colab-runnable pair (Cell A baked in) per [[feedback_deliver_colab_as_ipynb]] — delivered on the FIRST ship (D6)"
      contains: "cvcspc"
    - path: ".planning/phases/07-image-based-errors-cvcspc/figures/results.pkl"
      provides: "Pickled dict — the SSL triplet-accuracy curve, per-seed baseline + CVCSPC-finetune curves, ensemble val+test scores/labels/(ids), the val-tuned threshold, test F1 (CVCSPC ensemble + single-model + per-seed), the Plan-02 baseline control, the ssl_lift, the val_test_gap, paper_targets {cvcspc:0.8694, simsiam:0.8286, openpose_tdm:0.8340}. The source of truth for the Plan 05 notebook pack + FINDINGS."
      contains: "paper_targets"
  key_links:
    - from: "backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py"
      to: "backend/training/aqa/harness/image_train.run_image_epoch (dataset_cls=ShallowSquatDataset) + the Plan-03 backbone.pt (loaded via load_state_dict strict=False into a fresh resnet18+Linear(512,1))"
      via: "notebook cell imports; the build_cvcspc_finetune_model helper loads backbone_state_dict, NOT ImageNet-from-scratch"
      pattern: "backbone_state_dict"
    - from: "backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py"
      to: "backend/training/aqa/eval/metrics.{threshold_sweep, f1_per_error, pr_auc_per_error, confusion_matrix_per_error} — REUSED UNCHANGED"
      via: "notebook cell imports"
      pattern: "from backend.training.aqa.eval.metrics import"
    - from: ".planning/phases/07-image-based-errors-cvcspc/figures/results.pkl"
      to: "the per-seed best.pt (phase07/shallow_squat_cvcspc_finetune_seed{N}) + the Plan-03 cvcspc_v1 triplet_acc_history + the Plan-02 baseline control"
      via: "the notebook gathers per-seed scores -> ensemble -> threshold -> test eval -> pickle"
      pattern: "results.pkl"
---

<objective>
Fine-tune the CVCSPC-pretrained backbone (Plan 03's backbone.pt) for Shallow-Squat depth-error detection across multiple seeds, ensemble (mean-of-sigmoids) + val-tuned threshold, evaluate on the official 540-crop test split, and write results.pkl — the headline Phase 7 result + the source of truth for the Plan 05 deliverable pack (IMG-01 + IMG-02). This is Wave 3 — it consumes BOTH the Plan-03 CVCSPC backbone AND the Plan-02 baseline control, and produces the F1-vs-paper comparison + the baseline->CVCSPC lift.

Purpose: IMG-02 (Shallow-Squat detector; F1 on the official split, now via the CVCSPC-init fine-tune) + IMG-01 (the CVCSPC pipeline, the fine-tune-from-SSL half) + ROADMAP Phase 7 success criterion 2 ("Shallow-Squat ... reports F1 on the official splits") + the paper comparison. The fine-tune reuses the Plan-01 image_train trainer with ONE delta — a model builder that loads the CVCSPC backbone_state_dict (strict=False) into resnet18(weights=None) + a fresh nn.Linear(512,1) head (the SSL projector is discarded; Pitfall 6). The paper uses end-to-end fine-tuning for single-image errors (RESEARCH §Downstream Fine-Tune). The comparison: our baseline (Plan-02 control) -> our CVCSPC ensemble -> paper CVCSPC 0.8694 (CITED Table 3). The SSL lift = (CVCSPC ensemble) - (baseline). NOTE (defense framing): there is NO paper supervised-ImageNet row, so the baseline is the only control; SimSiam 0.8286 is the paper's image-SSL row for context.

Output: the Phase 7 fine-tune notebook (Cell A + the backbone-loading helper + multi-seed fine-tune + ensemble + test eval + results.pkl) delivered as paired .py + .ipynb; the per-seed best.pt on Drive at phase07/shallow_squat_cvcspc_finetune_seed{N}/; results.pkl under figures/ — the headline F1 + raw scores + the comparison numbers.

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]] on the Colab. The fine-tunes run SEQUENTIALLY (seed 42 first, then 1337, 7). Each seed is a resume=True cell (disconnect-safe). ResNet-18 fine-tune is light (minutes/epoch). After git pull in a running kernel, restart the runtime or importlib.reload ([[reference_colab_module_reload_after_git_pull]]). No fabricated metrics — every number from a real run ([[feedback_ai_correctness]]); tqdm on any cell >2 min.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md
@.planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md
@.planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md
@.planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md
@.planning/phases/07-image-based-errors-cvcspc/07-02-PLAN.md
@.planning/phases/07-image-based-errors-cvcspc/07-03-PLAN.md
@.planning/phases/06-overhead-press/06-04-PLAN.md
@.planning/phases/04-squat-motion-disentangling-ssl/04-04-PLAN.md
@backend/training/aqa/harness/image_train.py
@backend/training/aqa/harness/cvcspc_pretrain.py
@backend/training/aqa/harness/md_finetune.py
@backend/training/aqa/datasets/shallow_squat.py
@backend/training/aqa/eval/metrics.py
@backend/training/aqa/notebooks/06_ohp_supervised_baseline.py
@CLAUDE.md

<interfaces>
This plan CONSUMES the Plan-03 backbone.pt + the Plan-02 baseline + the UNCHANGED image_train trainer + eval/metrics.py. It writes notebook cells + a small in-notebook model-builder helper + results.pkl. NO new production .py logic (the fine-tune model builder is a notebook cell — it loads backbone_state_dict into the Plan-01 build_baseline_model architecture; if a reusable helper is cleaner, it may be a thin function in image_train.py, but prefer the notebook cell to keep this plan notebook-only).

Reused UNCHANGED:
- image_train.run_image_epoch(*, run_name, ..., resume=True, dataset_cls=ShallowSquatDataset, checkpoint_phase="phase07") — for the CVCSPC fine-tune, the model is built fresh from the CVCSPC backbone INSIDE the notebook (see below) OR — if run_image_epoch is to load the backbone — the cleanest path is a notebook helper that constructs the model, loads the backbone, and the notebook drives the per-seed training via run_image_epoch with a model-injection. PREFER: mirror md_finetune's build_finetune_model pattern as an in-notebook helper build_cvcspc_finetune_model(backbone_path) -> resnet18(weights=None) + load_state_dict(ckpt["backbone_state_dict"], strict=False) + model.fc=nn.Linear(512,1); then run a per-seed training loop in the notebook that reuses image_train's _build_dataloaders + _val_pass + the checkpoint primitives (OR, if run_image_epoch accepts a model factory, pass it). NOTE to the executor: check run_image_epoch's signature from Plan 01 — if it builds the model internally via build_baseline_model with no injection seam, add a minimal model_builder kwarg to run_image_epoch (default build_baseline_model) mirroring the dataset_cls seam, so the CVCSPC fine-tune passes build_cvcspc_finetune_model. This is the ONE small seam this plan may add to image_train.py; surface it, mirror the Plan-01 dataset_cls seam exactly.
- image_train.build_baseline_model + _build_dataloaders + _val_pass + ImageConfig — reused.
- harness.colab.stage_shallow_squat_images (Plan 01) — stages the labeled crops for the fine-tune.
- eval/metrics.threshold_sweep / f1_per_error / pr_auc_per_error / confusion_matrix_per_error.

Comparison numbers (RESEARCH §Supervised, CITED — use EXACTLY these):
- Paper CVCSPC (the target row): 0.8694.
- Paper SimSiam (image SSL): 0.8286.
- Paper OpenPose-TDM (2D pose): 0.8340.
- NO paper supervised-ImageNet row — the Plan-02 baseline is the only supervised control; the SSL lift = (CVCSPC ensemble) - (baseline).

Aggregation order (D4): per-seed -> sigmoid -> mean-of-sigmoids across seeds (np.mean of per-seed sigmoid scores) -> single threshold tuned on the ensemble VAL scores (threshold_sweep) -> apply to test.
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D2-finetune | The CVCSPC fine-tune loads the Plan-03 backbone_state_dict (strict=False) into resnet18(weights=None) + a fresh nn.Linear(512,1) head (the SSL projector discarded — Pitfall 6), end-to-end fine-tune, Adam lr=1e-4 wd=0, BCEWithLogitsLoss(pos_weight ~1.28), 50ep/8-patience cosine, batch 32, 224². The paper uses end-to-end fine-tune for single-image errors (RESEARCH §Downstream). Via the image_train trainer with a backbone-loading model builder (the ONE small seam, mirroring the dataset_cls seam). | CONTEXT D2 + RESEARCH §Downstream Fine-Tune / Pitfall 6 |
| D4-ensemble | Mean of sigmoid scores across the seeds (in [0,1]); a SINGLE decision threshold tuned on the ENSEMBLE val scores via threshold_sweep (NOT per-seed thresholds then vote). Per-seed test F1 also reported (variance evidence). 3-seed default, 2-seed acceptable (CONTEXT D4). | CONTEXT D4 |
| D-compare | Comparison = our baseline (Plan-02 control) -> our CVCSPC ensemble -> paper CVCSPC 0.8694 (CITED Table 3). NO paper supervised-ImageNet row (RESEARCH §Supervised) — the baseline is the only control; SimSiam 0.8286 is the image-SSL context. The SSL lift = (CVCSPC ensemble macro/F1) - (baseline F1). NO fabricated numbers. | RESEARCH §Supervised + [[feedback_ai_correctness]] |
| D-loss-deviation | The 3-term-vs-2-term loss deviation (the SSL loss used in Plan 03) is DOCUMENTED honestly in FINDINGS (Plan 05), NOT silently reconciled — the backbone was pretrained with the 3-term code version; this is recorded as a faithful-to-code, deviates-from-paper-Eq.1 choice. | CONTEXT D2/D7 + RESEARCH §Loss |
| D3-eval | eval/metrics.py reused UNCHANGED. Per-error (single-head) threshold sweep on val, applied to test. PR-AUC threshold-free. F1 computed in code (NOT hand-typed). | CONTEXT D5 + RESEARCH §Evaluation |
| D8-resultspkl | results.pkl written BEFORE any figure (the source of truth) — the SSL triplet-accuracy curve, per-seed baseline + CVCSPC-finetune curves, ensemble val+test scores/labels/(ids), the val-tuned threshold, test F1 (CVCSPC ensemble + single-model + per-seed), the Plan-02 baseline control, the ssl_lift, the val_test_gap, paper_targets {cvcspc:0.8694, simsiam:0.8286, openpose_tdm:0.8340}. Figures are Plan 05. | CONTEXT D1 (deliverable) + [[project_supervisor_visualizations]] |
| D7 | persistent_workers (via _build_dataloaders, unchanged); map_location='cpu' on resume + the backbone load; best.pt write does not clobber latest.txt (update_latest=False); metrics_history from latest.txt-pointed checkpoint for viz; each seed its own run_dir; one runnable unit at a time; restart runtime / importlib.reload after git pull; images_root/labels_path/splits_root explicit. | CONTEXT D7 |

</decisions>

<determinism_checklist>
## Determinism / disconnect-safety preconditions (carry Phase 3/4/6 + D7)

1. _set_global_seed(seed) per seed (42, 1337, 7) — covers Python/NumPy/torch-CPU/torch-CUDA RNG (the image_train trainer, Plan 01).
2. persistent_workers via _build_dataloaders (unchanged, num_workers>0 guard).
3. map_location='cpu' on load_latest_checkpoint resume AND on the backbone.pt load in build_cvcspc_finetune_model.
4. 4-RNG capture/restore in every epoch payload + at resume.
5. best.pt write passes update_latest=False so latest.txt keeps pointing at the latest epoch_NNN.pt.
6. Checkpoint EVERY epoch + prune_checkpoints(keep_last=3, keep_best=True). A disconnect loses at most one epoch per seed.
7. metrics_history loaded from the latest.txt-pointed checkpoint for the per-seed curves (NOT best.pt); the SSL triplet-accuracy curve from cvcspc_v1's latest.txt-pointed checkpoint.
8. config_hash via hash_config (the ImageConfig recipe keys + the backbone path so a resume with a different backbone correctly mismatches); each seed its OWN run_dir (shallow_squat_cvcspc_finetune_seed{42,1337,7}).
9. No AMP (fp32).
10. images_root/labels_path/splits_root explicit to the staged dirs.
11. All val/test scores gathered on CPU then a single sklearn call (no per-batch F1). The ensemble produces sigmoid means in [0,1] so pr_auc_per_error's [0,1] assertion holds.
12. results.pkl written BEFORE any figure; restart runtime / importlib.reload after git pull.

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE ML training + evaluation pipeline (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: the headline F1 arithmetic correctness, val-test leakage (the held-out test stays held out), the CVCSPC-not-from-scratch backbone load, the head reset, and not committing dataset media/weights. Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| Plan-03 backbone.pt -> fine-tune init | build_cvcspc_finetune_model loads backbone_state_dict (strict=False) into resnet18(weights=None) + a fresh Linear(512,1). A wrong load (ImageNet-from-scratch, or not resetting the head) silently discards the SSL contribution. |
| Ensemble/threshold tuning -> the held-out test split | The threshold tuned on VAL only; test stays held out. The val-test-gap monitor guards over-tuning. |
| Computed metrics -> the comparison + results.pkl | The comparison numbers must be arithmetically correct + match the CITED paper Table 3 (CVCSPC 0.8694); the baseline control loaded from Plan 02 (not hand-typed); mean-of-sigmoids (not logits). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Tampering | build_cvcspc_finetune_model could load ImageNet-from-scratch instead of the CVCSPC backbone, OR not reset the head (silently discarding the SSL contribution) | mitigate | build_cvcspc_finetune_model uses resnet18(weights=None) + explicit load_state_dict(ckpt["backbone_state_dict"], strict=False) + model.fc=nn.Linear(512,1) (head reset — Pitfall 6); Step 1 asserts the backbone path is phase07/shallow_squat_cvcspc_v1/backbone.pt (NOT an ImageNet/from-scratch path). MEDIUM — path-asserted + the explicit load. |
| T-07-16 | Tampering | Headline-number arithmetic error (the Phase-3 precedent the user caught) | mitigate | The test F1 is computed in code via f1_per_error at the val-tuned threshold (NOT hand-typed); the comparison uses the CITED Table 3 numbers (0.8694/0.8286/0.8340) + the Plan-02 baseline loaded from the notebook namespace/Drive (not hand-typed); Step 4 recomputes + asserts. MEDIUM — computed in code. |
| T-07-17 | Information disclosure | Val-test leakage — the threshold over-tuned to val, inflating the test number | mitigate | The threshold (threshold_sweep) tuned on VAL only; the val-test F1 gap reported + flagged if large. MEDIUM — the gap monitor is the leakage guard. |
| T-07-18 | Tampering | Ensemble inhomogeneity or mean-of-logits instead of mean-of-sigmoids | mitigate | All seeds use the same recipe + the same CVCSPC backbone; the ensemble applies sigmoid BEFORE the mean + the scores are in [0,1]. Per-seed test F1 recorded (variance). MEDIUM — one recipe + the [0,1]-checked aggregation. |
| T-07-19 | Integrity | Committing dataset media / weights (the per-seed best.pt) | accept | All weights gitignored / Drive-staged. This plan commits only the notebook .py/.ipynb + results.pkl (small, no media). The best.pt live on Drive. Stage files by name only. LOW — gitignore contract established. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). The Step-0 dep-version probe is the guard. No [ASSUMED]/[SUS] packages. |

T-07-16 (headline arithmetic) is the highest-priority threat given the Phase-3 precedent; mitigated by computing F1 in code + the CITED Table 3 numbers + loading the baseline from Plan 02. No web-app threats apply.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Notebook Cell A + Step 0 stage + Step 1 backbone-path assert + build_cvcspc_finetune_model + Step 2 multi-seed fine-tune (42 first, then 1337/7)</name>
  <files>
    backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py,
    backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.ipynb
  </files>
  <read_first>
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (Cell A + Step 0 stage pattern — copy + swap to the labeled-crop staging),
    backend/training/aqa/harness/md_finetune.py (build_finetune_model lines 84-108 — the load-backbone-strict=False + reset-head pattern to mirror as build_cvcspc_finetune_model; the per-seed run pattern),
    backend/training/aqa/harness/image_train.py (run_image_epoch + build_baseline_model + _build_dataloaders + _val_pass — the trainer the fine-tune drives; CHECK whether run_image_epoch has a model-injection seam — if not, add a minimal model_builder kwarg mirroring dataset_cls, default build_baseline_model, and surface it),
    .planning/phases/07-image-based-errors-cvcspc/07-03-PLAN.md (the backbone.pt contract — phase07/shallow_squat_cvcspc_v1/backbone.pt, backbone_state_dict, fc=Identity),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Downstream Fine-Tune — end-to-end, reset the head to Linear(512,1); Pitfall 6 head reset; §SSL Model the strict=False load),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D2 fine-tune-from-CVCSPC + D4 multi-seed)
  </read_first>
  <action>
    Land Cell A + Step 0 + Step 1 + the model-builder helper + Step 2 in the fine-tune notebook (11_shallow_squat_cvcspc_finetune.py + .ipynb pair, Cell A baked in — D6). Cell A: clone-or-pull + sys.path (verbatim; Phase-7 title + the cvcspc module sanity list). Step 0: _envinit first; the dep-version probe (NO PyAV — images use PIL); GPU assert; mount_drive; stage_shallow_squat_images(DRIVE_ROOT_3001, local_root="/content/squat_shallow_images") (the labeled crops — the fine-tune trains on these). Step 1: BACKBONE_PATH = os.path.join(MYDRIVE, "FitNova/checkpoints/phase07/shallow_squat_cvcspc_v1/backbone.pt"); assert os.path.exists(BACKBONE_PATH), "Plan 03 backbone.pt must exist" (T-07-15 path assertion — NOT an ImageNet/from-scratch path).

    The model-builder helper cell: build_cvcspc_finetune_model(backbone_path) -> nn.Module — mirror md_finetune.build_finetune_model: from torchvision.models import resnet18; model = resnet18(weights=None); ckpt = torch.load(backbone_path, map_location="cpu", weights_only=False); model.load_state_dict(ckpt["backbone_state_dict"], strict=False); assert model.fc.in_features in (512, the Identity case) — since the backbone saved fc=Identity, load strict=False then set model.fc=nn.Linear(512,1) (the fresh head — Pitfall 6, the SSL projector discarded). Return model. (If run_image_epoch has no model-injection seam, add a minimal model_builder kwarg to image_train.run_image_epoch — default build_baseline_model, mirroring the dataset_cls seam exactly — and the fine-tune passes model_builder=lambda: build_cvcspc_finetune_model(BACKBONE_PATH). Surface this one-line seam in the commit + the SUMMARY.)

    Step 2 = the multi-seed CVCSPC fine-tune. Structure as SEQUENTIAL per-seed cells (one runnable unit each): for seed N in (42, 1337, optionally 7): config = ImageConfig(); result{N} = run_image_epoch(run_name=f"shallow_squat_cvcspc_finetune_seed{N}", drive_root=MYDRIVE, images_root="/content/squat_shallow_images/crops_unaligned", labels_path="/content/squat_shallow_images/labels_shallow_depth.json", splits_root="/content/squat_shallow_images/splits", seed=N, config=config, resume=True, max_epochs=config.max_epochs, dataset_cls=ShallowSquatDataset, model_builder=lambda: build_cvcspc_finetune_model(BACKBONE_PATH)) (or the notebook-driven loop if no seam). resume=True (disconnect-safe). After all seeds: print the per-seed val F1 table (best_f1_val + best epoch) — the D4 variance evidence.

    Single runnable unit per seed (ASYNC, multi-session possible) — per-seed paste-back of best_f1_val + the resume log on any disconnect; the per-seed val-F1 table at the end. Per [[feedback_one_cell_at_a_time_strict]], do NOT pre-write Step 3 until all seeds complete + the table is pasted back.

    Atomic commit (per seed completion): feat(07): notebook Step 2 CVCSPC fine-tune seed{N} (Task 1, D2/D4).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py').read()); print('parse ok')" && grep -q "build_cvcspc_finetune_model" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "backbone_state_dict" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "shallow_squat_cvcspc_finetune_seed" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "shallow_squat_cvcspc_v1/backbone.pt" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook .py parses; Cell A + Step 0 + Step 1 + the model-builder helper + Step 2 present; .ipynb pair shipped; sequential per-seed cells (42 first, then 1337, 7)
    - BACKBONE_PATH asserted to be phase07/shallow_squat_cvcspc_v1/backbone.pt (T-07-15); build_cvcspc_finetune_model loads backbone_state_dict (strict=False) + resets the head to nn.Linear(512,1) (Pitfall 6 — the SSL projector discarded)
    - If a model-injection seam was needed: image_train.run_image_epoch got a minimal model_builder kwarg (default build_baseline_model, mirroring dataset_cls) — surfaced in the commit; otherwise the notebook-driven loop is used
    - Runnable unit (Colab, multi-session possible): per-seed paste-back of best_f1_val + the per-seed val-F1 table (D4 variance); on disconnect, "Resumed from epoch_NNN.pt"
    - per-seed best.pt on Drive at phase07/shallow_squat_cvcspc_finetune_seed{N}/
  </acceptance_criteria>
  <dependencies>Plan 03 (backbone.pt on Drive), Plan 02 (the image_train trainer + the baseline control — same trainer)</dependencies>
  <done>The per-seed CVCSPC fine-tune best.pt are on Drive from the shared Plan-03 backbone via the image_train trainer (the backbone loaded NOT-from-scratch, the head reset); per-seed val F1 recorded (D4 variance); the inputs for the ensemble exist</done>
</task>

<task type="auto">
  <name>Task 2: Notebook Step 3 — mean-of-sigmoids ensemble + val threshold sweep</name>
  <files>backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py</files>
  <read_first>
    backend/training/aqa/eval/metrics.py (threshold_sweep + f1_per_error + pr_auc_per_error — REUSED UNCHANGED; binary single-head, 1-D arrays),
    backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py (the Task-1 build_cvcspc_finetune_model + the per-seed best.pt paths — Step 3 reloads each seed),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (the OHP ensemble + threshold pattern — the per-seed score gather, the aggregation order, the single-threshold-on-ensemble; adapt 2-head -> 1-head),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Evaluation — the ensemble + the threshold protocol),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D4 — mean-of-sigmoids + single ensemble-val threshold)
  </read_first>
  <action>
    Append Step 3 (ensemble + val threshold). For each seed N in the trained set: ckpt = torch.load(phase07/shallow_squat_cvcspc_finetune_seed{N}/best.pt, map_location='cuda'); model = build_cvcspc_finetune_model(BACKBONE_PATH); model.load_state_dict(ckpt["model_state_dict"]); model.eval(); run a val pass + a test pass on the OHP... (Shallow-Squat) val/test loaders (dataset_cls=ShallowSquatDataset; gather sigmoid scores on CPU, no per-batch F1) -> per_seed_val_scores[N] (529,), per_seed_test_scores[N] (540,). Capture test ids once if available. Ensemble = mean-of-sigmoids across seeds: ensemble_val = np.mean([per_seed_val_scores[N] for N], axis=0) (529,) in [0,1]; ensemble_test similarly (540,). Tune the single threshold on the ensemble VAL scores: t_star, f1_val = threshold_sweep(val_labels, ensemble_val) (single binary head, D4 — one threshold on the ensemble, NOT per-seed-then-vote). Print: per-seed val F1 (each at 0.5, D4 variance), the ensemble val F1 + the threshold. Store ensemble_val/test, per_seed_*, t_star, test_ids for Steps 4.

    Single runnable unit (Colab) — user pastes back: per-seed val F1 (variance) + the ensemble val F1 + the val-tuned threshold.

    Atomic commit: feat(07): notebook Step 3 CVCSPC ensemble + val threshold (Task 2, D4 / RESEARCH §Evaluation).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py').read()); print('parse ok')" && grep -q "threshold_sweep" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "ensemble_val" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "ensemble_test" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 present; loads the per-seed best.pt via build_cvcspc_finetune_model; gathers per-seed val+test sigmoid scores; ensembles via mean-of-sigmoids (in [0,1], NOT logits — T-07-18); a SINGLE threshold on the ensemble VAL scores via threshold_sweep (NOT per-seed-then-vote)
    - Per-seed val F1 (variance) + the ensemble val F1 + the threshold printed
    - Runnable unit (Colab): user pastes back the per-seed val F1 + the ensemble val F1 + the threshold
  </acceptance_criteria>
  <dependencies>Task 1 (the per-seed best.pt)</dependencies>
  <done>The seeds are ensembled by mean-of-sigmoids; the single decision threshold is tuned on the ensemble val scores; the ensemble val+test scores are ready for the test eval</done>
</task>

<task type="auto">
  <name>Task 3: Notebook Step 4 — final test eval + the baseline->CVCSPC->paper comparison + the val-test gap + results.pkl</name>
  <files>
    backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py,
    .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl
  </files>
  <read_first>
    backend/training/aqa/eval/metrics.py (f1_per_error, pr_auc_per_error, confusion_matrix_per_error — the final test metrics),
    backend/training/aqa/notebooks/06_ohp_supervised_baseline.py (the OHP Step 5 test-eval + results.pkl pattern — the summary table + the pickle structure to mirror; the D6 post-hoc monitors / val-test gap),
    .planning/phases/07-image-based-errors-cvcspc/07-02-PLAN.md (the Plan-02 baseline control numbers — the SSL-lift control; how they were recorded in the baseline notebook namespace / re-loadable from the baseline best.pt),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Supervised — the EXACT paper Table 3 numbers: CVCSPC 0.8694, SimSiam 0.8286, OpenPose-TDM 0.8340; NO supervised-ImageNet row; double-check every value),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D1 the results.pkl deliverable + the comparison; D7 metric honesty + the 3-term-vs-2-term deviation to document)
  </read_first>
  <action>
    Append Step 4: final test eval at the val-tuned threshold + the comparison + the val-test gap + results.pkl. Apply t_star to the ensemble TEST scores: test_pred = (ensemble_test >= t_star).astype(int); test_f1 = f1_per_error(test_labels, test_pred) (computed in code, NOT hand-typed — T-07-16); test_pr_auc = pr_auc_per_error(test_labels, ensemble_test); test_cm = confusion_matrix_per_error(test_labels, test_pred). ALSO compute the single-best-seed test F1 (at t_star) + the per-seed test F1 (D4 variance). Load the Plan-02 baseline control number (from the baseline notebook's recorded value, or re-load phase07/shallow_squat_baseline_seed*/best.pt + re-run the baseline ensemble test pass if not cached). Print the summary table: our baseline (control) F1; our CVCSPC ensemble F1; paper CVCSPC 0.8694; paper SimSiam 0.8286; paper OpenPose-TDM 0.8340 (use EXACTLY these — RESEARCH §Supervised, arithmetic pre-verified). Compute + print the SSL LIFT = (CVCSPC ensemble F1) - (baseline F1).

    POST-HOC MONITORS (print + flag, not hard-fail): (a) val_test_gap = abs(ensemble_val_f1 - test_f1); flag if > 0.05 ("over-tuned to val"); (b) the NO-LIFT NOTE: if test_f1 <= baseline_f1 (the CVCSPC did not beat the supervised baseline), record it honestly for the SUMMARY (a valid result — "faithfully reproduced CVCSPC on Shallow-Squat; the lift over the supervised baseline was X" — the baseline -> CVCSPC -> paper narrative stands regardless); do NOT open-ended iterate here (the projection-head ablation was Plan 03's escape; here the result is the result).

    Write results.pkl (BEFORE any figure — the source of truth, T-07-16) to .planning/phases/07-image-based-errors-cvcspc/figures/ (mkdir parents first): the dict per the PATTERNS/OHP results.pkl schema — ssl_triplet_acc (the triplet-accuracy curve from cvcspc_v1's latest.txt-pointed checkpoint), finetune_seeds {42:curves,1337:...,7:...} (per-seed train/val loss + val F1 from each seed's latest.txt-pointed checkpoint), baseline_seeds (the Plan-02 per-seed curves if available), ensemble_val_scores (529,), ensemble_test_scores (540,), val_labels, test_labels, test_ids (if available), best_threshold, test_f1 {cvcspc_ensemble, single_model, per_seed}, test_pr_auc, test_confusion, baseline_control (the Plan-02 baseline F1 + threshold), ssl_lift, val_test_gap, paper_targets {cvcspc:0.8694, simsiam:0.8286, openpose_tdm:0.8340}, the loss_deviation_note ("SSL pretrained with the 3-term code loss; paper Eq.1 is 2-term"), run provenance.

    Single runnable unit (Colab) — user pastes back: the full summary table (our baseline -> CVCSPC ensemble -> paper rows) + the SSL lift + the val-test gap + the results.pkl write confirmation. The orchestrator double-checks the F1 against the printed values + the §Supervised paper numbers.

    Atomic commit: feat(07): notebook Step 4 test eval + baseline->CVCSPC->paper + val-test gap + results.pkl (Task 3, D-compare / RESEARCH §Supervised).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py').read()); print('parse ok')" && grep -q "results.pkl" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "0.8694" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "ssl_lift" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py && grep -q "paper_targets" backend/training/aqa/notebooks/11_shallow_squat_cvcspc_finetune.py</automated>
  </verify>
  <acceptance_criteria>
    - Step 4 present; test eval at the val-tuned threshold; test_f1 computed in code via f1_per_error (NOT hand-typed — T-07-16)
    - The comparison rows match RESEARCH §Supervised EXACTLY: paper CVCSPC 0.8694, SimSiam 0.8286, OpenPose-TDM 0.8340; the baseline control loaded from Plan 02 (not hand-typed); the SSL lift = (CVCSPC ensemble) - (baseline) computed + printed
    - The val-test gap reported (flag if >0.05); the no-lift case recorded honestly if it occurs (no open-ended iteration)
    - results.pkl written (BEFORE figures) with the full schema incl. ssl_triplet_acc, finetune_seeds, ensemble val+test scores/labels, the threshold, test F1 (ensemble+single+per-seed), baseline_control, ssl_lift, val_test_gap, paper_targets {cvcspc:0.8694,simsiam:0.8286,openpose_tdm:0.8340}, the loss_deviation_note
    - Runnable unit (Colab): user pastes back the summary table + the SSL lift + the val-test gap + the results.pkl write confirmation
  </acceptance_criteria>
  <dependencies>Task 2 (the ensemble val+test scores)</dependencies>
  <done>The headline Phase 7 Shallow-Squat test F1 is computed (CVCSPC ensemble + single-model) at the val-tuned threshold; the comparison places it against the CITED paper CVCSPC 0.8694 + the Plan-02 baseline control; the SSL lift + the val-test gap are measured; results.pkl (the source of truth for Plan 05) is on disk — IMG-02's F1-compared-to-published deliverable is computed</done>
</task>

</tasks>

<verification>
## Mapping to Phase 7 ROADMAP Success Criteria + IMG-01/IMG-02

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| SC1 (complete): the CVCSPC pipeline trains baseline + self-supervised models | Task 1 (fine-tune from the SSL backbone) | per-seed best.pt on Drive; the CVCSPC-init fine-tune trained |
| SC2 (complete): Shallow-Squat reports F1 on the official split + compared to published | Task 3 | Test F1 at the val-tuned threshold vs paper CVCSPC 0.8694 (CITED); the SSL lift over the baseline |
| IMG-02: final Shallow-Squat test F1 | Task 3 | The summary table + results.pkl |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| IMG-02 final test F1 (CVCSPC-init, on the official split) | Task 3 | integration (Colab paste-back) | 3 |
| The baseline->CVCSPC lift (computed in code) | Task 3 | integration | 3 |
| Multi-seed ensemble + per-seed variance (D4) | Task 1 + Task 2 | integration | 3 |

(The notebook deliverable pack + FINDINGS + the phase SUMMARY are Plan 05.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user on Colab
- The multiple CVCSPC fine-tune seeds ran from the shared Plan-03 backbone via the image_train trainer (the backbone loaded NOT-from-scratch via build_cvcspc_finetune_model, the head reset to Linear(512,1)); per-seed best.pt on Drive
- The ensemble is mean-of-sigmoids (in [0,1]) with a single decision threshold tuned on the ensemble val scores (D4); per-seed test F1 reported (variance)
- Final Shallow-Squat test F1 reported at the val-tuned threshold (CVCSPC ensemble + single-model + per-seed); the comparison vs paper CVCSPC 0.8694 (CITED) + the Plan-02 baseline control; the SSL lift = (CVCSPC ensemble) - (baseline) computed in code; the val-test gap reported
- F1 computed in code (NOT hand-typed — T-07-16); the comparison numbers match RESEARCH §Supervised exactly; the 3-term-vs-2-term loss deviation documented in results.pkl (for FINDINGS)
- results.pkl written (BEFORE any figure) with the full schema — the source of truth for the Plan 05 notebook pack + FINDINGS
- Disconnect-safe: each seed resumes cleanly (resume paste-back logs "Resumed from epoch_NNN.pt"); build_cvcspc_finetune_model loads the CVCSPC backbone (NOT from-scratch — path-asserted, T-07-15); images_root/labels_path/splits_root explicit
- The notebook is delivered as paired .py + .ipynb (Cell A baked in — D6); every D2/D4 decision carries an inline citation in the plan (NOT in source — project rule); NO fabricated numbers
- One accurate threat_model: offline pipeline, no high-severity threats; T-07-16 (headline arithmetic — computed in code) is the priority; T-07-SC accept (zero new installs)
</success_criteria>

<output>
After Task 3 (results.pkl on disk + the headline F1-vs-paper computed), this plan is complete. Plan 05 (Wave 4) builds the Shallow-Squat deliverable pack (docs/notebooks EDA + training + evaluation) + figures + the FINDINGS_SHALLOW_SQUAT.md section from results.pkl + the local Shallow-Squat archive, and authors the phase SUMMARY. No SUMMARY.md for this plan individually; the orchestrator advances to Plan 05 once Task 3's results.pkl + the headline numbers are confirmed. The phase SUMMARY is authored in Plan 05.
</output>

<revision_log>
Phase 7 Plan 04 written 2026-05-30. Wave 3: the multi-seed CVCSPC fine-tune + ensemble + val-tuned threshold + test eval + results.pkl (mirrors Phase 4 Plans 03+04 / Phase 6 Plan 04). 3 tasks. depends_on BOTH 07-02 (the baseline control + the image_train trainer) AND 07-03 (the backbone) — it cannot run until both the baseline and the SSL backbone exist. NO new production .py logic EXCEPT one possible small seam: if image_train.run_image_epoch builds the model internally via build_baseline_model with no injection point, a minimal model_builder kwarg (default build_baseline_model, mirroring the Plan-01 dataset_cls seam) is added so the CVCSPC fine-tune passes build_cvcspc_finetune_model — flagged in the interfaces + Task 1 to surface at execution.

Key framing: the comparison is our baseline (Plan-02 control) -> our CVCSPC ensemble -> paper CVCSPC 0.8694 (CITED Table 3). There is NO paper supervised-ImageNet row — the baseline is the only control; SimSiam 0.8286 is the image-SSL context. The SSL lift = (CVCSPC ensemble) - (baseline). The 3-term-vs-2-term loss deviation (the SSL loss in Plan 03) is documented in results.pkl + FINDINGS (Plan 05), not silently reconciled. The no-lift case is recorded honestly (a valid result) — the projection-head ablation was Plan 03's escape; here the result is the result.

Pitfall 6 honored: build_cvcspc_finetune_model loads backbone_state_dict (strict=False) + resets the head to a fresh Linear(512,1) (the SSL projector discarded). T-07-15: BACKBONE_PATH asserted to be the CVCSPC backbone (not from-scratch). T-07-16 (headline arithmetic — the Phase-3 precedent the user caught): F1 computed in code + CITED §Supervised numbers + baseline loaded from Plan 02, not hand-typed.

Project rule honored: NO AI-narration/citation comments in source — the citations live in this plan + the commit messages.

Security: accurate. Offline pipeline (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-07-15 (CVCSPC-not-from-scratch — path-asserted + the explicit load), T-07-16 (arithmetic — highest priority, computed in code), T-07-17 (val-test leakage — the gap monitor), T-07-18 (ensemble homogeneity + mean-of-sigmoids), T-07-19 accept (no weights committed), T-07-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
