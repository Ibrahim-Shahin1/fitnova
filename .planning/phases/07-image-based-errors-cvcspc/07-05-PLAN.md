---
phase: 07-image-based-errors-cvcspc
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - docs/notebooks/09_shallow_squat_eda.py
  - docs/notebooks/09_shallow_squat_eda.ipynb
  - docs/notebooks/10_shallow_squat_training.py
  - docs/notebooks/10_shallow_squat_training.ipynb
  - docs/notebooks/11_shallow_squat_evaluation.py
  - docs/notebooks/11_shallow_squat_evaluation.ipynb
  - docs/notebooks/README.md
  - docs/eval/FINDINGS_SHALLOW_SQUAT.md
  - docs/eval/shallow_squat_test_scores.csv
  - docs/figures/shallow_squat_class_balance.png
  - docs/figures/shallow_squat_samples.png
  - docs/figures/shallow_squat_training_curves.png
  - docs/figures/shallow_squat_pr_curve.png
  - docs/figures/shallow_squat_confusion_matrix.png
  - docs/figures/shallow_squat_f1_vs_paper.png
  - .planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md
autonomous: false
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "The 3-notebook Shallow-Squat analysis pack (docs/notebooks/09_shallow_squat_eda, 10_shallow_squat_training, 11_shallow_squat_evaluation) exists as paired .py + executed .ipynb mirroring the Squat/OHP packs, each pre-executed with figures/tables rendered — built ONLY from real data: results.pkl (Plan 04), the local Shallow-Squat archive, and the recorded training artifacts; NO fabricated metrics or curves"
    - "The evaluation notebook (11) reports the official-540-crop-test-split F1 (Shallow-Squat) with the PR curve + confusion matrix + per-seed-vs-ensemble + the F1-vs-paper comparison (our baseline -> our CVCSPC -> paper CVCSPC 0.8694; SimSiam 0.8286 / OpenPose-TDM 0.8340 for context), every number sourced from results.pkl + cross-checked against RESEARCH §Supervised — the academic deliverable for IMG-02"
    - "docs/eval/FINDINGS_SHALLOW_SQUAT.md has the headline result (Shallow-Squat F1 vs paper CVCSPC 0.8694), the baseline->CVCSPC SSL lift, the per-seed variance, the val->test gap, the honest framing (no paper supervised-ImageNet baseline; the baseline is the only control), AND the 3-term-vs-2-term loss-deviation note (the SSL backbone used the code's 3-term loss; the paper Eq.1 is 2-term — documented, not reconciled); docs/eval/shallow_squat_test_scores.csv has the per-crop ensemble scores + labels (from results.pkl)"
    - "The phase SUMMARY (07-05-SUMMARY.md) declares: the production checkpoint(s) (the CVCSPC backbone + the fine-tune seeds + the ensemble protocol + the threshold), the headline F1 vs paper, the SSL lift, the image-modality + 2nd-method (CVCSPC) contribution to the thesis, deviations (the 3-term loss; any projection-head ablation), and the descope notes (BarbellRow/IMG-03 cancelled; serving descoped per D1 — IMG-01/02 met by model+eval+viz)"
    - "ROADMAP + REQUIREMENTS + STATE are reconciled via gsd-tools (not direct edits where the tool supports it): IMG-01 + IMG-02 marked addressed by model+eval+viz; IMG-03 recorded as descoped (BarbellRow cancelled); the SC3 'served via the API' descope recorded with the D1 reason; STATE updated to Phase 7 complete"
  artifacts:
    - path: "docs/notebooks/09_shallow_squat_eda.py"
      provides: "Shallow-Squat EDA notebook — official splits (2542/529/540), class balance (43.9% pos / 1584-of-3611), sample positive/negative crops, image-size distribution, the pixel-mean/std-vs-ImageNet sanity check. Mirror docs/notebooks/01_squat_eda / 05_ohp_eda. Built from the local Shallow-Squat archive."
      contains: "shallow_squat"
    - path: "docs/notebooks/10_shallow_squat_training.py"
      provides: "Shallow-Squat training-diagnostics notebook — the CVCSPC SSL triplet-accuracy curve (convergence above chance), the supervised baseline + CVCSPC-finetune train-vs-val loss + val-F1 early-stop, the baseline->CVCSPC lift. Built from results.pkl ssl_triplet_acc + finetune_seeds + baseline. Mirror 03_squat_training / 07_ohp_training."
      contains: "shallow_squat"
    - path: "docs/notebooks/11_shallow_squat_evaluation.py"
      provides: "Shallow-Squat evaluation notebook — official-test-split F1, score distribution, PR curve, confusion matrix, per-seed vs ensemble, the F1-vs-paper comparison (baseline -> CVCSPC -> paper CVCSPC 0.8694 / SimSiam 0.8286 / OpenPose-TDM 0.8340). Built from results.pkl. Mirror 04_squat_evaluation / 08_ohp_evaluation."
      contains: "0.8694"
    - path: "docs/eval/FINDINGS_SHALLOW_SQUAT.md"
      provides: "The Shallow-Squat findings doc — headline F1 vs paper, the SSL lift, per-seed variance, the val-test gap, the no-paper-supervised-baseline framing, the 3-term-vs-2-term loss-deviation note, the image-modality + 2nd-method contribution"
      contains: "Shallow"
    - path: "docs/eval/shallow_squat_test_scores.csv"
      provides: "Per-crop ensemble test scores + labels + (ids) for Shallow-Squat (from results.pkl ensemble_test_scores + test_labels + test_ids) — mirror squat_test_scores.csv"
      contains: "score"
    - path: ".planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md"
      provides: "Phase 7 closeout — what shipped, the headline F1 vs paper + the SSL lift, per-seed variance, deviations, the descope notes (IMG-03 BarbellRow cancelled; serving descoped per D1), the production-checkpoint declaration, the image-modality+2nd-method contribution, the Phase 8 handoff"
      contains: "Phase 7"
  key_links:
    - from: "docs/notebooks/11_shallow_squat_evaluation.py"
      to: ".planning/phases/07-image-based-errors-cvcspc/figures/results.pkl (Plan 04 — the source of truth for all numbers + raw scores)"
      via: "the notebook loads results.pkl + plots; NO recomputation, NO fabrication"
      pattern: "results.pkl"
    - from: "docs/notebooks/09_shallow_squat_eda.py"
      to: "the local Shallow-Squat archive (labels_shallow_depth.json + splits + crops_unaligned)"
      via: "the EDA notebook reads the local archive (offline) for class balance + sample crops"
      pattern: "labels_shallow_depth"
    - from: ".planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md"
      to: "ROADMAP + REQUIREMENTS (IMG-01 + IMG-02 addressed by model+eval+viz; IMG-03 descoped; SC3 descoped per D1)"
      via: "gsd-tools reconciliation"
      pattern: "IMG-02"
---

<objective>
Build the Shallow-Squat visualization deliverable — the 3-notebook analysis pack (docs/notebooks/09-11_shallow_squat_*) + figures + FINDINGS_SHALLOW_SQUAT.md + shallow_squat_test_scores.csv — from results.pkl (Plan 04) + the local Shallow-Squat data, mirroring the existing Squat/OHP packs, and author the phase SUMMARY (IMG-01 + IMG-02 + the deliverable per CONTEXT D6). This is Wave 4 — the closeout: no training, only assembling the academic deliverable from the real artifacts produced in Plans 02-04, then reconciling ROADMAP/REQUIREMENTS/STATE.

Purpose: IMG-02 (F1 compared to the published CVCSPC number) + IMG-01 (the full baseline+SSL pipeline, evaluated + visualized) + CONTEXT D6 (the deliverable = the Squat/OHP pattern for Shallow-Squat: results.pkl + F1-vs-paper + the notebook pack + FINDINGS, mirroring docs/notebooks/ + docs/eval/). The defense (2026-06-03) weights visualizations heavily ([[project_supervisor_visualizations]]); this pack adds the IMAGE modality + the 2nd method (CVCSPC) to the thesis alongside the video MD-SSL work (Squat, OHP). Every number + plot comes from results.pkl or the local Shallow-Squat data — NO fabrication ([[feedback_ai_correctness]]). The 3-term-vs-2-term loss deviation is documented honestly, not reconciled.

Output: 3 paired .py + executed .ipynb notebooks under docs/notebooks/ (09-11_shallow_squat_*); the README.md updated with the Shallow-Squat rows + headline; 6 Shallow-Squat figures under docs/figures/; FINDINGS_SHALLOW_SQUAT.md; shallow_squat_test_scores.csv; the phase SUMMARY; ROADMAP/REQUIREMENTS/STATE reconciled (IMG-01/02 met by model+eval+viz; IMG-03 descoped; SC3 descope per D1).

Execution is INTERACTIVE per [[feedback_interactive_execution]] / [[feedback_one_cell_at_a_time_strict]]. The notebooks are delivered as paired .py + .ipynb ([[feedback_deliver_colab_as_ipynb]]) and pre-executed (open -> figures rendered). Most of this plan runs LOCALLY (results.pkl + the local Shallow-Squat archive — no GPU, no Drive) except where a notebook needs a sample crop (the local crops suffice). The SUMMARY is the closeout — it declares the production checkpoint + the headline for the defense + the Phase 8 handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md
@.planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md
@.planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md
@.planning/phases/07-image-based-errors-cvcspc/07-VALIDATION.md
@.planning/phases/07-image-based-errors-cvcspc/07-04-PLAN.md
@.planning/phases/06-overhead-press/06-05-PLAN.md
@docs/notebooks/README.md
@docs/eval/FINDINGS.md
@$HOME/.claude/get-shit-done/templates/summary.md
@CLAUDE.md

<interfaces>
This plan CONSUMES results.pkl (Plan 04) + the local Shallow-Squat archive. NO new production logic, NO training. It writes notebooks + figures + FINDINGS + the CSV + the SUMMARY.

Inputs:
- .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl (Plan 04) — the source of truth: ssl_triplet_acc, finetune_seeds {42,1337,7}, baseline_seeds (if available), ensemble_val/test_scores, val/test_labels, test_ids, best_threshold, test_f1 {cvcspc_ensemble, single_model, per_seed}, test_pr_auc, test_confusion, baseline_control, ssl_lift, val_test_gap, paper_targets {cvcspc:0.8694, simsiam:0.8286, openpose_tdm:0.8340}, loss_deviation_note.
- The local Shallow-Squat archive (labels_shallow_depth.json + splits/{train,val,test}_ids.json + crops_unaligned/) for the EDA notebook (class balance, sample crops, image-size distribution).
- The Squat/OHP packs docs/notebooks/01-08 + docs/eval/FINDINGS.md + docs/figures/{squat,ohp}_*.png — the structure + style to mirror (color palette, layout).

Deliverable templates (mirror per PATTERNS / the OHP pack):
- docs/notebooks/01_squat_eda / 05_ohp_eda -> 09_shallow_squat_eda; 03_squat_training / 07_ohp_training -> 10_shallow_squat_training; 04_squat_evaluation / 08_ohp_evaluation -> 11_shallow_squat_evaluation. (NO separate data-pipeline notebook — the image pipeline is simple enough to fold into the EDA notebook; the planner chose 3 notebooks for Shallow-Squat vs OHP's 4, since there is no trajectory/BBox pipeline to document.)
- docs/eval/FINDINGS.md (Squat/OHP sections) -> a NEW docs/eval/FINDINGS_SHALLOW_SQUAT.md (or an appended section — the planner uses a dedicated FINDINGS_SHALLOW_SQUAT.md per the planning brief, mirroring FINDINGS_OHP.md if that is the established pattern).
- docs/eval/squat_test_scores.csv -> shallow_squat_test_scores.csv.

gsd-tools reconciliation (D1/D6 — via the tool where supported, else a careful direct edit):
- Mark IMG-01 + IMG-02 addressed by model+eval+viz; record IMG-03 descoped (BarbellRow cancelled); record the SC3 'served via the API' descope with the D1 reason; update STATE to Phase 7 complete. Use the gsd-tools query/commit handlers per the orchestrator's convention; if a handler is unavailable, edit REQUIREMENTS.md/ROADMAP.md/STATE.md directly with the same content (the Phase 6 close did this for OHP-01/SC3).
</interfaces>
</context>

<decisions>
## Locked decisions for this plan

| # | Decision | Source |
|---|----------|--------|
| D6-deliverable | The deliverable = the Squat/OHP pattern, for Shallow-Squat: a 3-notebook pack (09-11_shallow_squat_*) mirroring docs/notebooks/01-04 + 05-08 + figures + FINDINGS_SHALLOW_SQUAT.md + shallow_squat_test_scores.csv, all from results.pkl + the local Shallow-Squat data. 3 notebooks (not 4) — no trajectory/BBox pipeline to document, so the image pipeline folds into EDA. Backend API integration is DESCOPED (frontend cancelled) — NO serving/endpoint notebook. | CONTEXT D6 + the planner's notebook-count discretion |
| D1-descopes | ROADMAP Phase-7 SC3 ("these errors are served through the inference API") is reinterpreted: IMG-01/IMG-02 met by trained/evaluated models + the paper comparison + the visualization pack. IMG-03 (BarbellRow) is descoped (cancelled). Reflect both in ROADMAP/REQUIREMENTS at close — IMG-01/02 met by model+eval+viz, the API criterion + IMG-03 descoped with the D1 reason. | CONTEXT D1 + the milestone scope |
| D-no-fabrication | EVERY number + plot in the notebooks/FINDINGS/CSV comes from results.pkl or the local Shallow-Squat archive. NO fabricated metrics or curves. The F1-vs-paper numbers cross-checked against RESEARCH §Supervised (paper CVCSPC 0.8694, SimSiam 0.8286, OpenPose-TDM 0.8340). If results.pkl lacks a field a figure needs, the figure is omitted + noted — never invented. | [[feedback_ai_correctness]] |
| D7-deviation | FINDINGS_SHALLOW_SQUAT.md states the 3-term-vs-2-term loss deviation explicitly (the SSL backbone was pretrained with the code's 3-term loss; the paper Eq.1 shows 2 terms — documented, faithful-to-code, not reconciled) AND the no-paper-supervised-baseline framing (the baseline is the only control; the SSL lift = CVCSPC - baseline). Honest, defensible — no overclaiming. | CONTEXT D7 + RESEARCH §Loss / §Supervised |
| D-notebooks | Notebooks delivered as paired .py (jupytext percent) + executed .ipynb ([[feedback_deliver_colab_as_ipynb]]); pre-executed (figures rendered on open); mirror the Squat/OHP color palette + layout. The EDA notebook reads the local archive; the training + evaluation notebooks read results.pkl. | CONTEXT D6 + PATTERNS |
| D7 | Figures saved as PNG to docs/figures/ BEFORE plt.show() with defensive mkdir; one runnable unit per notebook (or per figure subsection) ([[feedback_one_cell_at_a_time_strict]]); commit per notebook (docs(07)). | CONTEXT D7 |

</decisions>

<determinism_checklist>
## Hygiene preconditions

1. NO fabrication: every number/plot from results.pkl or the local Shallow-Squat archive; cross-check the F1-vs-paper numbers against RESEARCH §Supervised.
2. Figures saved BEFORE plt.show() with Path(out).parent.mkdir(parents=True, exist_ok=True).
3. Notebooks paired .py + executed .ipynb; the .ipynb is the pre-executed artifact (figures rendered).
4. results.pkl is read-only here (the source of truth, written in Plan 04) — this plan does NOT recompute metrics, only visualizes them.
5. The SUMMARY numbers match results.pkl exactly (the orchestrator double-checks the F1 + the lift).
6. ROADMAP/REQUIREMENTS/STATE reconciled via gsd-tools where supported, else a careful direct edit with the D1 descope recorded + its reason.
7. One runnable unit per notebook; commit per notebook (docs(07)).
8. NO AI-narration/citation comments in the notebook source (project rule) — the notebooks document findings factually; the loss-deviation note is a factual finding in FINDINGS, not a code comment.

</determinism_checklist>

<threat_model>
## Trust Boundaries

This phase is an OFFLINE deliverable-assembly task (D1 descopes serving): NO network surface, NO auth, NO untrusted runtime input, NO PII. Inputs are results.pkl (our own artifact) + the research-use-only / non-commercial Fitness-AQA dataset. NO high-severity threats. The genuine constraints are: NO fabricated metrics (academic integrity), the dataset license (non-commercial, don't redistribute media), and not committing dataset media. Per the planning brief: accurate, not boilerplate.

| Boundary | Description |
|----------|-------------|
| results.pkl -> notebooks/FINDINGS/CSV | The deliverable numbers must come from results.pkl (not hand-typed or invented). A fabricated number misrepresents the academic result — the single integrity risk. |
| Local Shallow-Squat archive (research-use-only) -> EDA notebook | The EDA notebook reads the local archive for class balance + sample crops. License: non-commercial. Sample crops in the .ipynb are illustrative (a few), not a redistribution of the dataset. |
| The notebooks/figures -> git | Commit the .py + .ipynb + the small PNGs + the CSV; NO dataset media, NO weights (gitignored). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | Fabricated metrics/curves in the notebooks or FINDINGS (the academic-integrity risk) | mitigate | EVERY number + plot loads from results.pkl (Plan 04) or the local Shallow-Squat archive; the F1-vs-paper numbers cross-checked against RESEARCH §Supervised (CVCSPC 0.8694); if results.pkl lacks a field, the figure is omitted + noted, NEVER invented ([[feedback_ai_correctness]]). The SUMMARY numbers match results.pkl. MEDIUM — the source-of-truth discipline + the cross-check. |
| T-07-21 | Integrity | Committing dataset media (sample-crop .ipynb cells embedding many crops) | accept | The EDA .ipynb embeds only a few illustrative sample crops (not a redistribution); the crops themselves are gitignored / Drive-staged; results.pkl (small — scores + curves, no media) is committed under .planning/ as the Squat/OHP ones are. Stage files by name only. LOW — the Squat/OHP packs set the precedent; license-compliant. |
| T-07-22 | Integrity | The SUMMARY / FINDINGS overclaiming (e.g. claiming a CVCSPC lift that did not occur, or comparing to a non-existent paper supervised-ImageNet baseline) | mitigate | The no-paper-supervised-baseline framing is stated explicitly; the comparison is baseline -> CVCSPC -> paper CVCSPC only (the CITED rows); the SSL lift (or no-lift) is reported from results.pkl honestly; the 3-term-vs-2-term deviation is documented. MEDIUM — the honest-framing discipline. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new pip installs (RESEARCH Package Legitimacy Audit). matplotlib/numpy already present. No [ASSUMED]/[SUS] packages. |

T-07-20 (fabrication) is the integrity risk for a deliverable-assembly plan; mitigated by the source-of-truth discipline (results.pkl + the local archive) + the RESEARCH §Supervised cross-check. No web-app threats apply.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: docs/notebooks/09_shallow_squat_eda + 10_shallow_squat_training (.py + executed .ipynb) from the local archive + results.pkl</name>
  <files>
    docs/notebooks/09_shallow_squat_eda.py,
    docs/notebooks/09_shallow_squat_eda.ipynb,
    docs/notebooks/10_shallow_squat_training.py,
    docs/notebooks/10_shallow_squat_training.ipynb,
    docs/figures/shallow_squat_class_balance.png,
    docs/figures/shallow_squat_samples.png,
    docs/figures/shallow_squat_training_curves.png
  </files>
  <read_first>
    docs/notebooks/01_squat_eda.py (the EDA structure — splits load, class balance, sample frames; the color palette + layout to mirror),
    docs/notebooks/05_ohp_eda.py (the OHP EDA — the closest analog for the per-error balance + sample-grid pattern; adapt to the single Shallow-Squat binary error),
    docs/notebooks/03_squat_training.py + docs/notebooks/07_ohp_training.py (the training-diagnostics structure — SSL convergence, supervised train-vs-val, the baseline->SSL lift; mirror for Shallow-Squat with the triplet-accuracy curve instead of linear-probe),
    .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl (Plan 04 — ssl_triplet_acc + finetune_seeds + baseline_seeds + baseline_control + ssl_lift; load + plot, NO recomputation),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (the verified Shallow-Squat numbers — 2542/529/540, 43.9% pos, 1584-of-3611),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the results.pkl schema + the deliverable-notebook structure + the color palette)
  </read_first>
  <action>
    Create 09_shallow_squat_eda + 10_shallow_squat_training (paired .py + executed .ipynb), mirroring the Squat/OHP packs.

    09_shallow_squat_eda: load the local Shallow-Squat archive (labels_shallow_depth.json + splits/{train,val,test}_ids.json). Report + visualize (from the REAL archive, no fabrication): official splits (2542/529/540); class balance (43.9% positive / 1584-of-3611 — a bar, save to docs/figures/shallow_squat_class_balance.png; note the near-balance vs Squat KIE's 14%); sample positive vs negative crops (a few illustrative shallow vs deep squats side-by-side, save to docs/figures/shallow_squat_samples.png — not a redistribution); image-size distribution (are all crops the same size? a histogram from a sample); the pixel-mean/std-vs-ImageNet sanity check (confirm the ImageNet norm is the right contract). Use the Squat/OHP color palette. ALSO fold in a short data-pipeline section (the image pipeline is simple — JPEG -> 224² -> ImageNet norm; no trajectory/BBox, unlike OHP) so a separate pipeline notebook is unnecessary.

    10_shallow_squat_training: from results.pkl — the CVCSPC SSL triplet-accuracy curve (rising above 50% chance — the convergence evidence; contrast: there is no linear-probe here, the triplet-accuracy IS the SSL monitor); the supervised baseline + the CVCSPC-finetune per-seed train-vs-val loss (the overfit-onset / early-stop narrative) + val-F1; the baseline->CVCSPC lift (baseline_control vs the CVCSPC ensemble from results.pkl). Save docs/figures/shallow_squat_training_curves.png. NO recomputation — load + plot.

    Execute both notebooks (jupytext --to notebook --execute, or run cells + save) so the .ipynb is pre-rendered. Save figures BEFORE plt.show() with defensive mkdir.

    Single runnable unit — user pastes back: the executed 09_shallow_squat_eda.ipynb (class-balance + sample-crops + the size/norm sanity figures) + the executed 10_shallow_squat_training.ipynb (the triplet-accuracy + the baseline/CVCSPC training curves + the lift). Verify the counts/balance match CONTEXT (2542/529/540, 43.9%) + the SSL/F1 numbers match results.pkl.

    Atomic commit: docs(07): Shallow-Squat EDA + training-diagnostics notebooks (Task 1, D6 / IMG-01).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['docs/notebooks/09_shallow_squat_eda.py','docs/notebooks/10_shallow_squat_training.py']]; print('parse ok')" && test -f docs/notebooks/09_shallow_squat_eda.ipynb && test -f docs/notebooks/10_shallow_squat_training.ipynb && test -f docs/figures/shallow_squat_class_balance.png && test -f docs/figures/shallow_squat_training_curves.png</automated>
  </verify>
  <acceptance_criteria>
    - 09_shallow_squat_eda + 10_shallow_squat_training exist as paired .py + executed .ipynb (figures rendered on open); mirror the Squat/OHP structure + palette
    - EDA numbers match CONTEXT (2542/529/540, 43.9% pos) — from the REAL local archive, no fabrication; the data-pipeline section folded into EDA (no separate pipeline notebook)
    - The training notebook shows the CVCSPC triplet-accuracy curve (above chance) + the baseline/CVCSPC train-vs-val + the baseline->CVCSPC lift — all from results.pkl
    - 3 figures saved before plt.show() (class_balance, samples, training_curves)
    - Runnable unit: user pastes back the two executed notebooks (figures rendered)
  </acceptance_criteria>
  <dependencies>Plan 04 (results.pkl + the local archive + the trained artifacts exist)</dependencies>
  <done>The Shallow-Squat EDA + training-diagnostics deliverable notebooks exist (mirroring the Squat/OHP packs), built from the real local archive + results.pkl; the near-balance + the CVCSPC convergence + the baseline->CVCSPC lift are documented for the defense</done>
</task>

<task type="auto">
  <name>Task 2: docs/notebooks/11_shallow_squat_evaluation (.py + executed .ipynb) + F1-vs-paper + the per-crop CSV from results.pkl</name>
  <files>
    docs/notebooks/11_shallow_squat_evaluation.py,
    docs/notebooks/11_shallow_squat_evaluation.ipynb,
    docs/figures/shallow_squat_pr_curve.png,
    docs/figures/shallow_squat_confusion_matrix.png,
    docs/figures/shallow_squat_f1_vs_paper.png,
    docs/eval/shallow_squat_test_scores.csv
  </files>
  <read_first>
    docs/notebooks/04_squat_evaluation.py + docs/notebooks/08_ohp_evaluation.py (the evaluation structure — test F1, score distribution, PR + confusion, per-seed vs ensemble, the F1-vs-paper comparison; mirror for Shallow-Squat single-head),
    .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl (Plan 04 — the SOURCE OF TRUTH: ssl_triplet_acc, finetune_seeds, ensemble_test_scores, test_labels, test_ids, test_f1, test_pr_auc, test_confusion, baseline_control, ssl_lift, val_test_gap, paper_targets — load + plot, NO recomputation),
    .planning/phases/07-image-based-errors-cvcspc/07-RESEARCH.md (§Supervised — the EXACT paper Table 3 numbers to cross-check: CVCSPC 0.8694, SimSiam 0.8286, OpenPose-TDM 0.8340; the no-supervised-baseline framing),
    .planning/phases/07-image-based-errors-cvcspc/07-PATTERNS.md (the deliverable-notebook structure + the results.pkl schema),
    docs/eval/squat_test_scores.csv (the CSV format to mirror for shallow_squat_test_scores.csv)
  </read_first>
  <action>
    Create 11_shallow_squat_evaluation (paired .py + executed .ipynb), built ONLY from results.pkl (Plan 04). NO recomputation, NO fabrication — load + plot.

    11_shallow_squat_evaluation (mirror 04_squat / 08_ohp evaluation): from results.pkl — the official-test-split F1 (Shallow-Squat, CVCSPC ensemble + single-model); the score distribution (positive vs negative crop scores); the PR curve (docs/figures/shallow_squat_pr_curve.png) with the operating point; the confusion matrix (docs/figures/shallow_squat_confusion_matrix.png) at the val-tuned threshold; per-seed vs ensemble (the variance evidence); THE HEADLINE F1-vs-paper comparison (docs/figures/shallow_squat_f1_vs_paper.png — grouped bars: our baseline control, our CVCSPC ensemble, paper CVCSPC 0.8694, paper SimSiam 0.8286, paper OpenPose-TDM 0.8340; annotate the bars; cross-check every number against RESEARCH §Supervised). State the no-paper-supervised-baseline framing (the baseline is the only supervised control; the SSL lift = CVCSPC - baseline) + the 3-term-vs-2-term loss-deviation note (from results.pkl loss_deviation_note) + the val-test gap. Write docs/eval/shallow_squat_test_scores.csv from results.pkl (id (if available), score, label — mirror squat_test_scores.csv).

    Execute the notebook so the .ipynb is pre-rendered. Save figures BEFORE plt.show() with defensive mkdir.

    Single runnable unit — user pastes back: the executed 11_shallow_squat_evaluation.ipynb (the test F1 + PR + confusion + the headline F1-vs-paper comparison) + the shallow_squat_test_scores.csv. Cross-check the F1 numbers against results.pkl + RESEARCH §Supervised.

    Atomic commit: docs(07): Shallow-Squat evaluation notebook + F1-vs-paper + per-crop CSV (Task 2, D6 / IMG-02 / RESEARCH §Supervised).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('docs/notebooks/11_shallow_squat_evaluation.py').read()); print('parse ok')" && test -f docs/notebooks/11_shallow_squat_evaluation.ipynb && test -f docs/figures/shallow_squat_f1_vs_paper.png && test -f docs/eval/shallow_squat_test_scores.csv && grep -q "0.8694" docs/notebooks/11_shallow_squat_evaluation.py</automated>
  </verify>
  <acceptance_criteria>
    - 11_shallow_squat_evaluation exists as paired .py + executed .ipynb; built ONLY from results.pkl (no recomputation, no fabrication)
    - It reports official-test-split F1 + score distribution + PR + confusion + per-seed-vs-ensemble + the F1-vs-paper comparison (baseline -> CVCSPC -> paper CVCSPC 0.8694); numbers cross-checked against results.pkl + RESEARCH §Supervised
    - The no-paper-supervised-baseline framing + the 3-term-vs-2-term loss-deviation note + the val-test gap are stated (honest, no overclaiming)
    - 3 figures saved (pr_curve, confusion_matrix, f1_vs_paper) + shallow_squat_test_scores.csv written from results.pkl
    - Runnable unit: user pastes back the executed notebook + the CSV; the F1 numbers match results.pkl
  </acceptance_criteria>
  <dependencies>Task 1 (the EDA/training notebooks — the pack ordering); Plan 04 (results.pkl)</dependencies>
  <done>The Shallow-Squat evaluation deliverable notebook exists (mirroring the Squat/OHP packs), built from results.pkl; the headline F1-vs-paper comparison + the per-crop CSV are rendered — IMG-02's F1-compared-to-published deliverable is visualized</done>
</task>

<task type="auto">
  <name>Task 3: README.md Shallow-Squat rows + FINDINGS_SHALLOW_SQUAT.md + phase SUMMARY + ROADMAP/REQUIREMENTS/STATE reconciliation</name>
  <files>
    docs/notebooks/README.md,
    docs/eval/FINDINGS_SHALLOW_SQUAT.md,
    .planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md
  </files>
  <read_first>
    docs/notebooks/README.md (the Squat/OHP table + headline — add the Shallow-Squat rows + headline),
    docs/eval/FINDINGS.md (the Squat/OHP sections — mirror the structure for FINDINGS_SHALLOW_SQUAT.md; if a FINDINGS_OHP.md exists as a separate file, mirror that file pattern),
    .planning/phases/07-image-based-errors-cvcspc/figures/results.pkl (the headline numbers for the SUMMARY + FINDINGS — load, do not hand-type),
    $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template structure),
    .planning/phases/07-image-based-errors-cvcspc/07-CONTEXT.md (D1 — the IMG-03 + SC3 descopes + the reconciliation action; D7 the deviation; the production-checkpoint declaration),
    .planning/phases/06-overhead-press/06-05-PLAN.md (the Phase-6 SUMMARY + reconciliation structure — the gsd-tools pattern to mirror, and how Phase 6 flipped OHP-01/SC3)
  </read_first>
  <action>
    Three closeout deliverables + the reconciliation:

    1. docs/notebooks/README.md: add the 3 Shallow-Squat rows to the table (09_shallow_squat_eda / 10_shallow_squat_training / 11_shallow_squat_evaluation with their one-line covers, mirroring the Squat/OHP rows); add a Shallow-Squat headline-result line (F1 vs paper CVCSPC 0.8694, from results.pkl); update the "other exercises / methods" note (Shallow-Squat now done — the image modality + the CVCSPC method; BarbellRow cancelled). Numbers from results.pkl.

    2. docs/eval/FINDINGS_SHALLOW_SQUAT.md: the Shallow-Squat findings doc (mirror the Squat/OHP FINDINGS structure): the headline result (F1 vs paper CVCSPC 0.8694), the baseline->CVCSPC SSL lift (from results.pkl), the per-seed variance, the val->test gap, the no-paper-supervised-baseline framing (the baseline is the only control; SimSiam 0.8286 / OpenPose-TDM 0.8340 for context), the 3-term-vs-2-term loss-deviation note (the SSL backbone used the code's 3-term loss; the paper Eq.1 is 2-term — documented, not reconciled), any projection-head ablation (if Plan 03 ablated 512->512), the image-modality + 2nd-method (CVCSPC) contribution to the thesis, and the data-source note (results.pkl + local archive, no fabrication). Numbers from results.pkl, cross-checked against RESEARCH §Supervised.

    3. .planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md: the phase SUMMARY per the template — what shipped (the 2 dataset modules + the 2 trainers + the colab staging/frame-extract from Plan 01; the supervised baseline from Plan 02; the CVCSPC SSL backbone from Plan 03; the fine-tune + ensemble + eval from Plan 04; the 3-notebook pack from Plan 05); the headline F1 vs paper + the SSL lift; the per-seed variance + the val-test gap; deviations (the 3-term loss; any projection-head ablation; the model_builder seam if added to image_train); the DESCOPE notes (IMG-03 BarbellRow cancelled; ROADMAP SC3 "served via the inference API" descoped — frontend cancelled, IMG-01/02 met by model+eval+viz); the production-checkpoint declaration (the CVCSPC backbone at phase07/shallow_squat_cvcspc_v1/backbone.pt + the fine-tune seeds at phase07/shallow_squat_cvcspc_finetune_seed{N}/best.pt + the ensemble protocol mean-of-sigmoids + the threshold); the image-modality + 2nd-method contribution; the Phase 8 handoff (Phase 8 = ensemble + full evaluation + the visualization pack — the CVCSPC result feeds the EVAL comparison). Numbers from results.pkl.

    4. Reconcile via gsd-tools (where supported, else a careful direct edit mirroring the Phase-6 close): mark IMG-01 + IMG-02 addressed by model+eval+viz; record IMG-03 descoped (BarbellRow cancelled) — it is ALREADY marked [~] descoped in REQUIREMENTS, so confirm/retain that; record the SC3 descope with the D1 reason in ROADMAP; update STATE to Phase 7 complete (progress 7/8 phases, the plan count). Use the gsd-tools query/commit handlers per the orchestrator's convention; if unavailable, edit REQUIREMENTS.md (IMG-01/IMG-02 -> complete with the date + the headline; IMG-03 stays descoped) + ROADMAP.md (Phase 7 -> complete, SC3 descope note, the plan list) + STATE.md (Phase 7 complete) directly with the same content.

    Single runnable unit — user pastes back: the updated README.md (Shallow-Squat rows + headline), FINDINGS_SHALLOW_SQUAT.md, the SUMMARY, and the reconciliation output (IMG-01/02 + IMG-03/SC3 descopes recorded; STATE Phase 7 complete). Verify every number matches results.pkl.

    Atomic commit: docs(07): Shallow-Squat README + FINDINGS + phase SUMMARY + ROADMAP/REQUIREMENTS/STATE reconciliation (Task 3, D1/D6).
  </action>
  <verify>
    <automated>grep -qi "shallow" docs/eval/FINDINGS_SHALLOW_SQUAT.md && grep -qi "shallow_squat\|shallow-squat" docs/notebooks/README.md && test -f .planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md && grep -qiE "descope|SC3|inference API|IMG-03" .planning/phases/07-image-based-errors-cvcspc/07-05-SUMMARY.md && grep -q "0.8694" docs/eval/FINDINGS_SHALLOW_SQUAT.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md has the 3 Shallow-Squat rows + the headline (F1 vs paper from results.pkl); FINDINGS_SHALLOW_SQUAT.md has the headline, the SSL lift, per-seed variance, the val-test gap, the no-paper-supervised-baseline framing, AND the 3-term-vs-2-term loss-deviation note
    - The SUMMARY declares the production checkpoint(s) (the CVCSPC backbone + the fine-tune seeds + the ensemble protocol + the threshold), the headline F1 + SSL lift, deviations, and the descope notes (IMG-03 BarbellRow cancelled; SC3 served-via-API descoped — IMG-01/02 met by model+eval+viz)
    - Every number in README/FINDINGS/SUMMARY matches results.pkl (no fabrication, no hand-typed drift); the F1-vs-paper cross-checked against RESEARCH §Supervised
    - ROADMAP/REQUIREMENTS/STATE reconciled: IMG-01/02 addressed, IMG-03 + SC3 descoped with reasons, STATE Phase 7 complete
    - Runnable unit: user pastes back the README + FINDINGS + SUMMARY + the reconciliation output
  </acceptance_criteria>
  <dependencies>Task 2 (the evaluation notebook + results.pkl-derived numbers)</dependencies>
  <done>The Shallow-Squat deliverable pack is complete (README + FINDINGS + the 3 notebooks + figures + CSV); the phase SUMMARY declares the production checkpoint + the headline + the descopes; ROADMAP/REQUIREMENTS/STATE reconciled — Phase 7 (IMG-01 + IMG-02) closed, IMG-03 descoped</done>
</task>

</tasks>

<verification>
## Mapping to Phase 7 ROADMAP Success Criteria + IMG-01/IMG-02

| Item | Tasks that deliver it | Acceptance proof |
|------|----------------------|------------------|
| The visualization deliverable (CONTEXT D6) | Tasks 1, 2 | The 3-notebook Shallow-Squat pack + figures + shallow_squat_test_scores.csv, built from results.pkl + the local archive |
| SC2 (the published-comparison deliverable) | Task 2 + Task 3 | 11_shallow_squat_evaluation's F1-vs-paper figure + FINDINGS_SHALLOW_SQUAT.md (baseline -> CVCSPC -> paper CVCSPC 0.8694) |
| IMG-01/IMG-02 closeout + the IMG-03/SC3 descopes (D1) | Task 3 | The SUMMARY + the reconciliation (IMG-01/02 met by model+eval+viz; IMG-03 + SC3 descoped) |

## Per-VALIDATION sub-requirement coverage (this plan's portion)

| Sub-ID | Task | Test type | Wave |
|--------|------|-----------|------|
| The notebook deliverable + FINDINGS (manual review) | Tasks 1-3 | manual (the supervisor reviews the pack) | 4 |
| The headline F1-vs-paper deliverable | Task 2 | the f1_vs_paper figure + FINDINGS, from results.pkl | 4 |

(All training sub-requirements IMG-01/IMG-02 a..g are covered by Plans 01-04.)
</verification>

<success_criteria>
- All 3 tasks complete with paste-back acceptance from the user
- The 3-notebook Shallow-Squat pack (docs/notebooks/09-11_shallow_squat_*) exists as paired .py + executed .ipynb, mirroring the Squat/OHP packs, built ONLY from results.pkl + the local Shallow-Squat archive — NO fabricated metrics or curves
- 11_shallow_squat_evaluation reports the official-test-split F1 + PR + confusion + per-seed-vs-ensemble + the F1-vs-paper comparison (baseline -> CVCSPC -> paper CVCSPC 0.8694, CITED) — the academic deliverable for IMG-02
- docs/eval/FINDINGS_SHALLOW_SQUAT.md has the headline, the SSL lift, per-seed variance, the val-test gap, the no-paper-supervised-baseline framing, AND the 3-term-vs-2-term loss-deviation note; docs/eval/shallow_squat_test_scores.csv has the per-crop scores
- docs/notebooks/README.md has the Shallow-Squat rows + headline; 6 Shallow-Squat figures under docs/figures/
- The phase SUMMARY declares the production checkpoint (the CVCSPC backbone + the fine-tune seeds + the ensemble protocol + the threshold), the headline F1 + SSL lift, deviations, the descope notes (IMG-03 + SC3), and the image-modality + 2nd-method contribution
- ROADMAP/REQUIREMENTS/STATE reconciled: IMG-01/02 met by model+eval+viz; IMG-03 + SC3 descoped with reasons; STATE Phase 7 complete
- Every number across README/FINDINGS/SUMMARY/notebooks matches results.pkl + cross-checks against RESEARCH §Supervised — [[feedback_ai_correctness]] satisfied
- One accurate threat_model: offline deliverable-assembly, no high-severity threats; T-07-20 (fabrication — the source-of-truth discipline), T-07-22 (overclaiming — the honest framing), T-07-SC accept
</success_criteria>

<output>
After Task 3 (the pack + SUMMARY + reconciliation), Phase 7 is COMPLETE — IMG-01 + IMG-02 met by the trained/evaluated models + the paper comparison + the visualization pack; IMG-03 (BarbellRow) descoped; the ROADMAP SC3 ("served through the inference API") descoped per D1 (frontend cancelled). The orchestrator advances to Phase 8 (Ensemble, Evaluation & Visualization Pack — the CVCSPC result feeds the final EVAL comparison). This is the closing plan; its SUMMARY is the phase SUMMARY.
</output>

<revision_log>
Phase 7 Plan 05 written 2026-05-30. Wave 4: the 3-notebook Shallow-Squat deliverable pack (docs/notebooks/09-11_shallow_squat_*) + figures + FINDINGS_SHALLOW_SQUAT.md + shallow_squat_test_scores.csv + the phase SUMMARY + the ROADMAP/REQUIREMENTS/STATE reconciliation. 3 tasks. NO training, NO new production logic — this plan assembles the academic deliverable from results.pkl (Plan 04) + the local Shallow-Squat archive, mirroring the existing Squat/OHP packs. Mostly local (no GPU/Drive) except sample-crop loading (the local crops suffice).

Notebook count = 3 (not OHP's 4): no trajectory/BBox data-pipeline to document for the image path, so the simple JPEG->224²->ImageNet pipeline folds into the EDA notebook. The planner's notebook-count discretion (CONTEXT delegated it).

D1 closeout: IMG-01 + IMG-02 met by model+eval+viz; IMG-03 (BarbellRow) descoped (cancelled — already [~] in REQUIREMENTS); the ROADMAP SC3 ("served through the inference API") descoped (frontend cancelled, same stance as OHP) — reconciled via gsd-tools where supported, else a careful direct edit mirroring the Phase-6 close. Honest framing carried into FINDINGS + the evaluation notebook: no paper supervised-ImageNet baseline (the baseline is the only control); the 3-term-vs-2-term loss deviation documented (the SSL backbone used the code's 3-term loss, the paper Eq.1 is 2-term) — not reconciled, per [[feedback_ai_correctness]]; the image-modality + 2nd-method (CVCSPC) contribution to the thesis is the narrative.

Project rule honored: NO AI-narration/citation comments in the notebook source — the notebooks document findings factually; the loss-deviation note is a factual FINDINGS entry, not a code comment.

Security: accurate. Offline deliverable-assembly (D1 descopes serving): no network/auth/untrusted-input/PII; no high-severity threats. T-07-20 (fabrication — the integrity risk, mitigated by the results.pkl source-of-truth + the RESEARCH §Supervised cross-check), T-07-21 accept (no dataset media committed — illustrative sample crops only), T-07-22 (overclaiming — the honest no-supervised-baseline + loss-deviation framing), T-07-SC accept (zero installs). No web-app threats invented.

No revisions yet — initial PLAN.md.
</revision_log>
